# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Build & Test

```bash
go build -o portal .                    # build binary
go test ./...                           # run all tests
go test ./cmd -run TestAttachCommand    # run single test
go test ./internal/tmux/...             # run package tests
go test -v ./cmd -run TestOpen          # verbose single test
```

Lint via `golangci-lint run` (config in `.golangci.yml`: the standard set plus the `modernize` linter). Not wired into CI — it's run locally / from the Mint release step. No code generation.

Tests **must not** use `t.Parallel()` — the cmd package injects mocks via package-level mutable state (`bootstrapDeps`, `openDeps`, `attachDeps`, etc.) and cleans up with `t.Cleanup()`.

### Visual capture harness

`cmd/capturetool` is a **separate offline program** (NOT a `portal` subcommand) that renders a named deterministic fixture of the real TUI through the shared `tui.Build` constructor with every tmux seam faked (`internal/capture`) — it opens no tmux server and reads no real config (an import guard keeps `internal/capture` out of the production binary). `testdata/vhs/` holds one **`vhs`** `.tape` per canonical screen plus committed reference PNGs; `vhs testdata/vhs/<tape>` re-captures a screen. To view a screen live: `go run ./cmd/capturetool --fixture <name>` (`--appearance dark|light`; `capture.FixtureNames()` lists fixtures). This is the visual-verification harness for the Modern Vivid reskin.

## Architecture

Portal is a CLI for interactive tmux session management, built with **Cobra** (CLI) + **Bubble Tea v2** (TUI) + **Lipgloss v2** (styling). The TUI is reskinned to the **Modern Vivid** design — an owned mode-matched canvas, a closed colour-token layer, restyled chrome/modals/loading, and a concurrent cold-boot flip (see "Modern Vivid TUI" and "Cold-path startup flip" below).

### Command flow

`main.go` → `cmd/root.go` → subcommands. Root's `PersistentPreRunE` handles tmux server bootstrap (auto-starts server if not running) and injects a shared `tmux.Client` + `serverStarted` flag into `cmd.Context()`.

**Key command:** `cmd/open.go` — the main entry point (`portal open` / `x`). When given a path argument, resolves and creates/attaches a session directly. With no args, launches the TUI picker.

### Resolution chain (cmd/open.go → internal/resolver/)

Path arguments go through: direct path detection → alias lookup → zoxide query → TUI fallback with filter text.

### Session connection (two modes)

- **Outside tmux (bare shell):** `AttachConnector` uses `syscall.Exec` to hand off the process to `tmux attach-session -A` (atomic, replaces the portal process)
- **Inside tmux:** `SwitchConnector` uses `tmux switch-client` (two-step: create detached session, then switch)

### Internal packages

| Package | Role |
|---------|------|
| `tmux` | Wraps tmux CLI via `Commander` interface (`RealCommander` → `os/exec` with separate `Run`/`RunRaw` for trim vs verbatim output). Client methods cover sessions (ListSessions — appends `#{@portal-dir}` to the `list-sessions -F` format and parses it into `Session.Dir` (trailing unbounded `SplitN` slot survives embedded pipes; absent → empty), NewSession, NewSessionWithCommand, NewDetachedSessionNoCwd, HasSession, KillSession, RenameSession, SwitchClient, CurrentSessionName), windows/panes (NewWindow, SplitWindow, ListPanes, ListPanesInSession, ListWindowsAndPanesInSession, ListAllPanes, ListAllPanesWithFormat, ResolveStructuralKey, SendKeys, RespawnPane, SelectLayout, SelectPane, ResizePaneZoom, CapturePane, ActivePaneCurrentPath — `display-message -p -t <session> -F '#{pane_current_path}'` for the lazy dir-resolution fallback), environment (ShowEnvironment, SetSessionEnvironment), server lifecycle (ServerRunning, StartServer, EnsureServer), options (SetServerOption, SetSessionOption, GetServerOption, TryGetServerOption, UnsetServerOption, ShowAllServerOptions), and global hooks (ShowGlobalHooksForEvent, AppendGlobalHook, UnsetGlobalHookAt — `ShowGlobalHooksForEvent` runs the per-event `show-hooks -g <event>` read; the no-arg `ShowGlobalHooks` enumeration was removed because tmux 3.6b's global read is blind to `pane-*`/geometry `window-*` events). Also exposes `BootstrapPortalSaver` / `EnsurePortalSaverVersion` for the `_portal-saver` session lifecycle — `EnsurePortalSaverVersion` runs the Component A kill-barrier: `kill-session` then a 5s exit poll, then identity-checked direct SIGKILL escalation against the recorded `daemon.pid` if the orphan refuses to die. `SaverPanePIDOrAbsent(*Client, sessionName) (pid int, present bool, err error)` is the tri-state helper consumed by the orphan-sweep, Component D self-supervision probe, and `_portal-saver` end-state assertions — collapses `ErrNoSuchSession` and `ErrEmptyPaneList` into `present=false` while passing other errors through. `WindowGroup` is the shape returned by `ListWindowsAndPanesInSession` — a window-grouped pane enumeration consumed by the TUI scrollback preview |
| `state` | Resurrection state model: schema, capture, atomic commit, scrollback dump/replay, FIFO plumbing, marker helpers (notably `@portal-restoring` via `IsRestoringSet`), pane-key resolution, paths. Contains the `portal state daemon` runtime invariants (capture loop, FIFO sweep, version guard). Also exposes `AcquireDaemonLock(stateDir)` / `ErrDaemonLockHeld` — the `daemon.lock` flock-based singleton primitive enforcing N ≤ 1 daemons per state directory (lock fd is retained in a `cmd` package-level var for the daemon's lifetime). Component C strengthens this with a `daemon.pid` pre-check (identifies the recorded PID; if alive and a `portal state daemon`, refuse with `ErrDaemonLockHeld` *before* opening `daemon.lock`) plus a post-flock `fstat`/`stat` inode cross-check against the `O_EXCL|O_CREAT` open and a bounded 3-iteration retry loop on inode mismatch to absorb the unlink+recreate race. `IdentifyDaemon(pid)` is the shared identity-check primitive consumed by Components A/B/C — wraps `ps -o pid,comm,args -p <pid>` against the canonical `PortalDaemonArgvPattern` and returns one of three results (`IdentifyAlive` for a live `portal state daemon`, `IdentifyDead` for canonical pid-not-found, and a transient-error case where the caller applies component-specific policy). `PgrepPortalDaemons()` is the canonical `pgrep -fx '^portal state daemon( |$)'` enumeration — single source of truth used by bootstrap step 4 (orphan sweep) and tests; eliminates prod/test drift risk. `TailScrollback(path, n)` is a single-fd reverse-chunked read that returns at most the last n newline-terminated records of a `.bin` file with a three-shape return contract (`(bytes, nil)` / `(nil, nil)` for ENOENT-or-empty / `(nil, err)` for OS errors) — used by the TUI scrollback preview |
| `log` | **The single owner of all logging machinery** — no `*slog.Logger` is constructed anywhere else (a source-walking guard test enforces this). Wraps `log/slog` behind a swappable-handler indirection: `For(component)` returns a component-bound child logger (safe before `Init`, cached at package init), `Init`/`Close` configure the process-wide handler and emit the `process:` lifecycle markers, `SetTestHandler` is the test-only capture/silence seam. The custom handler owns calendar-daily rotation (`portal.log` symlink → `portal.log.<date>`, inode-identity reopen, `O_CREAT\|O_EXCL` first-of-day open), a configurable size-cap overflow valve, bounded retention with per-deletion breadcrumbs, `component:`-prefix text rendering, and the lifecycle-marker level-filter bypass. Also exposes `OrDiscard`/`Discard` (nil-tolerant silent logger), `Took(start)` (the reserved cycle-summary `took` attr), `ResolveProcessRole(args)` (argv → one of 6 roles), `CombinedOutputWithContext` (stderr-preserving `exec.Cmd` boundary helper), and `SweepLogsForClean` (the `portal clean --logs` sweep). Must stay a leaf — it must NOT import `internal/state`. See "Logging & observability" below |
| `restore` | Two-phase restore engine consumed by bootstrap step 6: phase A reconstructs skeleton (sessions/windows/panes via new-session/new-window/split-window with `respawn-pane -k` swapping the default shell for the hydrate helper), phase B applies geometry (select-layout, select-pane, resize-pane -Z) and replays scrollback through per-pane FIFOs |
| `session` | Session creation pipeline: git root resolution → project persistence → name generation (`{project}-{nanoid}`) → tmux session creation. `create.go` stamps the tmux session user-option `@portal-dir = <resolvedDir>` (`PortalDirOption`) at creation (best-effort, swallowed). `QuickStart` likewise anchors `@portal-dir` at creation: its `ExecArgs` are a single chained tmux invocation (`new-session -d … ; set-option -t <name> @portal-dir <dir> ; attach-session -t <name>` — literal `;` argv separators) that creates the session detached, stamps it while detached (before `attach-session` blocks the chain), then attaches. Creating detached gives the in-server point to stamp that the former `new-session -A` (attach-immediately) handoff lacked. `GenerateSessionName` guarantees the name is new, so plain `new-session -d` always creates (no `-A` needed). `dirresolve.go` (`ResolveSessionDir` + the `PaneCurrentPathReader` seam) derives a session's directory live from its active pane's `current_path` → git-root when `@portal-dir` is absent; this is the TUI's lazy grouped-render fallback for legacy un-stamped sessions and feeds `project.CanonicalDirKey` so the lookup key matches stored `Project.Path`. The fallback never stamps tmux (a session's pane cwd can drift from its origin dir; freezing that drift would permanently mis-group it) — the TUI caches the guess in-memory only. (The former `dirstamp.go` / `ResolveAndStampDir` / `PaneStamper` derive-then-restamp helper was removed when the lazy fallback stopped stamping.) |
| `resolver` | Path resolution chain with interface-based DI (AliasLookup, ZoxideQuerier, DirValidator) |
| `tui` | Bubble Tea model with page state machine: Loading → Sessions → Projects → Preview. `LoadingMinDuration = 1.2s` enforces a minimum loading-page display window. The `pagePreview` arm renders a read-only scrollback preview when `Space` is pressed on the Sessions page; the `previewModel` is constructor-injected with `TmuxEnumerator` and `ScrollbackReader` seam interfaces (production adapters wire `*tmux.Client` and the `state.TailScrollback` helper, with `stateDir` resolved once at TUI construction). The dismiss handler dispatches a sessions-list refresh on the `pagePreview → pageSessions` transition so externally-killed sessions disappear from the post-dismiss list. Also owns **session grouping** (see "Session grouping & tags" below): `grouping.go` builds the grouped item slice (`buildByProject`/`buildByTag` → the shared `orderedSessionItems` sort + `injectGroupHeaders` skeleton via `assembleGroups`), `session_item.go` carries the `SessionItem` (with render-layer group metadata `GroupKey`/`GroupHeading`/`CatchAll`) and the `HeaderItem` (a real, non-selectable list row — `FilterValue()==""` — rendered as the dimmed `"Heading ··· N"` separator), and `model.go`'s `rebuildSessionList` is the single mode-aware re-render chokepoint driving the `s`-key cycle, `SessionListTitle()`, the footer hint, the "No tags yet" signpost, flatten-on-filter, and the lazy dir-resolution pass. `ensureSessionRowSelected` / `skipHeaderRow` keep the cursor off `HeaderItem` rows (initial selection + navigation), and the `s` toggle resets the paginator to page 1. `restore.go` owns the owned-canvas **terminal-background restore-on-exit**: the model paints the mode-matched canvas by SETTING the terminal default background via OSC 11 (so the canvas fills the window gutter), and on exit `RestoreTerminalBackground` SETs the captured original back for terminals that ignore the OSC 111 reset. A **canvas-echo guard** (`sameHexColour` against the canvas hex) skips that set-back when the captured "original" equals the canvas — the async OSC 11 query can race the canvas set, so a terminal may echo the canvas back as its "original"; re-applying it would re-stick the canvas after Bubble Tea's OSC 111 reset (the `sessions-no-tags-signpost` fixture exposed this in Ghostty). Do **not** drop the guard |
| `tui/theme` | The closed **Modern Vivid (MV)** colour-token layer (`internal/tui/theme`) — ~20 named semantic role tokens (Tokyo Night family), each with a **Light and Dark** variant; `Token.ColorFor(mode)` resolves per the owned-canvas mode, `theme.MV` is the single built-in theme, and `Mode`'s zero value (`Dark`) is the no-answer fallback. Every renderer references a token — no raw hex at call sites, enforced by the glob-based `colour_literal_guard_test.go` in `internal/tui` (which excludes only this subpackage). `contrast_test.go` numerically verifies each token clears its WCAG floor against the exact owned canvas (dark vs `#0b0c14`, light vs `#e1e2e7`, resolved independently; light values measured against `#e1e2e7`, NOT pure white — see the spec §2.9 corrigendum) |
| `project` | JSON-backed store (`~/.config/portal/projects.json`) with atomic writes. The `Project` record carries a `tags []string` field (missing field decodes to nil/empty — no migration). `tags.go` holds the tag mutation API: `NormaliseTag` (the sole canonical-form function — trim + reject-empty, **case preserved** so a tag stores/displays exactly as typed; matching is therefore case-sensitive — "Work" ≠ "work"), `AddTag`/`RemoveTag` (normalised, per-project deduped, persisted via `saveTagMutation`; unknown path → `ErrProjectNotFound`), and the `findByPath` lookup helper. `pathkey.go` exposes `CanonicalDirKey` (ExpandTilde → Abs → EvalSymlinks → Clean) — the single canonical key form for matching a directory path against stored `Project.Path`. `index.go` provides `Index` / `NewIndex` / `Match(dir) (Project, key, ok)` — a once-per-project-load `map[canonicalKey]Project` lookup that replaces per-render canonicalisation (`Match` returns the computed key so callers don't re-canonicalise) |
| `prefs` | Leaf JSON store (`~/.config/portal/prefs.json`) owning UI preferences — the last-used session-list grouping mode and the owned-canvas light/dark **`appearance`** override (`{"session_list_mode": "flat"\|"by-project"\|"by-tag", "appearance": "auto"\|"light"\|"dark"}`). `SessionListMode` enum (`ModeFlat`/`ModeByProject`/`ModeByTag`) and `Appearance` enum (`AppearanceAuto`/`AppearanceLight`/`AppearanceDark`), both tolerant decode (missing/empty/corrupt/unrecognised → `ModeFlat` / `AppearanceAuto`, no hard error); `auto` runs OSC 11 light/dark detection while `light`/`dark` pin the canvas and skip detection. Persisted via `fileutil.AtomicWrite`. Deliberately a leaf (stdlib + `fileutil` only, no `internal/log`) — prefs is outside the closed audit-trail/log-component vocabulary, so it must not import the logging machinery (avoids an import cycle with `internal/tui`) |
| `alias` | Flat key=value file store (`~/.config/portal/aliases`) |
| `hooks` | JSON-backed `Store` (`~/.config/portal/hooks.json`) holding per-pane on-resume commands keyed by structural pane key. Pure persistence — no execution. Hook firing is now driven by the hydrate helper's exec chain (`portal state hydrate`), not by the cmd layer at attach time |
| `bootstrapadapter` | Production adapters wiring concrete `*tmux.Client`, hooks store, and state package functions to the `cmd/bootstrap` Orchestrator's seam interfaces |
| `warning` | Canonical `Warning` shape and `WriteLines` stderr helper for soft bootstrap warnings. Single source of truth straddling the `cmd → tui` import boundary; `cmd/bootstrap` aliases the type and both CLI and TUI emission paths delegate to `WriteLines` so output is byte-identical |
| `xdg` | Leaf package resolving `$XDG_CONFIG_HOME` (with `~/.config` fallback). Single source of truth consumed by both `cmd/config.go` and `internal/state/paths.go` |
| `tmuxout` | Leaf, dependency-free helpers for parsing `tmux show-*` output (e.g., `StripMatchedOuterQuotes`). Imported by both `internal/tmux` and `internal/state` to avoid an import cycle |
| `tmuxerr` | Leaf, dependency-free error sentinels for tmux command failures (`ErrNoSuchSession`, `ErrEmptyPaneList`, etc.). Factored out of `internal/tmux` to break a latent import cycle — consumed by `internal/tmux` (wrapping) and other internal packages that need to discriminate tmux error classes without depending on the tmux client itself |
| `portaltest` | Test-only helpers — production code must not import. Flagship `IsolateStateForTest(t) (env []string, stateDir string)` returns per-test isolated env that scrubs developer `XDG_CONFIG_HOME` and registers a fingerprint-diff cleanup backstop. Also exposes `SpawnIsolatedDaemon` / `RegisterSubprocessCleanup` (the canonical subprocess reap pattern for daemon-spawning integration tests), `SnapshotStateDir` / `DiffFingerprints` / `FormatDelta` / `Fingerprint` (state-dir fingerprinting; deltas include a `hashed-changed` label for content-only edits), `PgrepPortalDaemons` (forwards to `state.PgrepPortalDaemons`), `ReadPortalLogSafe` (read portal.log without failing on ENOENT), and `AssertLogLevelResolved` (scans portal.log for the `process: log-level resolved` line by pid — integration tests that set `PORTAL_LOG_LEVEL` MUST assert propagation with it). `*testing.T`-first parameter on the flagship helper structurally enforces test-only consumption for most exports; the small set of exceptions (fingerprint helpers, `ReadPortalLogSafe`) rely on contributor discipline. See "Test isolation for daemon-spawning tests" below |
| `fileutil` | Shared utilities — `AtomicWrite` (temp file + rename) used by the hooks/project stores and the `sessions.json` commit. Also exposes the write-phase sentinels (`ErrWriteTempCreate` / `ErrWriteWrite` / `ErrWriteFsync` / `ErrWriteRename`) + `ClassifyWriteError`, which the state-mutation audit trail maps to an `error_class` attr on a failed mutation. Stays audit-unaware (no logging of its own) so it can be shared with out-of-scope `sessions.json` |
| `storelog` | Leaf composition package exposing `EmitCleanStaleSummary` — the shared clean-stale batch terminal Warn/Info emission for the hooks and project stores. Imports both `internal/log` and `internal/fileutil`, so it lives outside both (which can't import each other) to avoid the import cycle |
| `logtest` | Test-only — production code must not import. The shared `Sink` capture-`slog.Handler` (single source of truth for the `<LEVEL> <msg> key=value` rendered-body contract, plus a structured attr-value map and typed accessors `AttrString` / `IntAttr` / `RequireDuration` / `HasAttr` / `OnlyRecord`). Consumed via thin embedded-field wrappers across cmd / state / restore / tui / store test surfaces |
| `fuzzy` | Substring-based fuzzy matching/filtering |
| `restoretest` / `tmuxtest` / `portalbintest` / `transienttest` | Test-only helpers — production code must not import these. `restoretest` holds restore-specific scaffolding (sessions.json seeders, signal-hydrate drivers, marker-clear pollers, integration-tagged `BuildPortalBinaryDir`/`BuildPortalBinaryStable` wrappers). `tmuxtest` provides real-tmux socket fixtures. `portalbintest` provides general-purpose `go build` / PATH-stage helpers (`BuildPortalBinary`, `StagePortalBinary`, `ProjectRoot`) consumed by restore, daemon, and saver integration tests. `transienttest` provides the shared `list-panes -a` tmux-transient scaffolding (`Commander` + `FailureMode` + `PassThrough`/`FailExitNonZero`/`FailEmptyStdout`, `SocketCommander` pass-through, `SeedHooksJSON`/`HooksJSONBytes`/`ResolveHooksFilePathFromEnv` hook seeders) consumed by the bootstrap-step-11 and `portal clean` integration tests — single canonical declaration so the failure-mode contract cannot drift between the two destructive consumers of `ListAllPanes` |

### Config path resolution (cmd/config.go)

All config files (`projects.json`, `aliases`, `hooks.json`, `prefs.json`) resolve via `configFilePath`: per-file env var → `XDG_CONFIG_HOME/portal/` → `~/.config/portal/`. On first access, `migrateConfigFile` performs a one-shot move from the old macOS path (`~/Library/Application Support/portal/`) — never overwrites existing files at the new path. `configFileComponents` maps each file to its log component for the migrate breadcrumb; `prefs.json` maps to the empty component (`""`), which suppresses the migrate log emission while still performing the move (prefs is outside the closed log-component vocabulary). `prefs.json` resolves via `prefsFilePath` / `loadPrefsStore` (`cmd/clean.go`), wired into the TUI at construction through the `WithInitialMode` / `WithModePersister` / `WithAppearance` options in `cmd/open.go` (the appearance pref is read once at construction and feeds the owned-canvas mode gate).

### Logging & observability

All logging flows through `internal/log` — bind a component logger once per package via `var logger = log.For("<component>")`, then call `logger.{Debug,Info,Warn,Error}(msg, attrs...)` with slog attrs. `main.go` calls `log.Init` first thing (before Cobra parses argv, so `process_role` is resolved from `os.Args`) and owns the single `os.Exit` via a panic-recovering exit shape — **bare `os.Exit` outside `main` is prohibited** (the daemon self-eject is the one sanctioned exception, and it pairs itself via `log.Close`). `process:` lifecycle markers (`start` / `exit` / `exec` / `panic` / `log-level resolved`) bracket every run and **bypass the level filter** so they are always present as forensic tripwires.

- **Format & rotation:** human-readable text with a `component:` prefix, so `grep "<component>:" portal.log` reconstructs a subsystem's behaviour at the production-default level (JSON also available from the handler). `portal.log` is a symlink to a calendar-daily `portal.log.<date>`; a size-cap valve overflows to `portal.log.<date>.N`; past-day files are sealed `chmod 0400`. Retention defaults to 30 days with one INFO breadcrumb per deleted file.
- **Closed taxonomy:** 15 component names + a closed attr-key vocabulary + four levels (production default **INFO**). New components/attrs require amending the spec — never invent at call-site. Baseline attrs (`pid` / `version` / `process_role`) are injected per-record by the handler.
- **Env:** `PORTAL_LOG_LEVEL` (`debug`/`info`/`warn`/`error`, invalid → `info` + WARN), `PORTAL_LOG_ROTATE_SIZE` (e.g. `500M`/`1G`, default 500 MiB), `PORTAL_LOG_RETENTION_DAYS` (default 30). `portal clean --logs` forces a full retention sweep (preserves rotated logs by default).
- **What's instrumented:** every `hooks.json` / `aliases` / `projects.json` mutation emits a breadcrumb from the **store method** (the chokepoint, not the caller); cycle summaries (`capture: tick complete`, `bootstrap: orchestration complete`, `restore: skeleton/geometry complete`, the `clean:` sweeps) emit one INFO per cycle with per-item detail at DEBUG; saver/daemon lifecycle and the hydrate exec chain have closed event catalogs.

### DI / testing pattern

All external dependencies use small interfaces (1-3 methods). Commands expose package-level `*Deps` structs (e.g., `bootstrapDeps`, `openDeps`, `hooksDeps`) — tests set these to mock implementations and restore via `t.Cleanup()`. Integration tests in `cmd/root_integration_test.go` build the binary and test via subprocess execution.

#### Test isolation for daemon-spawning tests

Any test that runs `portal state daemon` directly OR via `portal open` / bootstrap MUST call `portaltest.IsolateStateForTest(t *testing.T) (env []string, stateDir string)` (in `internal/portaltest/isolated_env.go`) and apply the returned env to every spawned subprocess (`cmd.Env = env`). Without this, the spawned daemon inherits the developer's `$XDG_CONFIG_HOME` and writes to the real `~/.config/portal/state/` — the slow-open / empty-previews / zombie-session incident is the canonical example of how a leaked test daemon corrupts the live install. The helper also registers a `t.Cleanup` fingerprint-diff backstop that walks the developer's state dir post-test and fails on any delta; the backstop is **defence-in-depth, not a substitute** for the env override. No lint or CI enforcement exists — the rule is contributor-discipline plus the structurally-mandatory `*testing.T` parameter (which prevents importing the helper from non-`*_test.go` code).

For daemon-spawning tests, prefer `portaltest.SpawnIsolatedDaemon` + `portaltest.RegisterSubprocessCleanup` over hand-rolling `exec.Cmd` / `cmd.Wait`. These helpers wrap the canonical SIGKILL+Wait+reap pattern that prevents zombie subprocesses and ensures the test's `*testing.T` sees deterministic teardown order — both are load-bearing on macOS where unreaped subprocesses can hold open the state dir's file descriptors past the test's `t.Cleanup` window.

### Server bootstrap

`PersistentPreRunE` runs a ten-step `bootstrap.Orchestrator` (in `cmd/bootstrap/`) for commands needing tmux (all except version, init, help, alias, clean). Step ordering is load-bearing; "Return" is the post-step boundary, not a numbered step:

1. **EnsureServer** — start the tmux server if not running. `StartServer` creates the server with an initial detached session named `_portal-bootstrap` (`tmux.PortalBootstrapName`) — an **intentional, permanently-present anchor**, not a leak. A tmux server with zero sessions exits immediately, so this session keeps the freshly-started server alive long enough for the rest of bootstrap (notably `EnsureSaver` and `Restore`) to run. It is created on every cold server start and is **never killed by production code**; like `_portal-saver`, its leading-underscore name is filtered out of `ListSessions` and excluded from `sessions.json` capture, so it is invisible to the picker and never saved/restored. Do **not** mistake its lingering presence in raw `tmux list-sessions` output for an orphan to clean up — it is load-bearing.
2. **RegisterPortalHooks** — install global tmux hooks (e.g. `client-attached` running `portal state signal-hydrate`) idempotently. Registration is declarative per-event "ensure exactly one": each managed event is read via `ShowGlobalHooksForEvent` (`show-hooks -g <event>`) and converged to a single Portal entry carrying the current desired body. Per-event reads are mandatory because the no-arg global `show-hooks -g` is blind to `pane-*`/geometry `window-*` events on tmux 3.6b — the blind spot that previously let duplicate `portal state notify` hooks stack unbounded on `pane-focus-out`/`window-layout-changed`; any pre-existing stack now self-collapses to one on the next bootstrap (no dedicated cleanup migration). `UnregisterPortalHooks` (`portal hooks reset`) reaps via the same per-event seam, and `managedEvents` is the single source of truth from which both the teardown event set and the teardown fingerprint set are derived. The former `migrateHydrationHooks` / `migrateSessionClosedHook` one-shot helpers are folded into this unified convergence path.
3. **Set `@portal-restoring`** — must precede saver/restore so the daemon and hydrate helpers can detect they are inside a restoration window.
4. **SweepOrphanDaemons** — best-effort `pgrep -fx '^portal state daemon( |$)'` enumeration; identity-checked SIGKILL of any candidate that is not the `_portal-saver` pane's PID. Runs before `EnsureSaver` so the new saver-pane daemon's first tick is uncontested by leftover daemons from prior server lifetimes. Composes with the kill-barrier in `EnsureSaver` (which handles the single recorded `priorPID`) to converge multi-orphan installs to a singleton in one bootstrap. Errors log at WARN under `ComponentBootstrap` and are swallowed; never escalates to a fatal abort.
5. **EnsureSaver** — bootstrap (or version-upgrade) the `_portal-saver` detached session that hosts `portal state daemon`. Best-effort; failure surfaces as a `SaverDownWarning`.
6. **Restore** — invoke `internal/restore` to reconstruct skeleton + geometry + scrollback FIFOs from the saved state. Never escalates to a fatal abort; corrupt state surfaces as a warning.
7. **EagerSignalHydrate** — best-effort write of the hydrate signal byte to every freshly-armed `@portal-skeleton-*` pane's FIFO. Production wiring uses `state.DefaultFIFOSignaler` / `state.SendHydrateSignal` — the no-seam entry point that pins `OpenFIFOForSignal` + `time.Sleep`. The lower-level `state.WriteFIFOSignal` is the seam-bearing variant used only for retry-ladder unit tests. Runs while `@portal-restoring` is still set so daemon `captureAndCommit` suppression remains in force during helper-driven scrollback replay. Eliminates the per-session signaling gap that would otherwise leave N-1 saved sessions' helpers waiting on `client-attached` events that never fire for non-attached sessions. A non-nil return logs at WARN under `ComponentBootstrap` and is swallowed; never escalates to a fatal abort.
8. **Clear `@portal-restoring`** — fatal on failure (the marker must not leak past bootstrap).
9. **CleanStaleMarkers** — best-effort cleanup of `@portal-skeleton-*` server-option markers whose paneKey is no longer represented by a live pane. Runs after Clear so it observes post-restore tmux state, and before Sweep so any stale markers protecting orphan FIFOs are unset first, allowing those FIFOs to be reclaimed in the same bootstrap.
10. **SweepOrphanFIFOs** — best-effort cleanup of orphan `hydrate-*.fifo` files whose paneKey is no longer represented by a live `@portal-skeleton-*` marker.

(The former step 11 **CleanStale** — hooks stale-entry cleanup — was removed from the orchestrator and re-homed on the `_portal-saver` daemon, which runs it on a throttled idle-tick cadence; `portal clean` remains the manual backstop.)

After step 10 the orchestrator returns the accumulated warnings. How they (and the per-step progress) reach the user depends on the path — see **Cold-path startup flip** below: the warm / CLI path runs the bootstrap synchronously and drains warnings post-bootstrap (or after the `LoadingMinDuration` = 1.2s loading-page pad), while the cold + TUI path runs it concurrently and warnings ride the progress channel to a post-load notice band.

### Cold-path startup flip (concurrent bootstrap)

On the **cold + TUI path** — a fresh tmux server *and* the TUI picker, decided by a cheap `tmux has-server` probe (`shouldRunConcurrentBootstrap`) — the 11-step bootstrap runs **concurrently** with the TUI instead of synchronously in `PersistentPreRunE`. Bubble Tea launches immediately on the honest loading page; the orchestrator runs in a goroutine streaming a `tea.Msg` per real step (and per restored session) over a **progress channel** (`cmd/bootstrap_progress.go`, `cmd/bootstrap/progress_emitter.go`) carrying `serverStarted` + progress, and the model transitions to Sessions on `BootstrapCompleteMsg`. The 11 real steps map to **5 friendly labels** (`internal/tui/loading_progress.go` is the single mapping authority); only `Restoring sessions` carries the `N/M` counter, fed by a per-session callback injected at `internal/restore/restore.go`. A **fatal** step becomes a `tea.Quit` with an in-TUI `state.red` error frame + non-zero exit (not a `PersistentPreRunE` error return); **soft warnings** ride the channel and surface as a post-load notice band after the picker appears (replacing the old stderr/alt-screen flush). The first real paint gates on the detect-or-timeout appearance gate so the correct canvas shows from frame one.

The **warm path** (server already up — `serverStarted=false`) and the **CLI / direct-path** (`!isTUIPath`) keep today's untouched synchronous bootstrap with no loading page and zero new risk. The TUI is inert during loading (animation only), which contains the restore/daemon race surface. Startup-ordering and warm-path parity are covered by `cmd/concurrent_*_test.go` (daemon-spawning tests under `IsolateStateForTest`).

### Daemon self-supervision

The `portal state daemon` runs a per-tick **saver-membership self-check** before each `captureAndCommit`. On every ticker fire (1s `TickerPeriod`), the daemon asks "am I still the pane process of the live `_portal-saver` session?" via `tmux.SaverPanePIDOrAbsent` plus a `pane_pid == os.Getpid()` comparison. A failing probe (saver absent, pane pid mismatch, or transient tmux error) increments an in-process consecutive-absence counter. A passing probe resets it to 0.

When the counter reaches `selfSupervisionHysteresisTicks = 3` consecutive ticks (declared in `cmd/state_daemon.go`; clamp-floor + clamp-ceiling pinned by `TestSelfSupervisionHysteresisTicks_ClampInvariant`), the daemon ejects in a fixed, load-bearing order:

1. Emits `daemon: self-eject ticks=N threshold=3` (INFO under the `daemon` component).
2. Calls `log.Close(0)`, which emits the paired `process: exit code=0` lifecycle marker (and does **not** itself exit) — so the self-eject stays a *marked* termination and the four-way `process:` classification holds (no unpaired `process: start`).
3. Calls `os.Exit(0)` immediately (via the `osExit` seam) — bypassing `daemonShutdownFunc` so the divergent-view daemon does **not** execute one more `captureAndCommit` / `gcOrphanScrollback` cycle on its way out (same reasoning as Component A's straight-to-SIGKILL choice). No `daemon: shutdown` line fires on this path.
4. Leaves `daemon.pid` stale on disk. The next acquire's Component C pre-check handles this correctly (recorded PID is dead → pre-check skips → new daemon acquires cleanly). Cleanup logic to delete `daemon.pid` before the eject would be racy against a concurrent pre-check and would invert the layered-enforcement contract — **do not add it**.

The hysteresis value justification is documented in-source as a comment block above the constant (per-scenario tick counts measured against real tmux 3.6b, 2× safety factor, clamp result, measurement date, binary version). The integration-tagged harness `cmd/state_daemon_hysteresis_measurement_test.go` re-runs the experiment to verify the safety-factor invariant whenever it executes.

### Resume hooks

Per-pane hooks are registered via `portal hooks set --on-resume "cmd"` and persisted in `hooks.json`. `portal hooks rm --on-resume` removes the entry for the current pane (resolved from `$TMUX_PANE`); pass `--pane-key <key>` as a literal pass-through to remove an entry for any structural key, bypassing the `$TMUX_PANE` requirement (enables pruning of entries whose pane no longer exists). They fire **only inside the hydrate helper's exec chain** (`portal state hydrate`), which is launched as the initial process of each restored pane via `respawn-pane -k` during bootstrap step 6. After the helper finishes scrollback replay it looks up the saved structural hook key in `hooks.json` and exec's either `sh -c '<HOOK>; exec $SHELL'` or a bare `$SHELL` — meaning hooks fire on reboot recovery, not on every detach/reattach inside the same tmux server lifetime. The structural key is preserved across base-index drift so lookups stay addressable. Stale hook entries are cleaned lazily by the daemon and explicitly via `portal clean`.

### Modern Vivid TUI

The picker is reskinned to **Modern Vivid (MV)** — a token-based design on an owned canvas. This is a *reskin, not a rebuild*: the list model, grouping, filtering, and persistence are behaviourally unchanged; the structure below is the styling layer.

- **Owned mode-matched canvas.** Portal paints its own opaque backdrop on every cell — near-black `#0b0c14` (dark) / near-white `#e1e2e7` (light) — in two layers: leaf styles carry `.Background(canvas)`, and an **outer full-terminal fill** (the last layer in `model.go`'s `View`) pads to `termW × termH` so no edge bleeds and empty rows are painted. The fill never participates in the list's height budget, so a vertical change (e.g. a notice band appearing) re-pads underneath it without perturbing the one-row-per-delegate pagination invariant. `restore.go` set-and-restores the terminal *default* background via OSC 11 (see the `tui` row).
- **Light/dark detection (`appearance_gate.go`).** The canvas mode is chosen by a **detect-or-timeout first-paint gate**: an OSC 11 query (`tea.RequestBackgroundColor`) races a ~50ms timeout; the first to resolve wins (single-resolution — never paint-then-flip), with a **dark** no-answer fallback. The `appearance` pref (`auto|light|dark`) overrides — `light`/`dark` pin the canvas and skip both detection and the wait. `NO_COLOR` is the one carve-out: no canvas, no detection, colourless render on the terminal's native fg/bg (state stays glyph-backed — `●`/`▌`/`⚠`/`✓` — never colour-only).
- **Render structure (files in `internal/tui`).** Shared chrome: `header.go` (block `PORTAL` wordmark + violet `▌` caret + subtitle + 2px rule), `section_header.go` (page label + count + `/ to filter` hint), `footer.go` / `filter_footer.go` (condensed keymap footer + right-aligned `? help`; `right_anchored_row.go` shares their geometry). The **keymap descriptor** (`keymap.go`, `keymapEntry` with `Core` / `RightAligned` / `Destructive` flags) is the single source for **display** — it drives both the footer and the `?` help body; `keymap_dispatch_guard_test.go` guards descriptor↔dispatch drift (dispatch is separate). Modals share a **blank-screen layer** (`modal.go` clears the page to the canvas + `placeModalOnClearedCanvas`) and a joined-panel frame (`panel.go`'s `renderJoinedPanel`): `help_modal.go` (descriptor-driven `?` help), `kill_modal.go` / `delete_modal.go` over the shared `destructive_confirm.go` builder, `rename_modal.go`, and the two-mode `edit_modal.go` (see grouping section). Notice bands route through a **single-slot arbiter** (`notice_band.go`) — the `▌` left-bar no-tags signpost / command-pending banner / inline flash (`sessions_flash.go`) — placed under the title separator, recomputing the list height when they appear/clear. Edge states: `empty_states.go`, `filtering_no_matches.go`. The honest loading screen is `loading_view.go` (+ `loading_progress.go`).
- **Keymap revision (§12.2).** Navigation is **arrows only** — all vim aliases (`h/j/k/l`, `g/G`), `PgUp/PgDn/Home/End`, and uppercase bindings are dropped (move `↑`/`↓`, page `Ctrl+↑`/`Ctrl+↓`); `pinArrowOnlyNav` strips them from the live `bubbles/list` keymap on **both** Sessions and Projects. `k` = kill (Sessions); `d` = delete (Projects); `x` toggles Sessions⟷Projects in both directions; `s` is Sessions-only (cycle grouping); `?` is newly bound (it was previously swallowed so `bubbles/list` couldn't self-toggle help) and opens the per-page help modal.

### Session grouping & tags

The sessions list renders in three modes cycled by the `s` key on the sessions page: **Flat** → **By Project** → **By Tag** → Flat. The cycle is unconditional (always includes By Tag, even with zero tags or zero sessions) and the last-used mode persists in `prefs.json` on every press (best-effort; failure non-fatal). The mode shows in the title via `SessionListTitle()` (`Sessions` / `Sessions — by project` / `Sessions — by tag`); the footer adds only an `s switch view` hint. While the `/` filter is focused `s` is a literal filter character.

- **Tags are directory-anchored (v1).** Tags live on the `Project` record (`projects.json`), not per session. A session's effective tags = its directory's tags, looked up live at grouped-render time. Tags are managed only in the projects edit modal (a Tags chip-field beside Name/Aliases), never per session and with no CLI. The edit modal (`edit_modal.go`, spec §8.2) is a **two-mode (navigate/edit) immediate-persist** model uniform across all three fields — a deliberate behaviour change from the old "tags persist live, Name/Aliases batch" asymmetry, so **Name and Aliases now persist immediately too**. Navigate mode: `Tab`/`Shift+Tab` (or `↑`/`↓`) move between fields, `←`/`→` move across chips + the trailing `+ add` slot (entering a chip field lands on `+ add`); `x` deletes a focused chip; `Esc` closes. Edit mode (one element live, entered by `Enter`/`e`, or `Enter`/`+` on `+ add` which spawns an empty chip already in edit): `Enter` **commits & persists that single item** (no dirty state, no save key, no batch), `Esc` discards just that edit (a brand-new empty chip vanishes). Falling-out rules: empty-on-commit deletes, empty Name reverts to prior, duplicate-on-commit silently dedupes (tags case-sensitive). `Esc` never discards saved work. A tag change reloads projects so the grouping index refreshes. Deferred: per-session `@portal-tags` + `--tag=`, live-grouped filtering, tag exclusion.
- **Session → directory resolution.** Each live session is mapped to its directory via the `@portal-dir` session user-option (stamped at creation from the git-root — by both `create.go` and `QuickStart`, so a session stays anchored to its origin dir even after the pane `cd`s away). When absent (legacy sessions created before the stamp shipped), the grouped render derives the directory from the active pane's `current_path` → git-root and uses it for *this* render, caching the guess **in-memory** (into `m.sessions`, so later rebuilds in the same picker session skip the pane read) — gated to grouped modes only; Flat and the zero-tags signpost perform zero pane reads. The guess is never stamped back to tmux (drift would freeze a wrong dir); it re-derives on the next picker launch, so a session that has `cd`'d back self-corrects. The lookup key and stored `Project.Path` are both reduced via `project.CanonicalDirKey`.
- **Render-layer grouping (the key invariant).** Group headings are **real, non-selectable `HeaderItem` rows** interleaved before each group's session rows by `injectGroupHeaders` — every list row (header or session) is exactly one delegate line, so `bubbles/list` pagination is exact and a page can never render more lines than the viewport (the earlier in-`SessionItem` heading injection drew uncounted extra lines, overflowing the frame so the terminal scrolled the title and cursor off the top — the original cursor-invisible / missing-title / left-shift bug). `HeaderItem.FilterValue()==""` so headers vanish the moment a filter query is typed (flatten-on-filter for free); `ensureSessionRowSelected` + `skipHeaderRow` keep the cursor on session rows for initial selection and arrow/paging nav across group boundaries (the §12.2 keymap revision dropped `g`/`G` and the other vim/page-jump aliases — see "Modern Vivid TUI"). Indentation (delegate): group headers are indented to align with the title box's left edge (`groupHeaderIndent`, col 2); grouped session rows nest one level further in (`groupRowIndent` — cursor at col 2, name at col 4) when `GroupKey != ""`; Flat rows render flush at col 2 as before. By Project is Pattern A (one item per session under its project name; key is canonical path). By Tag is Pattern B (one item per `(session, tag)` pair — a multi-tag session repeats under each heading). Unresolved/untagged sessions collect in pinned **Unknown** (By Project) / **Untagged** (By Tag) catch-alls (counted, empty-suppressed, each with their own `HeaderItem`). Zero-tags-anywhere By Tag shows a "No tags yet" signpost over the plain list (degrade-with-message, not silent flatten). `internal/tui/model.go`'s `rebuildSessionList` is the single chokepoint all entry points (the `s` toggle, `SessionsMsg` refresh, `ProjectsLoadedMsg`, and the projects-edit → sessions transition) route through. **Build constraint:** the picker must not be routed through `lipgloss/tree` — grouping is pure `lipgloss` styling layered into the existing `SessionDelegate`.

## Release

Uses goreleaser (`.goreleaser.yaml`). Version injected via ldflags: `-X github.com/leeovery/portal/cmd.version`. Tagged releases trigger GitHub Actions → homebrew tap update.
