AGENT: duplication
FINDINGS: none
SUMMARY: No significant cross-file duplication warranting consolidation was found in the topic's changed files. The implementation is small and deliberately DRY: it reuses shared renderers (spawn.UnsupportedNoopMessage via unsupportedFlashText), a single gate predicate (unsupportedBannerActive), and shared test helpers (unsupportedResolvedModel, appleTerminalIdentity/ghosttyIdentity/nativeResolve, markTwo, resolveDetection, keymapHasMKey, assertFlatFixtureSet), rather than re-authoring them per task. The near-duplicate patterns that do exist are all either below the Rule-of-Three threshold or explicitly mandated-separate by the spec — consolidating them would be a regression against design intent, not an improvement.

NOTES (examined and deliberately NOT flagged, so a later reader knows they were considered):

- Shape-selector IsNull() branching. Three functions branch on spawn.Identity.IsNull() to pick a remote-vs-named string: spawn.UnsupportedNoopMessage (internal/spawn/message.go:79, shared with the CLI open-burst), tui.unsupportedFlashText (internal/tui/burst_progress.go:460, delegates to the former — pre-existing, out of topic scope), and the NEW tui.multiSelectBlockedFlashText (internal/tui/model.go:3516). The control-flow shape is identical, but the copy families are distinct AND spec §5 explicitly requires multiSelectBlockedFlashText to be TUI-local and separate from the CLI-shared UnsupportedNoopMessage (block flash is intent-only "isn't available"; reactive no-op is "nothing opened"). Each is a 3-4 line pure function. Extracting a generic shape-selector would be premature abstraction (code-quality.md: "Avoid premature abstraction for code used once or twice") and would couple deliberately-decoupled copy. No action.

- Byte-literal copy strings repeated across test files. The plain-language unsupported strings ("can't open new windows over a remote connection — nothing opened", the named variant, and the two "multi-select isn't available…" strings) appear as byte-literals in internal/spawn/message_test.go, internal/tui/burst_unsupported_noop_test.go, internal/tui/multi_select_entry_block_test.go, and cmd/open_burst_run_test.go. This is DELIBERATE golden-string pinning — the tests self-document (open_burst_run_test.go:550-559) that the literals are intentionally NOT spawn.UnsupportedNoopMessage(id) so a copy drift from spec §5 is caught rather than silently tracked. Consolidating into a shared constant would defeat the regression barrier. No action.

- Shared predicate prefix DetectUnsupported() && !multiSelectMode. Appears in unsupportedBannerActive() (model.go:4737, plus && !IsNull()) and in sessionsHelpKeymap()'s mBlocked (model.go:4760). Two-term boolean sub-expression in two call sites, semantically distinct gates (named-banner-active vs m-is-a-dead-end). Below the Rule-of-Three / three-similar-lines threshold; a helper would add indirection without removing meaningful duplication. No action.

- Parallel fixture tests. TestSessionsUnsupportedTerminalFixture and TestSessionsUnsupportedNullFixture (internal/capture/capture_test.go:883,933) share ~10 lines of Deps()/InitialDetection/tui.Build/ActivePage boilerplate. This is idiomatic parallel test structure; the session-set assertion is already extracted (assertFlatFixtureSet). Not worth a shared driver for two mirror cases with divergent assertions. No action.

- Fixture clone pattern. sessionsUnsupportedNullFixture (fixtures.go:487) follows the established "fx := sessionsFlatFixture(); fx.name = …; fx.initialX = …; return fx" pattern used by five sibling fixtures — it reuses the base builder rather than duplicating the 12-session set. Correctly DRY.

Scope note: the recurring "m.emitUnsupportedNoop(m.detectIdentity); m.setFlash(unsupportedFlashText(m.detectIdentity))" pair in internal/tui/burst_progress.go (lines 443-444 and 495-496) was NOT assessed as a finding — burst_progress.go is not among this topic's changed files (Hard Rule 3, plan-scope only).
