AGENT: standards
FINDINGS:
- FINDING: Vestigial single-element table loop in TestUnsupportedHeader_ExactlyOneRow (residue of §6 NULL-branch removal)
  SEVERITY: low
  FILES: internal/tui/unsupported_banner_test.go:110-122
  DESCRIPTION: Spec §6 deleted the NULL banner render branch and the test that exercised it. This test still iterates an anonymous struct table with a single element `{"named", "com.apple.Terminal"}` — the loop was originally two cases (named vs null) and now varies over exactly one value, so the `for`/`t.Run`-less loop scaffolding no longer earns its keep. It is a harmless leftover of the removal, not a behavioural drift, but it reads as if a second case were intended. Every other assertion in the file correctly moved to named-only.
  RECOMMENDATION: Inline the single case (drop the one-element loop) so the test reads as the named-only assertion it now is, matching the rest of the file's named-only posture post-§6.
SUMMARY: The four TUI sub-fixes, the spawn message rewrite, the dead-NULL-branch removal, and all test rework/additions/fixture conform to the specification. Copy strings match the §5 table byte-for-byte (blocked-entry flash and UnsupportedNoopMessage, both shapes); the banner-split `!IsNull()` gate, the identity-blind `DetectUnsupported()` entry block with per-shape flash, the call-site `sessionsHelpKeymap()` filter gated on `DetectUnsupported() && !multiSelectMode`, and the retained reactive backstop all match §2–§4; §6 removal is complete (no `unsupportedNullLabel`, no `bundleID==""` render branches, `TestUnsupportedHeader_NullIdentityNoHostLocal` deleted); §7 tests were reworked to the in-flight path, the NULL fixture is registered and captured, the latent guard-coupling inline note is present, no `t.Parallel()`, no raw hex at call sites, no integration-lane leakage. Build and unit tests for internal/tui, internal/spawn, internal/capture pass. Only one low-severity test residue found.
