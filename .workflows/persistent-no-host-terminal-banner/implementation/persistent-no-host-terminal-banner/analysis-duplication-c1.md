AGENT: duplication
FINDINGS:
- FINDING: CLI unsupported-burst no-op test duplicates the pre-existing atomic-no-op test almost verbatim
  SEVERITY: medium
  FILES: cmd/open_burst_run_test.go:522-598 (TestRunOpenBurst_UnsupportedTerminal_CopyIsPlainLanguage), cmd/open_burst_run_test.go:477-520 (TestRunOpenBurst_UnsupportedTerminal_AtomicNoop)
  DESCRIPTION: The new task-3-2 test re-implements, near line-for-line, the entire arrange block (events / inner / adapter / conn / mint / openBurstDepsForTest / the bursterBuilt NewBurster spy / the two-attach surfaces slice) AND the six no-op invariant assertions (err != nil, err.Error() == want, !bursterBuilt, len(inner.Calls)==0, len(conn.calls)==0, len(mint.calls)==0) already present in the pre-existing AtomicNoop test in the same file. That is ~30 lines duplicated. The ONLY substantive divergence is intentional and spec-mandated (§5/§7): the new test asserts against a hardcoded byte-literal `want` (to catch spec copy drift) plus a NULL row, whereas the old test asserts against the computed `spawn.UnsupportedNoopMessage(id)` (to catch picker/CLI renderer drift). The two distinct assertions are worth keeping; the surrounding arrange+assert scaffold is not worth duplicating. Notably the tui side already factored the identical no-op invariants into a shared `assertAtomicNoOp` helper (internal/tui/burst_unsupported_noop_test.go:56) — the cmd side has `openBurstDepsForTest` but no equivalent assert helper, so this boilerplate was copy-pasted across the task boundary instead of extracted.
  RECOMMENDATION: Extract a shared cmd-side helper mirroring the tui side's assertAtomicNoOp — e.g. `runUnsupportedOpenBurstNoOp(t, id) (err error, inner *spawntest.FakeAdapter, conn *recordingConnector, mint *recordingMint, bursterBuilt bool)` that owns the arrange + Execute, plus an `assertOpenBurstAtomicNoOp(...)` for the five structural invariants. Have both TestRunOpenBurst_UnsupportedTerminal_AtomicNoop and the new CopyIsPlainLanguage table cases call it, keeping only their two divergent `err.Error()` assertions (computed vs byte-literal) at the call sites. This preserves the deliberate two-purpose drift-detection split while removing the duplicated scaffold.

- FINDING: Repeated unsupported-burst model arrange block re-inlined in the new preflight test file
  SEVERITY: low
  FILES: internal/tui/burst_preflight_before_unsupported_test.go:68-75, internal/tui/burst_preflight_before_unsupported_test.go:101-108, internal/tui/burst_observability_test.go:36-41 (obsTwoSessions helper)
  DESCRIPTION: The new task-2-2 file inlines the 4-5 line arrange block twice — `sessions := []tmux.Session{{Name:"alpha",Windows:1},{Name:"bravo",Windows:2}}; ack := &spawntest.FakeAckChannel{}; adapter := &spawntest.FakeAdapter{Ack: ack}; m := NewModelWithSessions(sessions); wireUnsupportedBurstSeams(&m, adapter, ack)`. The identical alpha/bravo session slice already has a shared constructor, `obsTwoSessions()`, and the seam-wiring (`wireUnsupportedBurstSeams`) and mark step (`markTwo`) are already shared helpers — yet the slice literal and the ack/adapter/NewModelWithSessions plumbing were copy-pasted rather than routed through a small constructor. This is low impact: the meaningful seams are already extracted and the block matches an established convention that predates this fix (the same block appears in burst_unsupported_noop_test.go), so the residual duplication per call site is small.
  RECOMMENDATION: Optional consolidation — add a `newUnsupportedBurstModel(t) (Model, *spawntest.FakeAdapter, *spawntest.FakeAckChannel)` helper (returning a model with obsTwoSessions() wired via wireUnsupportedBurstSeams) and use it from the new preflight tests. Because the pattern predates this topic across the burst test suite, treat this as a convention cleanup rather than a required change for this fix; do not expand it into a refactor of the pre-existing noop/observability tests unless the wider suite is being touched.
SUMMARY: The source changes (message.go, model.go, section_header.go, fixtures.go) are clean with no new duplication — the two new IsNull()-branching flash helpers are deliberately distinct copy per spec. The duplication is confined to the test surface: one medium finding where the new CLI copy-regression test re-implements the full arrange+assert of the existing atomic-no-op test (the cmd side lacks the shared assert helper the tui side already has), and one low finding where the new preflight test file re-inlines an already-shared burst-model arrange block.
