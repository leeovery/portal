AGENT: standards
FINDINGS: none
SUMMARY: Implementation conforms to specification and project conventions. All four coordinated TUI sub-fixes plus the shared spawn message rewrite match the spec exactly: the IsNull() banner-split discriminator (unsupportedBannerActive = DetectUnsupported() && !multiSelectMode && !detectIdentity.IsNull()), the proactive DetectUnsupported() entry block in handleMultiSelectToggle with the visible per-shape blocked-entry flash (Fork B1) and retained reactive backstop (Fork A1), the call-site help filter gated on DetectUnsupported() && !multiSelectMode (sessionsKeymap() left a pure static constant so keymap_dispatch_guard stays green), and the Topic 6 removal of the dead NULL render branch / unsupportedNullLabel / TestUnsupportedHeader_NullIdentityNoHostLocal (renderUnsupportedHeader/unsupportedLeftCluster are now named-only). All user-facing copy is byte-exact against the §5 copy set — the em-dash (U+2014), the middot (U+00B7), the plain-language UnsupportedNoopMessage (both shapes), and the two TUI-local blocked-entry strings — and the named block flash honours the non-repetition constraint (no identity / no `see docs`). The latent guard-coupling inline note (spec §4/§8) is present at the entry-block gate. Tests were reworked per §7 (in-flight-before-resolve entry, NULL-standard-header inversion, copy assertions, help-suppression cases a/b/c, named two-row co-render with both ⚠ glyphs) and the sessions-unsupported-null fixture + tape were added per the Visual requirement. No t.Parallel() in the changed test surface; copy functions asserted with exact equality; no raw hex at call sites; no new log components or attr keys; no state/daemon/prefs footprint introduced.
