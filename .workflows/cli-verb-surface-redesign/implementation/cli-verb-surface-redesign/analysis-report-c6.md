---
topic: cli-verb-surface-redesign
cycle: 6
total_findings: 4
deduplicated_findings: 4
proposed_tasks: 1
---
# Analysis Report: CLI Verb Surface Redesign (Cycle 6)

## Summary
Cycle 6 surfaced 4 findings (1 duplication, 1 standards, 2 architecture) — no cross-agent duplicates, so all four are distinct groups. Exactly one is a genuine, low-risk, non-churn improvement: retarget four in-source comments that still cite redesign-deleted files (`cmd/state_cleanup.go`, `cmd/attach.go`) as live consumers/provenance, which misdirect anyone tracing those seams. The other three are verbatim or reframed recurrences of items already assessed-and-rejected across cycles 1–5 (the doctor stale-classification mirror, the stringly-typed open-target domain switch, and the host-terminal Detector seam not routing through the shared bundle) — each re-evaluated on this cycle's framing and discarded again, with the prior rationale reaffirmed and the new framing addressed below.

## Dedup / Grouping Notes
- No cross-agent duplicates this cycle; the two architecture findings, the one duplication finding, and the one standards finding are all distinct groups (distinct subjects and distinct fixes).
- Cross-cycle dedup: duplication #1 recurs from cycles 1/2/4/5; architecture #1 recurs from cycles 2/3/5; architecture #2 is the open-burst analog of the doctor seam raised and comment-fixed in cycle 5 (Task 1). Standards #1 (the four stale file-name comments) is verified new — those specific `internal/tmux` / `internal/resolver` sites were not caught by the cmd-layer comment sweeps of cycles 1–2.
- Standards #1 normalised into Task 1 (comment-only). All four cited sites were verified against current source: `cmd/state_cleanup.go` and `cmd/attach.go` are both deleted; the surviving consumers are `cmd/uninstall.go` (`buildUninstallDeps` for `UnregisterPortalHooks`; `killSaver` for the `_portal-saver` `KillSession`) and `internal/tmux/portal_saver.go`.

## Discarded Findings
- doctor's diagnose re-derives the stores' `CleanStale` stale classification (duplication, low) — Verbatim recurrence of the finding discarded in cycles 1, 2, 4, and 5. The cycle-6 framing proposes the same `StaleRecords()` / `StaleKeys()` read-only-classifier extraction cycles 4/5 already assessed and rejected, and the duplication agent itself flags the blocker (the hooks path "must keep doctor's empty-live-set hazard guard at the caller, not inside the shared classifier") and offers "leave as-is" as acceptable. The hooks half is genuinely not cleanly extractable: the empty-live-set hazard-guard policy is caller-specific — `runHookStaleCleanup`/daemon must defer-and-warn (never delete) while `doctor.checkStaleHooks` must report `checkNotEvaluable` (never "all stale") — so a shared classifier returning a plain stale list would make doctor report `checkPass "no stale hooks"` on an empty live set, regressing the load-bearing honesty property the in-source comments call out. The projects half is cleanly extractable but small (~10-line `os.Stat` switch, at/below the proportionality bar), and each block is a deliberately-documented "READ-ONLY mirror". No materially new argument this cycle; the divergence risk is latent (predicates currently consistent), not live. Low severity, so never-discard-high does not apply. Discarded, unchanged.
- Open-target routing domain is a bare string with a default-less `resolveOpenSurfaces` switch (architecture, medium) — Recurrence of the domain-enum-promotion finding discarded in cycles 2 (A3), 3, and 5 (where it was rated low). Cycle 6 escalates to medium and adds a consistency argument (the sibling `spawn.SurfaceKind` is a typed enum and `globExpandableDomain` carries a `default:false`, so "the codebase already knows the pattern"). Assessed on its own merit: the escalation is not backed by a genuinely-new reachable-bug argument — the "silently drops with no error and no log line" and missing-`default:`-arm points were already the basis of cycle 5's low rating, and reachability is unchanged (`openTargetPins` emits exactly the five handled domains, and the map↔cobra pairing is drift-guarded by `TestOpenTargetPinsCoverValueTakingFlags`). The consistency observation is not a correctness argument: the domain strings ("session"/"path"/"alias"/"zoxide") deliberately match the spec's `resolve` log-taxonomy `domain` attr values, so promoting them to a typed enum is churn against a spec-governed vocabulary, and the default-arm-only variant is defensive code against a test-guarded, provably-closed, unreachable state. Medium, not high — never-discard-high does not apply. Discarded, prior rationale reaffirmed.
- Host-terminal Detector seam is constructed three ways, so the shared bundle doesn't single-source it (architecture, low) — New angle on the seam whose doctor instance was comment-fixed in cycle 5 (Task 1); this cycle targets the open burst, which defaults its `Detector` via `spawnDetector(cmd)` rather than reading `sharedSeams().Detector`. Verified against source: `spawnDetector` is a *deliberate* lean detector-only default that avoids the bundle's eager `terminals.json` read (`buildResolver`), is explicitly test-guarded (`cmd/open_burst_seams_test.go:100` asserts the Detector default "should route through spawnDetector"), and its comments are accurate — they say "the same detector," not a false shared-bundle claim, so unlike cycle 5's doctor case there is no accompanying comment-accuracy defect to fix. The finding's only proposed change is the structural collapse (retire `spawnDetector`, route through the bundle), which trades that deliberate laziness property and breaks a guarding test for marginal abstraction-tidiness — churn against working, tested, deliberately-lazy DI wiring in a 6th converging cycle, directly parallel to the doctor structural collapse cycle 5 explicitly scoped out. Low severity. Discarded.
