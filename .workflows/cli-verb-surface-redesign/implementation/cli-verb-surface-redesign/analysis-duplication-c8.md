AGENT: duplication
FINDINGS:
- FINDING: `doctor`'s read-only stale diagnosis re-derives the same stale-entry classification (and hazard guard) the prune path already owns
  SEVERITY: low
  FILES: cmd/doctor.go:413-441 (checkStaleHooks), cmd/doctor.go:443-458 (countStaleHookKeys), cmd/doctor.go:465-491 (checkStaleProjects), cmd/run_hook_stale_cleanup.go:90-141 (runHookStaleCleanup, incl. hazard guard 119-126), internal/hooks/store.go:249-298 (CleanStale, pre-existing), internal/project/store.go:191- (CleanStale, pre-existing)
  DESCRIPTION: `portal doctor` diagnoses stale entries and `portal doctor --fix` prunes them, and the two are contractually meant to agree on the same subject — but each computes "what is stale" independently. For hooks this now duplicates across TWO plan files, not just plan-to-preexisting: `checkStaleHooks` (doctor.go) is explicitly documented as "a strictly READ-ONLY mirror of runHookStaleCleanup's mass-deletion hazard guard" and re-implements that whole skeleton (load persisted → list live keys → if live empty && persisted non-empty defer/not-evaluable → else classify), while `countStaleHookKeys` re-implements the exact `key ∉ liveSet` membership rule that `hooks.Store.CleanStale` (the set `--fix` actually prunes via runHookStaleCleanup) uses at store.go:265-271. For projects, `checkStaleProjects` re-implements verbatim the `os.Stat` three-way switch `project.Store.CleanStale` uses to select removals (`err == nil` → live; `errors.Is(err, os.ErrNotExist)` → stale; `default` → retained). This is the parallel-computation-that-must-stay-in-sync shape code-quality.md's "Compose, Don't Duplicate" warns against: the diagnosis and the repair run through different code, so any future change to the stale rule or the hazard-guard ordering on one side (e.g. treating permission-denied as stale, key normalisation) silently desyncs the reported count from what `--fix` deletes — inside a single command whose contract is that `--fix` repairs exactly what it diagnoses. Kept low because each block is small (~10-15 lines), the logic is simple/stable, the hazard-guard ordering is heavily commented as a deliberate mirror, and this exact coupling was flagged in cycle 6 and evidently accepted; re-surfaced only because it is still present on a full fresh pass and now spans plan-to-plan for the hooks arm.
  RECOMMENDATION: Have the diagnosis DERIVE the stale set from the same source the prune consumes rather than recomputing it. Hooks: add a read-only classifier (e.g. `hooks.Store.StaleKeys(live []string) []string` or an unexported `staleKeys` helper) that returns the ∉-live set; `CleanStale` selects removals via it and `countStaleHookKeys` counts `len()` of it, so "which key is stale" lives once (keep the empty-live-set hazard guard at the callers — doctor's not-evaluable vs runHookStaleCleanup's defer differ, but both can gate on one shared "is the live set trustworthy?" predicate). Projects: mirror with a read-only `project.Store.StaleRecords() ([]Project, error)` consumed by both `CleanStale` and `checkStaleProjects`. If the store-method churn on the two pre-existing files is judged not worth the payoff, leave as-is — the divergence risk is real but small — but do not add a further independent copy of either classifier.
- FINDING: `completeSessionNames` and `completeAliasKeys` are byte-identical prefix-filter completers differing only in their candidate source
  SEVERITY: low
  FILES: cmd/completion.go:40-48 (completeSessionNames), cmd/completion.go:81-89 (completeAliasKeys)
  DESCRIPTION: Both completers are the same loop — allocate `matches`, range the candidate slice, keep entries `strings.HasPrefix(x, toComplete)`, and return `(matches, cobra.ShellCompDirectiveNoFileComp)`. The only differences are the source function (`completionSessionNames()` vs `completionAliasKeys()`) and the loop-variable name. The prefix-filter-and-NoFileComp contract (which cobra does NOT apply itself for dynamic completers, per both functions' own comments) is thus authored twice; a change to that contract (e.g. case-insensitive prefix, or a different ShellCompDirective) would have to land in two places. This is only a 2-instance repetition, so it sits below code-quality.md's Rule-of-Three threshold ("avoid premature abstraction for code used once or twice") — flagged as a clean, low-cost consolidation candidate rather than a must-fix, especially since a third Portal-owned completion namespace would immediately make it a Rule-of-Three case.
  RECOMMENDATION: Extract a single `prefixCompletions(candidates []string, toComplete string) ([]string, cobra.ShellCompDirective)` helper holding the loop + the `ShellCompDirectiveNoFileComp` return, and reduce both completers to `return prefixCompletions(completionSessionNames(), toComplete)` / `return prefixCompletions(completionAliasKeys(), toComplete)`. The two source seams (`completionSessionNames` / `completionAliasKeys`) legitimately stay distinct (one builds a DefaultClient, the other loads the alias store) — only the identical filter body is consolidated.
SUMMARY: The feature's two-callers-of-one-service core (the multi-target `open` burst and the picker burst) is exemplary — Burster, PartitionResults/FirstPermission, SplitTriggerFirst/SplitNetN, every message renderer (message.go) and log emitter (logemit.go), and the resolve-decision line are all single-sourced in internal/spawn so the two paths provably cannot drift. The only residual duplication is doctor's read-only stale diagnosis paralleling the prune classification it is supposed to agree with (low; carried over from c6, now spanning plan-to-plan for hooks), and the two identical prefix-filter completers in completion.go (low; a 2-instance below-Rule-of-Three consolidation candidate).
