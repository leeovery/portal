AGENT: standards
FINDINGS: none
SUMMARY: Full fresh pass over the CLI verb-surface redesign — the public verb surface (open + pins + burst, kill, doctor, uninstall, hook), the hidden state namespace, the retired attach/spawn/clean/state-status/state-cleanup verbs, the resolve-decision log line, the closed spawn/resolve log taxonomy, the pinned-domain hard-fail contract, and unit-lane test isolation — all conform to the specification and project Go/Cobra conventions. No spec drift or MUST-DO/MUST-NOT-DO violation found.

Notes on items specifically verified against the spec's "pay attention to" list (all pass, recorded so the dismissal is on record):

1. Closed log taxonomy — the new `resolve` component is bound exactly once (`var resolveLogger = log.For("resolve")`, cmd/open.go) and emitted only from `emitResolveDecision` with message "resolved" and the locked attr set target/domain/resolved_path (cmd/open.go:435). internal/resolver is emission-free: its one `internal/log` import (gitroot.go) is the exec-boundary error helper `CombinedOutputWithContext`, not a slog emitter, so the "resolution driven from cmd/open.go, resolver stays a log-free library" intent holds. The spawn component's emitters (internal/spawn/logemit.go) are the single source for both burst callers and use only the spec-governed keys batch/terminal/bundle_id/resolution/session/ack/opened/total/detail; the TUI picker burst (internal/tui/burst_observability.go) delegates to them rather than inventing call-site attrs.

2. Pinned-domain hard-fail — every pin (-s/-p/-z/-a) resolves in one domain and hard-fails on a miss with no picker fallback (resolver ResolveSessionPin/ResolvePathPin/ResolveAliasPin/ResolveZoxidePin return errors, never MissResult; resolvePinAndOpen propagates them). Pinned -z surfaces ErrZoxideNotInstalled verbatim, distinct from the bare chain's silent fall-through. Multi-target pin misses are collected and aborted atomically (no picker).

3. Resolve-decision line — INFO level, guessing-chain targets only, glob and pin targets gated out (HasGlobMeta gate lives inside emitResolveDecision, shared by the single-target and burst bare arms), emitted on a miss (domain=miss, empty resolved_path), one line per guessing-chain target in a burst.

4. Test isolation — the changed unit-lane test files (open_multitarget_test.go, open_burst_run_test.go, doctor_test.go, uninstall_test.go, open_test.go) spawn no daemons and exec no built binaries; they drive command bodies through the injected *Deps seams, so the cmd TestMain TMUX/PORTAL_* poison keeps them hermetic. No machine-global process op introduced.

Additional conformance spot-checks that pass: attach/spawn deleted outright (retired_surface_test.go proves absence across child/alias/help/completion); clean and state status/cleanup gone; `hooks`→`hook` with a silent (non-Deprecated) cobra alias; state namespace Hidden; doctor & uninstall in skipTmuxCheck (bootstrap-exempt); doctor exit-code contract (checkFail/checkUnknown drive non-zero, checkInfo/checkNotEvaluable never do; down server unhealthy; host-terminal informational); doctor --fix down-server hook-prune safety delegated to runHookStaleCleanup's hazard guard; command passthrough scoped to mint surfaces only (composeOpenArgv) with byte-identical carry; net-N trigger-connects-last ordering; miss message "nothing resolved for '<t>' — try -f <t>" (U+2014); bare `portal` prints help (no Run/RunE); --ack hidden + validated top-of-RunE; SilenceUsage/SilenceErrors set on root; errors routed through main.classify with UsageError→2 and ErrDoctorUnhealthy silent→1.
