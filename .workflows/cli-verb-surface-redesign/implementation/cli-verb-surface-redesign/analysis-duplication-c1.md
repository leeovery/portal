AGENT: duplication
FINDINGS:
- FINDING: Four near-identical domain-pin dispatch blocks in openCmd.RunE
  SEVERITY: high
  FILES: cmd/open.go:222-233 (-s/session), cmd/open.go:246-257 (-p/path), cmd/open.go:271-282 (-a/alias), cmd/open.go:295-306 (-z/zoxide)
  DESCRIPTION: The four single-pin dispatch arms are structurally identical ~11-line blocks. Each does: `if cmd.Flags().Changed(<flag>)` → `val,_ := cmd.Flags().GetString(<flag>)` → `qr, err := buildQueryResolver(cmd); if err { return err }` → `result, err := qr.Resolve<Domain>Pin(val); if err { return err }` → `return openResolved(cmd, result, command)`. Only two things vary per block: the flag name string and the resolver method (ResolveSessionPin / ResolvePathPin / ResolveAliasPin / ResolveZoxidePin). ~44 lines of copy-paste that must stay in lockstep — a future change to the pin dispatch contract (e.g. threading a new arg into openResolved, or a new error-wrapping step) has to be applied four times, the classic copy-paste-drift-across-task-boundaries hazard (each pin arrived in a separate Phase-2 task). This is the highest-density repetition in the feature.
  RECOMMENDATION: Extract a single helper, e.g. `resolvePinAndOpen(cmd *cobra.Command, flag string, resolve func(*resolver.QueryResolver, string) (resolver.QueryResult, error), command []string) error`, and drive the four arms from a small ordered table of `{flag, resolveFn}` pairs (or four one-line calls). Each `Resolve*Pin` already has a uniform `(string) (QueryResult, error)` signature, so the closures are trivial. Collapses ~44 lines to a ~10-line helper plus four call sites and removes the four-way drift surface.

- FINDING: Single-target "nothing resolved" miss error string authored in two places
  SEVERITY: medium
  FILES: cmd/open.go:344, cmd/open_burst.go:125
  DESCRIPTION: The user-facing single-target miss message `fmt.Errorf("nothing resolved for '%s' — try -f %s", …)` (em-dash U+2014) is written verbatim in two independent sites: the bare-positional path in openCmd.RunE and the N=1 glob-expanding-to-zero arm of dispatchOpenBurst. Confirmed byte-identical (same em-dash, same format). The message wording + the `-f` escape-hatch suffix are a spec-governed contract (spec § Miss handling); duplicating the literal invites exactly the drift the codebase already guards against elsewhere — the sibling multi-target message IS single-sourced as `aggregatedMissError` (cmd/open_burst.go:91-93), and other user-facing strings are single-sourced (`unknownAliasError`, `killSaverInfoMessage`, `uninstallCompletionLine*`). This single-target message is the odd one out.
  RECOMMENDATION: Add a `singleMissError(target string) error` helper (co-located with the existing `aggregatedMissError` in cmd/open_burst.go) returning the one canonical format string, and call it from both sites. Mirrors the existing single-sourcing pattern for the multi-target variant.

- FINDING: Session-glob expansion block duplicated across resolver *All variants
  SEVERITY: medium
  FILES: internal/resolver/query.go:196-207 (ResolveBareAll), internal/resolver/query.go:226-236 (ResolveSessionPinAll); related skeleton at query.go:258-276 (ResolveAliasPinAll) and single-match forms query.go:134-141 (Resolve) / query.go:303-309 (ResolveSessionPin)
  DESCRIPTION: The "expand a glob against a name set into K SessionResults" block is near byte-identical in ResolveBareAll and ResolveSessionPinAll: `matches := MatchSessions(pattern, names)` → `if len(matches) == 0 { return []QueryResult{&MissResult{Target: pattern}}, nil }` → `results := make(...)` → `for _, m := range matches { results = append(results, &SessionResult{Name: m, Domain: "glob"}) }` → `return results, nil`. The same expand-then-zero-check-then-build skeleton recurs a third time in ResolveAliasPinAll (over `aliases.Keys()`, building validatedPath results) and the first-match single-result variant recurs in Resolve and ResolveSessionPin. `MatchSessions` is already shared, but the K-result wrapper around it is not — so the glob-to-SessionResult reduction lives in ≥2 identical copies plus structural siblings, a drift risk if the glob Domain tag or zero-match handling ever changes.
  RECOMMENDATION: Extract a `expandSessionGlobAll(pattern string, names []string) []QueryResult` (zero matches → single MissResult; else K SessionResult{Domain:"glob"}) consumed by both ResolveBareAll and ResolveSessionPinAll. Optionally a first-match variant for Resolve/ResolveSessionPin. The alias-domain expansion is a genuinely different body (per-key dir validation) so leave it, but the two session-domain copies should collapse to one.

- FINDING: resolve-decision log emission duplicated across single- and multi-target bare paths
  SEVERITY: low
  FILES: cmd/open.go:333-336, cmd/open_surfaces.go:64-68
  DESCRIPTION: The spec-governed `resolve` component INFO line is authored in two places: `if !resolver.HasGlobMeta(query) { domain, resolvedPath := resolveDecision(result); resolveLogger.Info("resolved", "target", query, "domain", domain, "resolved_path", resolvedPath) }` in the single-target bare path, and the same glob-gate + resolveDecision + `resolveLogger.Info("resolved", …)` emission for each "bare" target in resolveOpenSurfaces. This is a locked observability contract (spec § Wrong-guess feedback: attr keys target/domain/resolved_path, one line per guessing-chain target); two independent emission sites can drift the attr set or the glob gate apart.
  RECOMMENDATION: Extract a small `emitResolveDecision(target string, result resolver.QueryResult)` helper (co-located with `resolveDecision` in cmd/open.go) that applies the `!HasGlobMeta` gate and emits the single canonical `resolveLogger.Info` line, called from both the single-target RunE path and resolveOpenSurfaces' bare arm.

- FINDING: doctor read-only staleness checks re-implement the stores' CleanStale classification
  SEVERITY: low
  FILES: cmd/doctor.go:440-452 (countStaleHookKeys), cmd/doctor.go:459-485 (checkStaleProjects)
  DESCRIPTION: The new doctor checks re-author the exact staleness classification the stores already own, as parallel computations that must be kept in sync (the "Compose, Don't Duplicate" hazard in code-quality.md). countStaleHookKeys reproduces hooks.Store.CleanStale's "persisted key ∉ live set" membership test; checkStaleProjects reproduces project.Store.CleanStale's os.Stat classification (nil→live, ErrNotExist→stale, other error→retained). The code comments explicitly acknowledge each is a "READ-ONLY mirror" of the store's mutating method — an honest deliberate mirror, but the read-only copy and the mutating original can silently diverge on any future rule change (e.g. a new "retained" error class). Note: the fix touches the pre-existing store methods, which is why this is low rather than a clean in-scope extraction.
  RECOMMENDATION: Where cheap, have each store expose a pure predicate/counter (e.g. `hooks.CountStale(persisted, live) int`, `project.IsStaleDir(path) bool`) consumed by BOTH the store's CleanStale and doctor's read-only check, so classification lives in one place. If refactoring the stores is deemed out of scope, at minimum keep this drift risk on record.

- FINDING: exec-handoff process:exec marker emission duplicated
  SEVERITY: low
  FILES: cmd/open.go:121-134 (AttachConnector.Connect), cmd/open.go:551-564 (PathOpener.Open)
  DESCRIPTION: Both exec paths hand-roll the same pre-exec marker: drop argv[0] (the "tmux" program name) from the argv, then `log.For("process").Info("exec", "target", "tmux", "args", strings.Join(<argv[1:]>, " "))`, followed by the syscall.Exec handoff. Two ~5-line copies of the identical "log the exec marker just before replacing the process image" idiom (both carry the same "unbuffered writer, no Sync needed" reasoning comment). Small, but it is a load-bearing forensic tripwire (the terminal marker on the bare-shell handoff) that should read identically on both paths.
  RECOMMENDATION: Extract a `logExecHandoff(argv []string)` helper that strips argv[0] and emits the single canonical process:exec line, called from both AttachConnector.Connect and PathOpener.Open immediately before Exec.

- FINDING: completion prefix-filter loop duplicated
  SEVERITY: low
  FILES: cmd/completion.go:40-48 (completeSessionNames), cmd/completion.go:84-92 (completeAliasKeys)
  DESCRIPTION: The two completers share an identical body: iterate a candidate slice, keep entries where `strings.HasPrefix(candidate, toComplete)`, return `(matches, cobra.ShellCompDirectiveNoFileComp)`. Only the candidate source seam (completionSessionNames vs completionAliasKeys) differs. Just two instances (Rule of Three not yet met), so this is a watch-item rather than a mandate — but a third Portal-owned completion namespace would tip it over.
  RECOMMENDATION: If a third completer appears, extract `prefixFilter(candidates []string, toComplete string) ([]string, cobra.ShellCompDirective)`. For now, acceptable as-is; noted for the record.

SUMMARY: One high-impact extraction (the four copy-paste domain-pin dispatch arms in openCmd.RunE) plus two medium drift-risk duplicates (a byte-identical single-target miss string across open.go/open_burst.go, and the session-glob expansion block duplicated across the resolver *All variants). The rest are low-severity two-site emissions of governed contracts (resolve log line, exec marker) and read-only mirrors of store CleanStale logic worth single-sourcing to prevent divergence.
