AGENT: standards
FINDINGS: none
SUMMARY: Full fresh pass over the current tree (cmd/open*.go, kill/doctor/uninstall/hook/state/completion, internal/resolver, internal/spawn, process_role, and CLAUDE.md) against the CLI-verb-surface-redesign specification found no spec-conformance gaps or project-convention violations. Every governing decision is implemented faithfully: the single public `open` verb with the exact-session→path→alias→zoxide precedence, the glob pre-check (session-domain by construction, zero-match hard fail), user-visible-only session matching (leading-underscore-filtered ListSessionNames), the four domain pins (`-s/-p/-z/-a`, each hard-fail-never-picker, `-z` surfacing ErrZoxideNotInstalled), `-f` as the sole non-composing picker redirect (mutually-exclusive guard + empty-value guard), mint-scoped command passthrough with all four usage-error cases (`-e`+`--` coexist, empty `-e`, command-on-attach, zero-mint+command), and no-target+command → Projects-mode picker. The multi-target burst honours net-N/absorb-first/connect-last, atomic read-only pre-flight with aggregated miss reporting (no `-f` suffix at N≥2; `-f` suffix retained only at N=1 via the shared singleMissError with the U+2014 em-dash), leave-what-opened partial failure, per-window ack, and byte-identical command parity (single `-e` string carried as one argv unit to trigger local mint and spawned `open --path <dir> --ack … -- cmd` windows). `attach`/`spawn` are deleted outright with no aliases; `clean`/`state status`→`doctor`, `state cleanup`→`uninstall`; `hooks`→`hook` with the silent permanent `hooks` cobra alias; `state` namespace hidden; `doctor`/`uninstall`/`hook` bootstrap-exempt (skipTmuxCheck keyed on canonical Name()); bare `portal` prints help. doctor's exit-code contract (0 iff every checkFail-free; host-terminal informational-only; not-evaluable never drives exit), the down-server stale-hook data-loss guard (runHookStaleCleanup mass-deletion hazard deferral, spec §312), and the log-taxonomy amendment (`resolve` component bound only in cmd/open.go with attr keys target/domain/resolved_path, internal/resolver kept log-free, gated on !HasGlobMeta and never emitted for pins) are all present. Tab-completion slots (session names for bare/`-s`/`kill`; alias keys for `-a`; shell default for `-p`/`-z`) match the spec table. CLAUDE.md was updated to describe the new surface (no stale `portal attach`/`spawn`/`clean` references remain in production Go or docs); the retained dead `roleClean` process-role arm and dead-arm comment are deliberately documented (pruning a closed process_role value is itself a taxonomy amendment) and out of this spec's scope. `go build ./...` is clean.
