---
topic: cli-verb-surface-redesign
cycle: 12
total_findings: 4
deduplicated_findings: 4
proposed_tasks: 0
---
# Analysis Report: cli-verb-surface-redesign (Cycle 12)

## Summary
Cycle 12 is a deep convergence-tail pass: standards is CLEAN for the fourth consecutive cycle, and every raw finding across duplication and architecture is low-severity (one architecture item self-labelled medium is a correct-today API-surface smell, not a defect). After merit-based triage against the code and the established posture, all four findings are discarded — each is either a below-Rule-of-Three near-duplicate already discarded in prior cycles, a coincidental string match whose consolidation would create awkward cross-boundary coupling, a re-raise of a broad restructure already deliberately scoped out at cycle 8, or a self-described safe/test-guarded note requiring no action. Zero tasks proposed; the topic remains converged.

## Discarded Findings
- Prefix-match completer body authored twice (completion.go:40-48 / :81-89) — Verified against code: still EXACTLY 2 instances (completeSessionNames, completeAliasKeys), no third caller. Below the Rule-of-Three; discarded as a legitimate 2-instance pattern in cycles 8, 9, 10, and 11. Same posture applies — discard again.
- "No session found: %s" authored in cmd/kill.go:39 and internal/resolver/query.go:326 — Coincidental string match, not a genuine single-source opportunity. The two carry DIFFERENT semantic contracts: kill's is a kill-target-doesn't-exist validation (predates the redesign, owns its own wording); the resolver's is the `-s/--session` pin miss, byte-locked to the RETIRED `attach` command for byte-compat (a planner decision). They are not "the same message" — they share text by accident. Hoisting to one authoring site would couple cmd↔resolver and constrain a future edit of one contract by the other's, inverting the intent. Sibling resolver misses ("No alias found", "No zoxide match") are already single-sourced within the resolver; this cross-package pair is correctly left as an accepted two-site string. Discard.
- Resolver single/multi (*All-vs-single-pin) method families compose inconsistently; collected-miss contract split across resolver↔cmd (self-labelled medium) — RE-RAISE of cycle 8's architecture finding, which was DELIBERATELY SCOPED DOWN when task 14-2 (the exact-session-match helper) was authored; the broader "*All-vs-single-pin surface symmetry restructure" was EXPLICITLY declared out of scope then. The finding itself states the code is "correct today (well test-guarded)" and calls it an "API-surface smell," not a defect. The recommendation (have ResolveSessionPinAll/ResolveAliasPinAll wrap their single-pin siblings AND give path/zoxide a new collected-miss entry point so resolveOpenSurfaces makes one uniform call) IS the broad method-surface restructure already judged out of scope — there is no new, smaller, self-contained slice that wasn't already considered. A restructure of the resolver's method surface is a larger architectural change than a convergence-tail remediation cycle should introduce late; not high-severity, so discarding is permitted. Discard as already-considered-and-correctly-deferred.
- Multi-target routing depends on a hand-rolled argv rescan (invocation-shape assumptions) — Self-described as "currently safe," "risk is latent, not active": no value-taking persistent flags exist, openOwnArgs degrades to nil (single-target path) rather than a wrong open, and open_targets_guard_test.go pins the flag mirror. The finding's own recommendation is "No structural change required now given the guard test and safe degradation; keep it on the radar." This is a NOTE, not an actionable defect. Discard.
