AGENT: duplication
FINDINGS:
- FINDING: Command-on-attach usage-error string authored twice across the single/multi-target guards
  SEVERITY: medium
  FILES: cmd/open.go:326, cmd/open_burst_run.go:151
  DESCRIPTION: The exact user-facing string `"a command (-e/--) can only run in a newly-created session, not an existing one"` is hardcoded in two separate NewUsageError call sites — openResolved's *SessionResult arm (single-target attach-command guard) and runOpenBurstWithDeps's zero-mint-surface guard (the multi-target arity of the same rule). open_burst_run.go's own comment calls itself "the multi-target arity of the single-target attach-command guard (openResolved's *SessionResult arm)", confirming the two are the same message by intent. This is the lone shared open/burst message string NOT single-sourced: every sibling shared message in this feature is centralised (singleMissError/aggregatedMissError in open_burst.go; GoneMessage/PartialFailureMessage/UnsupportedNoopMessage in internal/spawn/message.go; the doctorRuntimeNotRunning, killSaverInfoMessage, burstPreSpawnErrorFlash, uninstallCompletionLine1/2 consts). A reword at one site would silently diverge from the other — the exact copy-paste-drift class the codebase deliberately closes elsewhere.
  RECOMMENDATION: Hoist the literal to a single package-level const in cmd (e.g. `const commandAttachOnlyMessage = "a command (-e/--) can only run in a newly-created session, not an existing one"`) and have both guards return `NewUsageError(commandAttachOnlyMessage)`. Matches the established const-for-shared-message convention already used by doctorRuntimeNotRunning / killSaverInfoMessage. Logic stays per-site; only the string is consolidated.

- FINDING: Near-identical prefix-filter bodies in the two shell-completion helpers
  SEVERITY: low
  FILES: cmd/completion.go:40, cmd/completion.go:84
  DESCRIPTION: completeSessionNames and completeAliasKeys have byte-identical bodies apart from the candidate source func and the loop variable name: allocate `var matches []string`, range the source slice, keep entries where `strings.HasPrefix(candidate, toComplete)`, and return `matches, cobra.ShellCompDirectiveNoFileComp`. Both re-implement cobra's prefix filtering (the comments note cobra does not prefix-filter dynamic completion returns) independently. Only two instances today (below the strict Rule of Three), but the two are exact structural twins and the extraction is zero-risk.
  RECOMMENDATION: Extract a tiny pure helper, e.g. `func prefixMatches(candidates []string, toComplete string) []string`, and have both completers do `return prefixMatches(completionSessionNames(), toComplete), cobra.ShellCompDirectiveNoFileComp`. Keeps the two source seams and the NoFileComp directive at the call sites while single-sourcing the filter loop.

- FINDING: Repeated server-down guard shape across the three doctor runtime checks
  SEVERITY: low
  FILES: cmd/doctor.go:490, cmd/doctor.go:517, cmd/doctor.go:547
  DESCRIPTION: checkDaemonAlive, checkSaverUp, and checkHooksRegistered each open with the identical three-line guard `if !serverUp { return checkResult{name: name, status: checkFail, detail: doctorRuntimeNotRunning} }` (three instances — the Rule-of-Three boundary). The detail string is already single-sourced via the doctorRuntimeNotRunning const, but the guard-and-return shape itself is copy-pasted. Marginal by proportionality (3 short lines each), noted as a clean, low-risk tidy rather than a high-impact block.
  RECOMMENDATION: Optional — factor a one-line helper `func notRunningResult(name string) checkResult { return checkResult{name: name, status: checkFail, detail: doctorRuntimeNotRunning} }` and replace the three guard returns with `if !serverUp { return notRunningResult(name) }`. Leave as-is if the team prefers each check to read fully self-contained.
SUMMARY: One medium finding — the command-on-attach usage-error string is hardcoded in both the single-target and multi-target guards, the sole shared open/burst message not centralised the way every sibling message is — plus two low, near-zero-risk tidies (twin completion prefix-filters; the repeated doctor server-down guard shape). The two burst pipelines and the resolver All-variants are otherwise already extensively single-sourced through internal/spawn shared helpers, so no structural duplication remains.
