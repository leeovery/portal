AGENT: standards
FINDINGS:
- FINDING: Stale source comments reference files the redesign deleted
  SEVERITY: low
  FILES: internal/tmux/hooks_unregister.go:15, internal/tmux/hooks_unregister.go:96, internal/tmux/tmux.go:382, internal/resolver/query.go:307
  DESCRIPTION: The redesign deleted cmd/state_cleanup.go (→ portal uninstall) and cmd/attach.go (→ portal open --session), but several in-source comments still name those non-existent files as the live consumers. hooks_unregister.go:15 says UnregisterPortalHooks is "consumed as a function value by cmd/state_cleanup.go" (the real caller is now cmd/uninstall.go's buildUninstallDeps), hooks_unregister.go:96 repeats "value by cmd/state_cleanup.go", tmux.go:382 lists "internal _portal-saver callers (cmd/state_cleanup.go, …)", and query.go:307 justifies a staticcheck directive as "silenced per the directive attach.go carried" (attach.go is deleted). The spec framed this as one intentional full-surface pass (spec § Overview / § uninstall — replaces state cleanup / § attach — Retired); the code moved correctly but the provenance comments now point readers at files that no longer exist, which will misdirect anyone tracing the seam. Purely documentation drift — no behavioural impact.
  RECOMMENDATION: Retarget the comments to the surviving consumers: cmd/state_cleanup.go → cmd/uninstall.go in hooks_unregister.go:15/:96 and tmux.go:382; and reword query.go:307 to reference the retained house-style directive without pointing at the deleted attach.go (the "No session found" verbatim message is preserved for byte-compat with the retired attach — say that instead).
SUMMARY: The cli-verb-surface-redesign implementation conforms to the specification across every major decision — the open verb collapse (attach/spawn absorbed), Axiom 1 net-N burst, Axiom 2 attach-vs-mint, glob pre-check, precedence chain, user-visible session set, domain pins with -z-absent explicit error and never-fall-back-to-picker contract, -f mutual exclusion, mint-scoped command with zero-mint usage error and no-word-splitting parity, hidden best-effort --ack, atomic multi-target pre-flight reporting every miss, the resolve log-component amendment (INFO, guessing-chain only, on-miss too), tab completion tiering, kill single+exact, uninstall byte-exact runtime-only teardown, doctor exit-code contract with informational host-terminal line and down-server hook-prune guard, state namespace hidden-but-invocable, hooks→hook rename with silent alias, doctor+uninstall bootstrap exemption, and bare-portal-prints-help. The only drift found is low-severity: stale comments still cite deleted files (state_cleanup.go, attach.go).
