AGENT: standards
FINDINGS: none
SUMMARY: Implementation conforms to the cli-verb-surface-redesign specification and project conventions. A full fresh pass over the touched surface (cmd/open*, resolver, spawn, doctor, uninstall, hooks, kill, completion, state, root, log/process_role, main) found no material spec-conformance or convention drift.

VERIFICATION NOTES (evidence the pass was exhaustive, not gaps):
- Governing principle & axioms: single public `open` verb; precedence chain exact-session→path→alias→zoxide→miss (resolver/query.go Resolve); Axiom 2 attach-vs-mint with no find-or-create (SessionResult→attach, PathResult→always mint). Glob pre-check routes session-domain-by-construction (glob.go HasGlobMeta; open_burst.go isMultiTarget).
- User-visible session set only: all session-domain resolution + completion source ListSessionNames (leading-underscore filtered); `open _portal-saver` is a miss. (query.go, completion.go)
- Miss = hard fail, no TUI fallback (confirmed removed); single-target keeps `-f <t>` hint, multi-target aggregated abort lists every unresolvable target with no `-f` (open.go singleMissError, open_burst.go aggregatedMissError/dispatchOpenBurst). Em-dash U+2014 exact.
- resolve log line: component `resolve` bound only in cmd/open.go via log.For("resolve"); attrs target/domain/resolved_path; INFO; gated on !HasGlobMeta; emitted once per bare guessing-chain target incl. MissResult; pins & globs emit none (emitResolveDecision, single-sourced across single + burst paths). log.For has no runtime allowlist, so the amendment needs no registry wiring.
- Domain pins -s/-p/-z/-a hard-fail, never picker; -z surfaces ErrZoxideNotInstalled verbatim while bare chain swallows it (query.go ResolveZoxidePin vs Resolve). -p reaches glob-named dirs by pinning literal path.
- -f sole non-composing flag: mutually exclusive with target and every pin, checked before resolution and pins (open.go RunE); `-f -e` variant carries command to Projects picker.
- Command passthrough mint-scoped: -e/-- mutual-exclusion + empty guard (parseCommandArgs); attach+command usage error single-sourced (commandAttachOnlyMessage) across single-target *SessionResult arm and multi-target zero-mint guard (hasMintSurface); command byte-identical/no word-split to every mint surface incl. trigger local mint (composeOpenArgv, connectTrigger); no-target+command → Projects picker.
- Hidden --ack: MarkHidden; validated at top of RunE (usage error before any tmux call); best-effort write is last act before handoff, still execs on failure (writeAckMarker DEBUG-logs and proceeds).
- Multi-target burst: ordered target set recovered from raw os.Args preserving interleave + repeats (open_targets.go orderedOpenTargets; guard test cited); trigger absorbs FIRST target and self-connects LAST; N−1 spawned first (SplitTriggerFirst, runOpenBurstWithDeps); net-N never N+1; no dedup; atomic read-only pre-flight (resolveOpenSurfaces); per-window ~8s ack timer from own spawn (burst.go awaitToken); leave-what-opened with trigger-connect independence; unsupported terminal → atomic no-op.
- Spawned-window contract: `open --session <name> --ack` / `--path <dir> --ack`; pinned hard-fail; alias/zoxide reduced to literal dir before travel (Surface, composeOpenArgv).
- kill single+exact unchanged (ExactArgs(1), no glob, no confirm). uninstall runtime-only: killSaver before unregister, touches no files, idempotent no-op on down server, byte-exact two-line message matching spec, leaves all sessions incl. _portal-bootstrap. doctor: catalog present, exit 0 iff all pass (doctorUnhealthy counts only checkFail), host-terminal checkInfo, --fix re-diagnoses and drives exit solely off post-repair, down-server stale-hook guard via not-evaluable/empty-live-set, log-sweep outside exit contract, ErrDoctorUnhealthy silent-exit. Both doctor & uninstall bootstrap-exempt (skipTmuxCheck).
- state namespace Hidden with prefix + child names preserved; hooks→hook rename with permanent silent cobra alias (no cmd.Deprecated); bare `portal` prints help (no root Run/RunE); no back-compat aliases for attach/spawn/clean; removed command files gone with no lingering registrations or user-facing references; tab-completion slots match (session-name / alias-key completers; -p/-z left to shell).
- Conventions: error-string casing exceptions ("No session found" etc.) are the deliberate spec-mandated byte-identical-parity choice carrying //nolint:staticcheck — spec-vs-idiom conflict correctly resolved in the spec's favour. cobra SilenceUsage/SilenceErrors on root cover the tree; RunE used throughout; cmd.OutOrStdout/ErrOrStderr used; no os.Exit in RunE. Build compiles clean.

NON-FINDINGS deliberately not raised (proportional rule): doctor renders saver before hooks whereas the spec lists hooks before saver — the spec defines an unordered catalog/set, not a locked render order, so this is cosmetic. A bare positional that is syntactically path-like but points at a nonexistent dir returns DirNotFoundError (hard error, no resolve line) rather than a MissResult — pre-existing inherited resolver behaviour, consistent with the `-p` "dir must exist" semantics, and not a decision this redesign's tasks introduced; the spec's "emitted on a miss too" refers to MissResult, which is emitted.
