AGENT: duplication
FINDINGS:
- FINDING: Twin prefix-filter bodies in the two shell-completion helpers
  SEVERITY: low
  FILES: cmd/completion.go:40, cmd/completion.go:84
  DESCRIPTION: completeSessionNames and completeAliasKeys have byte-identical bodies apart from the candidate source seam and the loop variable name: allocate `var matches []string`, range the source slice, keep entries where `strings.HasPrefix(candidate, toComplete)`, and return `matches, cobra.ShellCompDirectiveNoFileComp`. Both independently re-implement cobra's prefix filtering (the comments note cobra does not prefix-filter dynamic completion returns). Only two instances (below the strict Rule of Three), but they are exact structural twins in the same file and the extraction is zero-risk. This is the one mechanical near-duplicate left in a feature that otherwise single-sources every shared message and count-semantics decision through internal/spawn. (Also flagged in cycle 3; still present.)
  RECOMMENDATION: Extract a tiny pure helper, e.g. `func prefixMatches(candidates []string, toComplete string) []string`, and have both completers do `return prefixMatches(completionSessionNames(), toComplete), cobra.ShellCompDirectiveNoFileComp` / `prefixMatches(completionAliasKeys(), toComplete)`. Keeps the two source seams and the NoFileComp directive at the call sites while single-sourcing the filter loop. Optional — leave as-is if the two-instance count is judged below the extraction bar.

- FINDING: Repeated server-down guard shape across the three doctor runtime checks
  SEVERITY: low
  FILES: cmd/doctor.go:490, cmd/doctor.go:517, cmd/doctor.go:548
  DESCRIPTION: checkDaemonAlive, checkSaverUp, and checkHooksRegistered each open with the identical three-line guard `if !serverUp { return checkResult{name: name, status: checkFail, detail: doctorRuntimeNotRunning} }`. The detail string is already single-sourced via the doctorRuntimeNotRunning const, so only the guard-and-return shape is copy-pasted. Three instances at three short lines each — right at the Rule-of-Three count but well under the proportionality bar the quality standard sets ("three similar lines is not worth extracting; three similar 20-line blocks is"). Noted as a clean optional tidy, not a high-impact block. (Also flagged in cycle 3; still present.)
  RECOMMENDATION: Optional — factor a one-line helper `func notRunningResult(name string) checkResult { return checkResult{name: name, status: checkFail, detail: doctorRuntimeNotRunning} }` and replace the three guard returns with `if !serverUp { return notRunningResult(name) }`. Equally acceptable to leave as-is if each check is preferred to read fully self-contained.
SUMMARY: The feature is exceptionally well-consolidated — the two burst orchestrations (cmd/open_burst_run.go, internal/tui/burst_progress.go) and the resolver All-variants share every message, count-semantics, and log-emission decision through single-source helpers in internal/spawn (SplitTriggerFirst/SplitNetN, PartitionResults, FirstPermission, LogBatchSummary, GoneMessage, etc.) and cmd (emitResolveDecision, singleMissError/aggregatedMissError, commandAttachOnlyMessage, runHookStaleCleanup). Cycle 3's medium finding (command-on-attach string authored twice) is now resolved via the commandAttachOnlyMessage const. Only two low, near-zero-risk, optional tidies remain — the twin completion prefix-filters and the repeated doctor server-down guard — both below the extraction bar.
