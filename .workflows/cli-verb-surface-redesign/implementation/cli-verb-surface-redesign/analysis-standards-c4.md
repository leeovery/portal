AGENT: standards
FINDINGS:
- FINDING: `open` command Use/Short help text still describes the pre-redesign single-destination command
  SEVERITY: low
  FILES: cmd/open.go:152, cmd/open.go:153
  DESCRIPTION: The redesign's governing principle is "the public surface names what happens," and bare `portal` now prints help/usage (a new spec requirement, § Bare `portal`), so the help surface has elevated importance. But `openCmd.Use` = "open [-e cmd] [destination] [-- cmd args...]" and `Short` = "Open the interactive session picker or start a session at a path" still read like the pre-redesign command: they omit the entire attach/session-name capability, the domain pins (-s/-p/-z/-a/-f), and the multi-target (absorb/net-N) burst that absorbed `attach` + `spawn`. The Command Surface Summary row for `open` (spec §405) specifies "single public session verb; no-args → picker; flags -s/-p/-z/-a/-f, -e/--; absorb/net-N; hidden --ack" — a user reading `portal open --help` or `portal --help` gets a misleading picture of the flagship redesigned verb. The individual pin flags DO carry accurate descriptions (open.go:976-979), so only the top-level Use line and Short summary are stale. The spec does not dictate exact help copy, hence low severity; flagged because it partially undercuts the redesign's legibility intent at the surface the redesign made more prominent.
  RECOMMENDATION: Update `Use` to reflect the target-set shape (e.g. "open [targets…]" with a supporting Long description covering session names, path/alias/zoxide resolution, the -s/-p/-z/-a pins, and multi-target) and revise `Short` to name what the verb does now (open N portals to N targets; picker with no args). No behavioral change.
SUMMARY: The implementation conforms to the CLI-verb-surface-redesign specification across every substantive decision — single public `open` verb with the full precedence chain and domain pins, absorb/net-N burst with trigger-first-absorb / connect-last, atomic read-only pre-flight reporting every miss, mint-only command scoping with the zero-mint usage error, hidden `--ack` and hidden `state` namespace, the `resolve` log component (spec-governed, bound only in cmd/open.go with the locked target/domain/resolved_path attrs), `attach`/`spawn`/`clean` deletion, `kill` single+exact, `uninstall` runtime-only teardown with byte-exact completion copy, `doctor` (+ `--fix`) with the exit-code contract, informational host-terminal line, and down-server hook-prune data-loss guard, `hooks`→`hook` with the silent alias, bootstrap exemption for doctor/uninstall, bare `portal`→help, tab-completion slots, and the closed log-component/attr vocabulary. The only drift is one low-severity stale help string on the `open` command.
