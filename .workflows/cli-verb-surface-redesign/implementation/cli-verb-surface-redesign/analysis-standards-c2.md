AGENT: standards
FINDINGS:
- FINDING: Bare `portal` still classified as the TUI picker in the process-role taxonomy
  SEVERITY: low
  FILES: internal/log/process_role.go:66-68, internal/log/process_role.go:44, internal/log/process_role_test.go:32
  DESCRIPTION: The redesign made a deliberate, spec-level decision (spec § "Bare `portal` (no subcommand)"): bare `portal` prints help/usage and does **not** launch the picker — the control-plane root of the two-tier `x` = launcher / `portal`/`xctl` = management-plane split. ResolveProcessRole still maps the no-subcommand case to `roleTUI`, with the in-source comment "Bare `portal` (no subcommand token at all) is the TUI picker" and the routing-table comment "open … / x … / bare -> tui". process_role_test.go:32 pins `{"bare portal", []string{}, "tui"}`. Both the comment and the classification describe removed behaviour: bare `portal` is now closer to the control-plane root than to the TUI. Impact is forensic-only (process_role is a per-record log attr for multi-writer disambiguation; bare `portal` returns cobra's ErrHelp before PersistentPreRunE and emits only process start/exit markers), so there is no functional defect — but it is a genuine drift from the collective intent and a stale comment describing a retired surface.
  RECOMMENDATION: Update the comment to stop calling bare `portal` "the TUI picker" (it prints help now). If the taxonomy is to reflect the new semantics, reclassify the no-subcommand path to `roleBootstrap` (the control-plane default) rather than `roleTUI`, and adjust the pinned test case; `open`/`x` remain the only `tui` roles. If the classification is deliberately retained (bare invocation is rare and forensically inert), at minimum correct the two stale comments so they no longer assert removed behaviour.

- FINDING: CLAUDE.md incident-of-record cites the removed `state cleanup` command and a deleted test
  SEVERITY: low
  FILES: CLAUDE.md:114
  DESCRIPTION: The "Incident of record #2" note in the test-isolation section states that "two cmd tests (`TestVersionGuard_NotInvokedForExemptCommands`, `TestStateUserFacingSubcommandsExitZero`) ran the real `portal state cleanup` body uninjected". After the redesign, `portal state cleanup` no longer exists (subsumed by `uninstall`) and `TestStateUserFacingSubcommandsExitZero` is absent from the tree (only `TestVersionGuard_NotInvokedForExemptCommands` remains). The passage reads as a current example of a still-runnable command body, but the named surface and one of the two named tests are gone. The underlying lesson (Execute a real command body ⇒ inject every tmux-touching `*Deps`) is still valid, so this is a documentation-freshness issue in a historical note, not a behavioural gap.
  RECOMMENDATION: Either mark the incident explicitly as pre-redesign history (so the removed `state cleanup` / deleted test read as period detail, not current guidance) or re-anchor the example on a surviving command body, so a future reader does not go looking for a `state cleanup` body or the removed test.

SUMMARY: The implementation conforms to the CLI-verb-surface-redesign specification across every material decision point — single public `open` verb with the full grammar/pins/`-f`/`-e`/`--`/hidden `--ack`, Axiom-2 attach-vs-mint resolution, glob pre-check, user-visible session-set filtering, aggregated atomic pre-flight, trigger-absorbs-first/connects-last net-N burst, mint-only command parity, retired `attach`/`spawn`/`clean`/`state status`/`state cleanup`, `doctor`(+`--fix`)/`uninstall` with correct exit-code and bootstrap-exemption contracts, `hooks`→`hook` with the silent alias, fully-hidden `state` namespace, bare-`portal`-prints-help, and the spec-governed `resolve` log component. Only two low-severity, non-functional drifts remain: a stale bare-`portal`="TUI picker" classification/comment in the process-role taxonomy and a CLAUDE.md incident note referencing the removed `state cleanup` command and a deleted test.
