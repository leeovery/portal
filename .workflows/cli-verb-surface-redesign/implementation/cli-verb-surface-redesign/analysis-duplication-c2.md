AGENT: duplication
FINDINGS:
- FINDING: doctor's read-only stale checks re-author the stores' CleanStale classification
  SEVERITY: medium
  FILES: cmd/doctor.go:407-452 (checkStaleHooks + countStaleHookKeys), cmd/doctor.go:459-485 (checkStaleProjects), cmd/run_hook_stale_cleanup.go:100-140 (runHookStaleCleanup guard+classify), internal/hooks/store.go:257-271 (CleanStale ∉-live partition), internal/project/store.go:202-213 (CleanStale os.Stat switch)
  DESCRIPTION: The new `doctor` diagnosis independently reimplements the exact stale-classification logic that the repair paths already own, creating parallel copies that must stay in lockstep. (1) `checkStaleProjects` re-authors `project.Store.CleanStale`'s three-way `os.Stat` switch verbatim — `nil → live`, `os.ErrNotExist → stale`, `default → retained` — line-for-line the same classification. (2) `checkStaleHooks` + `countStaleHookKeys` re-author both the `∉ liveSet` membership classification from `hooks.Store.CleanStale` AND the empty-live-set mass-deletion hazard guard that `runHookStaleCleanup` (the designated single-source-of-truth) already implements — so the hazard-guard/deferral rule now exists in two places (the prune path in run_hook_stale_cleanup.go and the diagnose path in doctor.go). This matters because `doctor --fix`'s actual prune runs the real `CleanStale`/`runHookStaleCleanup`, while `doctor` (diagnosis) runs the re-authored copy: a future change to what "stale" means (key normalisation, a different stat policy, an adjusted hazard rule) updates one side and silently drifts the diagnosis from the repair — doctor could report "3 stale" that `--fix` does not prune, or vice versa. The doctor comments explicitly acknowledge these are hand-maintained "READ-ONLY mirror[s]," which is precisely the drift class the Compose-Don't-Duplicate standard warns against.
  RECOMMENDATION: Extract the classification into a shared read-only primitive each store owns and both paths consume. For projects: add `project.Store.StaleProjects() ([]Project, error)` (the `os.Stat` switch) and have `CleanStale` select removals from it and `checkStaleProjects` count from it. For hooks: add `hooks.Store.StaleKeys(live []string) []string` (the `∉ liveSet` partition) consumed by `CleanStale`, and have `checkStaleHooks` derive its stale count plus its hazard-guard/not-evaluable decision from the same `runHookStaleCleanup` guard logic rather than a re-mirror — so diagnosis and repair can never disagree.
- FINDING: doctorPaneCount is a byte-identical copy of state.countPanes
  SEVERITY: low
  FILES: cmd/doctor.go:635-643 (doctorPaneCount), internal/state/status.go:127-135 (countPanes)
  DESCRIPTION: `doctorPaneCount(idx state.Index)` and the pre-existing unexported `state.countPanes(idx Index)` are the identical triple-loop pane counter (`for sessions { for windows { total += len(w.Panes) } }`). doctor.go already imports `internal/state` and reads `state.Index` via `state.ReadIndex`; it only re-declares the counter because the state package's version is unexported. This is a small but exact duplicate introduced by the new doctor code.
  RECOMMENDATION: Export `state.CountPanes(idx Index) int` from internal/state (have the internal `countPanes` call it, or rename it) and replace `doctorPaneCount` in cmd/doctor.go with a call to it — one counter for the whole tree.
SUMMARY: The redesign's spawn/burst and resolver code is well-consolidated through shared helpers (internal/spawn logemit/message/classify/split, emitResolveDecision, expandSessionGlobAll), but the new `portal doctor` reimplements stale-classification logic the stores' CleanStale/runHookStaleCleanup already own (a diagnose-vs-repair drift risk) and re-declares a byte-identical pane counter that state already has.
