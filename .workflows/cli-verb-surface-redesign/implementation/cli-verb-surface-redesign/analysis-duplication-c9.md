AGENT: duplication
FINDINGS:
- FINDING: Stale-entry classification re-derived in the read-only diagnosis instead of shared with the prune
  SEVERITY: medium
  FILES: cmd/doctor.go:415-460 (checkStaleHooks + countStaleHookKeys), cmd/doctor.go:467-493 (checkStaleProjects), cmd/run_hook_stale_cleanup.go:90-141 (runHookStaleCleanup)
  DESCRIPTION: `doctor`'s read-only stale-hook diagnosis independently re-implements the same two rules the prune path owns: (1) the mass-deletion hazard guard — "live pane set empty AND persisted hooks present ⇒ do not classify everything stale" — is hand-written in checkStaleHooks (doctor.go:429-437) and again in runHookStaleCleanup (run_hook_stale_cleanup.go:119-126); and (2) the persisted-keys-∉-live-set difference is re-rolled in countStaleHookKeys (doctor.go:448-460) while the prune computes the identical set difference inside hooks.Store.CleanStale. checkStaleProjects (doctor.go:467-493) is the same shape a second time: it re-implements project.Store.CleanStale's tri-state os.Stat classification (nil→live, ErrNotExist→stale, other→retained) read-only. This is precisely the "compose, don't duplicate" hazard in code-quality.md — the diagnosis is a parallel computation of the same stale set the repair acts on, kept in agreement only by hand-mirroring and comments ("a strictly READ-ONLY mirror of runHookStaleCleanup's mass-deletion hazard guard"). Because the whole reason the hazard guard exists is data-loss safety (never wipe non-reconstructable user on-resume hooks on a down server), a silent drift where `doctor` reports green but `--fix` prunes (or vice-versa) has real stakes. The diagnose→repair agreement the spec requires currently rests on discipline, not a single source.
  RECOMMENDATION: Extract one read-only stale-classification primitive per resource that both the diagnosis and the prune consume. For hooks, add e.g. `hooks.ClassifyStale(persisted, live) (stale []string, notEvaluable bool)` (folding in the empty-live-with-persisted hazard rule) and have both checkStaleHooks and hooks.Store.CleanStale/runHookStaleCleanup derive their sets from it, so "what is stale" and "when is it not-evaluable" live in exactly one place. Apply the same treatment to project staleness (a shared read-only os.Stat classifier behind both checkStaleProjects and project.Store.CleanStale). Only the in-scope doctor.go side needs authoring here; the pruners already exist and would delegate to the shared primitive.
- FINDING: Byte-identical prefix-match completion loops
  SEVERITY: low
  FILES: cmd/completion.go:40-48 (completeSessionNames), cmd/completion.go:81-89 (completeAliasKeys)
  DESCRIPTION: completeSessionNames and completeAliasKeys are byte-for-byte identical except the candidate-source seam and the loop variable name: both allocate a matches slice, keep every candidate with strings.HasPrefix(candidate, toComplete), and return (matches, cobra.ShellCompDirectiveNoFileComp). Both were introduced in this feature (the open positional/-s/-a and kill positional completion slots), so this is fresh parallel logic rather than legacy shape. Minor by size, but it is genuine copy-paste that would drift if the directive or match rule changed on only one.
  RECOMMENDATION: Extract a small shared helper, e.g. `func prefixMatchComplete(candidates []string, toComplete string) ([]string, cobra.ShellCompDirective)`, and have both completers call it with completionSessionNames() / completionAliasKeys() respectively — collapsing the duplicated loop and directive to one site.
SUMMARY: The burst/spawn/resolver machinery is well consolidated — the shared trigger-split, partition, permission, message, and log-emission shapes already live once in internal/spawn and both burst callers delegate to them. The one substantive residual duplication is that `doctor`'s read-only stale-hook/stale-project diagnosis re-derives the hazard guard and stale-set classification the prune path owns, so diagnose and repair agree only by hand-mirroring; a shared read-only classifier would make them provably consistent. A minor byte-identical completion loop is the only other extraction candidate.
