AGENT: standards
FINDINGS:
- FINDING: Bootstrap warnings still direct users to the deleted `portal state status` command
  SEVERITY: high
  FILES: cmd/bootstrap/errors.go:57, cmd/bootstrap/errors.go:67
  DESCRIPTION: The redesign deletes `state status` and subsumes it into `portal doctor` (spec § Command Surface Summary → Removed public commands: `portal state status` → `portal doctor`; § state Namespace — Fully Hidden: "state has zero user-facing children"). Two live, user-facing soft-bootstrap warnings still point at that removed command: CorruptSessionsJSONWarning emits "Check `portal state status` or ~/.config/portal/state/portal.log." and SaverDownWarning emits "Run `portal state status` for details." These are the exact remediation surfaces a user sees when sessions.json is corrupt or the save daemon fails to start (both drained to stderr / the TUI notice band from PersistentPreRunE — root.go references SaverDownWarning explicitly). `state` now has no `status` child (only daemon / hydrate / signal-hydrate / notify / commit-now / migrate-rename are registered), so `portal state status` resolves to cobra's "unknown command" error. The redesign therefore leaves a user-facing instruction that points at a command it just deleted — a real conformance gap, not a comment. (errors.go was not itself in the impl file set, but the redesign that removed the target command owns the dangling pointer, and the task explicitly asks for now-removed-surface references in the current tree.)
  RECOMMENDATION: Repoint both warning lines at the successor surface — `portal doctor` (e.g. "Run `portal doctor` for details." / "Check `portal doctor` or ~/.config/portal/state/portal.log.") — so the remediation guidance names a command that still exists.

- FINDING: CLAUDE.md still describes the pre-redesign CLI surface (removed commands present, new commands absent)
  SEVERITY: medium
  FILES: CLAUDE.md:37, CLAUDE.md:80, CLAUDE.md:88, CLAUDE.md:115, CLAUDE.md:128, CLAUDE.md:157, CLAUDE.md:176-182
  DESCRIPTION: Across the impl commits CLAUDE.md received a single one-line edit (adding the `resolve` log component); the rest of the file still documents the old surface the redesign removed and omits the new one entirely. Stale prose describes: `portal spawn`/`cmd/spawn.go`/`spawn --detect` and `portal attach`/`cmd/attach.go`/`--spawn-ack` (line 37, the whole "Multi-window spawn (`portal spawn` + multi-select)" section at 176-182); `portal clean`/`clean --logs` as the manual backstop (lines 88, 128, 157) and `cmd/clean.go` as the home of loadPrefsStore (line 80 — that file is deleted; loadPrefsStore now lives in cmd/clean.go's replacement); and the bootstrap-exempt set "all except version, init, help, alias, clean" (line 115 — `clean` is deleted; the real exempt set is now doctor/uninstall/state/hook/alias/init/help/version). There is zero mention of `portal doctor`, `portal uninstall`, the `hooks`→`hook` rename, the `open` multi-target absorb/net-N surface, or the hidden `--ack` flag. Because CLAUDE.md is injected as project instructions on every session (human and agent), it now actively misdescribes the command surface the redesign was an "intentional one-pass" audit of.
  RECOMMENDATION: Update CLAUDE.md's command-surface prose to the redesigned surface: replace the Spawn/Attach command paragraph and the "Multi-window spawn" section with the `open` multi-target burst + hidden `--ack`; document `portal doctor [--fix]` and `portal uninstall`; rename hooks→hook references and note the silent `hooks` alias; and correct the bootstrap-exempt list (drop `clean`, add `doctor`/`uninstall`).

- FINDING: In-code comments across internal packages reference deleted files and removed commands
  SEVERITY: low
  FILES: cmd/open_burst_run.go:194, cmd/open_burst_run.go:202, cmd/open_burst_run.go:209, cmd/doctor.go:372, cmd/open.go:643, cmd/open.go:886, internal/spawn/split.go:7, internal/spawn/split.go:28, internal/spawn/message.go:11, internal/spawn/classify.go:6, internal/spawn/resolver.go:56, internal/spawn/adapter.go:15, internal/spawn/ack.go:38, internal/spawn/doc.go:9, internal/spawn/identity.go:12, internal/tui/burst_progress.go:250, internal/tui/burst_progress.go:405, internal/tui/burst_partial_failure.go:38, internal/log/retention.go (multiple), internal/state/status.go:20
  DESCRIPTION: Numerous comments still cross-reference the now-deleted `cmd/spawn.go` ("DIVERGENCE FROM runSpawn (cmd/spawn.go)", "runSpawn's permission arm", "the spawn CLI (buildSpawnDeps)", "`portal spawn` burst", "cmd/spawn.go's runSpawn ordering") and removed commands (`portal attach --spawn-ack`, `spawn --detect`, `portal clean --logs`, `portal state status`). The spec keeps internal package NAMES out of scope (internal/spawn is unaffected), but these are cross-references to a file/commands that no longer exist, so they now misdirect a reader tracing the burst/logging/status flow (the CLI caller is now the `open` burst in cmd/open_burst_run.go, not runSpawn). Low impact — internal comments only — but they are stale removed-surface references.
  RECOMMENDATION: Sweep these comments to name the surviving callers/surfaces (the `open` burst / cmd/open_burst_run.go; `doctor --fix` for the log sweep; `portal doctor` for status), or drop the specific dead file/command names.

- FINDING: process_role.go carries a now-unreachable `clean` role and stale surface doc
  SEVERITY: low
  FILES: internal/log/process_role.go:13, internal/log/process_role.go:33-35, internal/log/process_role.go:69-70
  DESCRIPTION: ResolveProcessRole maps `path[0] == "clean"` to roleClean, and the doc comment lists "hooks …  -> hooks_cli" and "clean  -> clean" as live rows of the closed 6-value process_role space. `portal clean` is deleted by the redesign, so the `case "clean"` arm is now dead (no real invocation reaches it) and the doc's `clean` row / `hooks` spelling describe removed/renamed surfaces. The process_role taxonomy is a spec-governed closed set, so mechanically pruning the value would itself need an amendment — but at minimum the doc comment describes a surface the redesign removed. Harmless at runtime (the arm is simply never taken).
  RECOMMENDATION: Update the doc comment to reflect the redesigned surface (the `clean` role is now unreachable; `hook`/`hooks` both map to hooks_cli), and note the dead arm; leave the closed-space value in place unless a log-taxonomy amendment removes it.
SUMMARY: The CLI code conforms to the redesign spec — open multi-target burst, domain pins, hidden --ack, doctor (+--fix, exit-code + down-server guard + host-terminal-informational), uninstall, kill single+exact, hooks→hook with silent alias, fully-hidden state, bootstrap exemptions, tab completion, resolve log component, and daemon-owned stale-project pruning are all present and correct. The drift is confined to documentation/references to removed surfaces: one user-facing bootstrap warning still points at the deleted `portal state status` (high), CLAUDE.md still describes the pre-redesign surface (medium), and scattered internal comments plus a dead process-role arm reference removed commands (low).
