AGENT: duplication
FINDINGS:
- FINDING: Two near-identical writeRealLogLine test helpers across state_test and cmd packages
  SEVERITY: low
  FILES: internal/state/status_test.go:20-34, cmd/state_status_test.go:24-30
  DESCRIPTION: Both packages declare a writeRealLogLine helper that renders a fixture line via the production seam log.RenderLineForTest(t, ts.UTC(), level, component, msg) and writes it to a portal.log file. The load-bearing part — the format itself — is already centralised in the seam; neither helper re-implements the format, so the original drift defect cannot recur through these wrappers. They are not byte-identical: the state version takes a path and O_APPEND-accumulates (so a single test can write multiple lines), while the cmd version takes a dir and os.WriteFile single-shots a single line; they also carry near-duplicate doc comments. This is parallel test scaffolding, not parallel production logic — the kind of low-value, slightly-divergent duplication the rule-of-three and proportionality guidance both apply to.
  RECOMMENDATION: Leave as-is for this cycle. Consolidating would require a new shared test-helper package (the two helpers live in different packages and cannot import each other's _test files), and the project's test-helper convention is package-scoped (logtest, restoretest, portaltest, etc.). The cost of standing up a new shared package — plus reconciling the append-vs-overwrite and path-vs-dir signature differences into one API — outweighs the benefit of deduplicating ~10 lines of thin, format-free wrapper. If a third consumer of the render seam ever appears, revisit by hosting a single writeRealLogLine in an existing or new test-only package keyed off log.RenderLineForTest. The intentional centralisation of the format in internal/log already neutralises the regression risk this duplication would otherwise carry.
SUMMARY: The production code deliberately and correctly centralises the log-line format in internal/log (parse + render seam), eliminating the parallel-definition defect that caused the bug; the only residual duplication is two thin, format-free test-helper wrappers in separate packages, whose consolidation is not worth a new shared test package.
