AGENT: standards
FINDINGS: none
SUMMARY: Implementation conforms to specification and project conventions. All four hook-key derivation stages (registration via ResolveHookKey, both stale-cleanup sites via ListAllPaneHookKeys, restore baking via tmux.HookKey from saved state) derive the key by the identical prefer-@portal-id-else-name rule; the firing path bakes the saved key and never reads the live @portal-id (ordering trap avoided). Stamping is best-effort at both creation paths (CreateFromDir swallows gen+set errors; QuickStart omits the set-option step on gen failure). Schema adds portal_id with tolerant decode and no Version bump; capture appends #{@portal-id} as the trailing column with captureFieldCount bumped 10→11 and lifts the id from grouped[name][0]; restore re-stamps in createSkeleton when non-empty. The four stale doc-comments (PaneTarget, PaneTargetExact, StructuralKeyFormat, ListAllPanes) are retired and the load-bearing "changing this format invalidates hooks.json" invariant is transferred verbatim to HookKey/HookKeyFormat. AC 1-8 covered by tests; cross-site consistency, durability-across-repeated-reboots, cleanup-survival, no-regression-upgrade, multi-pane, and the in-TUI-rename-is-bare-RenameSession guards are all present. No t.Parallel() in any test file; build compiles clean. The 6-char NanoID token width is left as the spec-permitted implementation detail (spec: "existing NewNanoIDGenerator scheme, widened if warranted"). No standards or spec-conformance drift found.
