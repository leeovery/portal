AGENT: duplication
FINDINGS:
- FINDING: Restore integration-test harness re-inlined across task boundaries instead of reusing same-package helpers
  SEVERITY: medium
  FILES: internal/restore/rename_reboot_hook_integration_test.go:186-212, internal/restore/rename_reboot_hook_integration_test.go:304-329, internal/restore/rename_reboot_durability_integration_test.go:76-98, internal/restore/rename_reboot_durability_integration_test.go:257-299, internal/restore/multipane_legacy_integration_test.go:81-96, internal/restore/multipane_legacy_integration_test.go:193-213, internal/restore/multipane_legacy_integration_test.go:252-264, internal/restore/multipane_legacy_integration_test.go:381-421
  DESCRIPTION: All three restore integration files (3-5 headline, 3-6 durability, 3-7 multipane/legacy) live in the same package (restore_test) and were written to share leaf helpers (restoreWithMarker, findCapturedSession, verifyHookKeyed, persistIndex, seedScrollback, assertHookFireCount) — good reuse. But two multi-line blocks are re-inlined verbatim rather than extracted: (1) the per-test setup preamble — BuildPortalBinaryDir + PrependPATH + IsolateStateForTest + t.TempDir/PORTAL_STATE_DIR setenv + EnsureDir + hooksPath/PORTAL_HOOKS_FILE setenv — appears 5 times across the three files; (2) the "reboot half" — KillServer, the "list-sessions succeeded after kill-server" verify, EnsureServer, OpenTestLogger, construct restore.Orchestrator, restoreWithMarker, and the "restored session missing live pane 0:0" verify — appears 5 times. The 3-6 durability file already factored the reboot half into rebootAndHydrate (line 263) and the setup+persist into captureAndPersist/seedScrollback/persistIndex, but the 3-5 and 3-7 files (authored for separate tasks) do not consume rebootAndHydrate and instead re-inline the identical sequence. This is copy-paste drift across task boundaries: an env-var name change, a new required setup step, or a reboot-sequencing fix must be applied in ~5 places or the files diverge silently. Each duplicated block is ~15-25 lines, so it clears the Rule of Three by a wide margin.
  RECOMMENDATION: Promote the two blocks to shared package-level helpers alongside the ones these files already share. Extract an isolated-env setup helper (returning stateDir + hooksPath) covering the build/PATH/IsolateStateForTest/state-dir/hooks-file preamble, and reuse the existing rebootAndHydrate helper (from rename_reboot_durability_integration_test.go) from the 3-5 runRenameRebootFire body and the 3-7 TestMultiPaneLegacy_* tests. No behaviour change — pure consolidation of already-identical test scaffolding into the package's existing shared-helper set.
- FINDING: cmd stale-cleanup survival tests share a near-identical seed-and-assert body
  SEVERITY: medium
  FILES: cmd/hookkey_no_regression_upgrade_test.go:47-125, cmd/rename_restore_cleanup_survival_integration_test.go:79-168
  DESCRIPTION: TestHookKeyNoRegressionUpgrade_UnstampedNameKeyedHookSurvives and TestRenameRestoreCleanupSurvival_KeepsRestoredIdKeyedHook are structurally the same test written twice for two tasks. Both: create a live tmux session (tmuxtest.New + EnsureServer + new-session + WaitForSession), compute a liveKey via tmux.HookKey, assert it with assertLiveHookKeyPresent, seed a two-entry hooks.json ("<liveKey>": {"on-resume": ...} + the identical stale key const "gone-session:0.0") via newTempHooksStore, run the identical non-vacuous pre-Load guard asserting both keys present, call runHookStaleCleanup(client, store, nil, false, nil), then run the identical post-Load assertions ("liveKey survived / staleKey swept") with near-verbatim error-message text. The only material differences are one line (the survival test adds SetSessionOption to stamp @portal-id, the upgrade test omits it) and two extra assertLiveHookKeyAbsent lines. The two files already correctly share assertLiveHookKeyPresent, newTempHooksStore, and keysOf, which shows the intent to consolidate — the seed/guard/run/assert spine simply was not lifted. Divergence risk: a future change to the seed shape, the runHookStaleCleanup call signature, or the post-cleanup assertion contract must be mirrored in both bodies.
  RECOMMENDATION: Extract a single parameterized helper (e.g. assertStaleCleanupKeeps(t, client, liveKey, staleKey, seed)) capturing the shared seed → pre-Load guard → runHookStaleCleanup → post-Load "survives/swept" assertion spine; have both tests call it, with the stamped-vs-unstamped SetSessionOption and the extra assertLiveHookKeyAbsent pin remaining as the per-test deltas. This removes the duplicated spine while preserving each test's distinct precondition.
- FINDING: verifyRenameHookFiredOnce duplicates assertHookFireCount(..., 1) in the same package
  SEVERITY: low
  FILES: internal/restore/rename_reboot_hook_integration_test.go:385-395, internal/restore/rename_reboot_durability_integration_test.go:307-317
  DESCRIPTION: verifyRenameHookFiredOnce (3-5 file) reads the hook-fire file, counts "HOOK_FIRED" occurrences, and asserts exactly one. assertHookFireCount (3-6 durability file, same restore_test package) does the identical read-and-count and asserts an arbitrary want. verifyRenameHookFiredOnce is exactly assertHookFireCount(t, file, 1) — its own doc-comment even notes it "Mirrors reboot_roundtrip_test.go's verifyHookFiredOnce shape". Two functions in the same package now embody the same read-count-assert logic; the fixed-marker string and the count-mismatch message are maintained in two places.
  RECOMMENDATION: Delete verifyRenameHookFiredOnce and replace its one call site (runRenameRebootFire) with assertHookFireCount(t, hookFireFile, 1). Single read-count-assert implementation for the whole package.
SUMMARY: Production code is well-consolidated for this fix — the hook-key derivation is centralized in tmux.HookKey/HookKeyFormat, the stale-cleanup algorithm in the single runHookStaleCleanup helper, and PortalIDOption in one constant; the @portal-id literal repetition and the two literal-guard tests are justified import-cycle/package-boundary duplication (spec-mandated, tripwire-guarded) and are not flagged. The actionable duplication is confined to the integration/real-tmux test suites, where independently-authored task tests re-inline setup/reboot preambles and a stale-cleanup seed-and-assert spine that same-package helpers already partially cover.
