AGENT: architecture
FINDINGS:
- FINDING: Two name-based public tmux methods (ListAllPanes, ResolveStructuralKey) are now dead — repointed away by this fix, tested only by their own unit tests
  SEVERITY: low
  FILES: internal/tmux/tmux.go:869 (ListAllPanes), internal/tmux/tmux.go:323 (ResolveStructuralKey)
  DESCRIPTION: Before this fix, (*Client).ListAllPanes() fed hook-cleanup (via cmd/clean.go) and (*Client).ResolveStructuralKey() fed hook registration (via cmd/hooks.go). The fix repointed both production consumers to the new hook-key siblings — ListAllPaneHookKeys and ResolveHookKey — leaving the two name-based wrapper methods with ZERO production callers (confirmed: their only call sites are internal/tmux/tmux_test.go:1568-1760, their own unit tests). Note the distinction from StructuralKeyFormat itself, which IS still live: the surviving non-hook structural consumer (cmd/bootstrap/stale_marker_cleanup.go:141) calls ListAllPanesWithFormat(StructuralKeyFormat) directly, and ResolveStructuralKey's format is reused by ListPanesInSession — so the FORMAT constant is load-bearing but the two WRAPPER methods are not. The spec's Stage 2 asserted "the name-based StructuralKeyFormat / ListAllPanes remain available for any non-hook structural use," but in the shipped state no such use routes through the ListAllPanes()/ResolveStructuralKey() methods. This is not a correctness risk and the drift hazard is well-mitigated: both methods' doc-comments were correctly retired to say "NOT the hook key ... hook-key derivation lives in HookKey / HookKeyFormat," which actively steers a future caller away from re-establishing name-based keying (the spec's named "Missed key-producing site" primary risk). The residual is only that two public methods now carry unit-test-only coverage and "canonical structural key" framing while having no production role.
  RECOMMENDATION: Leave as-is unless a lint gate flags unused exported methods. If tidied, the cheapest safe move is to keep the doc-comments' explicit "NOT the hook key" steering (do not delete that guardrail) and, at most, add a one-line note on each method that it currently has no production caller so a future reader does not assume it is a live path. Do not delete StructuralKeyFormat or ListAllPanesWithFormat — both remain live for non-hook structural enumeration. No change is required for correctness.
SUMMARY: Architecture is sound. The hook-key/tmux-target seam is cleanly decoupled (HookKey/HookKeyFormat own hook identity; PaneTarget/StructuralKeyFormat own tmux targeting, with all four stale doc-comments retired per spec), the three @portal-id literal embeddings are now bound in lockstep by three fast tmux-less guards including the new cmd/portal_id_binding_guard_test.go source-of-truth cross-check (resolving the cycle-2 finding), the restore ordering trap is handled correctly (hookKey baked from SAVED state in collectArmInfos; the live re-stamp is best-effort and never read by the firing path), the merge-path PortalID copy is guarded (findOrAppendSession + its pin test), and both rename triggers plus repeated-reboot durability and cleanup-survival have end-to-end integration coverage. The only residual is two now-dead name-based wrapper methods, low severity and already de-risked by their retired doc-comments.
