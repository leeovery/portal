AGENT: duplication
FINDINGS:
- FINDING: Rename+reboot integration harness (env-setup + reboot/restore scaffold) copy-pasted across 5 test functions in 3 files of the same restore_test package
  SEVERITY: medium
  FILES: internal/restore/rename_reboot_hook_integration_test.go:186-206 (runRenameRebootFire setup) + :303-322 (reboot/restore block), internal/restore/rename_reboot_durability_integration_test.go:76-124 (setup) + :263-298 (rebootAndHydrate), internal/restore/multipane_legacy_integration_test.go:81-96 + :193-206 (PerPaneHookRouting), :252-264 + :326-337 (GracefulLegacyDegradation), :381-394 + :415-425 (UnstampedNoHookLandsOnBareShell)
  DESCRIPTION: Two ~15-20 line blocks recur near-verbatim across the three Phase-3 integration files, all in the SAME restore_test package (so extraction has no import-cycle or cross-package cost). Block A — the isolated-env preamble — is repeated 5 times: BuildPortalBinaryDir -> PrependPATH -> IsolateStateForTest -> stateDir:=t.TempDir()+Setenv("PORTAL_STATE_DIR")+EnsureDir -> hooksPath+Setenv("PORTAL_HOOKS_FILE"). Block B — the reboot/restore scaffold — is repeated 4-5 times: ts.KillServer() -> TryRun("list-sessions") negative guard -> EnsureServer -> OpenTestLogger -> &restore.Orchestrator{Client,StateDir,Logger} -> restoreWithMarker. The durability file (task 3-6) already factored Block B into rebootAndHydrate and the seed/persist steps into seedScrollback/persistIndex/captureAndPersist, but the sibling 3-5 harness and all three 3-7 functions re-inline the same logic instead of calling those helpers (or a shared setup helper). This is the classic copy-paste-drift-across-task-boundaries pattern the analysis targets: three executors independently wrote the same reboot ceremony, and the scrollback fixture bytes ("\x1b[31mred\x1b[0m\nbefore reboot\n") plus the 10s/50ms WaitForSkeletonMarkersCleared timings are duplicated literals that must stay in lockstep but have no single source. A future change to the reboot sequence (e.g. an added post-EnsureServer settle, or a new required env var) must be applied at 5 sites or the tests silently diverge.
  RECOMMENDATION: Promote the already-extracted 3-6 helpers to a single shared _test.go file in package restore_test (e.g. rename_reboot_harness_test.go) and have all five functions consume them: a setupIsolatedRebootEnv(t) (string stateDir, string hooksPath) for Block A, and reuse the existing rebootAndHydrate for Block B (widen it to take the session name(s) if the 3-7 multi-pane leg needs it, rather than re-inlining). Fold seedScrollback/seedPaneScrollback into one variadic-or-(w,p) helper (seedPaneScrollback already generalises seedScrollback — drop the pane-0-only seedScrollback and call seedPaneScrollback(..,0,0)). No behaviour change; consolidates the reboot ceremony and the fixture literals to one definition each.

- FINDING: Third redundant copy of the one-method AllPaneLister stub in package cmd
  SEVERITY: low
  FILES: cmd/clean_test.go:727-734 (mockCleanPaneLister), cmd/bootstrap_production_test.go:107-116 (stubAllPaneLister)
  DESCRIPTION: mockCleanPaneLister is structurally byte-identical to stubAllPaneLister — both are `struct{ panes []string; err error }` whose sole method `ListAllPaneHookKeys() ([]string, error)` returns the canned pair. Both live in the same cmd package, and stubAllPaneLister is ALREADY shared between bootstrap_production_test.go and run_hook_stale_cleanup_test.go (which additionally keeps a distinct recordingHookKeyLister only because it needs call-counting — a genuinely different concern). mockCleanPaneLister adds no capability over stubAllPaneLister; it is a duplicate declaration that clean_test.go's ~10 call sites could satisfy with the existing stub.
  RECOMMENDATION: Delete mockCleanPaneLister and repoint clean_test.go's call sites to stubAllPaneLister (identical field names panes/err, so the composite literals need no edit beyond the type name). Keep recordingHookKeyLister as-is — its invocation counter is a distinct behaviour, not duplication.

SUMMARY: The production fix is well-consolidated (single HookKey/HookKeyFormat primitives, the explicitly-extracted runHookStaleCleanup, and the deliberately-distributed three-package @portal-id literal guards are all correct and NOT flagged). The actionable duplication is confined to the test layer: a ~20-line rename+reboot integration harness copy-pasted across 5 functions in 3 same-package files (medium — the 3-6 file already shows the extraction the siblings skipped), and a trivially-redundant third AllPaneLister stub in package cmd (low). The real-tmux hookkey test-setup parallelism was reviewed and left unflagged as idiomatic self-contained scaffolding.
