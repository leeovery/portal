AGENT: duplication
FINDINGS:
- FINDING: Three redundant "@portal-id" literal constants in one test package
  SEVERITY: low
  FILES: internal/tmux/hookkey_format_realtmux_test.go:43, internal/tmux/list_all_pane_hookkeys_realtmux_test.go:47, internal/tmux/hookkey_cross_site_realtmux_test.go:52
  DESCRIPTION: Three files in the SAME external test package (all `package tmux_test`) each declare their own package-level constant for the identical literal "@portal-id": `portalIDOption`, `hookKeyPortalIDOption`, and `crossSitePortalIDOption`. A fourth site (resolve_hookkey_realtmux_test.go:64) inlines the bare "@portal-id" literal directly. Redeclaring the *literal* across files to avoid importing session.PortalIDOption is a genuine and correctly-documented import-cycle avoidance (internal/session imports internal/tmux, so a tmux_test file importing session would close a cycle) — that decision is sound and should NOT change. What is redundant is that three distinct constants for the same value coexist WITHIN a single compilation unit (the tmux_test package): Go would let one shared const serve all three files. The differing names (portalIDOption / hookKeyPortalIDOption / crossSitePortalIDOption) also invite a reader to think they might differ, when the load-bearing invariant is that they are byte-identical to HookKeyFormat's embedded literal. Impact is minor: a future edit to one (e.g. if the option name ever changed) must be manually mirrored to the other two plus the inlined string, and the byte-identical-to-HookKeyFormat contract is asserted only by comment, not structurally.
  RECOMMENDATION: Collapse the three package-level constants into a single shared `const portalIDOption = "@portal-id"` declared once in the tmux_test package (any one of the existing files), and reference it from all three tests plus the inlined literal in resolve_hookkey_realtmux_test.go:64. Keep the existing "literal, not session.PortalIDOption, to avoid an import cycle; must stay byte-identical to HookKeyFormat" comment on the surviving declaration. This is a small tidy, not a correctness fix — the import-cycle avoidance itself is justified and stays.
SUMMARY: Production code is well-consolidated — the hook-key derivation funnels through single HookKey/HookKeyFormat primitives and the stale-cleanup path through a single runHookStaleCleanup helper, with no cross-file logic duplication. The only actionable duplication is three redundant "@portal-id" literal constants declared separately within the one tmux_test package (plus one inlined copy); the underlying import-cycle avoidance is justified, only the intra-package triplication is worth collapsing. Test scaffolding, fixture constants, and stamp-site parallelism (create.go / quickstart.go / restore) are idiomatic for this codebase and reflect deliberate mechanism differences, not duplication.
