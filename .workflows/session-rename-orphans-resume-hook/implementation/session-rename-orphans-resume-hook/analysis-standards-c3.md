AGENT: standards
FINDINGS: none
SUMMARY: Implementation conforms to specification and project conventions. All 8 Acceptance Criteria, the four Hook-Key Derivation stages (registration ResolveHookKey with abort-on-read-failure, cleanup ListAllPaneHookKeys, restore-baked HookKey from saved state, unchanged hydrate consumer), Cross-Reboot Persistence (additive portal_id field with no Version bump and tolerant decode, captureFormat's #{@portal-id} appended as trailing 11th column with captureFieldCount bumped to 11 in lockstep, best-effort restore re-stamp), and the retired-doc-comment deliverable (all four sites — PaneTarget, PaneTargetExact, StructuralKeyFormat, ListAllPanes — stripped of hooks.json-ownership claims with the load-bearing stability invariant transferred to HookKey/HookKeyFormat) are implemented and covered by tests. Best-effort stamping is honoured in both creation paths (CreateFromDir swallows gen+set-option errors; QuickStart omits the set-option step on gen failure via an independent second qs.gen call), the firing path never reads the live @portal-id (ordering trap avoided — hookKey is baked purely from saved state in collectArmInfos), the three "@portal-id" literal embeddings are pinned byte-identical by static guard tests, and no test uses t.Parallel(). Build, vet, and the PortalID/HookKey/capture/schema/create/quickstart unit tests all pass.

NOTES (non-findings, for transparency):
- Token width left at the existing 6-char NewNanoIDGenerator (62^6 ~= 5.7e10) rather than widened. The spec (Fix Overview → Its value; Risks → Token collision) explicitly frames width as an implementation detail — "the existing NewNanoIDGenerator scheme, widened if warranted" — and birthday-collision across a realistic session population (dozens to low thousands) stays negligible at this width. Keeping 6 chars is a legitimate in-spec choice, not drift; the accepted residual (hook-key cross-talk on a vanishingly-unlikely duplicate) is exactly the documented risk. Recording only so a future reviewer does not re-flag it.
