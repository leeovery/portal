AGENT: standards
FINDINGS:
- FINDING: Fix 4 compile-guard precondition discharges the spec's "confirm Ghostty-running" open nuance with reasoning, not the evidence/remedy the spec required
  SEVERITY: low
  FILES: internal/spawn/ghostty_compile_ghosttycompile_test.go:41-65
  DESCRIPTION: Spec §Fix 4 designated one "Assumption to confirm (during live-Mac implementation)": whether osacompile terminology resolution *requires Ghostty to be running*, since the t.Skip-on-installed gate does not cover the not-running case. It instructed that if resolution turns out to require a running Ghostty, the precondition must be adjusted to one of two remedies — ensure/require Ghostty running as part of the gate, or t.Skip when terminology cannot be resolved. The recorded confirmation (comment lines 41-58) only observes the Ghostty-RUNNING state (correct template → exit 0, broken template → -2741). It never exercises the installed-but-not-running case, so the spec's actual open question is not answered by the recorded evidence. The justification then pivots to "the spawn feature is only ever invoked from a running Ghostty," which conflates the *feature's* invocation with the *guard test's* invocation: the guard is a standalone manual test (go test -tags ghosttycompile) that can be run with Ghostty installed but closed. If resolution does require a running Ghostty, that invocation would produce the exact "false failure unrelated to the template" the spec warned against, and the installed-only gate adopts neither prescribed remedy. Impact is bounded — this is a manual prevention guard, not production behaviour, and compile-only sdef resolution plausibly does not need the app running — hence low severity.
  RECOMMENDATION: Either verify the installed-but-not-running behaviour on the live Mac and, if osacompile still resolves cleanly, record that explicitly in the comment (closing the open nuance with evidence rather than a feature-invocation analogy); or, if resolution does require a running Ghostty, adopt one of the spec's two prescribed precondition adjustments (gate on Ghostty running, or t.Skip when terminology cannot be resolved) so the guard cannot emit a false failure.
SUMMARY: Fixes 1-3 (corrected Ghostty `new window with configuration` template + preserved backslash-before-quote escape order, the spec-governed WARN "external window failed" catalog amendment spanning both AckFailed/AckTimeout with the permission-window exclusion, and the honest conditional `— nothing opened` / `— others left open` PartialFailureMessage(failed, othersOpened) renderer wired byte-identically through PartitionResults in both CLI and picker) conform to the specification, and all lockstep test updates and closed-attr-key / test-isolation conventions hold. The single finding is a low-severity gap where Fix 4's guard precondition leaves the spec's explicit "confirm Ghostty-running" nuance untested and dismissed by reasoning.
