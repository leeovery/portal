AGENT: standards
FINDINGS:
- FINDING: Fix 4 compile guard narrows the spec's "assert a zero exit" to "fail only on -2741"
  SEVERITY: low
  FILES: internal/spawn/ghostty_compile_ghosttycompile_test.go:112-134
  DESCRIPTION: The spec (§Fix 4 body and §Testing & Validation Requirements) instructs the prevention guard to compile ghosttyOpenScript(...) via osacompile and "assert a zero exit" — i.e. any non-zero exit fails the test. The implementation instead hard-fails ONLY when the compiler output contains the `-2741` drift discriminator, and t.Skips every other error (any non-2741 non-zero exit, or a non-exit run error) as "environmental." This is defensible: the spec's "Assumption to confirm" note explicitly authorized adjusting the precondition to avoid false failures and offered "t.Skip when terminology cannot be resolved" as a sanctioned option, and the implementation documents its live-Mac confirmation and precondition rationale at length (lines 49-88). The narrowing is a deliberate trade of forward-looking coverage for zero false-failures when Ghostty is installed-but-not-running. The residual gap: a FUTURE template drift that produces a terminology error with a different code than -2741 would be silently skipped ("environmental") rather than caught — whereas the spec's guard was intended to catch a wrong AppleScript template generally, not only the exact known-bad `make new surface configuration` form. Whether the -2741-only discrimination is acceptable is a judgment the spec's Assumption note invited; flagging so the divergence from the literal "assert a zero exit" is on the record.
  RECOMMENDATION: Accept as-is if the false-failure risk (Ghostty closed) is judged to dominate, since the note sanctions the t.Skip route. If broader drift coverage is wanted, tighten the gate to guarantee terminology IS resolvable (e.g. require Ghostty running, or probe with a known-good minimal `tell application "Ghostty"` script first) and then assert a plain zero exit on the real template, so a novel non-2741 terminology error also hard-fails. No behavioural change to production code either way.
SUMMARY: The four coordinated fixes conform to the specification with high fidelity — the corrected Ghostty template (exact sdef form, unchanged ghosttyEmbed escape order), the WARN "external window failed" split (one new spec-governed message string, no new attr keys, closed-key assertions in tests, CLI/picker parity preserved incl. the permission-arm asymmetry), and the conditional PartialFailureMessage(failed, othersOpened) copy (byte-identical to the spec table, single-sourced, parity-tested) all match. Test isolation holds: the manual and ghosttycompile files are the only build-tagged files, both excluded from the unit and integration lanes, and the unit/tui/cmd tests are fully seam-injected with no real tmux, daemon, binary exec, or process mutation. Build and vet (including both tagged files) are clean. The single low finding is the Fix 4 guard narrowing the "assert a zero exit" instruction to a -2741-only hard-fail — a documented, spec-note-sanctioned trade-off with a small forward-coverage cost.
