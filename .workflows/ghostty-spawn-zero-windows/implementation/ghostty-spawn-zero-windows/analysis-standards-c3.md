AGENT: standards
FINDINGS:
- FINDING: Fix 4 compile guard hard-fails only on the -2741 signature, narrower than the spec's "assert exit 0"
  SEVERITY: low
  FILES: internal/spawn/ghostty_compile_ghosttycompile_test.go:112-134
  DESCRIPTION: Spec §Fix 4 "Invocation (concrete)" states the guard should "Assert the process exits 0; a non-zero exit fails the test with the captured compiler output." The implementation instead hard-fails ONLY when osacompile's combined output contains the `-2741` drift discriminator and t.Skips every other non-zero exit / non-exit run error as "environmental." This resolves the genuine internal tension in the spec — the later "Assumption to confirm" clause explicitly authorizes "t.Skip when terminology cannot be resolved" — but it also skips a hypothetical future template regression that produces a DIFFERENT compile error (e.g. a terminology typo other than the known `make new surface configuration` form, which yields a different code). Fix 4's stated purpose is broad ("Prevents recurrence of a template-terminology regression"), so keying detection on the single known signature narrows the automated coverage below the literal "assert exit 0" intent. The choice is well-documented (the LIVE-MAC CONFIRMATION and PRECONDITION RATIONALE blocks), backstopped by the mandatory live validation (the -tags manual test + real ≥3-session burst), and defensible — but the reviewer should confirm the narrower-than-literal guard is intended, since a non-2741 regression would pass the automated tripwire silently.
  RECOMMENDATION: Accept as-is if the -2741-only signature is the intended contract (it is spec-sanctioned via "Assumption to confirm" and documented). If broader coverage is wanted, additionally fail on any non-zero exit whose output does NOT match a recognised environmental-skip precondition (e.g. Ghostty-not-running / osacompile-absent), so an unknown compile failure surfaces rather than skips — keeping the -2741 message as the specific, greppable diagnosis.
SUMMARY: The four spec fixes are implemented faithfully — the corrected `new window with configuration {command:…, wait after command:true}` template (Fix 1), the WARN "external window failed" split on !Confirmed() && non-permission with no new attr keys (Fix 2), the conditional `— nothing opened` / `— others left open` suffix keyed on `len(confirmed) > 0` from the shared PartitionResults chokepoint (Fix 3), and the ghosttycompile-tagged osacompile guard plus every mandated lockstep test update (Fix 4). One low-severity note: the compile guard's failure detection is narrowed to the single -2741 signature, slightly below the spec's literal "assert exit 0," though within its documented "Assumption to confirm" latitude.
