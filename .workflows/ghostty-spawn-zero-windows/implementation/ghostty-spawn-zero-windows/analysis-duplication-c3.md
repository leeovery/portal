AGENT: duplication
FINDINGS:
- FINDING: Picker partial-failure path derives FirstPermission + PartitionResults twice on the same results
  SEVERITY: low
  FILES: internal/tui/burst_partial_failure.go:43, internal/tui/burst_partial_failure.go:66, internal/tui/burst_partial_failure.go:117, internal/tui/burst_partial_failure.go:120
  DESCRIPTION: Within one non-all-confirmed terminal event, handleBurstPartialFailure calls spawn.FirstPermission(msg.Results) (line 43) and spawn.PartitionResults(msg.Results) (line 66), then hands the same msg.Results to burstPartialFailureFlash, which re-derives both independently — spawn.FirstPermission(results) (line 117) and spawn.PartitionResults(results) (line 120). So each of the two pure derivations runs twice per partial-failure render. This is deliberate: the file's comments state burstPartialFailureFlash was made "fully self-contained, owning the single FirstPermission + PartitionResults derivation so its correctness never depends on a caller passing a matching partition half." Both functions are shared internal/spawn chokepoints, pure, and operate on tiny slices, so there is no drift risk (they cannot diverge — the same source of truth is called) and the cost is negligible. This is a computational redundancy, not a copy-paste that can drift.
  RECOMMENDATION: No action is the valid default. If single-derivation is preferred over the self-containment guarantee, handleBurstPartialFailure already holds both the permission check and the confirmed partition, so it could thread the already-derived failed/othersOpened signal into a message-only helper rather than re-deriving inside burstPartialFailureFlash. Low priority — the current tradeoff is defensible and documented.

- FINDING: Representative "env-self-sufficient composed argv" literal re-authored across three spawn test files
  SEVERITY: low
  FILES: internal/spawn/ghostty_command_test.go:10, internal/spawn/ghostty_compile_ghosttycompile_test.go:101, internal/spawn/ghostty_openwindow_manual_test.go:41
  DESCRIPTION: Three test files each hand-roll a "representative composed argv" whose leading shape (`/usr/bin/env`, `-u`, `TMUX`, `-u`, `TMUX_PANE`, …) mirrors what the spawn layer actually composes for a spawned attach. realAttachArgv() (ghostty_command_test.go) and the manual test's argv (ghostty_openwindow_manual_test.go) predate this work unit; the new ghosttycompile guard (ghostty_compile_ghosttycompile_test.go, added by Fix 4) independently reproduces the same prefix. Each carries a deliberately different payload (attach argv vs `/bin/sh -c echo probe` vs the echo marker), so the divergence is intentional, but the shared env-stripping prefix is duplicated three ways. The real production composition lives in internal/spawn (the burster/compose path); these literals are hand-mirrors of that shape. Risk is drift-by-omission: if the composed argv prefix ever changes (e.g. another `-u VAR` is added), the compile guard and manual test would keep exercising a stale shape without any compile error to flag it.
  RECOMMENDATION: Low priority given the shared portion is only ~5 tokens and each test needs a distinct payload. If consolidated, extract a single untagged test helper in internal/spawn (e.g. representativeComposedArgv(payload ...string) []string that prepends the canonical `/usr/bin/env -u TMUX -u TMUX_PANE` prefix) so all three files — including the build-tagged guard and manual test — build the representative shape from one place and the prefix cannot drift silently.
SUMMARY: This cycle's four fixes stay tightly single-sourced — the AppleScript template correction, the WARN split inside the shared LogWindowResults, the conditional suffix kept within the single PartialFailureMessage renderer, and the new osacompile guard all reuse existing internal/spawn primitives rather than re-implementing them, so no significant new production duplication was introduced. The two findings are both low: a deliberate (drift-free) double-derivation in the picker partial-failure path, and a minor re-authoring of the representative composed-argv shape across three spawn test files. (Out of scope, consistent with cycle 2: the cross-package closedSpawnAttrKeys map + closed-key assertion helpers shared by internal/spawn/logemit_test.go and internal/tui/burst_observability_test.go, and the triplicated wantPermissionBody golden, predate this work unit — the latter is an intentional cross-caller parity anchor.)
