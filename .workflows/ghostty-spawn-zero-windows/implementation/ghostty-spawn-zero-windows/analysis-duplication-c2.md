AGENT: duplication
FINDINGS:
- FINDING: Picker partial-failure path recomputes FirstPermission + PartitionResults twice on the same results
  SEVERITY: low
  FILES: internal/tui/burst_partial_failure.go:43, internal/tui/burst_partial_failure.go:66, internal/tui/burst_partial_failure.go:117, internal/tui/burst_partial_failure.go:120
  DESCRIPTION: Within a single non-all-confirmed terminal event, handleBurstPartialFailure calls spawn.FirstPermission(msg.Results) (line 43) and spawn.PartitionResults(msg.Results) (line 66), then hands msg.Results to burstPartialFailureFlash, which independently re-derives both — spawn.FirstPermission(results) (line 117) and spawn.PartitionResults(results) (line 120) — on the same slice. So the same two pure derivations each run twice per partial-failure render. This was introduced deliberately by the Fix 2-1 refactor (commit 26a23299 "make burstPartialFailureFlash self-contained"), which moved the partition derivation inside the flash helper so its correctness never depends on a caller passing a matching partition half. The functions are shared chokepoints (internal/spawn), pure, and operate on tiny slices, so the cost is negligible and there is no drift risk — the redundancy is computational, not a copy-paste that could diverge. Flagging only for visibility; the self-containment tradeoff is defensible and explicitly documented in the file's comments.
  RECOMMENDATION: Optional and low-priority. If consolidation is desired, handleBurstPartialFailure already computes both the permission check and the confirmed partition, so it could pass the already-derived failed set (or the whole partition) into a helper rather than re-deriving inside burstPartialFailureFlash — but only if the reviewer values the single-derivation over the self-containment guarantee the current design deliberately chose. No action is the equally-valid default.
SUMMARY: This cycle's four fixes are tightly single-sourced — the AppleScript template correction, the WARN split in the shared LogWindowResults, the conditional suffix kept inside the single PartialFailureMessage renderer, and the new osacompile guard all reuse existing shared spawn primitives rather than re-implementing them, so no significant new duplication was introduced. The only near-duplicate is a low-severity, deliberate recomputation of two pure derivations within the picker partial-failure path. (Note: the cross-package closedSpawnAttrKeys map + closed-key assert helper shared between internal/spawn/logemit_test.go and internal/tui/burst_observability_test.go, and the triplicated wantPermissionBody golden literal, both predate this work unit and the latter is an intentional cross-caller parity anchor — neither is in scope for this cycle.)
