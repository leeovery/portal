AGENT: architecture
FINDINGS:
- FINDING: Picker partial-failure path recomputes the shared partition twice, giving burstPartialFailureFlash a hybrid (results, failed) signature
  SEVERITY: low
  FILES: internal/tui/burst_partial_failure.go:43, internal/tui/burst_partial_failure.go:64, internal/tui/burst_partial_failure.go:107-121
  DESCRIPTION: The new PartialFailureMessage(failed, othersOpened bool) signal is derived consistently from spawn.PartitionResults across both callers (CLI: cmd/spawn.go:179+210 derives len(confirmed) > 0 from the same PartitionResults call that produced `failed`; picker: burst_partial_failure.go:121 derives it identically), so the parity contract holds and there is no drift risk — the core seam is sound. The one architectural nit is on the picker side only: handleBurstPartialFailure already computes `confirmed, failed := PartitionResults(msg.Results)` (line 64) and calls FirstPermission(msg.Results) (line 43), then hands only `failed` to burstPartialFailureFlash(msg.Results, failed), which re-runs BOTH FirstPermission(results) (line 108) and PartitionResults(results) (line 120, discarding the recomputed `failed`). The helper's signature is therefore a hybrid — it receives one half of the partition (`failed`) as a parameter while re-deriving the other half (`confirmed`) internally from `results`. Correctness depends on the caller passing a `failed` that matches PartitionResults(results); it does today (single unexported caller, same chokepoint), so this is not a latent bug, but the helper is less self-contained than it could be and the double FirstPermission/PartitionResults scans are redundant. The CLI path (cmd/spawn.go) does not have this shape — it computes the partition once and reuses both halves inline.
  RECOMMENDATION: Make burstPartialFailureFlash self-contained by choosing one derivation source: either pass the already-computed pair — burstPartialFailureFlash(results, confirmed, failed) reusing what handleBurstPartialFailure holds — or take only `results` and compute confirmed/failed (and the permission scan) internally once. Either removes the recomputation and the caller-discipline coupling on `failed`. Low priority; the current code is correct and well-tested.
SUMMARY: The one API change (PartialFailureMessage's added othersOpened signal) is cleanly single-sourced and both callers derive it consistently from the shared PartitionResults chokepoint; the log WARN/DEBUG split, compile guard, and template fix compose well behind unchanged seams. The only observation is a low-severity picker-side redundancy where burstPartialFailureFlash recomputes the partition it is partially handed.
