AGENT: architecture
FINDINGS:
- FINDING: Fix 4 compile-guard asserts "not the one historical error code" instead of the spec's "template compiles" — protection narrows to the exact past regression
  SEVERITY: low
  FILES: internal/spawn/ghostty_compile_ghosttycompile_test.go:20, internal/spawn/ghostty_compile_ghosttycompile_test.go:112-134
  DESCRIPTION: Spec §Fix 4 defines the guard as "assert a zero exit" against a resolved Ghostty dictionary, with a t.Skip fallback narrowly scoped to "when terminology cannot be resolved" (Ghostty not running / osacompile absent). The implementation inverts the assertion's shape: it passes on a zero exit, but on a NON-zero exit it hard-fails ONLY when the compiler output contains the literal "-2741" (the code the historical `make new surface configuration with properties` form yields) and t.Skips every other non-zero result as "environmental." The skip predicate is therefore drawn around "not exactly -2741" rather than the spec's "terminology unresolvable," so the guard is a tripwire for re-introducing the one exact prior bug, not the general class Fix 4 advertises ("fails a drifted template WITHOUT a human in the loop"). A future drift to a different-but-still-invalid form against a resolved dictionary (a mistyped `new window`, a wrong property name, any error code that is not -2741) would compile-fail with a different AppleEvent code and be silently skipped — a false-green. The guard's own comment surfaces this tension: reason #1 argues terminology resolves from the installed static sdef regardless of whether Ghostty runs (which would make asserting a clean exit safe), while reason #2 hedges against reason #1 by skipping everything that is not -2741, which is what narrows it. This is a soundness/fragility observation on where the fail/skip line sits, not a correctness bug: the guard passes today, the mandatory manual live validation remains the real functional proof, and the spec did sanction a skip-when-unresolvable fallback. (Same observation stood in cycle 2 and remains unaddressed in the code.)
  RECOMMENDATION: Draw the skip boundary around "terminology unresolvable" rather than "not -2741": pass on a clean exit, skip only on the signatures that mean osacompile could not reach the dictionary at all (osacompile missing / non-exit run error, or an app-not-found / "Can't get application" resolution error), and fail on any other compile error against a resolved dictionary — restoring the general "did the template compile" coverage the spec's "assert a zero exit" intends while keeping the installed-but-not-running false-failure protection. Alternatively, follow the spec's other sanctioned option and require Ghostty running as part of the gate so a clean-exit assertion is unconditional. Low priority; the current guard is correct for the exact historical regression and is a documented best-effort secondary net behind the manual gate.
SUMMARY: The four-part fix composes cleanly: the template correction sits entirely behind the unchanged ghosttyEmbed/ghosttyOpenScript/mapGhosttyResult seams; the WARN/DEBUG per-window split (logemit.go) and the new PartialFailureMessage othersOpened signal (message.go) are both single-sourced through the classify.go/message.go/logemit.go chokepoints with byte-identical CLI/picker parity tests, and the cycle-1 hybrid-signature nit stays resolved (burstPartialFailureFlash is self-contained). The one remaining low-severity observation is that the Fix 4 compile-guard couples its fail decision to a single historical error code rather than the general "template compiles" property it advertises, under-delivering on its stated prevention goal for any future drift that is not the exact -2741 form.
