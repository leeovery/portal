TASK: Merge-gating live validation (ghostty-spawn-zero-windows-1-5) — the in-scope manual acceptance gate this topic owns.

ACCEPTANCE CRITERIA (from plan):
- CHECK 1: `go test -tags manual -run TestManual_OpenWindow_OpensRealGhosttyWindow ./internal/spawn/` passes on a live Mac inside Ghostty, with a real Ghostty window visually confirmed to open and run the command.
- CHECK 2: A real >=3-session picker multi-select burst confirms `opened 3/3`, token acks land, and the trigger self-attaches — NET-N windows, never N+1.
- CHECK 3: `go test ./...` (unit) and `go test -tags integration -p 1 ./...` (integration) both pass, inclusive of the lockstep test updates; `manual` and `ghosttycompile` tags remain excluded from both lanes.
- Compile-only validation (Task 1.4) is NOT accepted as a substitute for this functional gate.
- The merge is blocked until all checks above pass.

STATUS: Issues Found

SPEC CONTEXT:
The spec's §Testing & Validation Requirements labels live validation "Mandatory ... (merge-gating, load-bearing)" and is explicit that "Compile-only validation is insufficient — it proves the script parses, not that a window opens and runs its command. This live gate blocks the merge." §Scope & Non-Goals reinforces that the two live checks (the `-tags manual` test and a real >=3-session `opened 3/3` burst) are acceptance criteria "owned here," and §Context frames the entire defect's root cause as "the absence of live validation is what let this ship." Task 1.5 exists specifically to close that gap. This is the single most load-bearing acceptance requirement of the whole bugfix.

IMPLEMENTATION:
- Status: Partial (manual-test marker fix landed; live functional gate only partially satisfied — see below).
- Location:
  - internal/spawn/ghostty_openwindow_manual_test.go (marker argv switched from a spaced/;-laden `sh -c "echo …; sleep 5"` element to discrete tokens `echo portal spawn manual verification`).
  - Commit 96bac9e3 also flips tracking-only files (.tick/tasks.jsonl, manifest.json) marking the task/phase done and appends the live-validation NOTE.
- Notes:
  - The commit under review changes NO production code and NO integration/unit-lane-compiled code. The only Go change is inside a `//go:build manual` file, compiled into neither default lane. Task 1.5 is a validation/acceptance gate, so this is expected — the production fixes are in Tasks 1.1–1.4.
  - CHECK 1 partial: the recorded NOTE confirms a real Ghostty window OPENS live (this validates the Fix-1 template — the actual defect). However the original marker command mis-ran (the `sh -c "…; sleep 5"` element was shredded by ghosttyEmbed's space-join + Ghostty's `bash -c` re-split), so the strict "visually confirmed to open AND run the command" criterion was not met on first run. The marker was changed to discrete tokens to fix this. The commit/NOTE does NOT document a re-run of the corrected marker with a fresh visual confirmation — so "window opens" is confirmed, "runs the corrected command cleanly" is inferred, not recorded.
  - CHECK 2 NOT run live: the NOTE records "CHECK 2 — NOT run by user choice (touches live daemon/sessions); real path validated by code analysis." This is a direct, user-authorized deviation from the spec's merge-gating requirement. See BLOCKING ISSUES.

TESTS:
- Status: Adequate for a manual gate (the automated portion is intentionally minimal — the manual test asserts only `result.OK()`; the real check is the human's eyes, which is correct for an irreducibly-manual visual gate).
- Coverage:
  - CHECK 3 unit lane: VERIFIED GREEN by me. `go test ./...` → exit 0, all packages `ok`.
  - CHECK 3 tag exclusion: VERIFIED. `internal/spawn/ghostty_openwindow_manual_test.go` carries `//go:build manual` and `internal/spawn/ghostty_compile_ghosttycompile_test.go` carries `//go:build ghosttycompile`; neither lane passes those tags, so both are structurally excluded from unit and integration.
  - CHECK 3 integration lane: NOT deterministically green on my verification runs. `go test -tags integration -p 1 ./...` FAILED in `cmd/bootstrap` on `TestCompositeBootstrap_ConvergesPgrepToOneWithin6s`. See BLOCKING ISSUES #2 for the flake characterization.
- Notes: The marker-fix is not itself a behavioural test change; it corrects the manual probe's input so the manual gate can be re-run cleanly. No over/under-testing concerns in the changed file.

CODE QUALITY:
- Project conventions: Followed. The switch to discrete space/metacharacter-free tokens matches the codebase's own composeAttachArgv discipline; the added comment (internal/spawn/ghostty_openwindow_manual_test.go:34-40) accurately explains the space-join round-trip hazard.
- SOLID / Complexity / Idioms / Readability: Good. The change is a literal-slice edit plus an explanatory comment; no logic.
- Issues:
  - The code-analysis basis offered for skipping CHECK 2 is over-stated. The NOTE claims "composeAttachArgv emits only single-token metacharacter-free elements so it round-trips the space-join cleanly." That holds for the fixed literals and the `--spawn-ack <batch>:<token>` value (drawn from `session.NanoIDAlphabet`, which excludes space/`.`/`:`/`-`), but NOT categorically for the three runtime-variable elements composeAttachArgv also embeds: `PATH=<path>`, `<exePath>`, and `<session>`. Session names are NOT space-stripped — `session.SanitiseProjectName` (internal/session/naming.go:31) maps only `.` and `:` to `-`, leaving spaces intact — so a project derived from a spaced directory basename yields a session name like `My Project-abc123`, whose embedded space would be shredded by the exact ghosttyEmbed space-join + Ghostty `bash -c` re-split that broke the manual marker. This is the kind of end-to-end functional defect a live burst is designed to catch, so it materially weakens the code-analysis substitution for CHECK 2.

BLOCKING ISSUES:
- [BLOCKING PER SPEC — user-authorized deferral; user must adjudicate] CHECK 2 (the real >=3-session live burst) was NOT run. The spec is unambiguous that this live burst is "merge-gating, load-bearing" and that non-functional (compile/analysis-only) validation "is insufficient" and "blocks the merge." The recorded acceptance ("accepted on live window-open + code analysis + green lanes; strict CHECK 2 live burst deferred by user, fix-forward posture") substitutes code analysis for the very functional proof the spec says cannot be substituted, and re-defers the exact live-validation step whose absence the spec identifies as the root cause of the original ship. As verifier I cannot run this live-Mac check. Surfacing the tension for the user's decision: the recorded fix-forward acceptance directly contradicts the spec's merge-gate. If the user's authorization stands, this is an accepted deferral; measured against the spec text as written, the gate is not satisfied. It should be an explicit, eyes-open decision — not treated as silently "done." Additionally, the code-analysis that underpins the deferral is not airtight (spaced session names round-trip-shred — see CODE QUALITY), so the residual functional risk it purports to retire is not fully retired.
- [BLOCKING PER CRITERION — pre-existing flake, NOT caused by this commit] CHECK 3's "integration lane green" was not reproducible on my verification. `go test -tags integration -p 1 ./...` failed in `cmd/bootstrap` on `TestCompositeBootstrap_ConvergesPgrepToOneWithin6s`. Across 5 runs of that test: 2 pass, 3 fail — two convergence-deadline misses (`elapsed=6.048s` and `6.121s` vs a 6s budget, i.e. ~1-2% overshoot under load) and one `TempDir RemoveAll: directory not empty` teardown race. Both failure modes are the documented daemon-timing/teardown flakes CLAUDE.md calls out for the composite e2e suites, and the commit under review touches no integration-lane-compiled code, so this is a pre-existing environmental flake, not a regression from Task 1.5. It is not fixable within this task's scope. Reported here because the recorded NOTE's "unit + integration lanes BOTH green" claim is fragile: the integration lane is not deterministically green on this machine. The unit lane IS reliably green.

NON-BLOCKING NOTES:
- [bug] internal/spawn/command.go:27 (composeAttachArgv) + internal/session/naming.go:31 (SanitiseProjectName) — a session name containing a space (spaced directory basename → `My Project-abc123`; SanitiseProjectName strips only `.`/`:`) is embedded whole into the ghosttyEmbed space-joined `command:"…"` string and then re-split by Ghostty's `bash -c`, shredding the argv — the identical round-trip failure that broke the manual marker. Latent, edge-case, and in the Fix-1/ghosttyEmbed layer (out of Task 1.5's scope); the sound fix is per-argv quoting rather than a naive space-join, which is a design decision. Flagging so it is not lost, since it is precisely the class of bug the deferred live burst would expose.
- [do-now] internal/spawn/ghostty_openwindow_manual_test.go:21 — the function docstring still says the window's command "prints the marker line and sleeps so the window is observable," but the marker argv no longer sleeps (the inline comment at lines 38-40 explicitly states "so no `sleep` is needed"). Remove the stale "and sleeps" clause so the top docstring matches the corrected discrete-token marker.
