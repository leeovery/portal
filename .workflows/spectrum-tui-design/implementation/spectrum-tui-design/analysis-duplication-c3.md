AGENT: duplication
FINDINGS:
- FINDING: loading_view.go re-authors header.go's leaf canvas-style helper pair
  SEVERITY: low
  FILES: internal/tui/loading_view.go:249-265 (loadingStyle, loadingFg), internal/tui/header.go:87-104 (headerStyle, headerCanvasBg)
  DESCRIPTION: The loading screen independently re-implements the exact "role-token foreground over Background(canvas) for the mode, bare style under NO_COLOR" leaf-style pair that header.go already exports and that the rest of the TUI shares. `loadingFg(fg, mode, colourless)` is byte-identical in behaviour to `headerStyle(fg, mode, colourless)`; `loadingStyle(mode, colourless)` is byte-identical to `headerCanvasBg(mode, colourless)`. Every other surface authored in this work unit — section_header.go, footer.go, filter_footer.go, notice_band.go, the destructive/rename/edit/help modal files — already routes through the header.go pair; loading_view.go is the one render file that forked its own copy (a separate task executor, the §10 loading phase, did not see the §3 header helpers). This is the canonical copy-paste-across-task-boundary pattern: two names for one concept that must move together if the NO_COLOR carve-out or the canvas-background composition ever changes.
  RECOMMENDATION: Delete loadingStyle/loadingFg and point the loading render at the shared headerStyle/headerCanvasBg (or, if the loading code wants its own terse names, make them one-line aliases that delegate, the way SessionDelegate.rowBg/rowToken delegate to the shared free functions). No behaviour change — the bodies are already identical. This collapses the third independent copy of the leaf-paint rule into the single header.go source.

- FINDING: Six near-identical render*ModalOnClearedCanvas wrappers in modal.go
  FILES: internal/tui/modal.go:67-135 (renderModalOnClearedCanvas, renderHelpModalOnClearedCanvas, renderKillModalOnClearedCanvas, renderDeleteModalOnClearedCanvas, renderRenameModalOnClearedCanvas, renderEditModalOnClearedCanvas)
  SEVERITY: low
  DESCRIPTION: Five of the six functions are structurally identical: build a panel string from per-modal content, then `return lipgloss.Place(width, height, lipgloss.Center, lipgloss.Center, panel)`. They differ only in which content builder is called (renderHelpModalContent / renderKillModalContent / renderDeleteModalContent / renderRenameModalContent / m.renderEditProjectContent). The shared "centre this already-built panel on the cleared canvas" step is the §8.1/§13.5 blank-screen placement, repeated verbatim five times. Each was added by the task executor that reskinned its modal (3-4 help, 3-5 kill, 3-6 rename, 3-7 delete, 3-8/3-9 edit), so the placement line accreted one copy per modal across task boundaries. The risk is low because the body is a single trivial line, but it is the same parallel-wrapper smell that the team already collapsed elsewhere (destructive_confirm.go, modal_footer.go).
  RECOMMENDATION: Introduce one `placeModalOnClearedCanvas(panel string, width, height int) string` returning the lipgloss.Place result, and have each render*OnClearedCanvas wrapper call it with its built panel — or, more aggressively, have each modal's call site build its panel and call the single placement helper directly, dropping the per-modal wrappers entirely. Keeps the centring maths (and any future change to it, e.g. a non-centred placement) in exactly one place.

- FINDING: Right-anchored footer layout duplicated between footer.go and filter_footer.go
  SEVERITY: low
  FILES: internal/tui/footer.go:140-165 (footerKeyRow), internal/tui/filter_footer.go:130-152 (filterFooterRow)
  DESCRIPTION: footerKeyRow and filterFooterRow implement the same row geometry: render a left cluster, render the right-aligned `? help` anchor, then "if the right anchor doesn't fit beside the cluster (leftWidth+1+rightWidth > w) pad-left-and-return, else emit cluster + canvas flex spacer + right anchor". The narrow-degrade branch and the spacer computation are byte-equivalent; only the cluster renderer (renderFooterCluster vs renderFilterCluster) and the right-anchor source differ. filter_footer.go's own comments acknowledge it "mirrors footerKeyRow's right-anchor layout" — the mirroring is the duplication. The two diverged across the standard-footer task (3-1/3-2) and the filter-footer task (2-8/7), so a future change to the right-anchor degrade rule must be made in both.
  RECOMMENDATION: Extract the right-anchor row assembler — `assembleRightAnchoredRow(left string, leftWidth int, rightSeg string, rightWidth, w int, mode, colourless) string` owning the fit test, the headerPadRight degrade, and the flex-spacer join — and have both footerKeyRow and filterFooterRow call it after rendering their own left cluster and resolving the shared right `? help` anchor. The left-cluster fitting (fitLeftCluster's ellipsis logic) stays footer.go-specific; only the final right-anchor layout merges.

SUMMARY: The work unit is exceptionally well-consolidated — shared primitives (renderJoinedPanel, destructive_confirm.go, modal_footer.go, the rowBgStyle/rowTokenStyle/renderLeftBarColumn delegate helpers, the notice-band base, the keymap-descriptor-driven footers/help) already collapse what would otherwise be heavy cross-modal duplication, and most files carry comments documenting the prior de-duplication. The three residual findings are all low-severity parallel-wrapper / re-authored-helper cases that crossed task boundaries: loading_view.go forking header.go's leaf-paint pair, the six near-identical cleared-canvas modal wrappers, and the right-anchored footer layout mirrored between the standard and filter footers.
