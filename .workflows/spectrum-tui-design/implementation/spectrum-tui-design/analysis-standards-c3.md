AGENT: standards
FINDINGS:
- FINDING: Sessions/Projects footer renders the words "enter" / "space" where §3.4 pins the glyphs ⏎ / ␣
  SEVERITY: low
  FILES: internal/tui/keymap.go:88-90, internal/tui/keymap.go:128, internal/tui/footer.go:64-66, internal/tui/footer_test.go:41
  DESCRIPTION: §3.4 specifies the condensed Sessions footer "exactly" as `↑↓ navigate · ⏎ attach · / filter · ␣ preview · s switch view · x projects` plus right-aligned `? help`. The keymap descriptor stores the footer Key forms as the literal words "enter" and "space" (the help-modal HelpKey overrides them to ⏎/␣), and the footer renders Key directly — so the live footer reads `enter attach` and `space preview`, not the spec's `⏎ attach` / `␣ preview`. The footer's nav key is also rendered as "↑/↓" (with a slash) vs the spec's `↑↓`. The divergence is documented in-code as a deliberate descriptor decision ("the footer keeps 'enter'/'space'"), and the byte-exact footer test (footer_test.go:41) enshrines the word form, so this was a conscious choice rather than an oversight. Still, it is a literal divergence from the spec's verbatim §3.4 footer copy and from the Preview footer convention, which DOES use glyphs (`⏎ attach`, `␣ back` in §9.1 / previewKeymap). Flagging for the spec owner to ratify the word-vs-glyph footer choice (and align §3.4's wording) or correct the implementation. Low severity: cosmetic, fully legible, and internally consistent across Sessions/Projects.
  RECOMMENDATION: Either (a) ratify the word form by amending §3.4 to read `enter attach … space preview` (and note the slash-nav `↑/↓` form), so the spec and the byte-exact test agree; or (b) switch the Sessions/Projects footer Key forms to the ⏎/␣ glyphs to match §3.4 verbatim and the Preview footer convention. Prefer (a) if the word form tested better in the §15 in-terminal validation; the choice is the spec owner's, not the executor's.

SUMMARY: The spectrum-tui-design reskin conforms to the specification with very high fidelity — the owned-canvas paint + light/dark detect-or-timeout gate (§1/§2.6), the closed MV token vocabulary with the no-raw-hex guard (§2.8/§2.9), NO_COLOR carve-out (§2.5), the two-mode immediate-persist edit modal falling-out rules (§8.2), destructive kill/delete modals (§8.3/§8.6), descriptor-driven ? help on every page incl. the Preview overlay-without-blank (§8.5/§9.3), single-tone cyan preview chrome (§9.1), the 11→5 loading step mapping (§10.4), and the §11.1 empty states all match the decided behaviour, with documented spec-vs-terminal divergences (rounded-input corners, inside-tmux title decoration, the post-corrigendum Preview key bindings) all correctly resolved per the corrigendum. The single finding is a low-severity footer copy/glyph divergence (words "enter"/"space" vs the spec's ⏎/␣ glyphs) that was a conscious, in-code-documented descriptor decision worth ratifying against §3.4.
