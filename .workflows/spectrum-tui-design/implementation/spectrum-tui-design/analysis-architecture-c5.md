AGENT: architecture
FINDINGS:
- FINDING: stepLabelTable maps restore step 6 to "Replaying scrollback", forcing a special-case carve-out in three derived functions
  SEVERITY: low
  FILES: internal/tui/loading_progress.go:84-96, internal/tui/loading_progress.go:324-337, internal/tui/loading_progress.go:109-114
  DESCRIPTION: The §10.4 "single source of truth" stepLabelTable claims to encode the 11→5 mapping once, but step 6 ("Restoring sessions" vs "Replaying scrollback") is genuinely dual-mapped and the table can only hold one entry. The table entry is the completion mapping ("Replaying scrollback"), and the "Restoring sessions" label is then NOT derivable from the table at all — labelDone(), LabelForStep(), and the restoreStep constant each carry a bespoke RestoreM/step-6 branch to compensate. labelDone() even hard-codes `if text == LabelRestoringSessions { return p.completedSteps[restoreStep] }` — a label whose own constituent step is invisible in the table it supposedly derives from. The contract "everything derives from stepLabelTable" is therefore half-true: one of the five labels is a table exception threaded through several call sites. This is correct and well-tested today, but it is the one place the single-source-of-truth claim leaks, and a future edit to the restore-phase grouping must touch the table AND the three special-case branches in lockstep.
  RECOMMENDATION: Consider making the dual-mapping explicit in the data model rather than as scattered runtime branches — e.g. a small struct per step `{label, skeletonLabel}` (skeletonLabel non-empty only for step 6), so labelDone/LabelForStep/counterFor read the structured mapping instead of each re-deriving the step-6 special case. Low priority — the current form is correct and guarded by TestMappingCoversAllElevenStepsNoGaps; flagging only because it is the lone crack in an otherwise clean single-source-of-truth design.

- FINDING: keymap descriptor → live-dispatch correspondence is held only by guard tests, not by the type system
  SEVERITY: low
  FILES: internal/tui/keymap.go:14-21, internal/tui/keymap_dispatch_guard_test.go:97, internal/tui/model.go:2934, internal/tui/model.go:2249
  DESCRIPTION: The keymapEntry descriptor is the single source of truth for the two DISPLAY surfaces (footer + help modal), but key DISPATCH is a separate hand-coded switch over tea key codes in updateSessionList / updateProjectsPage / handlePreviewKey with no compiler link back to the descriptor. A binding change must be made in BOTH the descriptor and the dispatch switch; the correspondence is enforced only by keymap_dispatch_guard_test.go (contributor discipline + CI, not a compile error). The implementation is honest about this (the type doc spells it out) and the guard tests cover all three pages, so this is an accepted, documented seam rather than a defect — but it is the one place in this reskin where a "single source of truth" is split across a data descriptor and an independently-authored imperative switch, and the only thing preventing silent drift is a test that must be remembered to stay comprehensive. Noted as a latent maintenance seam, not a current bug.
  RECOMMENDATION: No change required for this work unit — the guard test is the pragmatic choice given bubbles/list owns the actual key matching. If the keymap grows, consider having the dispatch switch key off the descriptor entries (matching on a tea-key field added to keymapEntry) so the correspondence becomes structural rather than test-enforced. Document the guard-test obligation prominently for future contributors.

SUMMARY: The implementation is architecturally excellent — the shared joined-panel primitive (renderJoinedPanel) is reused across all five modals and the preview chrome, the destructive-confirm grammar and footer key-hint shapes are consolidated to single owners, the Session/Project delegates share rowBgStyle/rowTokenStyle/renderLeftBarColumn free functions, the notice-band single-slot arbiter prevents double-bands, the appearanceGate cleanly owns the detect-or-timeout race, the Deps/Build seam keeps the capture harness rendering the production TUI, and the concurrent cold-boot flip carries a documented four-invariant race review with end-to-end integration coverage. Import boundaries are clean (capture imports tui, never the reverse; production never imports the harness). The only findings are two low-severity single-source-of-truth seams (the step-6 dual-mapping leaking out of stepLabelTable, and the descriptor↔dispatch correspondence held by guard tests rather than the type system) — both correct and tested today, flagged only as latent maintenance seams.
