AGENT: standards
FINDINGS:
- FINDING: Session-list title carries the inside-tmux "(current: %s)" suffix not described by the spec title scheme
  SEVERITY: low
  FILES: internal/tui/model.go:951-978 (sessionListTitleForMode)
  DESCRIPTION: §3.2 / §4.2 / §13.2 pin the Sessions section-header label to exactly three forms — "Sessions" / "Sessions — by project" / "Sessions — by tag". When running inside tmux, sessionListTitleForMode composes a trailing "(current: <name>)" onto the mode base (e.g. "Sessions — by tag (current: foo)"), which is outside the spec's stated title vocabulary. The code documents this as a deliberate "DIVERGENCE FROM SPEC" — it preserves a pre-existing inside-tmux decoration rather than dropping it, consistent with the §1 "Reskin, not rebuild → preserve existing behaviour" mandate (the spec's title scheme is silent on the inside-tmux case). This is a defensible parity preservation, not a bug; flagged only because it is a literal divergence from the §3.2 title strings the spec enumerates, and because the spec never explicitly carved out the inside-tmux decoration. No functional impact and no contrast/role-token concern.
  RECOMMENDATION: No code change required — the divergence is justified parity. Optionally, confirm with the spec owner that the inside-tmux "(current: …)" suffix is intended to survive the reskin (the spec text could be amended to record the carve-out), so the title scheme is unambiguous for any future contributor.
SUMMARY: The implementation conforms to the specification with very high fidelity across every checked decision point — the §2.9 closed token vocabulary (no raw hex at call sites, enforced by a globbing guard test), the §12.1/§12.2 keymap revision (k=kill, x toggle, ←/→ window + Tab pane in preview, ? bound, no vim aliases, no uppercase, arrow-only nav pinned at the list KeyMap), the §8.2 two-mode immediate-persist edit modal with all falling-out rules (empty-on-commit delete, empty-Name revert, case-sensitive duplicate dedupe, cross-project alias collision silent revert), the §8.1/§13.5 modal blank-screen clear-to-canvas, the §8.3/§8.4/§8.6 destructive/rename modal restyles (state.red ▲, n dropped to Esc-only, orange always-editing rename with ◉ EDIT MODE badge), the §9 cyan peek-mode preview chrome, the §10.3/§10.4 honest loading screen tick mapping, the §2.6 OSC-11 detect-or-timeout no-flip gate, the §2.5 NO_COLOR carve-out, and the §11.1/§7.1/§7.3 edge-state copy and footer replacements. The only flag is one self-documented, parity-justified title divergence (low). All package guard/behaviour tests pass.
