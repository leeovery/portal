AGENT: duplication
FINDINGS:
- FINDING: `<key> <label>` footer key-hint helper independently reimplemented five times
  SEVERITY: high
  FILES: internal/tui/kill_modal.go:138 (killModalKeyHint), internal/tui/delete_modal.go:144 (deleteModalKeyHint), internal/tui/rename_modal.go:183 (renameModalKeyHint), internal/tui/pagepreview.go:318 (previewFooterHint), internal/tui/edit_modal.go:561 (editFooterGroup)
  DESCRIPTION: Five separate functions render the identical "key/glyph in accent.blue · single canvas spacer · label in text.detail" footer hint, each via the exact same three lines: `headerStyle(theme.MV.AccentBlue, mode, colourless).Render(key)` + `headerCanvasBg(mode, colourless).Render(" ")` + `headerStyle(theme.MV.TextDetail, mode, colourless).Render(label)` joined with `lipgloss.JoinHorizontal`. killModalKeyHint, deleteModalKeyHint, renameModalKeyHint and previewFooterHint are byte-identical; editFooterGroup adds only an empty-key fast path. The pattern emerged independently across tasks 3-4/3-5/3-7/3-9/3-10/4-6 — the comments even admit it ("the shared footer key-hint shape (mirrors killModalKeyHint)" at pagepreview.go:317, "Mirrors killModalKeyHint" at delete_modal.go). This is the modal/footer chrome convention drifting toward five maintenance points; a colour-role or spacing change must be applied in five places or they silently diverge.
  RECOMMENDATION: Extract one shared helper (e.g. `renderKeyHint(key, label string, mode theme.Mode, colourless bool) string`) into a shared file (footer.go or a new modal_footer.go), honouring the empty-key label-only fast path so editFooterGroup collapses onto it too. Re-point all five call sites. previewFooterHint and editFooterGroup should consume the same helper even though one is for a modal footer and one for the preview footer — the rendered shape is identical.

- FINDING: Destructive confirm modal (kill / delete) body, header, consequence-wrap and footer near-duplicated wholesale
  SEVERITY: high
  FILES: internal/tui/kill_modal.go:62-143, internal/tui/delete_modal.go:69-149
  DESCRIPTION: delete_modal.go is a near-verbatim clone of kill_modal.go — the comments throughout delete_modal.go say so ("Mirrors killModalHeaderRow", "mirrors the kill modal's session-name row", "Mirrors killModalConsequenceRows", "Mirrors killModalFooterRow", "Mirrors killModalKeyHint"). Concretely: (1) killModalHeaderRow/deleteModalHeaderRow are identical (`▲` glyph + gap + title, both state.red+bold via the same headerStyle calls); (2) killModalConsequenceRows/deleteModalConsequenceRows are identical word-wrap loops (`ansi.Wordwrap(<text>, <width>, "")` → `strings.Split` → per-line `headerStyle(TextDetail).Render`), differing only in the const text and width const (both 52); (3) the body-assembly pattern (name row · single canvas-painted blank separator · consequence rows) is duplicated; (4) the footer rows and key-hint helpers are duplicated (also covered by the finding above). Both modals are the same "destructive confirm panel: red ▲ title, red target name, text.detail consequence, y/esc footer" shape — delete just swaps the consequence copy and adds a path row.
  RECOMMENDATION: Extract a shared destructive-confirm-modal builder parameterised by title/glyph, target name, optional secondary line (path), consequence text, and footer verb (kill vs delete). A small struct of the varying copy + one render function would collapse delete_modal.go to its distinct strings (deleteTitle, deleteConsequence, the path row) plus a call. The shared destructive-header helper and the consequence-word-wrap helper are the highest-value extractions since they are literally identical code.

- FINDING: SessionDelegate and ProjectDelegate duplicate canvas/selection style methods and the left-bar column block
  SEVERITY: medium
  FILES: internal/tui/session_item.go:303-327 (rowBg/rowToken) + 367-374 (left-bar block), internal/tui/project_item.go:83-106 (rowBg/rowToken) + 158-162 (left-bar block)
  DESCRIPTION: ProjectDelegate.rowBg and ProjectDelegate.rowToken are byte-identical to SessionDelegate.rowBg and SessionDelegate.rowToken (the comments say "Mirrors SessionDelegate.rowBg" / "Mirrors SessionDelegate.rowToken") — same colourless guard, same bg.selection-vs-canvas branch, same Foreground/Background composition. Separately, the §3.3 left-bar column rendering (`bg.Render(padTo("", leftBarColumnWidth))` for the unselected case; `rowToken(..., AccentViolet, true).Render(selectorBar) + bg.Render(padTo("", leftBarColumnWidth-lipgloss.Width(selectorBar)))` for the selected case) is duplicated verbatim in renderSessionRow (session_item.go:370-374) and renderRowLine (project_item.go:158-162). These two delegates were written independently and converged on the same selection grammar; the bar-rendering logic and the row-style helpers now live in two places.
  RECOMMENDATION: Extract the row-background and row-token style logic into shared free functions (e.g. `rowBgStyle(mode, selected, colourless)` and `rowTokenStyle(base, fg, mode, selected, colourless)`) that both delegates call, and a shared `renderLeftBarColumn(styler, selected)` helper for the §3.3 2-cell selector-bar column. Both delegates already depend on the shared selectorBar/leftBarColumnWidth/padTo primitives in session_item.go, so the helpers have a natural home there.

- FINDING: Kill / delete / rename modal footer-row assembly duplicated
  SEVERITY: low
  FILES: internal/tui/kill_modal.go:129-134 (killModalFooterRow), internal/tui/delete_modal.go:134-139 (deleteModalFooterRow), internal/tui/rename_modal.go:174-179 (renameModalFooterRow)
  DESCRIPTION: The three modal footer-row functions are structurally identical: render a confirm key-hint, render a fixed-gap canvas spacer (the `"   "` *FooterGap const), render a cancel key-hint, join horizontally. They differ only in the key/label/gap constants (y/kill, y/delete, ⏎/rename — all paired with esc/cancel). This is a thin two-hint footer that has been hand-assembled three times. (Overlaps with the key-hint finding above — once the shared key-hint helper exists, this row assembly is a trivial two-call join that could also be a single `renderTwoHintFooter(confirmKey, confirmLabel, cancelKey, cancelLabel, ...)` helper.)
  RECOMMENDATION: After extracting the shared key-hint helper, add a `renderConfirmCancelFooter(confirmKey, confirmLabel, cancelKey, cancelLabel string, mode, colourless)` that the three modals share; the per-modal constants become arguments. Low severity because each instance is ~5 lines, but it is the same logic in three task-boundary copies and is the natural companion to the key-hint extraction.
SUMMARY: The MV reskin's modal/footer chrome shows clear cross-task copy-paste: a `<key> <label>` footer key-hint helper reimplemented five times, the kill and delete confirm modals near-verbatim clones, and the Session/Project list delegates duplicating their canvas/selection style methods and left-bar column rendering. Consolidating the key-hint helper and the destructive-modal builder are the two highest-value extractions; the delegate style helpers and the confirm/cancel footer assembly are smaller follow-ons.
