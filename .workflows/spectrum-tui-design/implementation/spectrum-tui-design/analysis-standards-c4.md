AGENT: standards
FINDINGS:
- FINDING: §10.4 friendly label drops the inline `(N/M)` form the spec text prescribes
  SEVERITY: low
  FILES: internal/tui/loading_progress.go:31,118-123, internal/tui/loading_view.go:430-455,485-497
  DESCRIPTION: §10.4 (and the §10.4 step table) names the restore label `Restoring sessions (N/M)` with a parenthesised inline counter. The implementation renders the label text as bare `Restoring sessions` (LabelRestoringSessions) and appends the counter as a separate space-joined `N / M` segment (no parens), keyed to the active row only. This is a reference-frame-governed choice — the loading_view.go comment cites the `Loading 6 — Combined (thick bar)` frame showing `8 / 12`, and §15.1 makes the Paper frame authoritative for layout/structure over the spec's typographic approximation. The N/M counter is correctly suppressed on the M=0 empty-restore case per §10.4. Flagging only because the rendered form diverges from the spec's literal `(N/M)` wording; the behaviour (counter on the active restore row only, suppressed at M=0) matches.
  RECOMMENDATION: No code change required — the reference frame governs the exact glyph/spacing per §15.1. Confirm the captured `loading-mv.png` vhs frame matches the `8 / 12` spaced form at the visual gate; if the human gate prefers the parenthesised form, that is a frame/spec reconciliation, not a code defect.

- FINDING: §7.3 no-matches glyph uses `∅` (U+2205) where spec prose writes `⌀` (U+2300)
  SEVERITY: low
  FILES: internal/tui/filtering_no_matches.go:26
  DESCRIPTION: §7.3 specifies "a dim `⌀` glyph (`text.faint`)" (U+2300 DIAMETER SIGN). The implementation uses `∅` (U+2205 EMPTY SET), with an inline comment stating "the §7.3 reference shows ∅ in text.faint". The two glyphs are visually near-identical null-set marks; the token (text.faint), placement (centred above the message), and copy are all spec-correct. Per §15.1 the Paper reference frame (`Filtering — no matches (MV)`) is authoritative for the exact glyph over the spec's typographic approximation. Genuinely low impact — a decorative glyph choice governed by the reference frame.
  RECOMMENDATION: No change required if the `Filtering — no matches (MV)` reference frame shows `∅`. The implementer documented the reference as the source; the human visual gate is the lock per §15.4.

- FINDING: §11.3/§11.4 info bands sit on a `bg.selection` tint not mentioned in spec prose
  SEVERITY: low
  FILES: internal/tui/notice_band.go:48-71,97-114
  DESCRIPTION: §11.3 (no-tags signpost) and §11.4 (command-pending banner) describe only an `accent.violet` left-bar over the list — neither §11.3, §11.4, nor the §11 intro mentions a background tint for the persistent violet info bands. The implementation renders both `bandInfo` and `bandCommand` on a `bg.selection` tint (tintToken). The §11 intro and §2.9 reserve `bg.selection` for the selected-row tint; reusing it here is a per-element decision beyond the spec's stated treatment for these bands. This is reference-frame territory — the `Sessions — no tags signpost (MV)` and `Projects — command pending (MV)` frames are authoritative for surface treatment (§15.1), and the implementation comment frames the two info bands as "one info-message element" sharing the violet-anchored surface. The tint is contrast-validated (TestForegroundOnTintPairings covers text.strong on bg.selection). Flagging for visibility: the spec prose itself does not authorise a tint on these bands, so the decision rests entirely on the reference frames.
  RECOMMENDATION: Confirm the two reference frames (`no tags signpost`, `command pending`) actually show the bg.selection tint behind the violet-bar bands at the visual gate. If they show a flat (untinted) violet-bar line per the spec prose, drop the tint for bandInfo/bandCommand. No correctness risk either way (contrast is gated); this is a fidelity check against the authoritative frames.

SUMMARY: The implementation conforms to the specification with exceptional fidelity, including the 2026-06-22 Corrigendum revisions (preview ←/→ window + Tab pane + ◉ marker + footer restructure, the no-fill border-colour focus/edit grammar, the §2.9 user-approved light-variant erratum). The closed-token vocabulary, contrast floor, keymap revisions (§12.2 arrows-only nav, k=kill, x-toggle, ? bind), and modal blank-screen behaviour are all correctly implemented and guard-tested. The three findings are low-severity reference-frame-vs-spec-prose reconciliations (loading counter form, no-matches glyph codepoint, info-band tint) — none is a behavioural or correctness defect; each is governed by an authoritative Paper frame per §15.1 and resolves at the visual gate.
