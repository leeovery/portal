AGENT: duplication
FINDINGS:
- FINDING: SessionDelegate.canvasBg / tokenStyle re-implement headerCanvasBg / headerStyle instead of delegating
  SEVERITY: low
  FILES: internal/tui/session_item.go:197-202 (canvasBg), internal/tui/session_item.go:214-221 (tokenStyle), internal/tui/header.go:87-94 (headerStyle), internal/tui/header.go:99-104 (headerCanvasBg)
  DESCRIPTION: Two foundational leaf-style rules exist as the canonical helpers in header.go: headerStyle (role-token foreground over Background(canvas) for the mode, bare under colourless) and headerCanvasBg (canvas background only, bare under colourless). SessionDelegate.canvasBg is a byte-for-byte re-statement of the headerCanvasBg body (the same `if d.Colourless { return NewStyle() } ... Background(theme.MV.Canvas.ColorFor(mode))`), and SessionDelegate.tokenStyle is the same rule as headerStyle plus a caller-supplied base style. The codebase already demonstrates the intended fix elsewhere: loading_view.go's loadingStyle (line 251) and loadingFg (line 259) deliberately delegate to headerCanvasBg / headerStyle "rather than re-implementing the rule, so the leaf canvas-paint carve-out lives in exactly one place," and SessionDelegate's own rowBg/rowToken (session_item.go:337,344) delegate to the shared rowBgStyle/rowTokenStyle free functions. canvasBg/tokenStyle are the one pair that re-derive the rule instead of delegating, so the colourless-fallback and canvas-resolution logic is duplicated across the delegate and the header layer. This is the same colour-resolution contract maintained in two places — a drift risk if the NO_COLOR carve-out or canvas resolution ever changes.
  RECOMMENDATION: Make SessionDelegate.canvasBg delegate to headerCanvasBg(d.Mode, d.Colourless) and SessionDelegate.tokenStyle delegate to headerStyle(fg, d.Mode, d.Colourless).Inherit(base) (or thread the base through), mirroring how loadingStyle/loadingFg and rowBg/rowToken already delegate to their shared sources. No behaviour change — the bodies are already identical; this just collapses the second copy onto the canonical one.

- FINDING: Solid-block "foreground == background" fill style authored inline in the loading bar
  SEVERITY: low
  FILES: internal/tui/loading_view.go:370-375 (filledStyle / trackStyle), internal/tui/notice_band.go:311-320 (renderCommandChip — Foreground(orange)/Background(bg.warning) chip)
  DESCRIPTION: renderLoadingBar builds its thick bar from two ad-hoc lipgloss styles where the Foreground and Background are set to the SAME token (accent.violet over accent.violet for the filled run; bg.track over bg.track for the track) so a full-block glyph paints as a flush solid cell. This solid-fill-block idiom (a token painted as both fg and bg to produce an opaque coloured cell) is a small bespoke style construction that sits outside the theme-aware leaf-style helpers used everywhere else, and it carries its own inline colourless branch (lines 363-368) that re-derives the NO_COLOR fallback rather than routing through a shared helper. It is a near-relative of the renderCommandChip fill treatment (notice_band.go), which is also a hand-built Foreground+Background style with its own colourless branch. Two independent inline fill-style constructions, each re-stating the colourless carve-out.
  RECOMMENDATION: Low priority given the bar's fill is genuinely distinct (fg==bg solid block) from a normal token-over-canvas run. If consolidated, extract a small solidFillStyle(token, mode, colourless) helper (token painted as both fg and bg, bare under colourless) and use it for the loading bar's filled/track runs; the command chip's distinct fg!=bg pairing stays its own. Only worth doing if a third solid-fill site appears — currently two, so flagged as a watch item rather than a mandatory extraction (Rule of Three not yet met).

SUMMARY: The implementation has exceptionally strong duplication discipline — the modal panel chrome (renderJoinedPanel), destructive-confirm grammar, footer key-hint shape, empty-state body, row delegate styling, notice-band base, section-header row core, and input-box helper were all already extracted into shared single-source helpers with explicit anti-drift comments. The only remaining items are minor: SessionDelegate.canvasBg/tokenStyle re-implement the canonical headerCanvasBg/headerStyle leaf-style rules instead of delegating like every sibling helper does (low, a clean collapse), and a bespoke solid-fill-block style is authored inline in the loading bar with a near-relative in the command chip (low, Rule-of-Three not yet met).
