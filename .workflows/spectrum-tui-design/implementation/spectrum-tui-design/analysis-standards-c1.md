AGENT: standards
FINDINGS:
- FINDING: Stale theme package doc + Token.Color() comments claim light/dark detection has not landed
  SEVERITY: low
  FILES: internal/tui/theme/theme.go:1-12, internal/tui/theme/theme.go:57-64
  DESCRIPTION: The package doc states "Detection (§2.6) lands in 1-7; until then ColorFor(Dark) is what every Color() call resolves to" and "Resolution currently defaults to the DARK variant ... Detection (§2.6) lands in 1-7." The Color() method comment repeats "Until light/dark detection lands (1-7) this is always the DARK variant." This is no longer accurate: detection HAS landed (appearance_gate.go + model.canvasMode + syncResolvedMode), and every live renderer resolves per-mode via ColorFor(mode) — Color() is only used by the dead cursorStyle var and the construction-time brightenHelpStyles (immediately overwritten by canvasHelpStyles in applyCanvasMode). The §2.8/§2.9 token layer itself is correct; only the narrative comments are stale and would mislead a future reader into thinking the resolver still hard-pins dark. No behavioural impact.
  RECOMMENDATION: Update the theme.go package doc and the Token.Color() doc to state that the resolved mode flows from the appearance gate (canvasMode) via ColorFor(mode), and that Color() is a dark-pinned convenience retained only for not-yet-mode-resolved call sites. Remove the "lands in 1-7 / until then" phrasing now that 1-7 has shipped.

- FINDING: Dead package-level cursorStyle var with a misleading "retained for the projects page" comment
  SEVERITY: low
  FILES: internal/tui/session_item.go:15-21
  DESCRIPTION: The package-level cursorStyle = lipgloss.NewStyle().Foreground(theme.MV.AccentViolet.Color()) is unreferenced in production. Its comment claims it is "Retained for the projects page (project_item.go), which still resolves the dark default until its own canvas restyle lands in a later phase," but project_item.go fully resolves per-mode through ProjectDelegate.rowToken → AccentViolet.ColorFor(d.Mode) and never reads cursorStyle. The only live cursorStyle in the codebase is a shadowing local in edit_modal.go:170. This is dead code (YAGNI / code-quality.md) plus a comment that misstates the actual project-page implementation. It is also the last surviving dark-pinned .Color() call at a would-be render site, so leaving it invites a future contributor to wire it back in and silently break light mode.
  RECOMMENDATION: Delete the unused cursorStyle var (and its comment) from session_item.go; the projects delegate already resolves accent.violet per mode.

- FINDING: Command-pending footer adds a "? help" anchor the §11.4 exact footer copy omits
  SEVERITY: low
  FILES: internal/tui/footer.go:84-86, internal/tui/filter_footer.go:130-152
  DESCRIPTION: §11.4 pins the command-pending footer copy exactly: "the footer becomes `⏎ run here · n run in cwd · esc cancel`." renderCommandPendingFooter routes through renderFilterFooter → filterFooterRow, which unconditionally appends the right-aligned `? help` anchor sourced from sessionsKeymap. So the rendered command-pending footer reads `⏎ run here · n run in cwd · esc cancel` ... `? help`, with a footer entry the spec's §11.4 copy does not list. This is defensible against the broader design (§8.5 binds `?` on every page; §11.4 keeps "the full Projects chrome"), but it is a literal divergence from the spec's pinned footer string and was likely an emergent side-effect of reusing the shared filter-footer machinery rather than a recorded decision. The golden-spec-copy memory (verify_copy_against_golden_spec) flags exactly this class.
  RECOMMENDATION: Confirm intent against §11.4. If the `? help` anchor is wanted on the command-pending footer, record that as an explicit decision (it is reasonable). Otherwise suppress the right anchor for the command-pending variant so the rendered copy matches the pinned §11.4 string.

- FINDING: Sessions/Projects footer renders "enter"/"space" words where §3.4 mockup copy shows the ⏎/␣ glyphs
  SEVERITY: low
  FILES: internal/tui/keymap.go:76-90, internal/tui/footer.go:262-267
  DESCRIPTION: §3.4 specifies the Sessions footer copy as `↑↓ navigate · ⏎ attach · / filter · ␣ preview · s switch view · x projects`, using the ⏎ and ␣ glyphs. The descriptor sets the footer Key forms to "enter" and "space" (with HelpKey overrides "⏎"/"␣" used only by the help modal), and the footer renders Key, so the live footer reads `enter attach` / `space preview`. This is a documented decision in keymap.go ("the footer keeps 'enter' / 'space'") under the "all symbols, caret for ctrl" key-glyph choice, so it is deliberate rather than accidental drift — but it diverges from the spec's pinned §3.4 footer string and the §15.1 Paper reference frame, which the verification mandate treats as the colour/structure/copy source of truth.
  RECOMMENDATION: Confirm the footer "enter"/"space" word forms were accepted at the visual gate against the `Sessions — Modern Vivid v2` frame. If the frame shows glyphs, either align the footer Key to ⏎/␣ or record the word-form choice as an approved spec-vs-frame divergence (the rationale is already in-source; it just needs to be sanctioned, not left as a silent mismatch).

SUMMARY: The implementation is a faithful, high-fidelity realisation of the specification: the §2.9 closed-token vocabulary, the owned-canvas + light/dark gate (§1/§2.6), the NO_COLOR carve-out (§2.5), the descriptor-driven footer/help single-source-of-truth (§8.5/§14.4), the two-mode edit modal with all §8.2 falling-out rules, every modal's exact consequence copy (§8.3/§8.4/§8.6), the §11 notice-band arbiter, the §10.4 11→5 loading step map, and the cold-path concurrent flip (§10.2) all conform. The only drift is low-severity: two stale/dead documentation artifacts in the theme/session-item layer that misstate the now-shipped detection, and two footer-copy divergences from pinned spec strings (a `? help` anchor on the command-pending footer and "enter"/"space" word forms) that should be confirmed as intended at the visual gate.
