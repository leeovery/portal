AGENT: standards
FINDINGS:

- FINDING: `portal clean` Load-error path silently degrades to early-exit Debug instead of canonical `Load failed` Warn
  SEVERITY: low
  FILES: cmd/clean.go:98-108
  DESCRIPTION: Spec acceptance criterion 4 requires that when `hookStore.Load()` returns a non-nil error, the cleanup emits the canonical `"stale-hook cleanup: hookStore.Load failed: %v"` Warn. The shared helper `runHookStaleCleanup` implements this correctly. The narrative comment at clean.go:87-97 asserts that when the callsite-local `Load()` at line 98 fails, control "falls through to runHookStaleCleanup ... emits the canonical Warn at its single declaration site." In practice the control flow does NOT fall through: line 98 discards the error with `_`, and `hooks.Store.Load` returns `(nil, err)` on non-ENOENT / non-malformed-JSON read failure (e.g. permission-denied — `internal/hooks/store.go:36-51`). Because `len(nil) == 0`, control enters the early-exit at line 105, emits `persisted=0, skipping` Debug, and returns nil — bypassing the helper. User-facing behaviour is fine (no destruction, command succeeds), but the log fingerprint diverges from the spec: rare Load failures are recorded as `persisted=0, skipping` Debug rather than the canonical `hookStore.Load failed` Warn. This re-introduces the "silent at the adapter" fingerprint that the spec explicitly named as a defect-class fingerprint to eliminate, for the narrow Load-error case. The in-source comment's claim of intent and the actual control flow are contradictory.
  RECOMMENDATION: At cmd/clean.go:98, capture the error: `existingHooks, loadErr := hookStore.Load()`. Only take the early-exit when Load succeeded with zero entries: `if loadErr == nil && len(existingHooks) == 0 { ... }`. When `loadErr != nil`, fall through to `runHookStaleCleanup` which re-Loads, deterministically reproduces the same failure, and emits the canonical Warn at its single declaration site.

SUMMARY: Cycle-1 findings (stale docstring, helper extraction, NoopLogger export) are resolved. One residual low-severity drift: `portal clean`'s Load-error path is swallowed into the `persisted=0` early-exit branch instead of emitting the spec-mandated Warn — contradicting the in-source comment's stated intent. All other spec requirements (helper repurposing, hazard-guard shape, mutual-exclusivity coverage, `IsolateStateForTest` discipline, `//go:build integration` tags, no `t.Parallel` in cmd-package, `*Deps` mock-injection pattern) check out cleanly.
