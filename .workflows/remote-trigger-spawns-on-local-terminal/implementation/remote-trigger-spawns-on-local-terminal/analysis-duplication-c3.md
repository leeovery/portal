AGENT: duplication
FINDINGS:
- FINDING: Ghostty bundle-record fixture literal repeated while sibling fixture strings are hoisted to named vars
  SEVERITY: low
  FILES: internal/spawn/detect_inside_test.go:39, internal/spawn/detect_inside_test.go:229
  DESCRIPTION: detect_inside_test.go hoists the Ghostty/Terminal command paths and .app paths into named package-level vars (ghosttyCommand, ghosttyAppPath, terminalCommand, terminalAppPath, lines 23-28) but leaves the paired bundle-record literal `{bundleID: "com.mitchellh.ghostty", name: "Ghostty"}` inlined in two reader-map fixtures: once inside localWalkSeams() (line 39) and again in the "fails safe to NULL when the most-active winner walk transiently fails" subtest (line 229). The same bundle id/name pair is thus authored twice in the one new file, half-single-sourced (the app path it keys off already has a name, ghosttyAppPath, but the id/name value does not). This is a consistency/drift nit, not high-impact duplication: only two occurrences (below the Rule-of-Three bar) and a small one-line record. A production const ghosttyBundleID ("com.mitchellh.ghostty") already exists in detect_outside.go, though the sibling detect_test.go deliberately re-declares expectation strings independently to avoid tautological mirroring, so keeping the test literal decoupled from the production const is a defensible counter-consideration.
  RECOMMENDATION: Optional, low priority. For symmetry with the already-hoisted ghosttyCommand/ghosttyAppPath vars, hoist the id/name to named test vars (e.g. ghosttyBundleName = "Ghostty" alongside a test-local ghosttyBundleIDLiteral) and reference them in both reader maps; or leave the second reader map to reuse localWalkSeams()'s reader (the fail-safe subtest only needs a bespoke walker, not a bespoke reader). Do not couple the test to the production ghosttyBundleID const if the codebase's convention is to keep expectation literals independent.
SUMMARY: The two changed files are essentially duplication-free: detect_inside.go composes the shared walkToBundle/transient/Identity abstractions rather than re-implementing the walk, selectTriggeringClient is a single unique helper, the tmuxClientLister->ClientActivity mapping is a deliberate documented adapter boundary, and detect_inside_test.go already extracts the shared localWalkSeams() fixture reused by detect_test.go. The only in-scope consolidation candidate is a low-severity repeated Ghostty bundle-record literal within the test file.
