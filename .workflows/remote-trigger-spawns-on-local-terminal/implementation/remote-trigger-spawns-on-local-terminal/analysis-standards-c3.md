AGENT: standards
FINDINGS: none
SUMMARY: Implementation conforms to the specification and project Go conventions. detectInsideTmux inverts the old filter-then-tiebreak order to select-winner-then-locality-check exactly as specified (max client_activity across ALL clients via strict `>` so first-listed wins an exact tie; walk only the winner; empty list -> clean NULL; transient winner walk -> NULL + ErrDetectTransient), the full docstring contract (lines 49-91) was rewritten to describe the new algorithm and the deliberately-dropped walk-resilience property with no stale "NULL-filtering is the primary signal" / "client_activity ONLY disambiguates locals" text left in place, the DI signature detectInsideTmux(session, lister, walker, reader) and clientLister seam are intact, and the two codified-bug tests were inverted/reframed (remote-most-active -> NULL; transient-winner-walk -> NULL+ErrDetectTransient) while the net-new local-most-active-with-idle-remote-bystander case and every pinned invariant (pure-remote, single-local, 2+ locals both orderings, first-listed tie, list-clients failure, single-client walk failure preserved distinctly) stay green. Tests pass; no t.Parallel() per project convention (correctly overriding the golang-testing skill default); assertions are exact-equality/errors.Is with no substring checks.

Verification notes (no drift found):
- Spec §"The Fix" steps 1-4: implemented verbatim (detect_inside.go:92-112, selectTriggeringClient detect_inside.go:118-126).
- Spec §"Edge Contracts to Pin": empty-list clean NULL (detect_inside.go:98-101); deterministic first-listed tie-break via strict `>` (detect_inside.go:121); epoch-second granularity accepted, no workaround added.
- Spec §"Owned Behaviour Change": dropped walk-resilience property is owned in the docstring (detect_inside.go:71-77) and locked by the reframed regression test (detect_inside_test.go:212-245).
- Spec §"Testing Requirements": inverted :133 -> detect_inside_test.go:108-114; reframed :196 -> detect_inside_test.go:212-245; net-new local-drives case -> detect_inside_test.go:123-130; single-client walk-failure preserved distinct from the reframed winner-walk case -> detect_inside_test.go:187-210. Session-passthrough assertion retained (detect_inside_test.go:149-151).
- Spec §"Scope": change is confined to detect_inside.go / detect_inside_test.go; detect.go and Detect() consumers untouched, consistent with the single-localized-change scope. Real end-to-end multi-client scenario is out of unit-test reach and flagged in the spec for manual verification — correctly not attempted in unit tests.
- code-quality.md SRP: winner selection extracted to a named pure helper (selectTriggeringClient), no boolean/untyped params, no magic numbers, low cyclomatic complexity via early returns.
