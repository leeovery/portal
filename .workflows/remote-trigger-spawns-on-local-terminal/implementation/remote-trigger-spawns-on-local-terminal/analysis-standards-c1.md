AGENT: standards
FINDINGS: none
SUMMARY: Implementation conforms to specification and project conventions. detectInsideTmux was inverted exactly as specified — select the max-client_activity winner across ALL clients (strict `>`, so first-listed wins an exact tie), then walk ONLY that winner, with resolved→drive / clean-NULL→no-op / transient→NULL+ErrDetectTransient-wrapped-WARN branches; empty client list and list-clients failure edges preserved verbatim. The docstring was fully rewritten to own the deliberately-dropped walk-resilience property, and the two sentences the spec flagged for inversion ("NULL-filtering is the primary signal" and "client_activity is used ONLY to disambiguate among host-local clients") are gone. The DI seam and `detectInsideTmux(session, lister, walker, reader)` signature are intact and the winner is computed in Go with no extra tmux round-trip, per the chosen approach. All prescribed test transforms are present and correct: the codified-bug `:133` test now asserts NULL for a remote-most-active mixed set; the `:196` resilience test is reframed to NULL+ErrDetectTransient when the winner walk flakes; the net-new "local most-active, remote idle bystander → drive" case is added (remote listed first to prove max-by-activity, not first-listed luck); and every pinned invariant (pure-remote NULL, single-local drive, empty→NULL, 2+ locals highest-activity, exact-tie first-listed, list-clients failure, and the distinct single-client walk-failure that the spec warned not to delete) is retained. Scope is exactly the single localized change plus its test — detect.go, the three Detect() consumers, and all unaffected paths are untouched. Tests pass. Non-table-driven subtest style matches the pre-existing file convention and the spec's transform-in-place instruction, so it is not drift.
