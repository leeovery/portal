AGENT: standards
FINDINGS: none
SUMMARY: Implementation conforms to the specification and project conventions. detectInsideTmux is inverted exactly as specified (select most-active winner across ALL clients with first-listed tie-break via strict `>`, then walk ONLY that winner via walkToBundle's three-shape contract); the empty-client guard precedes selectTriggeringClient so there is no clients[0] panic; the docstring is fully rewritten with the two spec-flagged sentences removed and the dropped walk-resilience property explicitly owned; and every required test transform is present and correct — inverted codified-bug test (remote most-active -> NULL), reframed resilience test (winner walk transient -> NULL + ErrDetectTransient), the net-new local-most-active-drives test, and the preserved-distinct single-client walk-failure test — with all pinned invariants intact and scope contained to detect_inside.go plus its test.
