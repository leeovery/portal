TASK: resume-hooks-silently-lost-6-19 — "Promote The Shared Log-Test Helpers Into internal/logtest" (tick-fda489, phase 6, implementation-analysis consolidation)

ACCEPTANCE CRITERIA:
- No test file outside `internal/logtest` declares a sink-install helper, a `(component, msg)` filter, or a level filter.
- The exact-level and at-or-above-level accessors are distinct and named so the difference is unmissable.
- `internal/logtest` imports `internal/log` with no cycle.
- Verdict count unchanged across all affected packages.
Tests (micro acceptance): `internal/logtest`'s own tests cover the new accessors, including the exact-vs-at-or-above distinction (a record at ERROR must not satisfy the exact-WARN accessor).

STATUS: complete

SPEC CONTEXT: None applicable. This is a phase-6 task generated by the implementation phase itself, not specified work — its authority is its own body (per the shared verifier context, phases 6–9 are implementation-analysis cycles). The specification governs resume-hook key durability and says nothing about test scaffolding. The task's own outcome statement ("`internal/logtest` owns the sink lifecycle, the level filter and the record-shape assertion for the whole repo") is the contract I judged against, and it is reflected in the `logtest` row of CLAUDE.md, which matches the code as it stands.

IMPLEMENTATION:
- Status: Implemented (delivered at 88191dcd, then carried forward correctly by later plan tasks 7-4/7-14/7-15/8-23/8-42/8-49/9-30, which reshaped the accessor surface without losing this task's outcome). Judged at HEAD, which is the source of truth.
- Location:
  - `internal/logtest/install.go:11` — `Install(t) *Sink`, the four-line install (fresh `Sink` + `log.SetTestHandler`, restore via that helper's own cleanup). This is the `logtest -> internal/log` edge.
  - `internal/logtest/assert.go:14`/`:25` — `RecordWant` + `AssertRecord`, the component-parameterised five-property audit block (level/msg/component/op/via), each mismatch reported separately.
  - `internal/logtest/capture.go:130` `AtExactLevel` and `internal/logtest/capture.go:135` `AtOrAboveLevel` — the two level filters, distinct names stating the difference on their face, with each doc comment naming the other.
  - `internal/logtest/capture.go:149` `Matching(component, msg)` — the `(component, msg)` filter, lifting `Record.Matches` (`capture.go:113`); `capture.go:157` `Only` terminates the chain.
  - `internal/logtest/leaf_guard_test.go:26` — the dependency allowlist pinning the package to `internal/harnesstest` + `internal/log` across both lanes, with a negative case proving the guard bites.
  - `internal/logtest/install_guard_test.go:31`/`:56` — `Install` is enforced as the only route to a capture handler; the three surviving hand-swaps are sanctioned by name with a written reason (`cmd/logging_capture_test.go` discard silencer, `cmd/open_test.go` level-gate handler, `internal/hooks/store_test.go` JSON rendering).
- Notes:
  - Family (a) — all eight named copies are gone. A repo-wide grep for `SetTestHandler` outside `internal/log`/`internal/logtest` returns exactly the three sanctioned sites named above; every other suite takes `logtest.Install(t)`.
  - Family (b) — `summariesFor`, `onlySummary`, `hydrateRecord` return no matches anywhere in the tree. Their call sites now read `sink.Records().Matching(c, m).Only(t, …)` (e.g. `cmd/state_daemon_cycle_summary_test.go:79`, `internal/state/fifo_sweep_summary_test.go:34`, `cmd/state_hydrate_replayed_log_test.go:68`), and the "no such summary" cases read as a zero-length `Matching(...)` — the same semantics the hand-rolled pair had.
  - Family (c) — `warnRecords` is gone and the constraint was honoured: `internal/spawn` holds 19 level assertions and every one of them is `AtExactLevel`; `AtOrAboveLevel` appears nowhere in that package, so none of the WARN assertions was widened to admit ERROR.
  - Family (d) — the five inline copies now call `logtest.AssertRecord` (`internal/alias/store_logging_test.go`, `internal/project/store_logging_test.go`, `internal/hooks/store_test.go`, `cmd/config_migrate_logging_test.go`, `internal/storelog/clean_stale_test.go`). The Do-list instruction to keep `hooksRecordWant`'s ergonomics working was met exactly: `cmd/logging_capture_test.go:44` keeps the type and `:51` delegates to `logtest.AssertRecord` with `Component: "hooks"` filled in, so `cmd/hooks_test.go:742` and `cmd/hookkey_vocabulary_test.go:259` are unchanged in shape.
  - Conversions strengthened rather than weakened the assertions in places: `internal/alias/store_logging_test.go` sites that previously checked only level/msg/op now also check `component` and `via`, because `AssertRecord` covers all five.
  - The remaining `&logtest.Sink{}` constructions across the tree (e.g. `cmd/bootstrap/bootstrap_test.go`, `internal/tmux/hooks_register_warn_test.go`) are sinks handed to an injected `*slog.Logger` seam, not process-wide handler swaps — outside the family this task consolidated and outside the guard's rule by design.

TESTS:
- Status: Adequate.
- Coverage:
  - `internal/logtest/install_test.go:10` proves `Install` routes component loggers into the returned sink; `:24` proves the restore by nesting an inner install inside a subtest and asserting the outer sink sees only the post-subtest line — which is the property a hand-rolled install was free to forget.
  - `internal/logtest/capture_test.go:334` covers the exact-vs-at-or-above distinction the task's Tests section names: a capture holding DEBUG/WARN/ERROR yields exactly the WARN record from `AtExactLevel(WARN)` (so the ERROR record does not satisfy it), and `:315` yields two from `AtOrAboveLevel(WARN)` over the same shape. The micro acceptance is met literally.
  - `internal/logtest/capture_test.go:355` (`Matching`) covers both the hit set and the empty case, and `:374` covers composing `Matching` with `AtExactLevel` — the shape the converted call sites actually use.
  - `internal/logtest/assert_test.go:15`/`:28` cover `AssertRecord` passing, and — driven through `harnesstest.Recorder` — reporting all five mismatches rather than stopping at the first, which is the property that makes it a drop-in for the inline blocks it replaced. `:49`–`:69` cover `AssertWriteFailure` pass, wrong `error_class`, and an error not wrapping the sentinel.
- Notes:
  - Verdict count: comparing every `_test.go` in the commit before and after, the count of `func Test` + `t.Run(` is identical in all 43 converted files; the only deltas are additions inside `internal/logtest` itself (`assert_test.go` +2, `install_test.go` +3, `capture_test.go` +4). The criterion holds, and the new verdicts are the task's own required coverage.
  - Not over-tested: each new test names one property, and the negative paths are driven through the shared `harnesstest.Recorder` rather than by duplicating the helper's logic in the assertion.
  - I did not execute any suite (reading only, per the review protocol); the judgement above is from reading the conversions and their call sites.

CODE QUALITY:
- Project conventions: Followed. `logtest` stays test-only and leaf-shaped; the `internal/log` edge is the one the task authorised and is pinned by `leaf_guard_test.go` across both lanes with `ForbiddingThirdParty`, so a future edge into every test package in the tree fails a guard rather than landing quietly. `AssertRecord`/`AssertWriteFailure` report through `harnesstest.TestingT`, keeping their own failure paths unit-testable, and `AssertWriteFailure` takes the sentinel as a parameter rather than importing `internal/fileutil` — consistent with the leaf rule. All helpers call `t.Helper()`.
- SOLID principles: Good. One filter per dimension, composed rather than crossed; `Records.filter` is the single traversal and `Record.Matches` the single home of the (component, msg) predicate, so `Matching` restates neither half.
- Complexity: Low. Every helper is a straight-line predicate or a fixed five-check block.
- Modern idioms: Yes — method chaining on a named slice type, `errors.Is` for the sentinel check, no reflection.
- Readability: Good. The doc comments state the contract that matters at each site (nil-when-empty, receiver untouched, exact-vs-threshold, why `Matching` lifts `Matches`), and the guard's sanctioned-site map records *why* each surviving hand-swap cannot take a `Sink`.
- Comment accuracy: The comments hold against the code. `install.go:9` describes exactly what `Install` does; `capture.go:128`/`:134` correctly describe the two level filters and point at each other; `assert.go:44` correctly explains why the sentinel is a parameter; the `logtest` row of CLAUDE.md matches the delivered surface. No process-artifact references (no task ids, phases or spec sections) in any of the changed code.
- Security: N/A — test-only scaffolding, no external input.
- Performance: N/A — per-call slice filters over capture-sized sets.
- Issues: None.

BLOCKING ISSUES:
- None.

FINDINGS:
- None.
