TASK: resume-hooks-silently-lost-8-36 — The Declared-Once Hydrate-Wait Invariant Is Half-Applied
(tick-fc632f; severity: drift; phase 8 — implementation-analysis cycle, so its authority is its own body)

ACCEPTANCE CRITERIA:
- No raw budget/tick pair remains in `internal/restore`.
- The short budget is one named declaration used by all five `cmd/bootstrap` hook-fire sites, and their waits are
  unchanged in length.
- The marker-count test fails under a budget shorter than the shared one.
- The non-contiguous reboot fixture pins a count, not just an absence.

STATUS: complete

SPEC CONTEXT:
The specification says nothing about test budgets (no match for "budget" anywhere in it), which is expected — this is a
phase-8 quality task generated by the implementation, judged against its own body. The one spec anchor it touches is the
H6 case: `specification.md:20` and the `:493` coverage row require the non-contiguous-window reboot fixture to assert
"the hook fires **and** survives the post-restore sweep". AC4's move from a file-presence wait to a counted assertion
tightens exactly that claim — a hook that fired twice, or a sibling's hook leaking into the file, previously satisfied
the presence check.

IMPLEMENTATION:
- Status: Implemented (commit 4b9bbad6, 7 files)
- Location:
  - `internal/restoretest/marker_count.go:11-31` — the two budgets now sit in one commented const block:
    `HydrateBudget`/`HydrateTick` (10s/50ms, unchanged) and the new `PaneReactionBudget`/`PaneReactionTick` (2s/50ms).
  - `internal/restoretest/marker_count.go:43` — `AssertMarkerCount` widened from `*testing.T` to
    `harnesstest.TestingT`, which is what lets the new overshoot case drive the exported entry point through a
    `Recorder`. Matches the convention CLAUDE.md states for fatal-on-failure helpers.
  - Holdouts routed: `internal/restore/integration_full_test.go:129` and
    `internal/restore/exit_closes_pane_integration_test.go:164` (both were the raw `10*time.Second, 50*time.Millisecond`
    pair), plus `internal/restore/exit_closes_pane_integration_test.go:53` (was the local `exitClosesPaneBudget, 50ms`).
  - Short pair routed at exactly five `cmd/bootstrap` sites:
    `eager_signal_hydrate_integration_test.go:87,147,155,236` and `phase2_hook_fire_integration_test.go:83`.
  - `cmd/noncontiguous_window_reboot_integration_test.go:400-405` — `WaitForFileExists` replaced by
    `AssertMarkerCount(t, p.markerFile, p.markerText, 1)` per stamped pane, with a comment naming why.
- Notes:
  - AC1 verified by enumeration, not by sampling: `internal/restore` now holds no `10 * time.Second` literal at all, and
    every `WaitForSkeletonMarkersCleared` / `WaitForFileExists` / `waitForPaneText` call site in the repo (17 of them)
    passes one of the two named pairs — the only exception is noted as an out-of-scope finding below.
  - AC2 verified: five sites, one declaration, values byte-identical to the 2s/50ms they replaced, so no wait changed
    length. `time` is still imported and used in `eager_signal_hydrate_integration_test.go:252-260`, and its removal
    from `phase2_hook_fire_integration_test.go` is correct — that file has no other `time.` reference.
  - AC4's counted assertion is sound for this fixture: `markerFile` and `markerText` are distinct per stamped pane
    (`cmd/noncontiguous_window_reboot_integration_test.go:241-258`), so a second firing of a pane's own hook fails the
    count, while a hook firing in the wrong *pane* is caught by the recorded `$TMUX_PANE` in
    `assertDivergentHookFiredInPane` (`:488-504`). The swap also removes a latent race the old wait had: the hook is
    `echo … >> file`, so `>>` creates the file before `echo` writes it, and `WaitForFileExists` could return on an empty
    file that `assertDivergentHookFiredInPane` then read as a content mismatch.
  - Considered and rejected as a finding: `internal/restore/exit_closes_pane_integration_test.go:23` still declares
    `exitClosesPaneBudget = 2 * time.Second`, and `awaitPaneGone` ticks at a literal `50 * time.Millisecond` (`:182`).
    That is a pane-*closure* wait, not a hydrate wait — the invariant this task closes is named for the hydrate wait in
    the task's own title and problem statement, and the budget it uses is already named locally rather than raw.
  - `AssertMarkerCount` reports through `Errorf`, where the replaced `WaitForFileExists` fatals. In the non-contiguous
    fixture that only changes where the run stops, not whether it fails: the later `assertDivergentHookFiredInPane`
    fatals on the same absence.

TESTS:
- Status: Adequate
- Coverage: `internal/restoretest/marker_count_test.go:163-205` now carries the three cases the task asked for.
  `"it waits out a marker that lands past a shorter budget"` (`:164`) writes at `pastProbeBudget` = 600ms and passes
  through the exported entry point; `"it fails when the caller's budget is shorter than the shared one"` (`:175`) drives
  the same 600ms delay through `probeAssertion`'s 400ms budget and requires a failure; `"it fails when more markers fire
  than expected"` (`:191`) seeds two markers and requires the exported entry point to fail a want of 1. The pair of the
  first two is what makes AC3 true — the old single case wrote at 100ms, so any budget above ~150ms satisfied it, and
  the bar is now 600ms.
- Notes:
  - The named `probeBudget`/`probeTick`/`pastProbeBudget` consts (`:12-25`) replace six repeated `400*time.Millisecond`
    literals across the file, and the derivation `pastProbeBudget = probeBudget + 200*time.Millisecond` keeps the two
    cases moving together — a change to `probeBudget` cannot silently invert either.
  - Not over-tested: `:191` overlaps `TestAssertMarkerCount_MarkerFiresMoreOftenThanWanted` (`:90`) in subject, but the
    two drive different entry points (exported function vs the internal `markerAssertion`), which is the distinction the
    parent test's name is about.
  - Flake margin checked: the shorter-budget case gives the poll 200ms of slack (400ms deadline, 600ms write), in the
    direction where a heavily loaded machine could produce a false pass rather than a false failure only if the poll
    loop stalled past its own deadline by >200ms. Comparable to the pre-existing 250ms margin at `:61`. Acceptable.
  - Unit-lane cost: the two timed subtests add ~1.2s of real sleeping to `go test ./...` (previously ~0.1s). Small
    against a lane CLAUDE.md describes as dominated by `internal/tui`, and it is the mechanism the task prescribed.
  - No test execution was attempted; adequacy is judged by reading.

CODE QUALITY:
- Project conventions: Followed. `restoretest` stays test-only; the widened parameter takes the shared
  `harnesstest.TestingT` rather than declaring a local subset, which is the rule CLAUDE.md states. All five routed
  `cmd/bootstrap` sites and both `internal/restore` sites keep their `//go:build integration` tags, so no
  binary-building or daemon-spawning test moved lanes. No `t.Parallel()` added.
- SOLID principles: Good. The budgets are data, declared once and consumed by name; `AssertMarkerCount` keeps its single
  responsibility and gained only a wider parameter type.
- Complexity: Low. The only structural change is one flat test split into three subtests.
- Modern idioms: Yes.
- Readability: Good. The const block's preamble ("declared together because what separates them is how much of the
  chain is still ahead of the wait when it starts") states the distinction at the declaration, which is exactly the
  "half-applied" defect the task names — the difference is no longer implied by five locals.
- Issues: The `PaneReactionBudget` doc's "Nothing but the pane's own shell is left to run" is a slight understatement
  for `phase2_hook_fire_integration_test.go:83`, where the already-spawned helper still replays scrollback before
  exec'ing the hook. The material claim it makes — the pane process is live, versus the cold chain `HydrateBudget`
  spans — holds at every one of the six sites, and the value at that site is unchanged, so this is a prose nuance
  rather than a claim the code falsifies.

BLOCKING ISSUES:
- None.

FINDINGS:
- [out-of-scope] [contained] cmd/bootstrap/reboot_roundtrip_test.go:492 — a fourth hydrate marker-clear wait the task's
  problem statement did not enumerate: `waitForSessionMarkerCleared(t, client, "alpha", 10*time.Second)` passes a raw
  budget to a local helper whose poll tick is a hard-coded `50 * time.Millisecond` (`:530`) — the same 10s/50ms pair
  the shared names now hold. Routing it through `restoretest.HydrateBudget` and `restoretest.HydrateTick` (the helper's
  `timeout` parameter already takes a `time.Duration`; the tick needs a parameter or the constant inlined) completes the
  invariant. FAILS: the "declared once" property the task exists to establish is still not whole — three waits in this
  same file (`:152`, `:506`, `:623`) already read the shared names, so a reader meets both forms in one file, and a
  future move of `HydrateBudget` would silently leave this site on the old value. Out of scope because the line predates
  this work unit (it came in with `built-in-session-resurrection`, commit c0dd24a2) and this task's commit did not touch
  the file — it is the user's to take or leave.
