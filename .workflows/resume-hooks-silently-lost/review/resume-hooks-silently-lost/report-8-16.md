TASK: resume-hooks-silently-lost-8-16 (tick-fe9223) — "Four Commander Fakes Outside cmd Collapse The Run/RunRaw Trim-Versus-Verbatim Split": promote `cmd`'s `scriptedCommander` to a shared test home, give it a fatal-on-`RunRaw` mode, re-point the four collapsed fakes, delete `cmd`'s local copy.

ACCEPTANCE CRITERIA:
- One declaration honours the split; the four local fakes are gone.
- `Run` returns trimmed output and `RunRaw` verbatim, in every package that fakes the interface.
- `internal/state`'s fatal-on-`RunRaw` stance survives as a mode, not a declaration.
- Both lanes pass.

STATUS: complete

SPEC CONTEXT: This is a phase-8 implementation-analysis consolidation task, not specified bugfix work — per the shared verifier context, its authority is its own body. The specification for `resume-hooks-silently-lost` mentions `Commander` once (line 494), and only to say a fake `Commander` modelling the intended semantics cannot catch the `show-options` probe discrimination, which is a real-tmux argument unrelated to this task. Judged against the task body.

IMPLEMENTATION:
- Status: Implemented
- Location:
  - `internal/commandertest/scripted.go` — the promoted fake: `Scripted` (ordered argv script + recorded call log), constructors `New`/`Quiet`/`Delegating`/`FromFunc`, entry builders `Returns`/`Fails`/`When`/`Answering`/`Doing`, matchers `Any`/`ArgvPrefix`, policies `AllowingUnmatched`/`DelegatingTo`/`Strict`, and the contract itself at `internal/commandertest/scripted.go:192` (`Trim`) and `:200` (`Verbatim`), consumed by `Run` (`:170`) and `RunRaw` (`:182`).
  - Re-pointed suites: `internal/tmux/tmux_test.go:10` (import; the old `MockCommander` declaration is gone), `internal/restore/session_test.go:13`, `internal/restoretest/restore_marker_test.go:8` (`serverOptionRecorder` now embeds `*commandertest.Scripted`, `internal/restoretest/restore_marker_test.go:19`), `internal/state/capture_test.go:34` (`captureMock.commander()`).
  - `cmd`'s local copy is deleted: `cmd/scripted_commander_test.go` and `cmd/scripted_commander_contract_test.go` no longer exist, and no reference to `scriptedCommander`, `newScriptedCommander`, `quietCommander`, `commanderRun`, `commanderRunRaw`, `allowingUnmatched`, `delegatingTo` or `argvPrefix` survives in `cmd/*.go` (one stale *message* does — see FINDINGS).
- Notes:
  - Criterion 1 verified by enumeration: the only remaining `RunRaw` method declarations in the tree are `internal/commandertest/scripted.go:173` (the promoted fake), `internal/tmux/tmux.go:48` (the real commander), `cmd/state_daemon_run_test.go:74`, `internal/tmuxtest/socket.go:106`, `internal/transienttest/commander.go:78`, `internal/transienttest/socket.go:29`, and `cmd/bootstrap/transient_listpanes_helpers_integration_test.go:195`. None of the four named collapsed fakes remains.
  - Criterion 2 verified against each of those: `cmd`'s bespoke `daemonFakeCommander` routes through `commandertest.Trim` (`cmd/state_daemon_run_test.go:71`) and `commandertest.Verbatim` (`:83`); `internal/tmuxtest/socket.go` and `internal/transienttest/socket.go` are real-tmux commanders that trim/return verbatim themselves; `internal/transienttest/commander.go` is a failure-injection wrapper delegating to `Inner`'s own `Run`/`RunRaw`; `panicCommander` panics on both and expresses no output contract. `Trim`/`Verbatim` match `RealCommander` exactly, including the empty-string-on-error behaviour (`internal/tmux/tmux.go:52-62`).
  - Criterion 3 verified: `internal/state/capture_test.go:39` builds the fixture's commander with `.Strict()`, and the deleted `captureMock.RunRaw` fatal declaration is gone. It is the only production-suite consumer of `Strict` in the tree (the other is the mode's own test at `internal/commandertest/scripted_test.go:197`).
  - The structural claim that the package is importable from inside `internal/tmux` holds and is compiler-enforced: `internal/tmux/option_discriminator_internal_test.go` is `package tmux` and imports `internal/commandertest`, so an `internal/tmux` import added to the fake would be a genuine import cycle rather than a silent regression.
  - `internal/commandertest` is imported by no non-test file, so the test-only rule is respected.
  - The one compile-time assertion lost in the move (`var _ tmux.Commander = (*scriptedCommander)(nil)`) is unavoidable — the package cannot import `internal/tmux` — and is redundant in practice: the fake is passed to `tmux.NewClient` at hundreds of sites, so a divergence fails to compile immediately.
  - Drift check on the promoted surface: nothing in the old `cmd` implementation was dropped. Added capability (`Answering`/`AnswerFunc`, `FromFunc`, `Strict`, `ResetCalls`, `MatchedCall(s)`/`FirstIndex`) is what the `RunFunc`-shaped fakes needed to be re-pointed at all. Every exported symbol has a consumer outside the package except `AnswerFunc` and `Commander`, which are the declared parameter/return types of `Answering`/`FromFunc` and `DelegatingTo`.

TESTS:
- Status: Adequate
- Coverage: `internal/commandertest/scripted_test.go` carries the contract's own coverage, including the three subtests the task names verbatim — `"it trims Run output"` (:179), `"it returns RunRaw output verbatim"` (:187) and `"it fatals on RunRaw in strict mode"` (:195, which also pins that `Run` is untouched by strict mode and that the post-fatal return is the empty pair). Alongside them: first-match-wins ordering, call recording/reset, the three unmatched policies (loud report, canned pair, delegate), scripted errors on both methods, per-entry side effects, predicate matching, and `FromFunc` trim/verbatim.
- Notes:
  - The "pure refactor" claim holds under inspection. Comparing every `t.Run` name in the commit's diff, exactly one subtest name changed and none was lost: `"calls show-hooks -g <event> and returns raw output"` → `"calls show-hooks -g <event> and returns the unparsed output"` (`internal/tmux/hooks_test.go:12`). That change is a correction, not a weakening: `ShowGlobalHooksForEvent` reads through `Run` (`internal/tmux/tmux.go:711`), so the old assertion's "verbatim" only held because the old fake collapsed the split. The replacement asserts the trimmed value against a two-entry, newline-terminated fixture, so a move of that read to `RunRaw` now fails the test — which is the whole point of the task.
  - No test function was removed anywhere in the commit (only `TestScriptedCommander` → `TestScripted`, moved with the fake).
  - The per-fixture "unexpected command" guards survive one-for-one in `internal/tmux/portal_saver_test.go`, `internal/tmux/portal_saver_lifecycle_events_test.go` and `internal/tmux/tmux_test.go`. `internal/state/capture_test.go`'s guard is now the shared fake's loud default — `Errorf` plus an error returned to the caller rather than the old `Fatalf`, which still fails the test.
  - Conversions that lost no discrimination: the `RunFunc` switch fakes became `commandertest.FromFunc` closures with identical bodies (`internal/restore/session_test.go`, `internal/tmux/portal_saver_test.go`); `internal/restoretest`'s `serverOptionRecorder` gained arity-exact matchers (`optionArgv`) so `-s`/`-su`/`-sv` can no longer answer for each other.
  - Not over-tested: the fake's suite asserts each behaviour once, and the four re-pointed suites gained no assertions beyond the one correction above.
  - Tests were judged by reading, not execution (no test run is in this reviewer's remit), so "both lanes pass" is verified only structurally: no dangling reference to any removed helper remains, and the later phase-9/10 commits build on this package.

CODE QUALITY:
- Project conventions: Followed. Untagged and stdlib-only beyond `internal/harnesstest` (which is itself stdlib-only), so it serves both lanes; test-only and imported by no production file; the fake reports through `harnesstest.TestingT` rather than declaring its own stand-in, per CLAUDE.md. The local `recordingTB` in `internal/commandertest/scripted_test.go:16` is the sanctioned exception CLAUDE.md names by hand ("their subject is what the helper *returns* after it fatals" — `harnesstest.Recorder.Fatalf` stops the helper, which this suite must not do).
- SOLID principles: Good. One responsibility (script an argv, record the call, apply the trim contract); the policy variants are composed at the construction site rather than branched inside; `Trim`/`Verbatim` are exported so a bespoke fake reuses the contract without inheriting the script.
- Complexity: Low. `dispatch` is the only branching path and it is linear.
- Modern idioms: Yes. `append([]string(nil), args...)` at `:212` and `:244` is carried over verbatim from the deleted `cmd` implementation and is the same shape already living at `cmd/state_daemon_run_test.go:64` and `:76`, so it introduces no new lint posture.
- Readability: Good. The package doc and the `Scripted` doc comment both state why the loud default exists and why `Errorf` rather than `Fatalf` is used on the unmatched path; the `Strict` doc states its `New`-only precondition.
- Issues: None beyond the finding below. `Strict()` on a `tb`-less fake (`Quiet`/`Delegating`/`FromFunc`) would nil-deref on `RunRaw`, but the constraint is documented at `internal/commandertest/scripted.go:112-113` and no call site does it — the only consumer is `internal/state/capture_test.go:39`, built by `New`.

BLOCKING ISSUES:
- None. All four acceptance criteria are met in substance.

FINDINGS:
- [in-scope] [contained] cmd/spawn_seams_test.go:51 — the failure message reads `"Exists returned false; quietCommander defaults to no error, want true"`, but this task renamed that call site's helper at `cmd/spawn_seams_test.go:41` from `quietCommander()` to `commandertest.Quiet()` and deleted the old declaration, so `quietCommander` now exists nowhere in the tree except inside this message. Change the message to name `commandertest.Quiet`. — FAILS: when the assertion fires, the reader is pointed at a helper they cannot find — grepping `quietCommander` returns only the message itself — instead of at `commandertest.Quiet`'s unmatched-argv default, which is what actually supplies the answer. Remedy is message text alone, so it is non-blocking. NOTE: this same line is already recorded as a finding in `.workflows/resume-hooks-silently-lost/review/resume-hooks-silently-lost/report-7-26.md:50` (attributed there to the task that introduced the name, with this task's rename identified as what made it stale) — dedupe rather than fix twice.
