TASK: resume-hooks-silently-lost-9-2 — "A Period-Bearing Session Name Is Unaddressable Through SessionTargetExact And Is Silently Dropped From sessions.json" (phase 9, implementation-analysis cycle; authority is the task body, not the specification)

ACCEPTANCE CRITERIA:
1. With a period-bearing session and a prefix-extension sibling both live, `HasSession`, `KillSession`, `RenameSession`, `SwitchClient`, `ShowEnvironment` and `SetSessionEnvironment` all address the period-bearing session and not the sibling.
2. A period-bearing session survives one capture cycle into `sessions.json` with its scrollback, and no `capture skipping vanished session` line is emitted for it.
3. `ValidateSessionName` still returns nil for `a.b`, and its existing acceptance test is unmodified.
4. Every moved site's argv is composed through a target helper, so `internal/tmux/target_composition_guard_test.go` stays green with no widening.
5. The two hand-composed `attach-session` argvs pin their target through the same helper the client uses for that command kind.

STATUS: issues_found (one non-blocking, comment-only finding; all five acceptance criteria met)

SPEC CONTEXT: The specification (`.workflows/resume-hooks-silently-lost/specification/.../specification.md`) does not cover target-form composition at all — grep for `SessionTargetExact` / `CoordTargetExact` / "period" returns nothing, and no corrigendum touches it. This is a phase-9 analysis task, so its body is the authority. The connection to the work unit is thematic and stated in the task: the same silent-loss shape the unit exists to fix (a live thing reported gone, with the only trace being the line that says it was gone) reached through a different door — `internal/state/capture.go:70-74` classifies a `show-environment` failure as `tmuxerr.ErrNoSuchSession`, counts it as natural churn, logs `capture skipping vanished session`, and drops the session and its scrollback.

IMPLEMENTATION:
- Status: Implemented
- Location (commit `a0cbed5d`, verified against HEAD):
  - `internal/tmux/tmux.go:88,97` (`HasSession`, `HasSessionProbe`), `:272` (`KillSession`), `:288` (`RenameSession`), `:296` (`SwitchClient`), `:614` (`ShowEnvironment`), `:792` (`SetSessionEnvironment`) — all moved to `CoordTargetExact`.
  - `internal/tmux/clients.go:19` (`ListClients`) — moved.
  - `cmd/open.go:95` and `internal/session/quickstart.go:65-68` — the two hand-composed `attach-session` argvs, both pinned through `tmux.CoordTargetExact` (quickstart hoists one `target` local shared by its `set-option` and `attach-session` steps).
  - Doc comments rewritten at `internal/tmux/tmux.go:421-447` (`SessionTargetExact`) and `:452-466` (`CoordTargetExact`); `CLAUDE.md`'s `tmux` row enumeration updated to match.
  - Test-side peer moved for the same reason: `internal/tmuxtest/socket.go:128` (`WaitForSession`), plus `internal/restore/multipane_legacy_integration_test.go` (3 sites, applied in the fix round).
- Verification performed:
  - `grep` for `SessionTargetExact` across the tree: three hits, all in `internal/tmux/tmux.go` (declaration at `:448` plus two doc mentions), and three test files (`exact_target_forms_test.go`, `target_type_test.go`, `target_composition_guard_test.go`). Zero production call sites — so CLAUDE.md's "taken by no production call site" claim is true as written.
  - `grep` for every production `"-t"` composition: all 34 sites route through `CoordTargetExact` / `PaneTargetExact` / `windowTargetExact` / `PaneIDTarget` or a pre-composed `Target` parameter. Nothing bare survives.
  - `internal/tmux/session_name_test.go:225-228` (the `a.b` acceptance test) was not touched by this commit; `git log -L 220,235` confirms its only later edit is task 9-4's adjacent addition, not a change to it. The commit's only edit to that file was `TestRenameSessionRefusesColon`'s argv expectation at `:64`, which is a wire-form pin, not the acceptance test.
- Notes: `SessionTargetExact` now has zero production callers and survives as vocabulary. That is task-sanctioned (the Do list presupposes it survives) and its doc states the reason. The composition guard's `exactTargetHelpers` allowlist still admits it, which is consistent with that retention.

TESTS:
- Status: Adequate
- Coverage:
  - `internal/tmux/period_session_target_realtmux_test.go` (new, 181 lines, unit lane — real tmux client, per-test `-S` socket, no daemon, no built binary, so the lane rule holds) carries four of the five named tests: `it still accepts a period-bearing name as addressable` (also pins the exactness in the other direction — `HasSession("my")` must stay false with `my.app` and `my-cool-app-abc123` live), `it kills the period-bearing session and not its prefix-extension sibling`, `it reads a period-bearing session's environment while a longer prefix sibling is live` (asserts the write landed on `my.app` and *not* on the sibling), `it renames and switches to a period-bearing session with a prefix sibling live` (drives a real attached client via a second isolated host server, then checks `ListClients` on both sessions).
  - `internal/state/capture_period_session_realtmux_test.go` (new, 111 lines, unit lane) carries the fifth: `it captures a period-bearing session into sessions.json instead of reporting it vanished` — seeds a recognisable scrollback marker, runs `state.CaptureStructure`, asserts the session is present, asserts `capture skipping vanished session` is absent from the sink, then captures/writes/commits and re-reads `sessions.json` and stats the scrollback file so the commit's GC cannot silently take the bytes.
  - Wire-form pins updated at every moved site: `internal/tmux/tmux_test.go:345,385,443,630,696,870,912`, `internal/tmux/clients_test.go:121-124` (exact `slices.Equal` argv comparison, replacing the substring check that would have been satisfied by both the old and new form), `internal/tmux/session_name_test.go:64`, `cmd/spawn_seams_test.go:53`, `cmd/open_test.go:2613,2633,2685`, `internal/session/quickstart_test.go:27-28`.
  - The two fake commanders that resolve a session out of a composed target (`internal/state/capture_test.go:69`, `cmd/state_daemon_capture_logging_test.go:229`) now strip the trailing `:` as well as the `=`.
  - Both new suites fail on revert: reverting `ShowEnvironment` makes the capture test's `capturedSession` assertion fail, and reverting the client methods makes the tmux suite's kill/env/rename/switch assertions fail.
- Notes: the mock-level exactness tests `TestHasSessionUsesExactMatchPrefix` (`internal/tmux/tmux_test.go:375`) and `TestKillSessionUsesExactMatchPrefix` (`:649`) now name `"foo"` and `"=foo"` as explicit regression arms, so a revert to the bare form is caught at unit level too. The `perSessionRoutes` table (`internal/tmux/exact_session_target_test.go:42`) still enrols only the eight routes that were already on the coordinate form, so the six moved here rely on their own scattered wire-form assertions rather than the shared table — the table documents itself as "a maintained set, not the client's whole exact-target surface", so this is the pre-existing scoping choice rather than something this task changed. Not over-tested: one subtest per named test, one fixture per subtest, no redundant arms.

CODE QUALITY:
- Project conventions: Followed. Lane rule respected (both new suites spawn real tmux clients only — no daemon, no `portal` binary build or exec — matching the existing `internal/state/capture_colon_session_realtmux_test.go` precedent). No `t.Parallel()`. Real-tmux fixtures go through `tmuxtest.New` per-test `-S` sockets with server-killing cleanup, and the second host server in `attachClient` is registered after the target socket so LIFO cleanup detaches the client before the target server dies. No logging-vocabulary changes. `CLAUDE.md`'s architecture row was moved in the same commit as the code.
- SOLID principles: Good. The change is a single-form substitution behind two existing constructors; `quickstart.go` hoists one shared `target` local rather than composing twice.
- Complexity: Low.
- Modern idioms: Yes (`slices.Contains` / `slices.Equal` in the new assertions, `harnesstest.PollUntil` rather than hand-rolled sleeps).
- Readability: Good. Each moved site that is not self-evident carries a short measured rationale (`cmd/open.go:88-94`, `internal/session/quickstart.go:47-52`, `internal/tmuxtest/socket.go:111-115`), and neither the code nor the comments reference task ids, phases or spec sections.
- Issues: one dangling doc cross-reference, below.

BLOCKING ISSUES:
- None. All five acceptance criteria are met in substance, and no behaviour is broken.

FINDINGS:
- [in-scope] [contained] internal/tmux/tmux.go:252 — the `ActivePaneCurrentPath` comment says tmux answers an unmatched `display-message` target with an empty expansion at exit 0 "(measured on 3.7c; the same fact CoordTargetExact's doc states from the target-form side)", but `CoordTargetExact`'s doc (`internal/tmux/tmux.go:452-466`) states no such thing, and neither does `SessionTargetExact`'s (`:421-447`). This commit created the dangle: it retargeted the pointer from `SessionTargetExact` to `CoordTargetExact` while deleting the sentence it points at — the pre-commit `SessionTargetExact` doc read "display-message instead returns empty with exit 0 — for a live session as much as a gone one", and neither rewritten doc carries it (CLAUDE.md's `tmux` row still does). Fix: restore the claim where the pointer sends the reader, by appending to the second paragraph of `CoordTargetExact`'s doc (after "...is not split into window and pane." at `:463`) a sentence such as "A bare `=name` there is read as a window or pane name and misses: list-panes falls through to the same fuzzy lookup, while display-message returns empty at exit 0 for a live session as much as a gone one." — which also restores parity with the CLAUDE.md row this task was asked to keep in step. Alternatively drop the parenthetical cross-reference at `:252-253` and keep only "measured on 3.7c"; either edit is safe applied alone. — FAILS: a reader who follows the pointer to corroborate the empty-at-exit-0 contract — the contract that makes `ActivePaneCurrentPath` return `("", nil)` instead of an error, which `internal/session/dirresolve.go`'s `ok==false` path depends on — finds nothing there, so the one statement of record for that behaviour is the Go doc that now cites an absent source and the CLAUDE.md row nothing in the source points at.
