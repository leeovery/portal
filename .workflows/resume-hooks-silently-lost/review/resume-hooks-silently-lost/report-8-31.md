TASK: resume-hooks-silently-lost-8-31 — "The Stand-Down Copy Is Pinned In Four Files And The Same Case Is Written Twice In Two More" (tick-266529, phase 8 implementation-analysis cycle)

ACCEPTANCE CRITERIA:
- A wording change to any stand-down phrase requires one edit.
- Every decline reason's home carries level+attrs, the rendered line and the file-untouched check.
- The `borrows` check fails when two production reasons render the same phrase (verified by re-pointing a map entry, then reverting).
- `bogusHooksStore` lives in the staging-helper file, and no coverage is lost anywhere in the collapse.

STATUS: complete

SPEC CONTEXT: Per the shared verifier context, a phase 6–10 task's authority is its own body rather than the specification — this is a test-corpus consolidation task generated by the implementation phase, with no production behaviour in scope. The surrounding subject is the hook-staleness stand-down vocabulary: `cmd/doctor.go:220-254` declares the shared phrase consts and the two surface vocabularies (`skippedPrunePhrases` for the `doctor --fix` repair line, `notEvaluableDetails` for the read-only diagnosis), keyed on the closed `hooksweep.Reason` set (`internal/hooksweep/reason.go:19-40`, six members). The invariant the corpus protects is that a reason's two surfaces name the same condition in their own register and that no reason silently borrows another's words.

IMPLEMENTATION:
- Status: Implemented (subsequently extended by sibling tasks 8-48, 9-6, 9-8, 9-9, 9-10, 9-12, which the current file reflects)
- Location:
  - `cmd/doctor_stand_down_copy_test.go:1-551` — the consolidated home. `standDownCopyCase` (`:32-54`) now carries `fixture` (deps + hooks.json path), `level`, `attrs`, `sharedPhrase`, `skippedLine`, `notEvaluableLine`, `postRepairNotEvaluable`; `standDownCopyCases()` (`:56-123`) holds one row per reason, six rows for six reasons.
  - `cmd/doctor_stand_down_copy_test.go:289-336` — `assertStandDownSweep` (caller outcome + no removals + hooks.json untouched + level/attrs) and `assertStandDownRepair` (repair line + hooks.json untouched + post-repair diagnosis).
  - `cmd/doctor_stand_down_copy_test.go:230-250` — `renderStaleHooksLine` / `renderSkippedPruneLine`, the two renderers a non-copy suite composes from instead of restating words.
  - `cmd/doctor_stand_down_copy_test.go:255-285` — `hooksPathState` / `assertHooksPathUnchanged`, which handle the directory-at-the-path fixture (`Staging{Unreadable: true}`) that a bytes-only comparison cannot.
  - `cmd/doctor_test.go:842-877` — `assertStalePrunesApplied` gained the projects.json existence precondition at `:854-856` (mirroring the pre-existing hooks one at `:846-848`); `readFileBytes` returns nil for an absent file (`cmd/testhelpers_test.go:130-133`), so the check bites.
  - `cmd/doctor_test.go:1587-1615` — `assertSkippedPruneLine` gained the default arm at `:1603-1604`, which fails any `Skipped stale hook prune:` line other than the expected one. This generalises the deleted `TestDoctorFixRestoreStandDownCopy` loop to all its call sites.
  - Deletions (verified gone by grep): `TestDoctorFixReportsSkippedHookPrune`, `runDoctorFixWithLister`, `TestHookSweepOutcomeNamesEveryDecline`, `TestDoctorFixReportsStandDownOnReadFailures`, `TestDoctorFixRestoreStandDownCopy`, plus the lock-timeout and single-report duplicate pins. No orphaned identifiers remain (`runDoctorFixWithLister`, `notStandDownReasons`, `skipReasons*` all return zero matches repo-wide).
- Notes:
  - AC4's named subject (`bogusHooksStore`) no longer exists anywhere in the tree — it was retired onto `hookstest.StageStore(t, Staging{Unreadable: true})` by the earlier task 7-16 (closed 2026-08-31, before 8-31 ran on 2026-09-03). The criterion's intent — no staging helper stranded in an outcome-test file — holds: the surviving split of that file (`cmd/hook_prune_verdict_parity_test.go`) declares no staging helper and stages through `hookstest.StageStore`. This is a divergence from the criterion's literal wording that is a strict improvement, not a loss.
  - The task's Do item 5 asked for an "existence precondition" on the projects read; what landed is a post-prune non-empty check. That is the form that actually discriminates the two causes (`readFileBytes` returns nil for an absent file, so the old `!strings.Contains(projectsAfter, liveDir)` arm would have reported "live project wrongly pruned" for a deleted file). Substance met.
  - `cmd/doctor_fix_hook_prune_move_test.go:35` re-pins `"Skipped stale hook prune: restore in progress"` as a literal, so re-wording `restoreStandDownPhrase` today touches `cmd/doctor.go:221`, `cmd/doctor_stand_down_copy_test.go:65-66` and that line. That file did not exist when 8-31 landed — `git log --follow` shows it created by task 9-12 (`a4898f41`) — and its header comment (`:10-13`) states the reason it does not compose from the vocabulary: its subject is that the words survived the package move, which composing from the moved vocabulary would make tautological. Deliberate and defensible, introduced by a later task; recorded here for the record, not filed as a finding against this one.

TESTS:
- Status: Adequate
- Coverage:
  - Completeness: `TestStandDownCopy/it enumerates every stand-down reason with no subtraction list` (`:342-358`) is bidirectional — every table row names a declared reason, every declared reason has a row, and no reason is covered twice. It ranges over `hooksweep.Reasons` rather than a hand-kept count, so a seventh reason fails rather than passing silently.
  - The `borrows` check (`:364-379`) now ranges over the production `hooksweep.Reasons` and compares `renderSkippedPruneLine(reason)` and `phraseFor(notEvaluableDetails, reason)` — the rendered values, not the table's expectations. AC3 is met in substance: two production reasons converging on one phrase fails it regardless of what the table says.
  - Per-reason coverage (AC2) is complete: `assertStandDownSweep` pins the outcome, the empty `Removed`, hooks.json untouched, the level and the attrs; `assertStandDownRepair` pins the repair line, hooks.json untouched again and the post-repair diagnosis; the third arm pins the rendered not-evaluable line. All six rows run all three.
  - No coverage lost in the collapse — every deleted assertion was accounted for against HEAD:
    * lock-timeout WARN + `ErrLockHeld` attr → copy-home lock row (`:113-121`).
    * lock/restore/empty-pane/store-read caller outcomes → `assertStandDownSweep`.
    * `TestHookSweepOutcomeNamesEveryDecline`'s raw-slug check → `it renders no raw reason slug on either surface` (`:411-420`); its uniqueness check → the enumeration and `borrows` subtests. The table it replaced covered five reasons; the survivor covers six.
    * `TestDoctorFixRestoreStandDownCopy`'s "no other skipped-prune line" loop → `assertSkippedPruneLine`'s new default arm, which now applies at all its call sites rather than one.
    * the daemon lock-timeout case folded into `cmd/hook_prune_single_report_test.go:48-66` is strictly stronger than what it replaced: it gained `assertHooksFileUnchanged` and widened the injected-logger check from one message to all WARNs.
    * `TestDoctorFixReportsLockedHookPrune`'s stale-seed case survives at `cmd/hook_prune_locked_test.go:13-25` — deliberately complementary to the copy home's live-seed lock row, whose live seed is forced by its exit-code duty.
  - The exactness split is correctly reasoned rather than uniform: `standDownErrorAttrExactly` (`:140-147`) takes the two rows whose error text is fully deterministic (marker read → `state.RestoreWindowActive` returns the reader's error unwrapped, `internal/state/markers.go:91-110`; pane read → `declineWarn(ReasonPaneReadFailed, "error", err)` with the raw stub error, `internal/hooksweep/sweep.go:82-84`), and `standDownErrorAttrCarrying` (`:149-156`) takes only the two whose attr embeds a temp path (`ErrStoreRead` and `ErrLockHeld` are both wrapped with the path — `internal/hooks/store.go:301` and `internal/hooks/lock.go:70`).
  - The level-dependent record selection in `assertStandDown` (`cmd/hookkey_vocabulary_test.go:236-254`) is at least as strong as the `sink.Records().Only(...)` assertions it replaced: a DEBUG stand-down must be the sink's only record and must produce no WARN, while a WARN stand-down must be the only record at WARN or above.
- Notes: Not over-tested. The table drives 6 × (sweep + `--fix`) plus 6 × 3 doctor runs for the exit-code arm, which is the price of one home per reason rather than redundancy; the fix-tracking record measures the suite at 0.65s. Every row runs under `t.Run(tc.name)`, so a failure names its reason. No `t.Parallel()` anywhere, per the project rule.

CODE QUALITY:
- Project conventions: Followed. Unit lane only (no build tag, no portal binary, no daemon). `t.Run("it …")` naming throughout. `strings.SplitSeq` rather than `strings.Split` in the new helpers, matching the `modernize` linter. Log capture goes through `logtest.Install` / `logtest.Sink` with the chained `Records().AtExactLevel(...)` / `.Only(...)` query form rather than a hand-rolled handler. Staging goes through `hookstest.StageStore` — the single path-based stager — with no hand-written marshal-and-write.
- SOLID principles: Good. The `attrs func(*testing.T, logtest.Record)` column keeps the per-reason variation in the row rather than in a switch inside the driver; the two named attr helpers avoid the boolean-parameter form the fix round explicitly rejected.
- Complexity: Low. `assertStandDownSweep` / `assertStandDownRepair` are linear; `hooksPathState`'s three-way (absent / file / directory) is the minimum the `Unreadable` fixture requires.
- Modern idioms: Yes.
- Readability: Good. Every struct field carries a comment stating what decides its value, including why `sharedPhrase` is empty for the empty-pane row (the two surfaces genuinely say different things — confirmed against `cmd/doctor.go:241` and `:252`) and why `postRepairNotEvaluable` is false only for the lock (confirmed: the read degrades past a held lock while the write stands down).
- Comment accuracy: Checked against the code. The file header's claim that a reason's whole coverage lives in its row holds for all six rows. `lockTimeoutStandDownDeps`'s comment ("the seeded entry is live, so the post-repair diagnosis … finds nothing stale") is what makes `assertStandDownRepair`'s `stale hook entr` check non-vacuous for that row, and it is accurate. `emptyPaneReadStandDownDeps`'s "two of them, so the count the line reports is not the same number as anything else on it" matches the `entries == 2` assertion.
- Issues: None that clear the reporting bar.

BLOCKING ISSUES:
- None.

FINDINGS:
- None.
