TASK: resume-hooks-silently-lost-7-16 — "hooks.json Test Staging Is Reimplemented Seven Ways Across Two Packages" (tick-98759a). Move `hooksStoreStaging` + `newStagedHooksStore` from `cmd` into `internal/hookstest` as the one path-based staging entry point, add an `unreadable` axis and an entries-shaped seed, retire six duplicate helpers onto it, and re-baseline the env-pointing route so it stages a sidecar by default.

ACCEPTANCE CRITERIA:
1. `internal/hookstest` exports one path-based staging entry point carrying all six axes, and it is the only path-based stager in `internal/hooks` and `cmd`.
2. The sidecar is created before any permission denial in every fixture that stages one.
3. The six retired helpers are gone; no suite hand-writes a hooks.json path plus sidecar inline.
4. The env-pointing route stages a sidecar by default, and `cmd/hooks_read_lock_test.go`'s baseline is taken from a locked read.
5. The three tests whose subject is the degraded read still exercise it, explicitly.
6. No `load-unlocked` breadcrumb appears in a fixture that did not ask for the sidecar-absent axis.
7. `go test ./...` passes with every converted assertion unchanged.

STATUS: issues_found

SPEC CONTEXT: None applies. This is a phase-7 implementation-analysis task, so its authority is its own body — the specification (`.workflows/resume-hooks-silently-lost/specification/resume-hooks-silently-lost/specification.md`) contains no material on hooks.json test staging (a grep for `hookstest` / `staging` / `fixture` returns one unrelated hit at :522 about a `captureFormat` fixture). The surrounding project rule the task serves is CLAUDE.md's `hookstest` row: the sidecar is absent on every install in the wild until its first mutation, so a fixture must state which of the two states it means rather than degrade by accident.

IMPLEMENTATION:
- Status: Implemented (one residual duplication — see FINDINGS)
- Location: `internal/hookstest/staging.go:13-142` (`Staging` + `StageStore`), delivered by commit `e5fbf673`; conversions across `cmd/doctor_test.go`, `cmd/doctor_summary_test.go`, `cmd/doctor_fix_theme_test.go`, `cmd/hooks_read_lock_test.go`, the five `cmd/run_hook_stale_cleanup_*` suites, `cmd/state_daemon_hook_cleanup_test.go`, `cmd/state_daemon_run_test.go`, `cmd/testhelpers_test.go`, and `internal/hooks/{cleanstale_snapshot,lock,lookup,persisted_key_width,read_lock,store_shape,store}_test.go`.
- Notes:
  - AC1 (first clause) met: the delivered `Staging` carried exactly the six named axes — `Dir`, `Seed`, `Entries`, `SidecarAbsent`, `WritesDenied`, `Unreadable`. (`Body` at `internal/hookstest/staging.go:31` and `HooksPath` at `:102` are later additions from task 8-27, not this task's.)
  - AC2 met and load-bearing: `internal/hookstest/staging.go:85-95` creates the sidecar before the `os.Chmod(dir, 0o500)`, with the reason stated at `:86-87`. `CreateHooksSidecar` now has exactly one caller outside its own file — `staging.go:88` — so the ordering rule is encoded in one place, which is the outcome the task asked for.
  - AC3 met: all six named helpers are gone from the tree (`storeWithContent`, `seedHooksFile` ×2, `seedReadFixture`, `seedThenDenyWrites`, `seedHooksDirectory`, `bogusHooksStore`, plus `hooksStoreStaging`/`newStagedHooksStore`) — a repo-wide grep for those identifiers returns nothing. Do-item 4 done: the inline sidecar staging in `internal/hooks/read_lock_test.go` is replaced by `Staging{Entries: …}` (now at `:342-345`).
  - AC4 met: `cmd/testhelpers_test.go:161-166` (`hooksFileInTempDir`) stages through `hookstest.StageStore` with the sidecar on, documented at `:151-159` as the second, env-pointing route; `cmd/hooks_read_lock_test.go:80-84` now installs the sink *before* the baseline `runHookList` and fails if that baseline degraded, so the `want` is taken from a locked read.
  - The retired `TestStagedHooksStoreSidecar` (deleted from `cmd/hooks_read_lock_test.go`) was re-homed to `internal/hookstest/staging_test.go:18-55` with both assertions intact — coverage moved rather than lost.
  - The `Unreadable` doc comment's justification ("Malformed JSON would not do: it decodes to an empty map instead of erroring", `staging.go:48-49`) is true against `internal/hooks/store.go:93-96`, which swallows the unmarshal error and returns an empty snapshot.
  - Comment corrections from the fix-tracking rounds landed and hold: `internal/hooks/cleanstale_snapshot_test.go:96-97` ("the delete set is narrower than the file") is true — the file holds two keys, the enumeration returns one live key, so one is deleted; `:119-121` is true — the aborted clean's sink assertion at `:138` is total (`len(recs) != 0`), and the `Set` breadcrumb is kept out of it by ordering.

TESTS:
- Status: Adequate
- Coverage: `internal/hookstest/staging_test.go` carries all six micro-acceptance tests verbatim, one per axis: `:18` sidecar-by-default (asserts both the sidecar file and the absence of a `load-unlocked` record), `:39` sidecar-absent (asserts the file is absent and `AssertDegradedRead` fires), `:57` sidecar-before-denial (asserts the sidecar exists *and* that `Set` fails with `fileutil.ErrWriteTempCreate` rather than at the acquire — which is what makes the ordering rule falsifiable), `:78` unreadable, `:93` entries seed, `:114` named-directory-that-does-not-exist.
- Notes:
  - The ordering test at `:57-76` is the strongest of the six: it discriminates the two failure sites (temp-create vs sidecar open), so reversing the two statements in `StageStore` fails it. AC2 is genuinely pinned, not merely asserted in prose.
  - Not over-tested: one subtest per axis, no redundant happy-path variants, no mocking.
  - Under-tested, acknowledged and accepted: the two mutual-exclusion guards at `staging.go:57-62` are unexercised. Driving a `t.Fatalf` guard needs a `testing.TB` stand-in, and `StageStore` takes `*testing.T` rather than `harnesstest.TestingT`, so `harnesstest.Recorder` cannot reach it. Consistent with the rest of `hookstest` (only `AssertLockWarn`, `HooksFileBytes` and `AssertHooksFileUnchanged` take the stand-in) and not worth a signature change for a guard that encodes no behaviour.
  - AC7 judged by reading rather than execution (no test run is in this role's remit). Every converted call site keeps its assertion body byte-identical; the two substantive edits are both required by the criteria — the `cmd/hooks_read_lock_test.go:80-84` re-baseline, and `internal/hooks/store_test.go:1079` where `bytes.Equal(after, body)` became `string(after) != body` because the seed changed from `[]byte` to `string` (equivalent comparison; `bytes` is still imported and still used once in that file, so the import is not orphaned).
  - No fixture was hollowed by the new sidecar default. The classes that could have shifted were checked: the directory-listing assertion at `internal/hooks/store_test.go:159-163` already admits `hooks.json.lock`; the `clean-stale`-scoped sink scan at `internal/hooks/store_shape_test.go:96-100` is message-filtered; the total sink assertion at `internal/hooks/cleanstale_snapshot_test.go:138` gets *stronger* under a staged sidecar (a degraded read would have added a record).

CODE QUALITY:
- Project conventions: Followed. Test-only helper in a `*test` package importing `internal/hooks` + `internal/xdg`, `t.Helper()` on every helper, fatal messages namespaced `hookstest.StageStore: …`, and no `t.Parallel()` anywhere in the change-set. Lane rule respected — nothing here builds or spawns a binary, so the unit lane is correct.
- SOLID principles: Good. One struct describing the fixture, one function applying it; the axes are orthogonal and the mutually exclusive seeds are refused at the door rather than silently last-wins.
- Complexity: Low. `StageStore` is a guard pair, a directory resolution, one 4-arm switch and two conditionals; `seedWays` (`:108-116`) keeps the exclusivity count out of the two call sites that need it.
- Modern idioms: Yes. `for i := range n` in the converted `numberedEntries` (`internal/hooks/read_lock_test.go:20-26`), `fmt.Sprintf` seeds, table-free subtests.
- Readability: Good. Each axis carries a doc comment stating not just what it does but why the default is what it is (`SidecarAbsent` at `:32-42` explains that the absence is transitional and must be asked for) — which is the part the six retired copies dropped.
- Issues: None beyond the finding below.

BLOCKING ISSUES:
- None. Nothing is broken, every acceptance criterion is met in substance, and the coverage the change moved arrived intact at its new home.

FINDINGS:
- [in-scope] [contained] internal/hooks/lookup_test.go:127 — the "unreadable hooks.json" fixture is still hand-rolled here and at `:168` (`filepath.Join(t.TempDir(), "hooks.json")` + `os.Mkdir(filePath, 0o700)`), in the same file whose `storeWithContent` this task retired, while the `Unreadable` axis this task added for exactly that case is used at twelve sites across five packages (`internal/hooks/cleanstale_snapshot_test.go:173`, `internal/hooks/cleanstale_read_sentinel_test.go:30`, `internal/hooksweep/{read_failure_test.go:16,sweep_move_test.go:78,sweep_test.go:93,:292}`, `cmd/hook_prune_single_report_test.go:20,:36`, `cmd/doctor_stand_down_copy_test.go:198,:485`, `internal/hookstest/{hooks_test.go:147,staging_test.go:79}`). `cmd/state_hydrate_test.go:1499` and `:1543` hold the same hand-rolled shape. Replace each with `store, _ := hookstest.StageStore(t, hookstest.Staging{Unreadable: true})` (`Staging{Dir: dir, Unreadable: true}` at the two `cmd/state_hydrate_test.go` sites, which need the hooks.json beside the fixture's FIFO in `dir`); converting `lookup_test.go:127` and `:168` alone leaves `os` unused in that file and its import must be dropped, while `filepath` stays for the path-only site at `:32` (and `cmd/state_hydrate_test.go` uses both packages dozens of times elsewhere, so its imports are untouched). — FAILS: acceptance criterion 1's second clause, "it is the only path-based stager in `internal/hooks` and `cmd`" — the fixture the task consolidated is still staged two ways inside `internal/hooks` itself, so a future change to how an unreadable hooks.json is built (the axis gaining a sidecar option, the store learning to distinguish EISDIR) has to be found in three places rather than one, which is the drift the task was written to end.
