TASK: resume-hooks-silently-lost-4-7 — "The Tests That Execute Hook List Inject The Seam It Now Reaches" (tick-982fc6)

ACCEPTANCE CRITERIA:
- All five sites inject a `PaneLister`
- The fake used is the existing `loudPaneHookLister`; no new fake is introduced
- No assertion changes and no test changes its verdict
- The injection is real, not decorative: removing the `len(list) == 0` early return from the list body makes these sites fail loudly rather than reach `tmux.DefaultClient()`
- `go test ./...` and `go test -tags integration -p 1 ./cmd/...` pass

STATUS: complete

SPEC CONTEXT:
The specification's §9.2 puts `hook list`'s fourth (location) column under unit-lane test "over a fixed enumeration", and §9.1 restates the binding lane rule, noting that a real-tmux `cmd` test is avoided precisely because `cmd`'s `TestMain` poisons `TMUX` package-wide. CLAUDE.md states the contract this task enforces from two directions: `hook list` "resolves each entry's location through **one** live `ListAllPaneHookKeys` enumeration per invocation (none at all when there are no entries)", and "A test that Executes a real command body MUST inject every tmux-touching `*Deps` seam". The task is a test-isolation hygiene item generated by the implementation rather than a specified behaviour change — its own body plus CLAUDE.md are the authority, and neither is contradicted.

IMPLEMENTATION:
- Status: Implemented
- Location:
  - `cmd/hooks_test.go:43` — "produces empty output when no hooks registered" now injects `withHooksDeps(t, HooksDeps{PaneLister: &loudPaneHookLister{t: t}})`
  - `cmd/hooks_test.go:63` — "produces empty output when hooks file does not exist", same injection
  - `cmd/hooks_test.go:673` — `TestHookCommandRename`'s "keeps hooks working as a silent cobra alias", same injection
  - `cmd/root_test.go:340-344` — the skipTmuxCheck table's new `case "list":` arm, injecting the same fake; `switch tt.argv[1]` means it covers both list rows (`{"hook","list"}` and `{"hooks","list"}`) through the mechanism the `set`/`rm` rows already used, as the task asked
  - Existing fake reused: `loudPaneHookLister` at `cmd/hooks_test.go:274-280` (declared by task 4-3); no new fake appears
- Notes:
  - The seam the injection fills is real at every one of these sites: `cmd/hooks.go:151` reads `hookSeams().PaneLister`, and `hookSeams` (`cmd/hooks.go:83-104`) fills an unset `PaneLister` with `buildHooksTmuxClient()` → `tmux.DefaultClient()`, which `internal/tmux/tmux.go:73-77` documents as honouring the ambient `TMUX` socket.
  - The task's premise holds for all five: each site's store is empty, so `cmd/hooks.go:147`'s `if len(list) == 0` returns before the read. For the two `cmd/root_test.go` rows the emptiness comes from `TestMain`'s poisoned `PORTAL_HOOKS_FILE` (`cmd/testmain_isolation_test.go:67`) plus `Store.load`'s ENOENT → empty-snapshot rule (`internal/hooks/store.go:85-91`), so those rows still exit 0 with the injection in place.
  - Criterion 4 (injection is load-bearing, not decorative) holds by construction: with the `len(list) == 0` return removed, `paneLocationsByToken(hookSeams().PaneLister)` reaches the injected fake at all five sites, and `loudPaneHookLister.ListAllPaneHookKeys` calls `t.Error` before returning `nil, nil` — a loud failure, not a real tmux dial. I did not execute the mutation (no test execution in this role); it is verified by reading the call path.
  - Completeness of the "five sites" claim, checked by reading every list-body driver in `cmd`: `cmd/hooks_test.go` lines 33, 97, 129, 145, 162, 182, 194, 210, 227, 244, 264 all run through `runHookList` with a `recordingPaneHookLister` or the loud fake already injected; `cmd/hooks_read_lock_test.go:75` and `:101` inject too. The one remaining `hooks list` Execute without an injection is `cmd/hooks_test.go:106-116` ("accepts no arguments"), where cobra's `ValidateArgs` rejects `extraarg` before `RunE` and before `PersistentPreRunE`, so no command body runs — correctly excluded from the five.
  - No drift from the task body: the sites, the fake and the mechanism are exactly what it prescribed.

TESTS:
- Status: Adequate
- Coverage: The task is itself a test change; the deliverable is that the five sites stay green with their isolation now stated rather than incidental. No assertions were added or altered — each site keeps its original verdict (empty stdout at `cmd/hooks_test.go:55-57` and `:75-77`; no deprecation/warning text at `:685-691`; `runner.calls == 0` at `cmd/root_test.go:357-359`).
- Notes: No new durable test is warranted and none was added — the behaviour "no entries means no enumeration read" already has its own subject test (`cmd/hooks_test.go:189-198`, "it takes no enumeration read when there are no entries") and a second one at `cmd/hooks_read_lock_test.go:94-110`. Adding a fourth would be redundant; using the loud fake as a silent guard at the other sites is the right weight. No over-testing: the injections carry no assertions of their own.

CODE QUALITY:
- Project conventions: Followed. The staging goes through `withHooksDeps` (`cmd/testhelpers_test.go:26-30`), which pairs its own `t.Cleanup` — so `cmd/seam_guard_test.go`'s prohibition on direct seam assignment is respected. Injection satisfies the CLAUDE.md rule that a test Executing a real command body inject every tmux-touching `*Deps` seam.
- SOLID principles: Good — the existing seam is used as designed; no new abstraction.
- Complexity: Low. The `cmd/root_test.go` arm extends the existing `switch tt.argv[1]` rather than adding a parallel mechanism, as the task required.
- Modern idioms: Yes.
- Readability: Good. The `cmd/root_test.go:341-343` comment states why the injection exists and what actually leaves the read untaken (the empty store, not a real server) — it holds against the code at `cmd/hooks.go:147-151`.
- Issues: None.

BLOCKING ISSUES:
- None.

FINDINGS:
- None.
