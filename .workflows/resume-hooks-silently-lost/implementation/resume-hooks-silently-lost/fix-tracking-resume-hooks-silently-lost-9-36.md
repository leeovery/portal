## Attempt 1

ISSUES:
- `internal/tmux/session_name_test.go:17`, `:30`, `:43`, `:257` — the four rename-refusal fixtures script `commandertest.Returns("", "rename-session")`, but the whole point of each is that `RenameSession` refuses **before composing the argv**, so `rename-session` is the one argv the subject must never issue. These are the only four scripted entries in either package that never match a call. The script was taken from the method rather than from the test's own intent, and it hands the fixture permission for exactly the failure it exists to detect. It also falsifies the package-wide claim in the new guard's own comment (`internal/tmux/commander_fake_loudness_test.go:12-13`, "The fixtures in this package script the argv their subject issues"), which is why the remedy is code rather than a comment edit. Proved by mutation (validation moved after the `Run` in `RenameSession`): as written, `it_refuses_a_rename_to_a_name_containing_a_colon` and `it_names_the_offending_character_in_the_refusal_message` both still PASS on the broken implementation; with an empty script all three colon subtests plus the flag-prefix one FAIL, and all stay green against the unmutated implementation.
  FIX: replace the fixture at those four lines with the empty-script form `commandertest.New(t)` — a refusal site's real argv set is the empty set, and `New` with no entries reports and errors on any call at all. Leave `internal/tmux/session_name_test.go:54` (`it renames a colon-free name unchanged`) exactly as it is: that entry is live and correct. Verified: `go test -run 'TestRenameSessionRefusesColon|TestValidateSessionNameFlagPrefix' ./internal/tmux/` passes with the four sites converted.
  ALTERNATIVE: keep the current script and add an explicit `len(mock.Calls()) != 0` assertion to the two subtests that lack one. Weaker and more code — it re-asserts per subtest what the fixture can enforce structurally once, and leaves the fixture form inconsistent with the loud default the task installs everywhere else. The reviewer recommends the empty script.
  CONFIDENCE: high

NOTES:
- The reviewer ran an instrumented `-overlay` sweep (scratchpad only; the repo was never written to) that failed any scripted entry never matched during its test. That answers the dead-entry question exhaustively for every converted site in both packages: exactly four dead entries in the whole conversion, all the ones above. Every other scripted entry matched a real call, which independently corroborates that the scripts track observed argv rather than guesswork.
- The executor's "zero found" is literally correct — no test *asserts* such an argv, and `mock.Calls()` records real calls, so such an assertion would already have been red. But the conversion did produce output of this class (four fixtures *scripting* an argv the production path never issues in that scenario) and it went unreported.
- Verified: 186 `Quiet` sites at HEAD → 0 (164 tmux + 22 restore), `internalMockCommander` deleted with the full argv scripted, `FromFunc` 193 → 193 with zero `FromFunc` lines in the diff.
- `geometryFake` is a real strengthening: all four call sites passing the `resize-pane` entry are genuinely zoomed fixtures, so no entry is carried dead.
- `ArgvPrefix` compares elements, not string prefixes, so `show-option` does not shadow `show-options`; the `set-hook -ga`/`-gu` pairs are correctly discriminated at two words.
- Granularity: nearly every converted script is a one-word subcommand prefix, so drift *within* a subcommand (`set-option -s` → `-g`, a changed `-F` format) is still answered rather than reported. In each such suite a sibling subtest asserts the full argv, so precision is not lost — but the property is pinned at subcommand granularity in most places. Tightening is cheap if it is wanted structurally.
- `internal/tmux/tmux_test.go:735` — subtest name `returns error when start-server fails` names a tmux subcommand `StartServer` never issues (it runs `new-session -d -s _portal-bootstrap`). Pre-existing stale wording; the scripted `new-session` is correct.
- The two new loudness guards construct a fresh fixture, so they pin the shared fake's unmatched default reached through the package's constructor — they would stay green if every fixture reverted to `Quiet`. Non-blocking.
