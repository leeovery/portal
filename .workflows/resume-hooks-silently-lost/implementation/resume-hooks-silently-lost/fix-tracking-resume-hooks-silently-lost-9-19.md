## Attempt 1

ISSUES:
- `internal/sourceguardtest/assertdepswithin.go:44` — the assertion now judges two configurations but names neither in any message it emits, so its output cannot be reproduced from what it prints. Two concrete symptoms. A dependency present in both readings is reported **twice with byte-identical text** ("`internal/hooks` transitively depends on X — it may reach no further than [...]"), which reads as a doubled bug rather than two lanes agreeing. A dependency reachable only from a tagged file prints a claim the reader cannot confirm — `go list -deps <pkg> | grep X` returns nothing, because the finding came from `-tags integration`. The same blindness applies to the fatals at `assertdepswithin.go:32` and `:49` and `packagedeps.go:130,134` (a tagged file that breaks `go list` fatals with "go list -deps <pkg>: ..." naming a command that succeeds when run as printed). This is the one place the codebase's own guard-message convention slips: every other fatal in this file explains precisely what was judged and why a pass would be vacuous.
  FIX: derive a lane label from the already-computed `cfg` and append it to the messages the tag-aware path can produce. Add an unexported method beside `depsConfig` in `packagedeps.go` — `func (c depsConfig) lane() string { if len(c.tags) == 0 { return "" }; return " under -tags " + strings.Join(c.tags, ",") }` — and interpolate it at `assertdepswithin.go:44` (`"%s%s transitively depends on %s …", pkg, cfg.lane(), d.Path, allowed`) and at the two `packageDeps` fatals, which already build a `depsConfig` at `packagedeps.go:128` and need only hold it in a local. The existing `errored(rec, …)` substring checks in `assertdepswithin_test.go` are unaffected; add the lane label to the expectation in the "reachable only from an integration-tagged file" subtest so the naming itself is pinned.
  ALTERNATIVE: replace the eight `for _, lane := range Lanes()` loops with a single `AssertDepsAcrossLanes(t, pkg, allowed, opts...)` that takes both readings, reports each offending dependency once, and names the lanes it appeared under. This also removes the 8x loop boilerplate and structurally prevents the duplicate report, but it is a larger API change across eight packages and gives up the property that a caller can ask for one specific configuration. The reviewer recommends the message qualifier — it is contained, and `Lanes()` is a good shape as it stands.
  CONFIDENCE: high

COMMENT_CORRECTIONS:
- `internal/sourceguardtest/buildconstraint.go:14` — dropped article makes the doc's first clause parse as "the build-constraint file" rather than the constraint a file states.
  OLD: // BuildConstraint returns the build constraint file states, and whether it
  NEW: // BuildConstraint returns the build constraint the file states, and whether it
- `internal/sourceguardtest/packagedeps.go:40` — the code falsifies the absolute claim: `go list -deps` reports only the package's non-test build, so every `_test.go` file's imports sit outside both readings, and a file behind any tag other than `integration` sits outside both as well.
  OLD: // runs over both, so no file can hold a dependency the guard never reads.
  NEW: // runs over both, so a dependency reachable only from an integration-tagged
  // file is judged rather than resolved away.

NOTES:
- The flagged deviation is adjudicated CORRECT and what landed satisfies the task. Criterion 5 and its Do item rest on a premise that does not hold: they treat the "it carries no build tag" subtest as a hand-rolled stand-in for the deps option, but the two police disjoint properties. `WithBuildTags` detects a forbidden dependency under a tag; it is blind to a tagged file that imports nothing forbidden — the case the subtest exists for, because a tag on a scanning primitive gates every guard built on it out of the unit lane whatever it imports. `internal/sourceguardtest/doc.go:5-6` states that contract in prose, so deleting the subtest would strand the doc claim with nothing enforcing it. Criterion 5's operative clause is "keeps its coverage", and only retention satisfies it. Do NOT delete the subtest in this fix round.
- All other acceptance criteria verified met, independently: one constraint reader tree-wide (grep for `constraint.Parse`, `IsGoBuild`, `IsPlusBuild`, `go/build/constraint` and the literal `//go:build` finds no other), all three prior classification behaviours preserved, the tag name declared once, and the tagged reading proved to bite via a `go list -overlay` probe (0 hits default, 3 hits under `-tags integration`).
- `SatisfiedWithout` beyond the literal Do list is right, not scope creep: routing `compiledInUnitLane` through `SatisfiedWith(expr)` would flip `//go:build darwin` out of the unit lane and change a guard policy the task says is unchanged.
- `Lanes()` has a two-element contract but only the integration element is pinned by a test. A file carrying `//go:build !integration` and a forbidden import would be visible only in the default reading, so dropping `WithBuildTags()` from `Lanes()` would silently lose coverage nothing would catch. A mirror fixture in `stageTaggedFixtureModule` would close it cheaply.
- The banked `go test` caching finding is confirmed: cmd/go's cache key tracks the test binary plus files the test process itself opens, and an integration-tagged file is neither an input to the default-lane binary nor visible through a `go list` subprocess's reads. Already recorded in the manifest; it caps the practical strength of criterion 4 until taken.
- `TestSatisfiedWith` (`buildconstraint_test.go:78`) covers both evaluators; the name only says half. Cosmetic — it is the name the task prescribed.
- The leaf guard's error message now prints the normalised expression (`integration`) where it previously printed the source line (`//go:build integration`). Slightly less greppable, not wrong.
