AGENT: duplication
FINDINGS:
- FINDING: Per-event show-hooks dispatch RunFunc re-implemented inline 8 times despite a shared helper existing
  SEVERITY: medium
  FILES: internal/tmux/hooks_register_test.go:546-564, internal/tmux/hooks_register_test.go:619-642, internal/tmux/hooks_register_warn_test.go:88-96, internal/tmux/hooks_register_warn_test.go:129-145, internal/tmux/hooks_register_warn_test.go:184-202, internal/tmux/hooks_register_warn_test.go:246-258, internal/tmux/hooks_migration_test.go:329-349, internal/tmux/hooks_migration_test.go:446-458
  DESCRIPTION: The convergence-path tests already share perEventDispatch (hooks_register_test.go:71) + parseSeededTableByEvent (hooks_register_test.go:102) for the "answer show-hooks -g <event> per-event, fatal on a no-arg global read, dispatch set-hook -ga/-gu" shape. But 8 tests across three in-scope files hand-roll a bespoke runFunc/RunFunc that re-implements the identical 3-branch skeleton. The guard string "convergence engine must read per-event, not the no-arg global show-hooks -g" is copy-pasted verbatim across 5 of them. These copies exist only because the shared helper can't inject a read error, a per-index unset error, or a CommandError, but each reproduces the whole skeleton just to vary one branch, so the shared structure and its per-event-read invariant can drift independently per copy. hooks_migration_test.go:319 documents the copy ("wraps the shared splitter directly... mirroring TestRegisterPortalHooks_PerIndexUnsetFailureWarnsAndContinues").
  RECOMMENDATION: Extend perEventDispatch (or add one sibling builder) to accept optional per-event fault injection: readErrFor map[string]error and unsetErrFor map[string]error (it already takes setHookErrFor). The fatal-guard and per-event read/set-hook branches then live in one place; the 8 bespoke runFuncs collapse to one-line helper calls passing fault maps. Tests needing a CommandError pass it through readErrFor.
- FINDING: Hook command-body literals duplicated as separate constants within the same test package
  SEVERITY: low
  FILES: internal/tmux/hooks_register_test.go:31, internal/tmux/hooks_register_realtmux_test.go:54
  DESCRIPTION: expectedNotifyCommand (hooks_register_test.go:31) and notifyCommandBody (hooks_register_realtmux_test.go:54) are byte-for-byte identical declarations of the notify run-shell body. Both live in package tmux_test - this is NOT the production/external-test import-boundary mirroring the topic note exempts (that covers test copies of unexported PRODUCTION constants). These are two test-side copies of the same string in the same package, authored independently in two files. Same shape applies to notifyFingerprint/commitNowFingerprint/signalHydrateFingerprint (hooks_register_realtmux_test.go:41,45,49), which restate substrings already embedded in the same package's full-body constants.
  RECOMMENDATION: Keep a single test-package copy of each command-body literal and fingerprint substring (the expected* set in hooks_register_test.go is the natural home) and have hooks_register_realtmux_test.go reference those instead of redeclaring them. Leave the deliberate below-the-import-boundary mirroring of the production unexported constants untouched per the topic note.
- FINDING: Second capturing slog.Handler authored alongside an equivalent one already in the test package
  SEVERITY: low
  FILES: internal/tmux/hooks_register_test.go:811-871
  DESCRIPTION: recordingMigrationLogger (new, hooks_register_test.go:811) is a near-duplicate of the pre-existing recordingSlogHandler (portal_saver_test.go:2629, consumed by the new hooks_register_warn_test.go tests). Both are slog.Handler implementations in the same tmux_test package sharing identical shared/bound/owner + WithAttrs/WithGroup capture scaffolding; the only real difference is recordingMigrationLogger eagerly projects component/reaped into typed fields while recordingSlogHandler stores raw records. The implementation already uses recordingSlogHandler in hooks_register_warn_test.go and then introduced a parallel recorder for the sibling files.
  RECOMMENDATION: Have recordingMigrationLogger embed or wrap the existing recordingSlogHandler (capture raw records once, expose infos/reaped/warns as thin accessors that filter/project stored records), so the WithAttrs/WithGroup/owner scaffolding lives in exactly one type. Treat the pre-existing recordingSlogHandler as the shared base; the change is additive to the new code only.
SUMMARY: One medium finding - eight in-scope hook tests re-implement the same per-event show-hooks dispatch RunFunc skeleton (verbatim fatal-guard string included) despite an existing shared helper. Two low findings cover within-package duplication of hook-body/fingerprint literals and a second capturing slog.Handler overlapping one already used in the package.
