AGENT: duplication
FINDINGS:
- FINDING: Production per-event read → fingerprint-filter → reverse-index unset loop duplicated between convergeEvent and UnregisterPortalHooks
  SEVERITY: medium
  FILES: internal/tmux/hooks_register.go:193-240, internal/tmux/hooks_unregister.go:87-115
  DESCRIPTION: Both production functions independently implement the same multi-step pipeline: (1) call c.ShowGlobalHooksForEvent(event), (2) on error emit the canonical WARN and wrap "show-hooks failed: %w", (3) ParseShowHooks(raw)[event], (4) collect entries whose Command contains a Portal fingerprint via containsAny, (5) sort the matching indices in descending order, (6) loop UnsetGlobalHookAt(event, idx) per entry. convergeEvent then appends; UnregisterPortalHooks stops after removal. The spec's "Reuse existing, tested primitives" / "teardown reuses portalEntriesFor + reverse-index UnsetGlobalHookAt" framing intended registration's eviction half to share teardown's primitives, but convergeEvent re-authored the read+filter+reverse-unset inline: it filters with a hand-rolled loop rather than portalEntriesFor, and re-derives the descending sort (sort.Reverse(sort.IntSlice) vs UnregisterPortalHooks' sort.Slice predicate). Two independent ~20-line eviction loops that must stay behaviourally identical (same reverse-order semantics, same best-effort-continue contract) are exactly the copy-paste-drift risk this fix is meant to retire.
  RECOMMENDATION: Extract a shared per-event eviction helper in the tmux package — e.g. evictPortalEntries(c *Client, logger *slog.Logger, event string, entries []HookEntry, fingerprints []string) (evicted int, err error) — that owns the descending-sort + UnsetGlobalHookAt loop with the per-index best-effort WARN. Have both convergeEvent and UnregisterPortalHooks's inner block call it, and route convergeEvent's "collect Portal-authored entries" through a parameterised portalEntriesFor-style filter so there is one definition of "Portal-authored entry on this event" and one reverse-order removal loop.
- FINDING: Canonical "show-hooks failed" WARN + wrap shape hand-duplicated across the two production callers
  SEVERITY: low
  FILES: internal/tmux/hooks_register.go:196-202, internal/tmux/hooks_unregister.go:90-98
  DESCRIPTION: The identical read-failure handling — logger.Warn("show-hooks failed", "error", err, "error_class", "unexpected") followed by a "show-hooks failed[ on <event>]: %w" wrap — is written verbatim in convergeEvent and in UnregisterPortalHooks. The message string, the error_class="unexpected" literal, and the wrap prefix form a single contract (hooks_register_warn_test.go pins all three as one shape via assertShowHooksWarnShape), but production carries two independent copies that could drift in message or attr.
  RECOMMENDATION: Fold into the shared per-event read helper from the first finding (or a small warnShowHooksFailure(logger, event, err) helper returning the wrapped error), so the WARN message + error_class + wrap prefix have exactly one production definition. This rides along naturally with extracting the read+filter+unset pipeline.
- FINDING: "Read event per-event → ParseShowHooks → count entries containing fingerprint" test helper re-authored ~7 times across three test files
  SEVERITY: medium
  FILES: internal/tmux/hooks_register_realtmux_test.go:70-84, internal/tmux/hooks_register_realtmux_test.go:239-246, internal/tmux/hooks_migration_test.go:39-55, internal/tmux/hooks_migration_test.go:106-122, internal/tmux/hooks_migration_test.go:278-293, cmd/bootstrap/reboot_roundtrip_test.go:1284-1308
  DESCRIPTION: countPortalEntriesForEvent (realtmux), hasPortalEntry (realtmux), countSignalHydrateEntries (migration), the inline loops in TestMigrateHydrationHooks_EvictsUnSeparatedThenInstallsFixed and ..._DoesNotEvictHandAuthoredHooksLackingFingerprint (migration), and verifyHydrationHookEntries (bootstrap round-trip) all implement the same body: ShowGlobalHooksForEvent(event) with t.Fatalf on error, ParseShowHooks(raw)[event], then iterate entries counting/matching strings.Contains(e.Command, fingerprint). hooks_register_realtmux_test.go and hooks_migration_test.go are in the same external tmux_test package, so countPortalEntriesForEvent could already serve countSignalHydrateEntries and both migration inline loops with zero new abstraction — they were independently authored across task boundaries. The bootstrap_test copy (verifyHydrationHookEntries) lives in a different package and cannot share an unexported tmux_test helper, so it is a separate consolidation question.
  RECOMMENDATION: Within the tmux_test package, make countPortalEntriesForEvent (or a small portalEntryCommandsForEvent(t, client, event, fingerprint) []string returning matching command bodies) the single source of truth and route countSignalHydrateEntries plus the two migration inline loops through it — countSignalHydrateEntries becomes a thin map-builder over it. Leave verifyHydrationHookEntries (different package) as-is unless a shared test-helper package is in scope; noted only because the same pattern crossed the package boundary.
- FINDING: Inline mock.Calls scanning for set-hook -ga / -gu re-implemented despite existing setHookCalls / unsetHookCalls extractors
  SEVERITY: low
  FILES: internal/tmux/hooks_register_test.go:348-360, internal/tmux/hooks_register_test.go:437-449, internal/tmux/hooks_register_test.go:474-484, internal/tmux/hooks_register_warn_test.go:147-152, internal/tmux/hooks_migration_test.go:387-397
  DESCRIPTION: hooks_unregister_test.go defines unsetHookCalls and hooks_register_test.go defines setHookCalls, yet several tests in the same package re-walk mock.Calls inline with the literal len(c) >= 4 && c[0] == "set-hook" && c[1] == "-ga" guards (and the -gu equivalent) to recover ordering or to extract the event-name prefix before "[". This is the same argv-shape decoding logic copied into multiple test bodies. Lower impact because each instance is short and they are assertions, but the argv guard literals (indices and "set-hook"/"-ga"/"-gu" strings) repeat enough to drift if the mock call shape ever changes.
  RECOMMENDATION: Where a test needs append/unset ordering relative to each other (the KDeepStackCollapse and StaleNotifyOnSessionClosed "append follows unset" assertions), add one small ordered accessor (e.g. setHookEvents(calls) returning ordered [verb, target], or an eventOfUnsetTarget splitter for the "[" prefix) and reuse it, rather than re-encoding the argv guards inline per test — keeping the mock-argv contract co-located with setHookCalls/unsetHookCalls.
SUMMARY: The two production eviction paths (convergeEvent and UnregisterPortalHooks) independently re-author the same per-event read → fingerprint-filter → reverse-index-unset loop and the same "show-hooks failed" WARN/wrap shape, despite the spec intending teardown's primitives to be reused; on the test side the "read-per-event → ParseShowHooks → count-by-fingerprint" helper was rewritten roughly seven times across three files. Consolidating the production eviction loop (medium) and the test count-by-fingerprint helper (medium) are the two highest-value extractions.
