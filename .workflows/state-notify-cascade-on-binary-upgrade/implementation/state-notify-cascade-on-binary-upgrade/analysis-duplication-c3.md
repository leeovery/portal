AGENT: duplication
FINDINGS:
- FINDING: Two parallel per-event mock-dispatch RunFunc builders for show-hooks/set-hook (teardown dispatcher lacks the no-arg-global-read fatal guard)
  SEVERITY: low
  FILES: internal/tmux/hooks_register_test.go:95-151, internal/tmux/hooks_unregister_test.go:18-35
  DESCRIPTION: The registration tests own `perEventDispatch` / `perEventDispatchWithFaults` and the teardown tests own `dispatchUnregisterHooks`. Both are MockCommander RunFunc builders modeling the same tmux contract for the same fix: answer a per-event `show-hooks -g <event>` read by returning only the queried event's lines, and answer `set-hook -gu <event>[idx]` with an optional per-target injected error (`unsetErrFor` in one, an unnamed map in the other). They were authored independently across the register/teardown task boundary and have already begun to drift: `perEventDispatchWithFaults` carries a load-bearing `t.Fatalf` guard that fails if the no-arg global `show-hooks -g` is ever issued (the exact invariant this whole fix exists to enforce), while `dispatchUnregisterHooks` has no such guard, so a teardown regression that reverted to the blind no-arg global read would pass silently under its mock. The shared production seam both exercise (`evictPortalEntries` + `ShowGlobalHooksForEvent`) is unified; only the test harness around it is forked.
  RECOMMENDATION: Have the teardown dispatcher reuse the register-side per-event skeleton. `perEventDispatchWithFaults` already accepts `readErrFor` + `unsetErrFor` fault maps and carries the no-arg-global-read fatal guard; `dispatchUnregisterHooks` needs only the show-hooks read + `set-hook -gu` legs, a strict subset. Either call `perEventDispatchWithFaults(t, table, nil, nil, unsetErrFor)` from the teardown tests, or extract the per-event read/unset skeleton (including the no-arg-global fatal guard) into one tmux_test-package helper both sides build on — closing the drift gap so teardown inherits the same "must read per-event" tripwire. The unified helper's line-scoping must accept both the `<event>[i] => '...'` (register) and `<event>[i] run-shell '...'` (unregister) fixture shapes by matching on the `<event>[` prefix, which it already effectively does.
- FINDING: Two parallel whole-table-to-per-event line-scoping helpers
  SEVERITY: low
  FILES: internal/tmux/hooks_register_test.go:186-200, internal/tmux/hooks_unregister_test.go:41-51
  DESCRIPTION: `parseSeededTableByEvent` (register) and `linesForEvent` (unregister) independently re-implement the same primitive: take a multi-line seeded show-hooks table and return only the lines belonging to one event, keyed on the `<event>[` prefix preceding the index bracket. `parseSeededTableByEvent` builds a full `map[event]->lines` (splitting on the first `[` and re-joining); `linesForEvent` filters for a single passed-in event via `HasPrefix(event+"[")`. Same fixture-scoping concept, two slightly different shapes (map-builder vs single-event filter) that must stay in agreement about how an event line is recognised. This is the fixture-side mirror of finding #1 and would naturally collapse alongside it.
  RECOMMENDATION: Pick one as the single tmux_test-package primitive for "scope a whole-table show-hooks fixture to one event's lines" and express the other in terms of it. Co-locating this with finding #1's dispatch consolidation removes a third independent definition of "an event line begins with `<event>[`".
SUMMARY: Production code (hooks_register.go, hooks_unregister.go, tmux.go) is clean — the register and teardown paths already share evictPortalEntries, warnShowHooksFailure, parseEventEntries, containsAny, and the managedEvents→portalEvents projection, with no duplication. The only duplication is low-severity and entirely test-harness-side: the registration and teardown test files independently re-implement the per-event MockCommander dispatch and the whole-table-to-per-event line-scoping, a fork that has already let one safety invariant (the no-arg-global-read fatal guard) exist on only one of the two paths.
