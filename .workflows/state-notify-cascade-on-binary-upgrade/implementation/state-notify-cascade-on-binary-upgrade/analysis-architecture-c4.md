AGENT: architecture
STATUS: clean
FINDINGS: none
SUMMARY: The per-event hook convergence architecture is sound — single-source-of-truth derivation, shared register/teardown seams, concrete boundary types, uniform failure contract, and non-vacuous real-tmux integration coverage of every AC and testing requirement. managedEvents is the one table; the event-set (managedEventNames() → portalEvents) and both fingerprint sets (managedEventFingerprintUnion() → teardownFingerprints() → portalCommandSubstrings) are all DERIVED from it, not independently authored; the teardown-only migrateRenameSubstring asymmetry is modeled correctly (explicitly appended, asserted absent from the registration union); parity tests TestPortalManagedEventSetParity + TestPortalTeardownFingerprintParity guard re-divergence. Shared helpers parseEventEntries / evictPortalEntries / warnShowHooksFailure are consumed by both convergeEvent and unregisterPortalHooks so per-event read/evict/WARN behavior cannot drift; the evictFailure struct lets each caller keep its own aggregation policy (register: best-effort WARN, not counted; teardown: fold event[index] into errors.Join) without homogenizing. ShowGlobalHooksForEvent preserves the deleted no-arg method's contract; exported UnregisterPortalHooks(*Client) error stays a thin wrapper over the injected-logger unregisterPortalHooks so cmd/state_cleanup.go's function-value consumption is undisturbed; UnregisterPortalHooksWithLogger closes the teardown-WARN testability gap. Concrete named structs at boundaries (managedEvent, evictFailure, HookEntry); no untyped containers. Real-tmux tests cover all 5 testing requirements + 8 ACs against a faithful oracle; the perEventDispatchWithFaults t.Fatalf pins the per-event invariant in one place. No genuine seam defect, integration gap, over/under-engineering, or missed composition opportunity.
