AGENT: standards
FINDINGS:
- FINDING: Test names reference deleted helpers that no longer exist (migrateHydrationHooks / migrateSessionClosedHook / convergeSessionClosed)
  SEVERITY: low
  FILES: internal/tmux/hooks_migration_test.go:73, :129, :169, :202, :254, :313, :365, :421; internal/tmux/hooks_register_warn_test.go:86, :123
  DESCRIPTION: The spec ("Migration-Helper Consolidation") decided to DELETE migrateHydrationHooks and migrateSessionClosedHook and fold their behavior into the single ensure-exactly-one path. The production deletion was done correctly (no production symbol or comment references them). However the test function names still encode the deleted helpers as their subject: TestMigrateHydrationHooks_* (8 functions in hooks_migration_test.go plus 1 in hooks_register_warn_test.go) and TestConvergeSessionClosed_ShowHooksFailureWarnIsNormalized. There is no migrateHydrationHooks, no migrateSessionClosedHook, and no convergeSessionClosed symbol — the production function is convergeEvent. The test bodies correctly drive RegisterPortalHooks and the surrounding doc comments accurately explain the helpers were deleted, so coverage is faithful; only the names are stale. Per the project convention that "comments must accurately describe the code they sit on," a test named after a symbol that no longer exists mildly misleads a future reader grepping for the (nonexistent) helper. Not a behavioral or coverage gap.
  RECOMMENDATION: Rename the affected test functions to describe the convergence behavior under test rather than the deleted helper (e.g. TestMigrateHydrationHooks_EvictsUnSeparatedThenInstallsFixed → TestRegisterPortalHooks_HydrationConvergesUnSeparatedToDashForm; TestConvergeSessionClosed_ShowHooksFailureWarnIsNormalized → TestRegisterPortalHooks_SessionClosedReadFailureEmitsCanonicalWarn). The file-level and per-test doc comments are already accurate and can stay.
SUMMARY: Production code conforms to the specification on every load-bearing decision. Verified: ShowGlobalHooks (no-arg) is fully deleted with no remaining production caller (AC6); both registration and teardown read per-event via ShowGlobalHooksForEvent (failed to show global hooks: %w wrap preserved); the ensure-exactly-one convergence in convergeEvent implements the per-event parameter table including the session-closed two-fingerprint union and the idempotent fast-path full-body equality check (AC 1/2/3/7); the substring predicate replaced exact-match uniformly with the documented behavioral change recorded; portal state migrate-rename is retained in teardown's portalCommandSubstrings only and never in the registration table; portalEvents is derived from managedEvents (parity guard present); per-event read failures fold into errors.Join and never short-circuit (AC5, teardown reaps at depth). Logging stays inside the closed taxonomy — bootstrap component, reaped attr, show-hooks failed WARN with error_class=unexpected, and the per-index failed to evict portal hook WARN carries only the error attr; no new component or attr key was invented. No t.Parallel() is used. Real-tmux socket fixtures back the blind-spot/no-growth/self-heal/teardown-at-depth/churn-free tests as the spec's Testing Requirements mandate. go build, go vet ./internal/tmux/..., and go test ./internal/tmux/... all pass. The sole drift is cosmetic test-name staleness referencing the two deleted migration helpers.
