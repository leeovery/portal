AGENT: standards
FINDINGS:
- FINDING: Six-event routing test references a deleted test name and a non-current spec section heading
  SEVERITY: low
  FILES: internal/tmux/hooks_register_six_event_routing_test.go:22-23, internal/tmux/hooks_register_six_event_routing_test.go:99-104
  DESCRIPTION: The file-level rationale comment claims "The existing TestRegisterPortalHooks_SessionClosedMigration's nonSessionClosedSaveTriggerEvents loop already asserts each of the six events resolves to notifyCommand". No test named `TestRegisterPortalHooks_SessionClosedMigration` exists anywhere in the codebase — under this spec's Migration-Helper Consolidation, `migrateSessionClosedHook` was folded into the unified convergence engine and the corresponding test was renamed/restructured (current session-closed tests are `TestRegisterPortalHooks_SessionClosedUnionFastPath` / `*SessionClosedSubstringEvictsPortalStateNotifyBody` / `*SessionClosedNonMatchingUserHookSurvives`, and the fresh-table six-event coverage now lives in `TestRegisterPortalHooks_FreshTable`). The same comment block also points reviewers at spec section "Hook Registration Migration → Registration Strategy", which is a heading from a different/prior spec, not this work unit's spec (whose heading is "Registration Redesign — Ensure Exactly One"). The project constraint requires comments to accurately describe the code they sit on; this comment misdirects a reader to a non-existent test as the stated justification for the gate's existence. The test body and assertions themselves are correct and conform to the spec.
  RECOMMENDATION: Update the comment to reference the current covering test (e.g. `TestRegisterPortalHooks_FreshTable`, which asserts each non-session-closed save-trigger event resolves to `expectedNotifyCommand`) instead of `TestRegisterPortalHooks_SessionClosedMigration`. Optionally re-anchor the stale "Hook Registration Migration → Registration Strategy" section pointer to this spec's "Registration Redesign" / per-event parameter table.
SUMMARY: Production code (hooks_register.go, hooks_unregister.go, tmux.go) conforms fully to the specification's per-event "ensure exactly one" design — per-event reads via ShowGlobalHooksForEvent, no-arg ShowGlobalHooks deleted (no production or test caller remains), the two migration helpers folded into convergeEvent, substring-predicate divergence between registration fingerprints and teardown portalCommandSubstrings preserved (NOTE: a separate architecture finding argues commit-now should be added to teardown — see analysis-architecture-c3), union fast-path on session-closed handled correctly, and logging confined to the closed bootstrap component / reaped attr / canonical show-hooks failed (error_class=unexpected) + failed to evict portal hook (error) WARNs with no invented components or attrs. Tests cover all eight Acceptance Criteria and five Testing Requirements via real-tmux socket fixtures, with no t.Parallel(); build and vet pass. The only standards drift is one stale doc comment.
