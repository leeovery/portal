AGENT: standards
STATUS: clean
FINDINGS: none
SUMMARY: Implementation conforms to the specification and project conventions; all 8 acceptance criteria are realised in production and guarded by faithful real-tmux tests. Verified: AC #1/#2/#7/#8 (convergeEvent ensure-exactly-one with the spec's 4-step algorithm, real-tmux no-growth + churn-free index-stability oracle); AC #3 (hydration `--` + session-closed two-fingerprint convergence intrinsic to the unified path); AC #4 (substring predicate matches only Portal bodies; user hooks survive register + teardown); AC #5 (teardownFingerprints() derives portalCommandSubstrings = managedEvents fingerprint union incl. commitNowSubstring + legacy migrate-rename; TestPortalTeardownFingerprintParity + real-tmux session-closed commit-now reap close the seam); AC #6 (no-arg ShowGlobalHooks gone; ShowGlobalHooksForEvent preserves the wrap + trimming Run path; ParseShowHooks unchanged). Migration helpers fully deleted (no production references); migrate-rename correctly teardown-only. Closed log taxonomy honored (bootstrap component, reaped/error/error_class attrs, canonical show-hooks-failed WARN error_class=unexpected; no invented attrs/components). No t.Parallel(). Comments/names accurately describe current code; no stale references to deleted helpers.
