AGENT: duplication
FINDINGS:
- FINDING: Teardown tests hand-roll the per-event dispatch RunFunc instead of routing through the shared perEventDispatchWithFaults helper
  SEVERITY: low
  FILES: internal/tmux/hooks_unregister_test.go:203-211, internal/tmux/hooks_unregister_test.go:237-251, internal/tmux/hooks_unregister_warn_test.go:29-37
  DESCRIPTION: Three teardown tests re-implement the per-event `show-hooks -g <event>` read / `set-hook -gu` dispatch skeleton inline as bespoke RunFunc literals, duplicating logic the package already centralises in perEventDispatchWithFaults (hooks_register_test.go:116) and its teardown shim dispatchUnregisterHooks (hooks_unregister_test.go:28). Each of the three scenarios has an exact match in the shared helper's fault channels: (a) hooks_unregister_test.go:237-251 ("single-event read failure folds") is equivalent to `perEventDispatchWithFaults(t, raw, nil, map[string]error{"pane-focus-out": sentinel}, nil)` — it even calls linesForEvent, the same per-event scoping the helper performs internally; (b) hooks_unregister_test.go:203-211 and (c) hooks_unregister_warn_test.go:29-37 ("every per-event read fails") match the fail-every-read shape the register-side warn tests already express through the helper as `perEventDispatchWithFaults(t, "", nil, readErrForAllManagedEvents(sentinel), nil)` (hooks_register_warn_test.go:90, :214). This is copy-paste drift across the register/teardown test boundary: the register-side warn suite routes these identical scenarios through the shared dispatcher, the teardown-side suite does not. Beyond the ~30 lines of duplicated dispatch wiring, the inline RunFuncs bypass the shared helper's load-bearing no-arg-global-read t.Fatalf guard (each inline RunFunc has only a generic catch-all `unexpected command` t.Fatalf, not the specific per-event tripwire) — the exact guard whose value dispatchUnregisterHooks' own docstring advertises as protecting the teardown path from silently reverting to the blind no-arg show-hooks -g read.
  RECOMMENDATION: Replace the three inline RunFunc literals with the existing shared helpers (no new abstraction needed). For hooks_unregister_test.go:237-251, call `perEventDispatchWithFaults(t, raw, nil, map[string]error{"pane-focus-out": sentinel}, nil)` directly (or extend dispatchUnregisterHooks to accept a readErrFor map). For the two fail-every-read cases (hooks_unregister_test.go:203-211 and hooks_unregister_warn_test.go:29-37), use `perEventDispatchWithFaults(t, "", nil, readErrForAllManagedEvents(sentinel), nil)`, mirroring the register-side warn tests — or expose a readErrFor parameter on dispatchUnregisterHooks so teardown tests reach the read-fault channel symmetrically with the unset-fault channel they already use. This collapses the divergent dispatch wiring onto one definition and gives all three teardown tests the no-arg-global-read guard for free.
SUMMARY: Production code (hooks_register.go, hooks_unregister.go, tmux.go) is cleanly consolidated — convergence and teardown share parseEventEntries, warnShowHooksFailure, evictPortalEntries, and containsAny with no duplication, and the event-set / fingerprint sets are derived from a single managedEvents source of truth. The only actionable duplication is three teardown tests that hand-roll the per-event dispatch RunFunc the shared perEventDispatchWithFaults helper already owns, diverging from the register-side warn tests and losing the helper's no-arg-global-read guard.
