SECTION: 5. Argv Composition + 6. Search Result Display + 7. Cold-Path Classification + 8. Tab Completion

SCOPE: Commit range `7068abc9a^..HEAD` (74d9922f9), filtered to the 84 files the `Topen-with-forced-filter-*` commits touched. Read in full: `cmd/open_search.go`, `cmd/completion.go`, `cmd/init.go`, `cmd/root.go` (bootstrap + `isTUIPath`), `cmd/open.go` (`openCmd.Long`, `RunE`, `openTUI`, `finishTUI`, `processTUIResult`, flag registration), `cmd/bootstrap_warnings.go`, `internal/tui/session_dir_column.go`, `internal/tui/session_item.go`, `internal/tui/search_filter.go`, `internal/tui/search_decision.go`, `internal/tui/picker_sessions.go`, `internal/tui/grouping.go`, `internal/tui/notice_band.go`, `internal/tui/build.go`, `internal/capture/fixtures.go` (search-results fixture), plus the behaviour-relevant arms of `internal/tui/model.go` (delegate, landing, loading gate, `BootstrapCompleteMsg`/`BootstrapFatalMsg`, `createSession`/`mintSession`) and `internal/resolver/search.go` / `path.go`. Followed out of the file list into `github.com/spf13/cobra@v1.10.2` (`command.go` execute ordering, `completions.go` probe parse) and `github.com/spf13/pflag@v1.0.10` (`argsLenAtDash` lifecycle) because the delivered code's correctness rests on both.

Run: `gofmt -l .`, `go vet ./...`, `golangci-lint run` (the project's three declared linters, each as CLAUDE.md spells it), `go build -o <scratchpad>/portal .`. The built binary was driven against a **disposable** tmux server on a private socket (`-S /tmp/ptl-rv.XXXX/tmux-501/default`, reached by the binary through `TMUX_TMPDIR`), never the default socket, never with `pgrep`/`pkill`, never spawning a daemon — `__complete` and the refused `open` lines are both bootstrap-exempt or refused before `PersistentPreRunE`. The emitted `portal init` scripts were driven through real `/bin/bash` 3.2.57, `/opt/homebrew/bin/bash` 5.3.15 and `zsh` 5.9. Both disposable servers were `kill-server`'d on their own socket and their temp dirs removed; every scratchpad artefact (binary, stub, emitted scripts) was deleted. `git status --porcelain` after the pass is byte-identical to the pre-pass snapshot. Not run: the integration lane, any whole-suite or package test run, `cmd/capturetool` live (no TTY in this environment); §6's rendered output was measured from the two committed captures instead.

MEASURED:
- "gofmt -l reports nothing and go vet ./... is clean" [6-1, 6-2, 6-3] — `gofmt -l .` → no output; `go vet ./...` → no output, exit 0 — holds
- "`go vet ./...`, `gofmt -l .` and `golangci-lint run ./...` are clean" [7-1] — all three run from the repo root; `golangci-lint run` → `0 issues.` — holds
- "`golangci-lint run` clean" / "reports nothing new" [8-1, 11-2, 12-5, 12-6] — `golangci-lint run` → `0 issues.` (config sets the `integration` tag and uncaps both limits, so one invocation covers the tagged files) — holds
- "`go build ./...`" [11-2, 8-1] — `go build -o <scratchpad>/portal .` succeeded; `go vet ./...` compiles every package including tests — holds
- "Every refusal today's seven flags produce is byte-identical and still fires from its own named arm, in the current order" [11-1] — `git show --stat f25d236d0` is a pure insertion (`cmd/open_search.go` +31/-0), so the six named arms are unchanged; driven end-to-end with `PATH=/nonexistent-no-tmux <portal> open …` (which makes any line that escapes the validator fail visibly at `CheckTmuxAvailable` instead): `/term -e ls` and `/term -- ls` → `cannot use a /term search with a command (-e/--)`; `/term other` and `other /term` → `…with another target`; `/term /other` → `…with another /term search`; `/term -s foo` and `/term -p /tmp/x` → `…with a domain pin (-s/-p/-z/-a)`; `/term -f text` → `…with -f/--filter`; `--ack a:b /term` → `…with --ack`; all exit 2 — holds
- "`go run ./cmd/capturetool --fixture sessions-search-results` … renders `~/code/portal` beside `portal-a1b2`, `~/code/portal-gateway` beside `api-work`, `/opt/portal-tools` beside `portal-notes`, an empty slot beside `legacy-port-shim` and a `…/`-prefixed tail on the deepest path" [10-5] — the live route could not be driven (Bubble Tea: `error opening TTY: open /dev/tty: device not configured` in this environment), so measured instead from the committed render at the tape's own geometry, `testdata/vhs/sessions-search-results.png`, read directly: all five forms present exactly as named (`portal-a1b2 ~/code/portal`, `api-work ~/code/portal-gateway`, `legacy-port-shim` with a bare name, `portal-design-exports-review …/testdata/reference/design-exports/frames`, `portal-notes /opt/portal-tools`), with `evvi-sync-engine` correctly absent — holds
- "In bash and zsh, completion after the function asks for `open`'s completions and offers live session names" [5-2, partial] — `portal init bash` sourced into real `/bin/bash` 3.2.57 and `/opt/homebrew/bin/bash` 5.3.15 with a `_init_completion` stand-in (the same stand-in `cmd/init_completion_shell_test.go` supplies, and required for the same reason: the `bash-completion` package is absent on this machine), over a recording `portal` stub on PATH: `x /po<TAB>` records `__complete|open|/po` and replies `/portal-a1b2`, rewriting `COMP_LINE` to `portal open /po` with `COMP_POINT` 15; `xctl open /po<TAB>` records the identical request with its line untouched. `portal init zsh` sourced into real zsh 5.9 with the compsys stand-ins: `x /po` → `/portal-a1b2`, `x /` → all four sessions, `x ""` → the four bare names, `x -- ls /po` → `FILECOMP *:filename:_files` — holds

NOT MEASURED:
- "`go test ./...` passes" [1-1, 1-2, 1-3, 1-4, 1-6, 2-1, 2-3, 3-1, 3-2, 3-3, 4-1, 5-1, 5-3, 12-1, 12-4] — the whole unit suite; my execution rules reserve it for the single later run over the corrected tree, and permit a single package's tests only to confirm a suspected defect (I suspect none in this section)
- "`go test ./...` passes and `go test -tags integration -p 1 ./...` passes" [1-5, 1-7, 2-2, 2-4, 3-4, 4-2, 4-3, 4-4, 5-2, 5-4, 11-2] — same reason for the unit half; the integration lane is forbidden outright on this machine (real tmux, real daemons, a built binary)
- "`go test -tags integration -p 1 ./...` passes, and `go test ./...` is unaffected" [4-5] — integration lane forbidden here
- "Every one of the cold-boot observations: the decision closure runs exactly once; all ten step events and the terminal complete precede it; `state.IsRestoringSet` false at that instant; the closure's live read returns the restored sessions; the model stays on `PageLoading` through every step; single-match quits with `SearchAttached()` and `PageLoading`; shared-fragment transitions to `PageSessions`; `FatalError()` nil on both runs" [4-5] — all of these require `go test -tags integration -p 1 ./cmd -run TestConcurrentColdBoot_SearchDecision` against a real cold tmux server, which is forbidden here
- "`go test ./...` is green" [4-6, 4-7, 4-8, 12-3] — whole unit suite; see above
- "`go test ./internal/tui/... ./cmd/...` passes with no edit to any test file" [10-1, 10-3] — the no-edit half is settled by the commits' diffstats; the passing half is a suite run
- "`go test ./internal/tui/...` passes with no test changed" [11-4] — suite run
- "`go test ./cmd -run TestCompleteOpenPositional` passes" and "The four existing subtests … still pass" [11-5] — a package test run. The contract they guard was measured directly instead: `portal __complete open -- ls /po` → no candidates + `ShellCompDirectiveDefault` (12-6's contract, which the criterion's own note flags as superseding its wording), `open -- /po` → `/portal-a1b2` + NoFileComp, `open api /po` and `open /po` → `/portal-a1b2` + NoFileComp
- "`go test -race ./internal/tui -run TestContainmentFilterIsRaceFreeAgainstAConcurrentSet`" and "`go test -race ./internal/tui` is green" [12-2, 6-3, 2-3] — a package test run under `-race`; the subject (`searchItemSource`'s filter rule) is §4's, not this section's
- "`TestMatchesSearchTerm`, `TestMatchesSearchTermUnresolvableHome` and the existing `FilterValue` subtests pass with no edit" [6-2] — package test run; subject is §4's matching rule
- "The five named search-count suites pass with no edit" [6-1] and "`TestOpenCommand_SearchForm_ExcludesNothingOutsideTmux`'s zero-read assertion" [8-1] — package test runs; subject is §3.2's searched set. The rule itself was measured through the completion surface (see COVERAGE)
- "The existing `internal/tui` and `cmd` suites pass unchanged, with no new call reaching a real tmux server" [7-2] — the second half is settled by reading; the first is a suite run
- "`go test ./internal/tui -run TestSearchDirColumn` and `go test ./internal/capture -run TestSessionsSearchResultsFixture` fail with the sessions-page key guard temporarily widened, and pass reverted" [3-5] — requires editing a tracked production file (`internal/tui/model.go:2547`) to run the experiment; the tree is not mine to modify
- "The column survives an `s` regroup, a `Space` preview and back, a `SessionsMsg` refresh, a marked-set mutation, a live theme swap, and a hand edit of the committed filter text" [3-3] — named as `go test ./internal/tui -run TestSearchDirColumn`. The property behind them was settled structurally instead (see COVERAGE: `ShowDir` is `m.searchForm`, written once at construction and never cleared)
- "A `-f` picker, a no-argument picker and a command-pending picker render their session rows byte-identically to today (existing suites green unmodified)" [3-3] — suite run; the gate was read (`internal/tui/build.go:132-134` supplies `WithSearchForm` only for a non-nil `Deps.Search`)
- "In fish, completion after the function asks for `open`'s completions and offers live session names" [5-2] — fish is not installed on this machine (`command -v fish` resolves nothing), so no fish leg can be driven. The emitted text was read instead (`cmd/init.go:135-140` → `complete -c x -f` then `complete -c x -w 'portal open'`, both taking the configured `--cmd` name)
- "A bash with the `bash-completion` package driving the emitted script with no `_init_completion` declared, read back through `BASH_COMP_DEBUG_FILE`" [5-5] — the package is not installed here and installing it would change the machine; cobra's own generated script bails before any completion runs without it, which is the 2026-09-14 corrigendum's recorded finding
- "`go test ./cmd` is green, including the verbatim shim expectation in `cmd/init_test.go`" [5-5] — package test run
- "Every `TestWarningsOwedAtTeardown` subtest other than the cancel one passes with no edit" [12-5] — package test run; the no-edit half is settled by the commit diff
- "A warm single-match attach writes every accumulated warning line to stderr, in order, before `openSessionFunc` is called" [4-3] — the end-to-end claim needs a live warm install with `_portal-saver` killed, which is a destructive act on this machine. Settled by reading instead (`cmd/open_search.go:243-246`)
- "The human visual check is recorded before sign-off, with the still capture and the live-view command presented" [3-4] — a gate record, not a measurement available to this pass
- [10-2], [5-6], [8-2] — the blocks record no unsettled criteria

FINDINGS:
- None

COVERAGE:
- §5.1 a refused line starts nothing — cobra@v1.10.2 `command.go:968` vs `:985` — measured: `ValidateArgs(argWoFlags)` precedes the `PersistentPreRunE` loop, and each of the nine refused lines above exited 2 with its usage message while the control line `portal open /term` (same `PATH=/nonexistent-no-tmux`) reached `Portal requires tmux…` at exit 1, proving bootstrap ran only when the validator passed
- §5.1 `--help` answers before the rule — cobra@v1.10.2 `command.go:930-935` — measured: `portal open /term --help` prints `open`'s long help and exits 0, ahead of `ValidateArgs`
- §5.1 the rule reaches a flag no arm names — cmd/open_search.go:103-105, :118-126 — read: `firstSetLocalFlag` walks `LocalFlags()` with `VisitAll` filtered on `Changed` (the comment's reason is correct: pflag records `actual` on the set the value parsed through, so `Visit` over the second set reports nothing), and `LocalFlags()` excludes parent persistent flags — of which `rootCmd` registers none (`grep -n 'PersistentFlags()' cmd/*.go` → no production hit), so the exemption is currently vacuous rather than load-bearing
- §5.1 refusal order is fixed — cmd/open_search.go:83-96 — read: a single `switch` with no fallthrough, second-sigil before second-target before command before `-f` before pin before `--ack`; measured consistent with the messages above
- §5.1 recognition is positional-independent — cmd/open_search.go:17-24, internal/resolver/path.go:25-27 — measured: `open other /term` and `open /term other` produce the same refusal
- §5.2/§5.4 a sigil never reaches the picker with a pending command — cmd/open_search.go:223, :231, :248 — read: all three `openTUIFunc` calls on the search route pass `nil` for `command`, and the `-e`/`--` line is refused upstream, so `commandPending` can never be true on a search-opened model
- §5.5 `-f` keeps its own contract and gains the help line — cmd/open.go:133-134, :227-241, :157-159 — read: `validateFilterFlag` is unchanged in substance (target/pin exclusion, empty-value refusal) and `openCmd.Long` carries the `-f` vs `/term` contrast line and the "composes with nothing" line
- §6.1 the directory column is on in every grouping mode — internal/tui/model.go:938 — read: `ShowDir: m.searchForm` on the single `sessionDelegate()` constructor, so the mode-aware `rebuildSessionList` chokepoint cannot vary it; grouping only changes the item slice
- §6.1 only the recorded directory is displayed — internal/tui/session_item.go:267, :277; internal/tui/grouping.go:16-21 — read: both render sites read `it.Session.Dir`, and the derived guess lives in the grouping-only `derivedDirs` map that `effectiveDir` consults and never writes back
- §6.1 an unrecorded directory leaves the slot empty — internal/tui/session_dir_column.go:24 — measured: `legacy-port-shim` renders as a bare name in both committed captures
- §6.2 one space, no separator glyph, count's own colour token — internal/tui/session_item.go:286-289, :252-255 — read: `dirCell = bg.Render(" ") + rowToken(countTok)`, `countTok` being `TextMuted`/`TextSecondary`-when-selected, the same token the window count takes; measured in testdata/vhs/sessions-search-results.png (dir and count share the muted rung, selected row one step brighter)
- §6.2 the NO_COLOR row is unchanged — internal/tui/session_item.go:180-183 — measured: testdata/vhs/sessions-search-results-nocolor.png shows the same single space and no bracket, glyph or separator standing in for the weight
- §6.2 left truncation lands on a separator — internal/tui/session_dir_column.go:33-45 — read: the loop walks left to right, so the first `/`-anchored tail that fits is the longest one that does; the i=0 candidate is provably unreachable (it is one cell wider than the whole value, which already failed the fit), so `…` can never be prefixed to an untruncated path; measured: `…/testdata/reference/design-exports/frames` in the committed capture
- §6.2 the home abbreviation is the rendered form at every width — internal/tui/session_dir_column.go:28 and internal/tui/session_item.go:268 — read: `resolver.AbbreviateHome` is applied on both the sized and unsized branches before any width test
- §6.2 the floor drops rather than truncating to noise — internal/tui/session_dir_column.go:47 — read: falling out of the loop returns `""`; worked through `/opt/portal-tools` (rungs `…/portal-tools` then nothing) and `~/code/portal` (rungs `…/code/portal`, `…/portal`, then nothing)
- §6.2 the directory gives way and the name never does — internal/tui/session_item.go:271-283 — read: the dir is fitted into `remaining = nameWidth - width(visibleName)` less one cell for the separator, so a name consuming its whole budget leaves the dir zero width; the assembled row is exactly `total` cells wide (arithmetic checked term by term against `used` at :261)
- §6.2 rendering never narrows the search — internal/resolver/search.go:9-14 vs internal/tui/session_dir_column.go — read: `SearchFields` builds from the whole abbreviated value with no width input; the truncation ladder is render-only
- §6.3 the column is scoped to a search-opened picker and survives a filter edit — internal/tui/model.go:567, :938; internal/tui/build.go:132-134 — read: `m.searchForm` is written once by `WithSearchForm`, applied only when `Deps.Search != nil`, and is never cleared by the landing, a regroup, a refresh or a filter edit; the term-less form sets it too (`WithSearchForm("")`)
- §7.1/§7.2 a sigil is classified as a picker invocation — cmd/root.go:173-178 — read: `len(preDashPositionals(cmd,args)) == 0 || len(searchFormPositionals(cmd,args)) > 0` after the pin veto; a sigil beside a pin is refused before `PersistentPreRunE`, so the veto cannot demote a legal sigil. Verdicts worked through for `open /term` (true), `open -- ls` (true, the second member §7.6 admits), `open -e ls` (true), `open ~/dir -- ls` (false), `open glob*` (false), `open -f text` (true)
- §7.1/§7.2 the same verdict routes the warnings — cmd/root.go:112-114, :146-148 — read: both `EmitTo(stderr)` calls are gated on `!isTUIPath`, so a sigil's warnings stay in the sink for the model
- §7.1 the sigil takes the concurrent bootstrap — cmd/root.go:189-194, cmd/open.go:632-640 — read: `shouldRunConcurrentBootstrap` = `isTUIPath && client != nil && !latchSatisfied`; `openTUI` starts the progress pipe and forces `serverStarted`, which is what parks the model on the loading page
- §7.5 a K=1 sigil still delivers its warnings, after teardown and before the attach — internal/tui/search_decision.go:51-62, internal/tui/model.go:486-487, cmd/open.go:620-629 — read: `applySearchDecision` quits without calling `surfaceBufferedWarnings`, so `bufferedWarnings` survives; `WarningsOwedAtTeardown` concatenates it with the staged set and `finishTUI` writes it before `processTUIResult` runs the connector. The warm-server counterpart is the explicit `bootstrapWarnings.EmitTo` at cmd/open_search.go:245
- §7.5/§3.7 a failed session-list read is not a bootstrap fatal — cmd/open.go:606-611, internal/tui/search_decision.go:54-57 — read: the error is carried on the model, returned by `processTUIResult` after the warnings write, and takes no in-TUI error frame
- §7.6 the loading gate holds until the decision answers, and the minimum span is untouched — internal/tui/model.go:1509-1524, :1641, :1693 — read: `dismissLoadingGate` is a third condition rather than a step, both gate messages still require `bootstrapComplete && minElapsed`, and a repeat while the decision is out holds the page
- §7.6 the command-pending picker paints from frame one — internal/tui/model.go:529-535 vs :661-667 — read: `WithCommand` is applied after the option list in `Build`, so its `PageProjects` overrides `WithServerStarted`'s `PageLoading`
- §7.6 the mint is held rather than gated, first stage winning — internal/tui/model.go:1844-1866, :1840-1842 — read: `createSession` returns early on `fatalActive`, stages `dir` under `bootstrapInFlight()` and refuses to re-target once `stagedMint` is set
- §7.6 the staged pick is replayed from the bootstrap's terminal event, after the warnings are staged — internal/tui/model.go:1669-1692 — read: the `BootstrapCompleteMsg` arm appends the warnings for a command-pending model first, then replays `mintSession(dir)`
- §7.6 the band announces the wait in place of the pick-a-project banner — internal/tui/notice_band.go:35-37, :205-211 — read: the `stagedMint` arm sits above the `commandPending` arm in the single-slot arbiter
- §7.6 a bootstrap fatal ends the run rather than minting — internal/tui/model.go:1700-1710 — read: `BootstrapFatalMsg` sets `fatalActive` (which `createSession` checks first) and returns `tea.Quit` for a command-pending model
- §8.1 offered words carry the sigil and are prefix-shaped — cmd/completion.go:70-83 — measured: `portal __complete open /po` → `/portal-a1b2`; `open /ort` → nothing; `open /e` → `/evvi-sync`
- §8.1 `/<TAB>` offers the whole searched set — cmd/completion.go:78 — measured: `portal __complete open /` → `/api-work /evvi-sync /legacy-port-shim /portal-a1b2`
- §8.1 the offered set is the searched set, not the live enumeration — cmd/completion.go:74, internal/tui/picker_sessions.go:13-23 — measured against the disposable server: inside tmux with the current session `evvi-sync`, `open /e` offers nothing and `open /` omits it; outside tmux the same two probes offer it, so nothing is held back when the read cannot name a session
- §8.1 Portal's own internal sessions are never offered — internal/tmux/tmux.go:151-188 (`parseSessionList`'s underscore filter) — measured: with `_portal-saver` and `_portal-bootstrap` live on the disposable server, `open /` offers neither and `open _` offers nothing
- §8.1 a `/`-bearing session name is held back from the sigil arm only — cmd/completion.go:75-77 — measured: with live sessions `foo/bar` and `foo-bar`, `open /foo` offers `/foo-bar` alone while `open foo` offers both
- §8.1 completion is bounded by the separator, and by nothing else — cmd/completion.go:104-111, cmd/open_search.go:46-49 — measured: `open -- ls /po` → no candidates + `ShellCompDirectiveDefault`; `open api /po`, `open -f port /po`, `open -s api-work /po`, `open -e ls /po`, `open --ack a:b /po`, `open /po /po` → `/portal-a1b2` + `NoFileComp`, each of those lines then refused on Enter by `validateSearchFormCollisions` (measured above)
- §8.1 the boundary word after the separator keeps the sigil arm — cmd/open_search.go:46-49 — measured: `open -- /po` → `/portal-a1b2` + `NoFileComp`
- §8.1 the `<=` in `completingPreDashPositional` is load-bearing — cobra@v1.10.2 `completions.go:369-373` and pflag@v1.0.10 `flag.go:1147`, `:1288` — measured by reading both sources: cobra probe-parses `append(finalArgs, "--")` before the real parse, and `FlagSet.Parse` never resets `argsLenAtDash` (only `Init`/`NewFlagSet` do), so a separator-free line reports `dash == len(args)`; traced through `open /po` (0<=0), `open api /po` (1<=1), `open -- ls /po` (1<=0 false), `open -- /po` (0<=0)
- §8.3 the correction reaches `open`'s completer from the session-opening function — cmd/init.go:65-80, :102-110, :168-176 — measured on `/bin/bash` 3.2.57, bash 5.3.15 and zsh 5.9: the request recorded is `portal __complete open /po`, identical to what `xctl open /po` records, where before the change the `x` path spent a `portal open __complete …` invocation
- §8.3 the correction follows the configured function name and leaves `xctl` alone — cmd/init.go:85, :105-110, :135-143, :171-175 — read: every registration line takes `cmdName` from the `--cmd` flag while the shim bodies are fixed names, and the `ctlName` registrations are byte-unchanged from before the change (`git diff` on cmd/init.go); measured: `xctl open /po` completes with its command line untouched
- §8.3 the session-opening function's filename fallback goes off where the shell can honour it — cmd/init.go:135-137, cmd/completion.go:39, :60, :82 — read: fish is registered `complete -c <name> -f` per §2.2, and every pre-separator arm returns `ShellCompDirectiveNoFileComp`; measured under zsh: `x -- ls /po` reaches `_files` while `x /po` does not
- §8.1/§4.3 the completion arm and the count arm search the same set — cmd/completion.go:34, cmd/open_search.go:177-183, internal/tmux/tmux.go:130-149 — read: `ListSessions` and `ListSessionsProbe` share `parseSessionList` (same underscore filter, same `SplitN` dir slot) and both routes then pass through `tui.PickerSessions`
