SECTION: test surface — the change-set's test files, held against everything the specification asks them to guard

SCOPE: commit range `7068abc9a559abb027ad61c6f0ab732c67a0688e^..HEAD`, filtered to the 87-path change-set recovered from `git log --oneline --name-only --pretty=format: --grep="impl(open-with-forced-filter): Topen-with-forced-filter-"`. Of those, 61 are `_test.go` files (4 integration-tagged: cmd/abridged_integration_test.go, cmd/concurrent_coldboot_integration_test.go, cmd/concurrent_search_decision_integration_test.go, cmd/reattach_integration_test.go). Read in full: internal/resolver/{search_fields,search_match,path}_test.go, internal/tui/{search_containment,search_filter,search_decision,search_dir_column,search_landing,search_landing_termless,session_dir_column,session_item,session_row_anatomy,picker_sessions,command_pending_staged_mint,staged_mint_band,restore_source_guard}_test.go, cmd/{open_search,open_search_deferred,open_search_warnings,completion,init_completion_shell,init,open_help,open_docs,concurrent_bootstrap_gate,open_picker_landing,testhelpers,testmain_isolation,concurrent_search_decision_integration}_test.go, internal/tmux/list_sessions_probe_test.go, internal/capture/{capture,swap_harness}_test.go; plus the production sources they guard (internal/resolver/{path,search}.go, internal/tui/{search_filter,search_decision,session_dir_column,picker_sessions,session_item,model}.go, cmd/{open_search,completion,root}.go, internal/tmux/tmux.go), README.md's `### \`x\` (open)` section, and the whole specification including its six corrigenda. Diff of every test-file deletion over the range reviewed line by line (182 deletions; all mechanical seam-signature respellings, the `/nonexistent-path-for-test` → `/nonexistent/path-for-test` re-point, the `/dir` → `/dir/sub` re-point, and the §8.3 init-registration re-pointing).

Run (project conventions, CLAUDE.md "Build & Test"): `gofmt -l .` (silent), `go build ./...` (clean), `go vet ./...` (clean), `go vet -tags integration ./...` (clean — type-checks the four integration-tagged files without executing them), `golangci-lint run` ("0 issues."), and the unit lane per package over every package the change-set touches: `go test -count=1 ./internal/resolver ./internal/tmux ./internal/capture`, `go test -count=1 ./internal/tui`, `go test -count=1 ./cmd ./cmd/bootstrap`, plus `go test -race -count=1 ./internal/tui`. Targeted verbose runs to confirm subtests execute rather than skip. The integration lane was NOT run (this machine hosts the developer's live tmux server and `portal state daemon`); no tracked file was modified, no tmux server started, no daemon spawned, no `pgrep`/`pkill` issued. Tree left as found (`git status --porcelain` shows only the pre-existing `.workflows/…/manifest.json` modification and the untracked review directory).

MEASURED:
- "`go test ./...` passes" [1-1, 1-2, 1-3, 1-4, 1-6, 2-1, 3-1, 3-2, 3-3, 5-1, 5-3] — run per package over every package the change-set touches: `go test -count=1 ./internal/resolver ./internal/tmux ./internal/capture` → ok 0.486s / 17.433s / 21.341s; `go test -count=1 ./internal/tui` → ok 38.186s; `go test -count=1 ./cmd ./cmd/bootstrap` → ok 14.520s / 1.266s. Packages outside the change-set are untouched by it and type-check clean under `go build ./...` and `go vet ./...` (vet type-checks test files) — holds
- "`go test ./...` is green" [4-6, 4-7, 4-8, 12-1, 12-3 (unit half), 12-4] — same per-package runs above — holds
- "`go test ./...` passes" (unit half of the two-lane criteria) [1-5, 1-7, 2-2, 2-4, 3-4, 4-2, 4-3, 4-4, 5-4] — same per-package runs above — holds
- "`go test ./internal/tui/... ./cmd/...` passes with no edit to any test file" [10-1, 10-3] — `go test -count=1 ./internal/tui` → ok 38.186s and `go test -count=1 ./cmd` → ok 14.520s; the no-edit half re-confirmed against `git show --stat 2830ae043` / `fb1d3b442` (each a one-file diffstat naming internal/tui/model.go alone) — holds
- "`go test ./internal/tui/...` passes with no test changed" [11-4] — `go test -count=1 ./internal/tui` → ok; `git show --stat 30848009a` touches internal/tui/model.go alone — holds
- "Every refusal today's seven flags produce is byte-identical and still fires from its own named arm, in the current order — the collision suite at `cmd/open_search_test.go:337-401` passes with no edit." [11-1] — `go test -count=1 -v ./cmd -run 'TestOpenCommand_SearchForm|TestValidateOpenArgs|TestValidateSearchFormCollisions|…'` → 223 subtests PASS, 0 SKIP, 0 FAIL; the seven arms at cmd/open_search.go:84-106 are exercised by the six named tests at cmd/open_search_test.go:337/356/362/381/387/397 plus the derived arm at :414 — holds
- "`go build ./...`, `go test ./...` and `golangci-lint run` are clean, and no test's semantics change beyond the three respelled calls." [11-2, unit half] — `go build ./...` clean, per-package unit lane green, `golangci-lint run` → "0 issues." — holds
- "`go test ./cmd -run TestCompleteOpenPositional` passes." [11-5] — `go test -count=1 -v ./cmd -run 'TestCompleteOpenPositional'` → PASS (7 subtests) — holds
- "The four existing subtests are unedited and still pass … the probe-parse subtest still pins the `<=`." [11-5] — measured at HEAD against 12-6's contract as the criterion's own note directs: cmd/completion_test.go:601-611 now asserts `ShellCompDirectiveDefault` and zero candidates for `-- ls /po`, cmd/completion_test.go:625/633 still offer `/portal-a1b2` on the two no-separator lines, and cmd/completion_test.go:678-693 still pins the `<=` against cobra's probe parse — all PASS — holds
- "The `sync.RWMutex` and the single accessor stay, and the concurrent-`set` test is green under `-race`." [12-2] — `go test -race -count=1 ./internal/tui -run 'TestContainmentFilter'` → ok 2.170s (TestContainmentFilterIsRaceFreeAgainstAConcurrentSet at internal/tui/search_filter_test.go:168 runs 500 concurrent `set`s against 500 filter passes) — holds
- "`internal/tui/search_containment_test.go` and `go test ./...` stay green." [12-2] — `go test -count=1 -v ./internal/tui -run 'TestSearchContainment…'` inside a 125-subtest PASS run; full package green — holds
- "`go test -race ./internal/tui` is green, including a test driving `set` concurrently with a filter pass" [6-3] — `go test -race -count=1 ./internal/tui` → ok 65.608s — holds
- "`go test ./...` passes … and `go test -race ./internal/tui` for `TestContainmentFilterIsRaceFreeAgainstAConcurrentSet`" [2-3] — both run, both green (see above) — holds
- "`go test ./...` is green and `golangci-lint run` reports nothing new." [12-5, 12-6] — per-package unit lane green; `golangci-lint run` → "0 issues." (the config sets the `integration` tag and uncaps both issue limits, so one invocation covers the tagged files) — holds
- "`go test ./...` passes; `go vet ./...`, `gofmt -l .` and `golangci-lint run ./...` are clean" [7-1] — all four run; `gofmt -l .` silent, `go vet ./...` clean, `golangci-lint run` → "0 issues." — holds
- "`go test ./...` is green, `gofmt -l` reports nothing and `go vet ./...` is clean" [6-1, 6-2] — same three commands, all clean — holds
- "`gofmt -l` reports nothing and `go vet ./...` is clean" [6-3] — both clean — holds
- "`go build ./... && go test ./...` green; `golangci-lint run` clean" [8-1] — all three run and clean — holds
- "The existing search-count suites pass unchanged, including `TestOpenCommand_SearchForm_ExcludesNothingOutsideTmux`'s assertion that `CurrentSessionName` is read zero times outside tmux" [8-1] — `go test -count=1 -v ./cmd -run 'TestOpenCommand_SearchForm'` → all PASS, including that assertion at cmd/open_search_test.go:666 — holds
- "Behaviour is unchanged on both routes: `TestOpenCommand_SearchForm_ExcludesTheCurrentSessionFromTheCount`, `…AttachesTheOtherMatchWhenTheCurrentSessionAlsoMatches`, `…CountsNothingOutWhenTheCurrentSessionReadFails`, `…ExcludesNothingOutsideTmux` and `internal/tui`'s \"excludes the current session when inside tmux\" all pass with no edit" [6-1] — the four cmd tests at cmd/open_search_test.go:612/627/642/657 and the tui subtest at internal/tui/rebuild_session_list_test.go:202 all PASS under `go test -count=1 ./cmd ./internal/tui` — holds
- "`TestMatchesSearchTerm`, `TestMatchesSearchTermUnresolvableHome` and the existing `FilterValue` subtests pass with no edit" [6-2] — `go test -count=1 ./internal/resolver ./internal/tui` green; the two at internal/resolver/search_match_test.go:10/139 and the FilterValue subtests at internal/tui/session_item_test.go:15-72 all run — holds
- "`TestContainmentFilterFallsBackWhenTheSourceIsOutOfStep` and the `TestSearchContainment*` suites pass with no edit" [6-3] — the nine `TestSearchContainment*` functions (internal/tui/search_containment_test.go:85/109/143/161/214/247/312/352) all PASS; the named fall-through test was removed by task 12-2 and is absent at HEAD, as the criterion's own note records — holds
- "The loading-gated routes are untouched: the existing `internal/tui` and `cmd` suites pass unchanged, with no new call reaching a real tmux server." [7-2] — `go test -count=1 ./internal/tui ./cmd` green, which also compiles the whole `internal/tui` test binary against the changed `createSession` two-value signature; no new file in the change-set references `tmux.DefaultClient` or a socket outside the sanctioned `tmuxtest` fixtures — holds
- "`go test ./cmd` is green, including the verbatim shim expectation in `cmd/init_test.go`." [5-5] — `go test -count=1 ./cmd` → ok 14.520s; the verbatim bash and zsh shim bodies at cmd/init_test.go:47-51 and :146-150 are asserted and PASS, and the four shell-driven cases at cmd/init_completion_shell_test.go:430/442/452/461 run real `bash` subprocesses — holds
- "In bash and zsh, completion after the function asks for `open`'s completions and offers live session names" [5-2, the two installed shells] — `go test -count=1 -v ./cmd -run TestInitCompletion` → every bash and zsh subtest PASS; the recorded request is `__complete|open|/po` for `x /po` (cmd/init_completion_shell_test.go:326) and `__complete|` for `xctl` (:358) — holds
- "for the zsh \"an empty word\" case to record `__complete|open|`, cobra's `words=(\"${=words[1,CURRENT]}\")` truncation must preserve the trailing empty word" [5-2] — `go test -count=1 -v ./cmd -run TestInitCompletion_AsksPortalForOpenCompletions` → `zsh/an_empty_word` PASS — holds
- "`go test ./...` is green." [3-5, the suite half] — per-package unit lane green; the only packages the task's diff can reach are internal/tui and internal/capture, both green — holds
- "A `-f` picker, a no-argument picker and a command-pending picker render their session rows byte-identically to today (existing suites green unmodified)" [3-3] — `go test -count=1 ./internal/tui` green; the two direct assertions are internal/tui/search_dir_column_test.go:116 (`-f` picker, directory absent) and :123 (no-filter picker, directory absent), and internal/tui/session_row_anatomy_test.go:327 proves a recorded directory changes nothing with `ShowDir` off, across both themes — holds
- "The column survives an `s` regroup, a `Space` preview and back, a `SessionsMsg` refresh, a marked-set mutation and a live theme swap" and "The column survives a hand edit of the committed filter text, including clearing it" [3-3] — `go test -count=1 -v ./internal/tui -run TestSearchDirColumn` → all 15 subtests PASS at width 220, including the drop at width 34 (internal/tui/search_dir_column_test.go:281-298) — holds
- "The decision closure is invoked exactly once …", "The model is on `PageLoading` after every step event", "`FatalError()` is nil on both runs" [4-5] — the in-process equivalents are measured: internal/tui/search_decision_test.go:214 (exactly one dispatch across a duplicated gate sequence), :173 (no dispatch on progress), :193 (none after a fatal), :137/:155 (held until both gates) all PASS under `go test -count=1 ./internal/tui`. The live cold-boot leg is under NOT MEASURED — holds at the model layer
- "`go test -tags integration -p 1 ./...` … the new file stays excluded [from the unit lane]" [4-5, unit half] — `go test -count=1 ./cmd` compiles and runs the untagged set with cmd/concurrent_search_decision_integration_test.go excluded (its `//go:build integration` line 1), and `go vet -tags integration ./...` type-checks it clean with no identifier collision in `package cmd` — holds
- "Nothing in the change-set is lane-relevant beyond the unit lane (the new test file is untagged and builds no binary)" [2-4] — enumerated: of the 61 test files in the change-set, exactly 4 carry `//go:build integration`, and no untagged file in the change-set references `portalbintest`, `SpawnIsolatedDaemon`, `IsolateStateForTest`, `exec.Command("go"…)` or the string `state daemon` — holds

NOT MEASURED:
- "`go test -tags integration -p 1 ./...` passes" [1-5, 1-7, 2-2, 2-4, 3-4, 4-2, 4-3, 4-4, 4-5, 5-2, 5-4, 12-3] — the integration lane is forbidden in this pass: this machine hosts the developer's live tmux server (~47 sessions) and a live `portal state daemon`, and the lane builds portal binaries, spawns daemons and drives `pgrep`. `go vet -tags integration ./...` is clean, so the four integration-tagged files in the change-set type-check; pass/fail and the real-cold-server timing are the orchestrator's later serial run
- "A warm single-match attach writes every accumulated warning line to stderr, in order, before `openSessionFunc` is called" — the end-to-end leg [4-3] — the in-process ordering is measured (cmd/open_search_warnings_test.go:76-101 captures stderr at the instant `openSessionFunc` runs and PASSes); the live leg needs a real `x /term` against a warm latched install with `_portal-saver` killed, which is a mutation of the developer's running system
- "On a cold server `open /port` … the honest loading page actually paints" [4-4], and every live-boot clause of [4-5] ("All ten real step events … were delivered before it ran", "`state.IsRestoringSet(client)` is false at the instant it runs", "The closure's own live read returns the restored sessions", "On the single-match term the model quits with `Selected()` set …", "On the shared-fragment term … the model transitions to `PageSessions`", "no `BootstrapFatalMsg` is observed") — these are statements about a live cold boot against a real tmux server; settling them is `go test -tags integration -p 1 ./cmd -run TestConcurrentColdBoot_SearchDecision`, which is the forbidden lane. Read: cmd/concurrent_search_decision_integration_test.go:144-154 and :167-258 assert each clause and would fail on the regression it names
- "In fish, completion after the function asks for `open`'s completions and offers live session names" [5-2] — fish is not installed here (`command -v fish` → exit 1), so every fish subtest takes the `requireShell` skip; observed directly: `--- SKIP: TestInitCompletion_AsksPortalForOpenCompletions/fish/*` with "fish is not installed on this machine". The emitted-registration half runs unconditionally and passes (cmd/init_test.go:245-247 pins `complete -c x -f\ncomplete -c x -w 'portal open'\n`); the live leg needs a machine with fish
- "A measurement contradicting the derivation closes the task with `cmd/init.go` unedited" [5-5] — settling it needs a bash with the `bash-completion` package installed, driving the emitted script with no `_init_completion` declared and reading `cur` back from `BASH_COMP_DEBUG_FILE` for a mid-line cursor. The package is absent here and the harness declares its own `_init_completion` stand-in by design (cmd/init_completion_shell_test.go:36-48)
- "`go run ./cmd/capturetool --fixture sessions-search-results` on a machine whose home is not `/home/user` renders … at the tape's 105x27 geometry" [10-5] — capturetool is an interactive Bubble Tea program; driving it needs a terminal or a `vhs` run, neither of which is available to a non-tty agent. The model-layer equivalent IS measured: internal/capture/capture_test.go:1071-1077 sets `HOME` to a fresh temp dir and asserts `portal-a1b2 ~/code/portal`, and the five column branches are asserted at :1079-1117, all PASS at the 120x40 harness width. Note for the record: the committed PNG (last written by `955b2a65f`, 2026-09-13) predates commit `3403de3bf` (2026-09-15), which changed both the fixture's home-path construction and the tape's run line, so the PNG is a render of the pre-10-5 route rather than the current one. Per CLAUDE.md these PNGs are scaffolding with no visual-regression obligation and are cleared at sign-off, so nothing in the repository depends on it
- "The human visual check is recorded before sign-off, with the still capture and the live-view command presented" [3-4] — a gate record, not a repository artefact; settled by the run's own gate. Live-view command: `go run ./cmd/capturetool --fixture sessions-search-results --theme tokyo-night`
- "With the sessions-page key guard temporarily widened to swallow `s` under an applied filter, both subtests fail; with it reverted, both pass." [3-5] — the experiment requires editing `internal/tui/model.go:2547`, a tracked file. This pass may not modify the tree. The passing half is measured (`go test -count=1 ./internal/tui ./internal/capture` green, and internal/tui/search_dir_column_test.go:130-141 fails loudly with "s no longer regroups from a committed filter" if the mode does not move)

FINDINGS:
- None

COVERAGE:
- The sigil recognition rule — every row of §2.2's table is a case — internal/resolver/path_test.go:109-165 against internal/resolver/path.go:25-27 — measured: `go test -count=1 ./internal/resolver` → ok; `/port` and `/` true, `/Users/leeovery/Code/portal`, `/tmp/`, `./port`, `~/port`, `port`, `""` false
- The path test is narrowed for exactly one shape and no other — internal/resolver/path.go:14 calls `IsSearchSigil` before the pre-existing `strings.Contains(arg,"/") || '.' || '~'` test — read; and cmd/open_search_test.go:273 drives a real `portal open <tempdir>` end to end and asserts the mint path, PASS
- Containment is case-folded, literal, and never joins the two fields — internal/resolver/search_match_test.go:10-137 (15 cases incl. `po*` not expanding, `po?t`/`po[rs]t` not matching, `"i /o"` spanning the join not matching, empty term false) — measured: `go test -count=1 ./internal/resolver` → ok
- The matched directory is the home-abbreviated form, never the raw path — internal/resolver/search_match_test.go:38-65 (the tilde matches; `filepath.Base(home)` and `Users` do not) and :139-145 (unresolvable `HOME` degrades to the raw path) — measured: same run
- A session with no recorded directory is searchable by name alone, with no trailing separator — internal/resolver/search_fields_test.go:12-33 and internal/tui/session_item_test.go:15-72 — measured: `go test -count=1 ./internal/resolver ./internal/tui` → ok
- §4.3's joined form for the two fuzzy routes is name + one space + abbreviated dir — internal/tui/session_item_test.go:26-72 against internal/tui/session_item.go:87 — measured: same run
- The widened match domain reaches all three entry points — internal/tui/model_test.go:2825 (`-f` finds a session by directory), :2846 (a hand-typed `/` filter does), :2874 (neither matches on the account name) — measured: `go test -count=1 ./internal/tui` → ok
- Containment narrows without reordering, and the picker's own rule reorders — internal/tui/search_containment_test.go:109-141 (fuzzy ranks `portal-a1b2` above `app-port`; containment keeps the list's order; `-f` and an unfiltered picker keep fuzzy) — measured: same run
- The containment set is identical across Flat, By Project and By Tag, and headers vanish under a term — internal/tui/search_containment_test.go:214-245 — measured: same run
- Containment is reproduced after an `s` regroup, a `Space` preview and back, and a refresh that drops a killed session — internal/tui/search_containment_test.go:247-310, driving the real `Update` path — measured: same run
- Containment holds on the text, not the act: edited → fuzzy, edited back → containment, cleared → everything — internal/tui/search_containment_test.go:312-350 against the `query != term` test at internal/tui/search_filter.go:66 — measured: same run
- The term-less form installs no containment filter, so the first typed character is the picker's own rule — internal/tui/search_containment_test.go:352-371 against internal/tui/search_filter.go:86 — measured: same run
- The filter pass is race-free against the rebuild that re-seeds its source — internal/tui/search_filter_test.go:168-188 against the `sync.RWMutex` at internal/tui/search_filter.go:24-27 — measured: `go test -race -count=1 ./internal/tui -run TestContainmentFilter` → ok 2.170s
- A pass handed a stale generation's targets ranks by containment at the targets' own indexes, and a target the source does not hold ranks nothing — internal/tui/search_filter_test.go:39-101 — measured: `go test -count=1 ./internal/tui` → ok
- Every row of a session repeated across tags is ranked, and a header's empty filter value is not — internal/tui/search_filter_test.go:58-101 — measured: same run
- The committed-filter landing: `FilterApplied`, the term as the value, cursor on the first surviving session row (never a header) in all three modes — internal/tui/search_landing_test.go:42-212 against internal/tui/model.go:1320-1341 — measured: same run
- K = 0 and a machine with no live sessions still land on Sessions, never Projects — internal/tui/search_landing_test.go:130-157 against the `m.searchForm` arm at internal/tui/model.go:1303 — measured: same run
- A search form takes precedence over an initial filter and keeps `-f`'s text off the model — internal/tui/search_landing_test.go:214-244 — measured: same run
- The bare slash lands focused-and-empty with every session visible, narrows on the first character, and treats `s` as a filter character — internal/tui/search_landing_termless_test.go:13-134 — measured: same run
- The directory column is scoped to a search-opened picker and off everywhere else — internal/tui/search_dir_column_test.go:92-128 against `ShowDir: m.searchForm` at internal/tui/model.go:938 — measured: `go test -count=1 -v ./internal/tui -run TestSearchDirColumn` → 15 subtests PASS
- The column survives regroup, preview-and-back, refresh, a marked-set mutation, a live theme swap, a hand edit and a clear — internal/tui/search_dir_column_test.go:130-219 — measured: same run
- The column reads only the recorded directory: a grouping-derived value is never displayed and no pane read is issued for it — internal/tui/search_dir_column_test.go:246-279 (asserts `m.derivedDirs` is populated and the value still absent from the rows, and `fakeStamper.reads` is empty) — measured: same run
- A narrow width drops the directory without dropping a row — internal/tui/search_dir_column_test.go:281-298 (width 220 → 34, visible-row set unchanged) — measured: same run
- Left-truncation cuts only at a separator, at every width from 1 up — internal/tui/session_dir_column_test.go:121-145 against internal/tui/session_dir_column.go:33-45 — measured: `go test -count=1 ./internal/tui` → ok
- The abbreviation is how the value renders, not a truncation rung — internal/tui/session_dir_column_test.go:26-37 (abbreviates even where the raw path would fit) — measured: same run
- The floor drops the directory rather than emitting a fragment or a separators-only tail — internal/tui/session_dir_column_test.go:56-79 — measured: same run
- Width is measured in display cells, not bytes — internal/tui/session_dir_column_test.go:147-159 (`/opt/日本語/tui`, 15 cells over 18 bytes) — measured: same run
- The directory begins exactly one space after the name, at three name lengths — internal/tui/session_row_anatomy_test.go:308-326 — measured: same run
- The directory takes the window count's token and its selected-row variant, and the separating space carries the selection tint — internal/tui/session_row_anatomy_test.go:472-525 against `countTok` at internal/tui/session_item.go:286 — measured: same run
- §6.2's NO_COLOR carve-out: the colourless row's text is byte-identical to the coloured row's — internal/tui/session_row_anatomy_test.go:527-538 — measured: same run
- The row never overflows and never leans on the pathological-width backstop, swept across widths and names — internal/tui/session_row_anatomy_test.go:574-621 — measured: same run
- The count is taken over the set the picker lists, less the attached session, degrading to no exclusion on a failed or empty read and taking no read at all outside tmux — cmd/open_search_test.go:612-672 and :747-789 against cmd/open_search.go:164-183 — measured: `go test -count=1 -v ./cmd -run TestOpenCommand_SearchForm` → all PASS
- Portal's internal `_`-prefixed sessions are filtered at the enumeration, so no term can count one — internal/tmux/list_sessions_probe_test.go:76-80 against internal/tmux/tmux.go:188-196, driven through both `ListSessions` and `ListSessionsProbe` — measured: `go test -count=1 ./internal/tmux` → ok 17.433s
- `searchCandidates` leaves the caller's enumeration unmodified — cmd/open_search_test.go:689-709 and internal/tui/picker_sessions_test.go:49-58 against the fresh-slice build at internal/tui/picker_sessions.go:17-22 — measured: `go test -count=1 ./cmd ./internal/tui` → ok
- A failed session-list read is an ordinary non-zero failure carrying tmux's own stderr, not a zero match, not a usage error, not a bootstrap fatal, and paints no picker — cmd/open_search_test.go:728-745, cmd/open_search_deferred_test.go:244-264 and internal/tmux/list_sessions_probe_test.go:23-46 — measured: same runs
- K = 0 writes nothing to stderr and exits zero — cmd/open_search_test.go:545-562 (asserts `err == nil` and `errBuf.Len() == 0`) — measured: `go test -count=1 ./cmd` → ok
- A sigil reaches no branch of the resolution chain: session lister, alias, zoxide and dir validator are all consulted zero times — cmd/open_search_test.go:209-217 with the counting seams at :86-120 — measured: same run
- A sigil emits no `resolve` component line at any match count — cmd/open_search_test.go:247-271 over a `logtest.Install` sink; the same sink and query shape is proven to capture the line positively at cmd/open_test.go:1901/1930/1962/2004, so the negative is not vacuous — measured: same run
- The seven collision arms fire in the specified order with the specified wording, and the derived arm catches a flag no named arm covers — cmd/open_search_test.go:337-437 and :888-911 against cmd/open_search.go:83-106 — measured: same run
- A refused line starts no bootstrap, consults no resolver seam and never reaches the picker — cmd/open_search_test.go:439-452 and :794-821 (the Args validator runs before `PersistentPreRunE`) — measured: same run
- `--help` still answers on a search-form line and on a malformed `-f` line — cmd/open_search_test.go:454-464 and :913-923 — measured: same run
- Recognition stops at `--`: a `/word` past the separator is the trailing command's own — cmd/open_search_test.go:67-82 and :473 against `preDashPositionals` at cmd/open_search.go:29-34 — measured: same run
- A sigil classifies as a picker invocation at any positional index, and exactly as `-f` does; a path positional and two targets do not; words past `--` are never inspected — cmd/concurrent_bootstrap_gate_test.go:28-164 against `isTUIPath` at cmd/root.go:173-177 — measured: `go test -count=1 -v ./cmd -run 'TestIsTUIPath|TestShouldRunConcurrentBootstrap'` → PASS
- `portal open -- <command>` is admitted to the concurrent set (the 2026-09-14 corrigendum's second member) — cmd/concurrent_bootstrap_gate_test.go:78-86 and :240-247 — measured: same run
- The count is deferred on the concurrent route and taken up front on the warm one, with zero session reads before the picker seam runs — cmd/open_search_deferred_test.go:43-219, reading `readsAtTUI` recorded at the seam (cmd/open_search_test.go:160) against the `deferredBootstrapFromContext` branch at cmd/open_search.go:229-233 — measured: `go test -count=1 ./cmd` → ok
- The decision is held until BOTH loading gates, runs exactly once across a duplicated gate sequence, never on a progress event, never after a fatal, and off the update goroutine — internal/tui/search_decision_test.go:137-237 and :280-300 — measured: `go test -count=1 ./internal/tui` → ok
- A K = 1 decision quits with the picker never composed (`ActivePage() == PageLoading`) and leaves the buffered warnings unsurfaced; a K != 1 decision dismisses to Sessions with the warning band — internal/tui/search_decision_test.go:63-115 — measured: same run
- Ctrl-C before and during the decision selects nothing, and a decision arriving after a fatal leaves the error frame standing — internal/tui/search_decision_test.go:239-255, :307-323, :325-353 — measured: same run
- §7.5: the accumulated warnings are written to the terminal, byte-identical to the CLI path's rendering, before the connector runs and exactly once; the sink is drained; a picker branch leaves them for the model — cmd/open_search_warnings_test.go:75-191 and :320-418 against `bootstrapWarnings.EmitTo` at cmd/open_search.go:238/245 — measured: `go test -count=1 -v ./cmd -run 'TestSearchForm_WarmRoute|TestWarningsOwedAtTeardown|TestFinishTUI'` → all PASS
- Warnings owed at teardown cover the attach, the failed read, a warm picker, a cancel, a cancel with the decision in flight, and a warning staged before the concurrent launch — cmd/open_search_warnings_test.go:193-318 — measured: same run
- §7.6's hold: a command-pending pick during a live bootstrap stages rather than mints, the first stage wins, the complete event replays it, a fatal mints nothing on the fatal or on a later complete, Esc still cancels, and the warm route still mints immediately — internal/tui/command_pending_staged_mint_test.go:49-187 — measured: `go test -count=1 ./internal/tui` → ok
- The staged-mint notice band displaces the pick-a-project banner, yields to a live flash, restores the banner once issued, and its rows are reserved in the projects list budget at a wrapping width — internal/tui/staged_mint_band_test.go:24-92 — measured: same run
- §8.1 completion: the term after the slash completes against the searched set with the slash kept, offers nothing for a non-prefix term, holds back a slash-bearing name and the attached session, and degrades to offering everything on an empty or failed current-session read — cmd/completion_test.go:350-461 against cmd/completion.go:70-83 — measured: `go test -count=1 -v ./cmd -run 'TestCompleteSearchTerm|TestCompleteOpenPositional|TestSearchCompletionWiring'` → all PASS
- Internal `_`-prefixed sessions are excluded from both completion arms against a real disposable tmux socket — cmd/completion_test.go:253-285 (`tmuxtest.New(t, "ptl-complete-")`) — measured: same run
- A non-sigil word keeps today's session-name completer byte for byte, including `/Users/lee`, `/tmp/`, `~/Code/pro`, `.` and the empty word — cmd/completion_test.go:474-513 — measured: same run
- The `--` bound, both corrigenda: a post-separator word is answered with no candidates and `ShellCompDirectiveDefault` so the shell's filenames survive (`-- ls src/`), while the boundary word immediately after the separator keeps the sigil arm, and every pre-separator word does too — including on lines the composition rule refuses (`open api /po`, `open --filter api /po`) — cmd/completion_test.go:586-695 against cmd/completion.go:104-112 — measured: same run
- The `<=` in `completingPreDashPositional` survives cobra's probe parse — cmd/completion_test.go:678-693 against cmd/open_search.go:46-49, driving the two real `ParseFlags` calls cobra makes — measured: same run
- §8.3's correction, emitted text: bash `complete -o default -F __start_portal_open x` plus the verbatim shim body, zsh `compdef _portal_open x` plus its verbatim shim, fish `complete -c x -f` + `complete -c x -w 'portal open'`; `xctl` untouched in all three — cmd/init_test.go:43-51, :143-150, :245-247 — measured: `go test -count=1 ./cmd` → ok
- The correction follows `--cmd <name>` and leaves no default-name registration behind — cmd/init_test.go:194-202, :296-302, :383-410, :434-... — measured: same run
- §8.3's correction, live: the emitted scripts driven through real bash and zsh over a recording stub record `__complete|open|/po` for `x /po`, `__complete|open|` for an empty word, `__complete|open|-s|` after a flag, and `__complete|` for `xctl` — cmd/init_completion_shell_test.go:317-364 — measured: `go test -count=1 -v ./cmd -run TestInitCompletion` → bash and zsh PASS (fish SKIP, absent)
- The shells switch filename fallback off for `x /tm<TAB>`, confirmed on cobra's own debug channel rather than by absence — cmd/init_completion_shell_test.go:378-401; the bash assertion is a true proxy because the `Activating no file completion` line sits inside the `type -t compopt == builtin` gate in the emitted script (line 100-102 of `portal init bash`). Measured on `bash` 5.3.15 (`/opt/homebrew/bin/bash`, where compopt is a builtin) and `zsh`; the machine's stock `/bin/bash` is 3.2 and is not what `exec.LookPath` resolves
- The bash shim preserves typed spacing, shifts `COMP_POINT` rather than pinning it, and completes a word under a mid-line cursor and on a line with leading whitespace — cmd/init_completion_shell_test.go:430-471 — measured: same run
- The shell harness is hermetic: it stages its own `portal` stub on PATH, builds no binary, contacts no tmux server, and inherits cmd's package-wide poison (`TMUX=/nonexistent/…`, `HOME` a temp dir, every `PORTAL_*` path poisoned) — cmd/init_completion_shell_test.go:205-222 read against cmd/testmain_isolation_test.go:49-88
- Lane purity: exactly 4 of the change-set's 61 test files carry `//go:build integration`, and no untagged file in it builds a portal binary, spawns a daemon or calls `IsolateStateForTest` — enumerated over the change-set; `go vet -tags integration ./...` clean
- `t.Parallel()` appears nowhere in the tree — measured: repo-wide grep over `*_test.go` → no matches
- Seam discipline: every new cmd test stages through `withOpenDeps`/`withBootstrapDeps`/`withFuncSeam` rather than assigning a seam, which `cmd/seam_guard_test.go` derives from the production sources and enforces — measured: `go test -count=1 ./cmd` → ok (the guard runs in that package)
- §9, `portal open --help`: the recognition rule, term-less form, outcomes by match count, match domain, composition rule, the single-segment cost with the `-p` escape, and the `-f` / `/term` distinction in one paragraph naming both — cmd/open_help_test.go:10-98; and `-f`'s usage stays a one-liner carrying no search wording — :100-114 — measured: `go test -count=1 ./cmd` → ok
- §9, README: the `/port` and `/` commented examples inside the section's first fenced block (extracted separately from the prose so the prose cannot satisfy them), plus the `` `/<term>` `` table row, `-p /tmp`, `portal init` and the `-f, --filter` row — cmd/open_docs_test.go:68-92 against README.md's `### \`x\` (open)` section, which I read in full and which also carries the rollout consequence, the fuzzy-vs-containment contrast and the `--` bound — measured: same run
- The `sessions-search-results` fixture lands search-opened on Sessions with the term committed, narrows to the containment set, renders the abbreviated path under an arbitrary `$HOME`, the directory-only match, the empty slot, the unabbreviated `/opt` path, the `…/` rung, and the column in By Project and By Tag — internal/capture/capture_test.go:1039-1135 — measured: `go test -count=1 -v ./internal/capture -run 'TestSessionsSearchResultsFixture|TestFixtureNamesIncludesSessionsSearchResults|TestModelAt_ReachesCapturedState'` → 44 subtests PASS
- The new fixture is enrolled in the swap-and-diff completeness table, which asserts the table covers every build-backed fixture — internal/capture/swap_harness_test.go:65 and :81-90 — measured: same run
- The restore-launch-site guard was strengthened rather than weakened: it now follows the writer one hop through a helper parameter, so routing the restore through `finishTUI` keeps both launch sites under the rule — internal/tui/restore_source_guard_test.go:163-275 — measured: `go test -count=1 -v ./internal/tui -run TestLaunchSites` → 3 subtests PASS, naming cmd/open.go and cmd/capturetool/main.go and pinning `os.Stdout`
- No unit-lane test file in the change-set depends on wall-clock timing for its verdict — enumerated: the only `time.After` in an untagged change-set test file is internal/tui/command_pending_bootstrap_test.go:52, a 100ms cap that skips the ~50ms appearance-gate command; the 50ms caps in internal/tui/model_test.go predate this work (commit c95703bf5)
- The change-set's 61 test files were read for deletions as well as additions: `git diff … -- '*_test.go'` shows 182 deleted lines, every one a mechanical seam-signature respelling (`openTUIFunc`'s `string` → `pickerLanding`), the two deliberate argument re-points a leading-`/` single segment now requires (`/nonexistent-path-for-test` → `/nonexistent/path-for-test` at cmd/bootstrap_warnings_test.go:269, `/dir` → `/dir/sub` at cmd/concurrent_bootstrap_gate_test.go:254), or the §8.3 init-registration re-pointing — read
