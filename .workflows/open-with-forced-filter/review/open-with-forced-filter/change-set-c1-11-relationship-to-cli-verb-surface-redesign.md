SECTION: 11. Relationship to `cli-verb-surface-redesign` (specification.md:464-477, plus the six `## Corrigenda` entries at :484-496)

SCOPE: Commit range `7068abc9a559abb027ad61c6f0ab732c67a0688e^..HEAD` (199 commits), filtered to the 85 files recovered from the task-commit grep. Read in final state: `cmd/open.go`, `cmd/open_search.go`, `cmd/completion.go`, `cmd/init.go`, `internal/resolver/path.go`, `internal/resolver/search.go`, `internal/tui/picker_sessions.go`, `internal/tmux/tmux.go`; followed the behaviour into `internal/resolver/query.go`, which the file list does not name because the change-set never touched it — it is the chain §11 says the sigil now bypasses, so its untouched state is itself the evidence. Read outside the change-set: the sibling specification `.workflows/cli-verb-surface-redesign/specification/cli-verb-surface-redesign/specification.md` (the document §11 makes claims about; no commit in the range touches it) at :35-72, :110-120, :224-240, :251-256, :464-478. Run: `gofmt -l .`, `go build ./...`, `go vet ./...`, `golangci-lint run ./...`, and two narrow unit-lane `cmd` runs under `-run` filters (no integration tag, no daemon, no binary build, no tmux socket). Nothing was written outside the output path; no tracked file was modified and no git write was made.

MEASURED:
- "Every refusal today's seven flags produce is byte-identical and still fires from its own named arm, in the current order — the collision suite at `cmd/open_search_test.go:337-401` passes with no edit." [11-1] — `go test ./cmd -run 'TestValidateOpenArgs|TestValidateSearchFormCollisions|TestOpenCommand_SearchForm'` → ok, 0.609s; every `TestValidateOpenArgs_*` subtest including `KeepsTodaysRefusalPrecedence` and `AdmitsEveryNonSearchLine`, plus `TestValidateSearchFormCollisions_RefusesAFlagNoNamedArmCovers` and `_AdmitsASearchFormThatSetNoFlag`, PASS — holds
- "`go test ./cmd -run TestCompleteOpenPositional` passes." [11-5] — ran exactly that (together with `TestCompleteSearchTerm|TestCompleteSessionNames|TestCompleteOpenPositional`) → ok, 0.525s, all subtests PASS — holds
- "The four existing subtests are unedited and still pass: `-- ls /po` matches …, the two no-separator lines still offer `/portal-a1b2`, and the probe-parse subtest still pins the `<=`." [11-5] — same run; measured against 12-6's superseding contract as the criterion's own note directs: `TestCompleteOpenPositionalSeparatorBound/it_offers_no_sigil_completion_for_a_word_among_a_trailing_command's_arguments`, `…/it_leaves_filename_completion_on_for_a_path_argument_to_the_trailing_command`, `…/it_still_offers_sigil_completions_for_a_pre-dash_word_on_a_line_with_no_separator`, `…/it_still_offers_sigil_completions_for_a_pre-dash_word_beside_another_target` and `…/it_reads_a_line_with_no_separator_as_pre-dash_despite_cobra's_probe_parse` all PASS — holds
- "`gofmt -l` reports nothing and `go vet ./...` is clean" [6-3] — `gofmt -l .` → no output; `go vet ./...` → no output, exit 0 — holds

NOT MEASURED:
- "`go test ./...` passes" / "`go test ./...` is green" [1-1], [1-2], [1-3], [1-4], [1-6], [2-1], [2-3], [3-1], [3-2], [4-1], [4-6], [4-7], [4-8], [5-1], [5-3], [12-1], [12-4] — a whole-suite sweep, which a change-set verifier may not run; it is the suite run the orchestrator takes over the corrected tree. The toolchain halves that *are* measurable are clean across the whole module (see MEASURED and COVERAGE): `go build ./...`, `go vet ./...`, `gofmt -l .` and `golangci-lint run ./...` all report nothing.
- "`go test ./...` passes and `go test -tags integration -p 1 ./...` passes" [1-5], [1-7], [2-2], [2-4], [3-4], [4-2], [4-3], [4-4], [5-4], [6-1 second criterion], [6-2 second criterion] — same whole-suite bar, plus the integration lane, which this machine's safety invariant forbids me outright (real tmux server, real daemons, built binaries).
- "`go test -tags integration -p 1 ./...` passes, and `go test ./...` is unaffected"; and the eight live-cold-boot observations under it ("The decision closure is invoked exactly once…", "All ten real step events…", "`state.IsRestoringSet(client)` is false…", "The closure's own live read returns the restored sessions", "The model is on `PageLoading` after every step event", "On the single-match term the model quits with `Selected()` set…", "On the shared-fragment term … transitions to `PageSessions`", "`FatalError()` is nil on both runs…") [4-5] — integration lane only; forbidden here.
- "`go test ./internal/tui/... ./cmd/...` passes with no edit to any test file" [10-1], [10-3] — the no-edit halves are settled by the recorded diffstats; the passing halves are two whole-package runs I have no suspected defect to justify. Their subject is §7/§10's, not §11's.
- "`go test ./internal/tui/...` passes with no test changed" [11-4] — same; subject is the picker's, not §11's.
- "`go build ./...`, `go test ./...` and `golangci-lint run` are clean, and no test's semantics change beyond the three respelled calls" [11-2] — the build and lint halves hold as measured module-wide; the `go test ./...` half is the reserved suite run. The criterion's integration-lane addendum (`go test -tags integration -p 1 ./cmd -run TestConcurrent`) is forbidden here.
- "`go test ./...` passes; `go vet ./...`, `gofmt -l .` and `golangci-lint run ./...` are clean" [7-1] — the three toolchain halves hold as measured; the suite half is the reserved run.
- "`go build ./... && go test ./...` green; `golangci-lint run` clean" [8-1] — build and lint halves hold as measured; the suite half is reserved.
- "`go test ./...` is green and `golangci-lint run` reports nothing new" [12-5], [12-6] — lint half holds (`golangci-lint run ./...` → "0 issues."); suite half reserved.
- "`go test -race ./internal/tui -run TestContainmentFilterIsRaceFreeAgainstAConcurrentSet`"; "`internal/tui/search_containment_test.go` and `go test ./...` stay green" [12-2]; "`go test -race ./internal/tui` is green, including a test driving `set` concurrently with a filter pass" [6-3] — a race-detector run over a whole package; its subject is the containment filter (the picker's search section), not §11's.
- "A model with no decision closure dismisses exactly as it does today, and `go test ./...` is green" [12-3] — reserved suite run plus an integration-lane cold-boot suite; subject is the search decision's section.
- "`go test ./cmd` is green, including the verbatim shim expectation in `cmd/init_test.go`"; and "A measurement contradicting the derivation closes the task with `cmd/init.go` unedited…" [5-5] — the second needs a bash with the `bash-completion` package installed, which this machine does not have; both belong to §8's completion subject.
- "In fish, completion after the function asks for `open`'s completions and offers live session names"; "`go test ./...` passes and `go test -tags integration -p 1 ./...` passes" [5-2] — fish is not installed on this machine, so the live fish legs skip; subject is §8's.
- "`go test ./internal/tui ./cmd`" halves of [7-2], [6-1 first criterion], [6-2 first criterion], [3-3]'s three criteria, [3-5]'s two criteria — whole-package runs whose subjects are the picker, the search column and the capture fixture; no suspected defect in §11's territory justifies them here.
- "`go run ./cmd/capturetool --fixture sessions-search-results` … renders `~/code/portal` beside `portal-a1b2` …" [10-5]; "The human visual check is recorded before sign-off…" [3-4] — a rendered-capture observation and a gate record; subject is the visual section's.
- "A warm single-match attach writes every accumulated warning line to stderr, in order, before `openSessionFunc` is called" [4-3] — the end-to-end half needs a live tmux server with `_portal-saver` killed; forbidden here. Subject is §7's warnings.
- [10-2], [5-6], [8-2] record "None" — no criterion to settle.

FINDINGS:
- None

COVERAGE:
- The sibling's target-resolution section opens with a numbered search-sigil pre-check naming this specification as the form's owner, exactly as §11:468 claims — `.workflows/cli-verb-surface-redesign/specification/cli-verb-surface-redesign/specification.md:51` — measured: `grep -n 'open-with-forced-filter' <sibling spec>` → two hits, :51 (step 1 of the three-step precedence list, ahead of the glob pre-check at :52 and the chain at :53) and :478
- The sibling's `## Corrigenda` section carries exactly one dated entry naming this work unit, as §11:468 claims — same file:464 (heading), :478 (`Corrigendum 2026-09-11 (from open-with-forced-filter)`) — measured: `awk '/^## Corrigenda/,0' <sibling spec> | grep -c '^> \*\*Corrigendum'` → 7 entries total, the other 6 from `theming-system`; the grep above pins this unit's as the only one naming it
- §11's stated grep result is reproducible verbatim — sibling spec:51, :478 — measured: `grep -n 'open-with-forced-filter' .workflows/cli-verb-surface-redesign/specification/cli-verb-surface-redesign/specification.md` → the pre-check step and the corrigendum entry, and nothing else
- "No further edit to that document is owed, and applying one again would duplicate it" — the change-set makes none — measured: `git diff --name-only 7068abc9a559abb027ad61c6f0ab732c67a0688e^..HEAD -- .workflows/cli-verb-surface-redesign/` → empty, and `git log --oneline -1 -- <sibling spec>` → `e6f388d88` dated 2026-09-11, confirmed an ancestor of the range's parent by `git merge-base --is-ancestor` → the correction predates the change-set and was not re-applied inside it
- The narrowing the sibling's step 1 states — `/tmp` is a sigil, `/tmp/` and `/Users/leeovery/Code/portal` remain path targets — is what the code implements — `internal/resolver/path.go:25-27` (`strings.HasPrefix(arg, "/") && strings.Count(arg, "/") == 1`) — run: `go test ./cmd -run TestCompleteOpenPositional` → the subtests `absolute path with a second segment`, `single segment with a trailing slash`, `tilde path`, `dot-relative path` and `bare word` all PASS on the non-sigil arm
- "A single-segment leading-`/` argument … never enters [the chain] at all" (§11:466) — `cmd/open.go:178-180` places the sigil arm ahead of the `-f` arm (:184), the multi-target burst gate (:194-198), the pin loop (:200-204) and the chain call (:218-225); independently, `internal/resolver/path.go:10-18` makes `IsPathArgument` refuse a sigil, and that is the chain's only path gate (`internal/resolver/query.go:105`) — read: a sigil reaching the chain by any route would fall through path to alias, zoxide and a miss rather than minting
- The precedence chain the sibling describes is untouched by this work, so Axiom 2, the bare-project-shorthand consequence and the rejection of project-prefix session matching are ratified in code as well as in prose (§11:472) — `internal/resolver/query.go:100-123` — measured: `git diff --name-only 7068abc9a…^..HEAD -- internal/resolver/query.go` → 0 files; `Resolve`'s four arms are exact-session → path → alias → zoxide → `MissResult`, unchanged
- The `-p` escape the sibling's step 1 names ("minting at a single-segment absolute directory") works — `internal/resolver/query.go:217-223` stats the literal path with no sigil test, and `searchFormPositionals` (`cmd/open_search.go:17-24`) scans positionals only, so `-p /tmp`'s flag value is never read as a search form — read
- Pinned-domain contract not breached: no pin path can reach the picker — `cmd/open.go:200-204` → `resolvePinAndOpen` (:258-269) → `openResolved` (:276-289), whose only exits are `openSessionFunc`, `openPathFunc` and an error — measured by enumeration: `grep -rn "openTUIFunc(" cmd --include="*.go" | grep -v _test.go` → exactly five production call sites, `cmd/open.go:186` (the `-f` arm), `cmd/open.go:207` (the no-target arm) and `cmd/open_search.go:223/231/248` (the sigil arm); none on a pin path
- "It has no unresolvable case to fall back from — a term matching nothing is a filter result that opens the picker on an empty list" (§11:474) — `cmd/open_search.go:221-248`: `searchDecision` returns `("", nil)` for any count other than one, and both the zero-match and the 2+-match routes land on `openTUIFunc` — run: `go test ./cmd -run TestOpenCommand_SearchForm` → `OpensPickerWhenNothingMatches`, `OpensPickerWhenTwoOrMoreMatch` and `OpensPickerWhenNoSessionsAreLive` PASS
- The sibling's "Session set — user-visible only" invariant (:61) survives the new search enumeration — `internal/tmux/tmux.go:188-197`: the `_`-prefix filter lives inside `parseSessionList`, which both `ListSessions` (:130-137) and the change-set's new `ListSessionsProbe` (:143-150) return through, so `_portal-saver` / `_portal-bootstrap` are unreachable by a `/term` search exactly as by a bare positional — read
- "`-f` as a non-composing flag … ratified rather than changed" (§11:472) — `cmd/open.go:235-246` still refuses `-f` beside a positional target or any of the four pins, and beside an empty value — run: `go test ./cmd -run TestValidateOpenArgs` → `RefusesFilterBesideEachDomainPinBeforeBootstrap` (`-s`, `-p`, `-a`, `-z`) and `RefusesAMalformedFilterLineBeforeBootstrap` (empty value, beside a positional target) PASS; the change-set moved these refusals from `RunE` to `Args` without altering the rule
- "The tab-completion principle … ratified" (§11:472) — every slot in the sibling's completion table (:230-236) is still served as written: `openCmd.ValidArgsFunction = completeOpenPositional` (`cmd/open.go:785`), `-s` → `completeSessionNames` (:786-788), `-a` → `completeAliasKeys` (:789-791), `-p` and `-z` left unregistered so the shell answers them — read; and `completeOpenPositional` (`cmd/completion.go:104-112`) keeps Portal-owned namespaces on the Portal arm and hands a post-separator word back to the shell with `ShellCompDirectiveDefault`
- The sibling's verbatim `x() { portal open "$@" }` body (:37) survives the `portal init` rewiring — `cmd/init.go:85` (bash), `:118` (fish), `:151` (zsh) — read: the literal was replaced by the `openFunctionExpansion = "portal open"` constant (`cmd/init.go:54`) with identical emitted text; only the completion *registration* changed, which the sibling's table does not quote
- The sibling's "a spawned window or script must never pop a TUI" (:116, :254) holds against the new form — `cmd/open_search.go:94-96` refuses a `/term` search beside `--ack`, and `cmd/open.go:169-173` parses `--ack` ahead of every branch — read: a spawned `portal open --session <name> --ack <batch>:<token>` line carries no positional at all, so `searchFormPositionals` returns empty and the pin arm runs
- Module toolchain clean over the whole delivered tree — repository root — measured: `gofmt -l .` → no output; `go build ./...` → exit 0, no output; `go vet ./...` → exit 0, no output; `golangci-lint run ./...` → "0 issues."
