SECTION: 1. Purpose and Scope / 2. The Sigil Form / 3. Resolution / 4. Match Domain and Matching Rule

SCOPE: Commit range `7068abc9a559abb027ad61c6f0ab732c67a0688e^..HEAD` (235 commits), filtered to the plan topic's file list. Read in final state: `internal/resolver/search.go`, `internal/resolver/path.go`, `cmd/open_search.go`, `cmd/open.go` (Args validator + RunE dispatch + `openTUI`/`processTUIResult`/`finishTUI`), `cmd/root.go` (`isTUIPath`/`shouldRunConcurrentBootstrap`), `internal/tmux/tmux.go` (`listSessionsArgs`/`ListSessions`/`ListSessionsProbe`/`parseSessionList`), `internal/tui/search_filter.go`, `internal/tui/search_decision.go`, `internal/tui/picker_sessions.go`, `internal/tui/session_item.go`, `internal/tui/session_dir_column.go`, `internal/tui/grouping.go`, `internal/tui/build.go`, and the relevant arms of `internal/tui/model.go` (`New`, `rebuildSessionList`, `resolveDerivedDirs`, `evaluateDefaultPage`, `applySearchLanding`, `dismissLoadingGate`, the `SessionsMsg`/`LoadingMinElapsedMsg`/`searchDecisionMsg`/`BootstrapCompleteMsg`/`BootstrapFatalMsg` arms, the sessions-page key guard). Followed the behaviour out of the file list into `charm.land/bubbles/v2@v2.1.0/list` (`SetFilterText`, `SetFilterState`, `SetItems`, `filterItems`, `SettingFilter`, the `FilterMatchesMsg` arm) because the delivered filter and landing are defined by what that library does with them. Read the corrigenda in full; the 2026-09-16 pair (collision withholding, cumulative record) governs §3.2 and §4.4 and was the text this section was held against. Test files read for coverage, not for reuse: `internal/tui/search_filter_test.go`, `internal/tui/search_containment_test.go`, `internal/tmux/list_sessions_probe_test.go`, `cmd/open_search_test.go`, `internal/tui/session_item_test.go`.
Run: `gofmt -l .`, `go vet ./...`, `golangci-lint run ./...`, `go build ./...`, and four targeted single-package test runs over this section's own subject (listed under MEASURED/COVERAGE). No whole-suite sweep. No tracked file modified; `git status --porcelain` after the pass shows only the two pre-existing workflow manifests and other verifiers' report files.

MEASURED:
- "`gofmt -l` reports nothing and `go vet ./...` is clean" [6-3] — `gofmt -l .` → no output; `go vet ./...` → no output, exit 0 — holds
- "`TestMatchesSearchTerm`, `TestMatchesSearchTermUnresolvableHome` and the existing `FilterValue` subtests pass with no edit" [6-2] — `go test -count=1 ./internal/resolver` → `ok ... 0.262s` (both named tests live there) and `go test -count=1 ./internal/tui -run 'TestSessionItem|…'` → `ok ... 0.718s` (the `FilterValue` subtests are `TestSessionItem`'s, `internal/tui/session_item_test.go:15-40`); the "no edit" half confirmed against the commit range — holds
- "`go test -race ./internal/tui` is green, including a test driving `set` concurrently with a filter pass" [6-3] — `go test -race -count=1 ./internal/tui -run TestContainmentFilter -v` → 17 PASS, 0 FAIL, no race report; `TestContainmentFilterIsRaceFreeAgainstAConcurrentSet` (`internal/tui/search_filter_test.go:168`) is the named test and is among them. Scope note: the `-run` filter narrows to the containment family; the package-wide race run was not taken — holds for the named test
- "`TestContainmentFilterFallsBackWhenTheSourceIsOutOfStep` and the `TestSearchContainment*` suites pass with no edit" [6-3] — `go test -count=1 ./internal/tui -run 'TestSearchContainment|…'` → `ok`; the named fall-through test no longer exists (removed by task 12-2, as the criterion's own note records), and the seven `TestSearchContainment*` functions in `internal/tui/search_containment_test.go` are green — holds
- "The `sync.RWMutex` and the single accessor stay, and the concurrent-`set` test is green under `-race`" [12-2] — read: `internal/tui/search_filter.go:34` declares the mutex and `:65-69` is the single `current()` accessor; `go test -race -count=1 ./internal/tui -run TestContainmentFilter` → PASS with no race report — holds
- "`go test -race ./internal/tui -run TestContainmentFilterIsRaceFreeAgainstAConcurrentSet` is green" [13-1] — run as part of `go test -race -count=1 ./internal/tui -run TestContainmentFilter -v` → `--- PASS: TestContainmentFilterIsRaceFreeAgainstAConcurrentSet (0.03s)` — holds
- "the six existing `TestContainmentFilter*` pins unedited" and "`gofmt -l .`, `go vet ./...` and `golangci-lint run` are clean" [14-1] — `go test -race -count=1 ./internal/tui -run TestContainmentFilter -v` shows the pre-14-1 pins green beside the two new generation tests; `gofmt -l .` → no output, `go vet ./...` → clean, `golangci-lint run ./...` → `0 issues.` — holds
- "Behaviour is unchanged on both routes: `TestOpenCommand_SearchForm_ExcludesTheCurrentSessionFromTheCount`, `TestOpenCommand_SearchForm_AttachesTheOtherMatchWhenTheCurrentSessionAlsoMatches`, `TestOpenCommand_SearchForm_CountsNothingOutWhenTheCurrentSessionReadFails`, `TestOpenCommand_SearchForm_ExcludesNothingOutsideTmux` and `internal/tui`'s `\"excludes the current session when inside tmux\"` all pass with no edit to any of them" [6-1] — `go test -count=1 ./cmd -run 'TestOpenCommand_SearchForm|…'` → `ok ... 0.453s` (the four cmd tests) and `go test -count=1 ./internal/tui -run 'TestRebuildSessionList|…'` → `ok` (the fifth is `TestRebuildSessionList`'s subtest at `internal/tui/rebuild_session_list_test.go:202`) — holds
- "The existing search-count suites pass unchanged, including `TestOpenCommand_SearchForm_ExcludesNothingOutsideTmux`'s assertion that `CurrentSessionName` is read zero times outside tmux" [8-1] — `go test -count=1 ./cmd -run 'TestOpenCommand_SearchForm|TestSearchCandidates|TestCurrentPickerSession|…'` → `ok ... 0.453s` — holds
- "`go build ./...`" [11-2] / "`go build ./...`" [8-1] — `go build ./...` → exit 0, no output — holds
- "`golangci-lint run` is clean" [11-2] / "`golangci-lint run` reports nothing new" [12-5, 12-6] / "`golangci-lint run ./...` is clean" [7-1] — `golangci-lint run ./...` → `0 issues.`, exit 0. Per CLAUDE.md this invocation carries the `integration` build tag and uncapped issue limits, so it type-checks and lints the integration-tagged files too — holds
- "Every refusal today's seven flags produce is byte-identical and still fires from its own named arm, in the current order — the collision suite at `cmd/open_search_test.go:337-401` passes with no edit" [11-1] — subject belongs to §5 Argv Composition, but measured here since it rides the same run: `go test -count=1 ./cmd -run 'TestValidateOpenArgs|TestValidateSearchFormCollisions|TestOpenCommand_SearchForm|TestSearchFormPositionals'` → `ok ... 0.453s` — holds

NOT MEASURED:
- "`go test ./...` passes" [1-1] — the project's unit lane is a whole-suite sweep, which this pass may not run; it is reserved for the executing pass over the corrected tree. Its subject (the three-argument grouping builders, §4.1's recorded-vs-derived separation) was settled by reading and by the targeted `internal/tui` run.
- "`go test ./...` passes" [1-2] — whole-suite sweep, reserved for the executing pass. Its subject (the `SessionItem.FilterValue()` widening, §4.1/§4.2/§4.3) is covered by the measured `TestSessionItem` and `TestSearchContainment*` runs, but the tree-wide effect on other pickers' suites needs the lane.
- "`go test ./...` passes" [1-3] — whole-suite sweep, reserved for the executing pass.
- "`go test ./...` passes" [1-4] — whole-suite sweep, reserved for the executing pass.
- "`go test ./...` passes and `go test -tags integration -p 1 ./...` passes" [1-5] — both lanes are whole-suite sweeps; the integration lane additionally needs `-p 1`, a real tmux server and built binaries, all outside this pass.
- "`go test ./...` passes" [1-6] — whole-suite sweep, reserved for the executing pass.
- "`go test ./...` passes and `go test -tags integration -p 1 ./...` passes" [1-7] — both lanes are whole-suite sweeps, reserved for the executing pass.
- "`go test ./...` passes" [2-1] — whole-suite sweep, reserved for the executing pass.
- "`go test ./...` passes and `go test -tags integration -p 1 ./...` passes" [2-2] — both lanes are whole-suite sweeps, reserved for the executing pass.
- "`go test ./...` passes" [2-3] — whole-suite sweep, reserved for the executing pass. The `-race` clause the criterion's note attaches to it was measured and holds (see MEASURED, [13-1]).
- "`go test ./...` passes and `go test -tags integration -p 1 ./...` passes" [2-4] — both lanes are whole-suite sweeps, reserved for the executing pass.
- "`go test ./...` passes" [3-1] — §6 (the search-opened row's directory column) is its owning section.
- "`go test ./...` passes" [3-2] — §6 (row geometry / directory column) is its owning section.
- "`go test ./...` passes" [3-3] — §6 (directory column) is its owning section.
- "A `-f` picker, a no-argument picker and a command-pending picker render their session rows byte-identically to today (existing suites green unmodified)" [3-3] — §6 (directory column scope) is its owning section.
- "The column survives an `s` regroup, a `Space` preview and back, a `SessionsMsg` refresh, a marked-set mutation and a live theme swap" and "The column survives a hand edit of the committed filter text, including clearing it" [3-3] — §6 (directory column) is its owning section.
- "`go test ./...` passes and `go test -tags integration -p 1 ./...` passes" [3-4] — §6 is its owning section, and both lanes are whole-suite sweeps.
- "The human visual check is recorded before sign-off, with the still capture and the live-view command presented" [3-4] — §6 is its owning section; a gate record is not observable from the tree.
- "With the sessions-page key guard temporarily widened to swallow `s` under an applied filter, both subtests fail; with it reverted, both pass." [3-5] — §6 is its owning section, and the experiment requires editing a tracked file (`internal/tui/model.go`), which this pass may not do. Read-only corroboration: the guard at `internal/tui/model.go:2547` is `if m.sessionList.SettingFilter() { break }`, and `bubbles/v2@v2.1.0/list.Model.SettingFilter()` is `filterState == Filtering` alone, so a `FilterApplied` sigil landing does reach the `s` arm.
- "`go test ./...` is green." [3-5] — §6 is its owning section; whole-suite sweep.
- "`go test ./...` passes" [4-1] — §7 (cold-start classification) is its owning section; whole-suite sweep.
- "`go test ./...` passes and `go test -tags integration -p 1 ./...` passes" [4-2] — §7 is its owning section; both lanes are whole-suite sweeps.
- "`go test ./...` passes and `go test -tags integration -p 1 ./...` passes" [4-3] — §7 is its owning section; both lanes are whole-suite sweeps.
- "A warm single-match attach writes every accumulated warning line to stderr, in order, before `openSessionFunc` is called" [4-3] — §7 (warning surfacing) is its owning section; the end-to-end half needs a live tmux server with `_portal-saver` down. Read-only corroboration within my section: `cmd/open_search.go:245-246` emits to `cmd.ErrOrStderr()` immediately before `openSessionFunc`.
- "`go test ./...` passes and `go test -tags integration -p 1 ./...` passes" [4-4] — §7 is its owning section; both lanes are whole-suite sweeps.
- "The warm path, every CLI path and the concurrent route's step sequence and labels are unchanged (existing suites green unmodified)" [4-4] — §7 is its owning section; the "green" half needs the lane.
- "On a cold server `open /port` … the same verdict `open -f port` gets on the same boot" [4-4] — §7 is its owning section; settling it needs the integration-tagged cold-boot suite.
- "`go test -tags integration -p 1 ./...` passes, and `go test ./...` is unaffected" [4-5] — both lanes are whole-suite sweeps; the integration lane needs a real tmux server.
- "The decision closure is invoked exactly once across the whole boot"; "All ten real step events, and the terminal complete event, were delivered before it ran"; "`state.IsRestoringSet(client)` is false at the instant it runs"; "The closure's own live read returns the restored sessions"; "The model is on `PageLoading` after every step event"; "On the single-match term the model quits with `Selected()` set, `SearchAttached()` true, and `ActivePage()` still `PageLoading`"; "On the shared-fragment term … the model transitions to `PageSessions`"; "`FatalError()` is nil on both runs and no `BootstrapFatalMsg` is observed" [4-5] — these are §3.4's subject but are observations of a live cold boot against real tmux; settling them needs `go test -tags integration -p 1 ./cmd -run TestConcurrentColdBoot_SearchDecision`, which this pass may not run (integration lane, real server, built binaries). Read-only corroboration: `internal/tui/model.go:1509-1520` dispatches the closure only once both `minElapsed` and `bootstrapComplete` hold, nils `m.searchDecide` in `applySearchDecision` (`internal/tui/search_decision.go:52`), and the `fatalActive` guards at `model.go:1648` and `:1660` deny it after a fatal.
- "`go test ./...` is green." [4-6] — §7 is its owning section; whole-suite sweep.
- "`go test ./...` is green." [4-7] — §7 is its owning section; whole-suite sweep.
- "`go test ./...` is green." [4-8] — §7 is its owning section; whole-suite sweep.
- "`go test ./...` passes" [5-1] — §8/§9 (completion and documentation) is its owning section; whole-suite sweep.
- "In fish, completion after the function asks for `open`'s completions and offers live session names" [5-2] — §8 is its owning section; fish is not installed on this machine (`command -v fish` resolves nothing), so the live leg is unobservable here.
- "`go test ./...` passes and `go test -tags integration -p 1 ./...` passes" [5-2] — §8 is its owning section; both lanes are whole-suite sweeps.
- "`go test ./...` passes" [5-3] — §9 (help text) is its owning section; whole-suite sweep.
- "`go test ./...` passes and `go test -tags integration -p 1 ./...` passes" [5-4] — §9 (README) is its owning section; both lanes are whole-suite sweeps.
- "A measurement contradicting the derivation closes the task with `cmd/init.go` unedited and that recorded; no further criterion applies in that case." [5-5] — §8 is its owning section; settling it needs a bash carrying the `bash-completion` package, which is absent on this machine.
- "`go test ./cmd` is green, including the verbatim shim expectation in `cmd/init_test.go`." [5-5] — §8 is its owning section; the four shell-driven cases spawn real `bash`/`zsh`/`fish` subprocesses and the criterion names the whole `cmd` package.
- "`go test ./...` is green, `gofmt -l` reports nothing and `go vet ./...` is clean" [6-1] — its `go test ./...` clause is a whole-suite sweep reserved for the executing pass; its `gofmt -l .` and `go vet ./...` clauses were run and are clean.
- "`go test ./...` is green, `gofmt -l` reports nothing and `go vet ./...` is clean" [6-2] — same: the suite clause is a whole-suite sweep; `gofmt -l .` and `go vet ./...` were run and are clean.
- "`go test ./...` passes; `go vet ./...`, `gofmt -l .` and `golangci-lint run ./...` are clean" [7-1] — §7 is its owning section for the suite clause; the three linter clauses were run here and are clean (`go vet` no output, `gofmt -l .` no output, `golangci-lint run ./...` → `0 issues.`).
- "The loading-gated routes are untouched: the existing `internal/tui` and `cmd` suites pass unchanged, with no new call reaching a real tmux server." [7-2] — §7 is its owning section; the "suites pass" half names two whole packages.
- "`go build ./... && go test ./...` green; `golangci-lint run` clean" [8-1] — its `go build ./...` and `golangci-lint run` clauses were measured clean; the `go test ./...` clause is a whole-suite sweep reserved for the executing pass.
- "`go test ./internal/tui/... ./cmd/...` passes with no edit to any test file" [10-1] — two whole packages; reserved for the executing pass.
- "`go test ./internal/tui/... ./cmd/...` passes with no edit to any test file" [10-3] — two whole packages; reserved for the executing pass.
- "`go run ./cmd/capturetool --fixture sessions-search-results` on a machine whose home is not `/home/user` renders `~/code/portal` beside `portal-a1b2` …" [10-5] — §6 (row display / capture fixture) is its owning section.
- "`go build ./...`, `go test ./...` and `golangci-lint run` are clean, and no test's semantics change beyond the three respelled calls." [11-2] — `go build ./...` and `golangci-lint run ./...` were measured clean; the `go test ./...` clause is a whole-suite sweep, and the integration-lane half the criterion's note names needs a real tmux server.
- "`go test ./internal/tui/...` passes with no test changed" [11-4] — a whole package; reserved for the executing pass. Scope note: the containment, landing, decision, session-item and rebuild families within it were run here and are green.
- "`go test ./cmd -run TestCompleteOpenPositional` passes." [11-5] — §8 (tab completion) is its owning section.
- "The four existing subtests are unedited and still pass: `-- ls /po` matches `completeSessionNames(\"/po\")` in both candidates and directive, the two no-separator lines still offer `/portal-a1b2`, and the probe-parse subtest still pins the `<=`." [11-5] — §8 is its owning section, and the criterion's own note records that task 12-6 superseded the first clause.
- "The `commandPending` branch, `WarningsOwedAtTeardown`'s own rule and the drop of a complete arriving after dismissal are unchanged, and `go test ./...` is green." [12-1] — §7 is its owning section; whole-suite sweep.
- "`internal/tui/search_containment_test.go` and `go test ./...` stay green." [12-2] — the containment suite was run here and is green (`go test -count=1 ./internal/tui -run 'TestSearchContainment|…'` → `ok`); the `go test ./...` clause is a whole-suite sweep reserved for the executing pass.
- "A model with no decision closure dismisses exactly as it does today, and `go test ./...` is green." [12-3] — §7 is its owning section for the cold-boot half; whole-suite sweep, and the two cold-boot suites need the integration lane and a real tmux server.
- "`completingPreDashPositional`, `validateSearchFormCollisions`'s seventh derived arm and the plain session-name completion at a second positional are untouched, and `go test ./...` is green." [12-4] — §8 is its owning section; whole-suite sweep.
- "`go test ./...` is green and `golangci-lint run` reports nothing new." [12-5] — §7 is its owning section for the suite clause; the lint clause was measured clean (`0 issues.`).
- "Every `TestWarningsOwedAtTeardown` subtest other than the cancel one passes with no edit." [12-5] — §7 (warning surfacing) is its owning section.
- "`go test ./...` is green and `golangci-lint run` reports nothing new." [12-6] — §8 is its owning section for the suite clause; the lint clause was measured clean (`0 issues.`).
- "`go test ./internal/tui` is green … and `go test ./...` is green." [13-1] — both are whole-package/whole-suite sweeps; the targeted containment run within `internal/tui` was measured and holds (see MEASURED).
- "`go test ./internal/tui` is green … `go test ./...` is green" [14-1] — both are whole-package/whole-suite sweeps; the `TestContainmentFilter*` half, `gofmt -l .`, `go vet ./...` and `golangci-lint run` were measured and hold (see MEASURED).

FINDINGS:
- None

COVERAGE:
- The recognition rule is a leading `/` with no further `/`, tested on shape alone and never against the filesystem (§2.2, §2.4) — internal/resolver/path.go:25-27 — read: `strings.HasPrefix(arg, "/") && strings.Count(arg, "/") == 1`, no `os.Stat`; measured: `go test -count=1 ./internal/resolver` → `ok` (covers `TestIsSearchSigil`, whose table carries `/port`, `/`, `/tmp/`, `/Users/...`, `./port`, `~/port`, `port`)
- A sigil is excluded from the path domain, and every other path argument reads exactly as before (§2.2 table) — internal/resolver/path.go:10-18 — read: `IsPathArgument` short-circuits on `IsSearchSigil` and is otherwise unchanged; it has exactly two references in non-test source (its own declaration and `internal/resolver/query.go:105`), so the narrowing reaches only the bare-positional chain
- `-p /tmp` still mints, so §2.4's escape hatch is intact — internal/resolver/query.go:217-223 — read: `ResolvePathPin` calls `ResolvePath` directly and never consults `IsPathArgument`
- Recognition is positional-independent and stops at `--`, so a `/word` in a trailing command's payload is that command's argument (§2.3) — cmd/open_search.go:17-34 — read: `searchFormPositionals` scans every pre-dash positional in argv order via `preDashPositionals`, which slices `args[:cmd.ArgsLenAtDash()]`; measured: `go test -count=1 ./cmd -run 'TestSearchFormPositionals|…'` → `ok` (covers `TestSearchFormPositionals_StopsAtDashSeparator`)
- The sigil never enters the resolution chain and cannot mint (§3.1) — cmd/open.go:178-179 — read: the search arm precedes the `-f` arm, the multi-target burst gate, the domain-pin dispatch and `qr.Resolve`, and returns unconditionally
- A term carrying glob metacharacters is literal text rather than a burst target (§4.3) — cmd/open.go:175-179 — read: the arm is placed ahead of `isMultiTarget(ordered)`; measured: `go test -count=1 ./cmd -run TestOpenCommand_SearchForm` → `ok` (covers `TestOpenCommand_SearchForm_GlobMetacharactersAreLiteralText`)
- The bare slash opens a focused, empty filter rather than minting at root or erroring (§2.5, §3.2) — cmd/open_search.go:222-223 and internal/tui/model.go:1320-1334 — read: a term-less form builds `&tui.SearchForm{}` with no `Decide`, and `applySearchLanding` selects `list.Filtering` for an empty term; `bubbles` `filterItems` short-circuits an empty filter value to the whole item set, so the list shows every session
- K is taken over the set the picker lists: Portal's `_`-prefixed internal sessions are absent, and the attached session is dropped (§3.2 and the 2026-09-14 corrigendum) — internal/tmux/tmux.go:188-195, cmd/open_search.go:164-183, internal/tui/picker_sessions.go:13-23 — read: `parseSessionList` filters `_`-prefixed names for both `ListSessions` and `ListSessionsProbe`; `searchCandidates` composes that with `PickerSessions(…, currentPickerSession(src))`; measured: `go test -count=1 ./internal/tmux -run TestListSessionsProbe` → `ok` (pins the `_portal-saver`/`_portal-bootstrap` filter for both readers and their identical argv) and `go test -count=1 ./cmd -run 'TestOpenCommand_SearchForm|TestSearchCandidates|TestCurrentPickerSession'` → `ok`
- The count and the picker's own list agree on the current-session exclusion — cmd/open.go:732-734 and internal/tui/model.go:1140-1145 — read: `openTUI` sets `cfg.insideTmux`/`cfg.currentSession` from the same `currentPickerSession(client)` helper the count uses, and `filteredSessions()` applies `PickerSessions` with it, so a failed `CurrentSessionName` read holds nothing back on either side
- K = 1 attaches through the connector the invocation already selects, with no third connection mode (§3.7) — cmd/open_search.go:246 → cmd/open.go:112-114, and cmd/open.go:602-616 — read: the warm route calls `openSessionFunc` → `buildSessionConnector(tmuxClient(cmd)).Connect(name)`, the same call the ordinary session-resolution route makes at cmd/open.go:285; the cold route sets `m.selected` and quits, and `processTUIResult` hands the same connector the same name
- A failed session-list read is not a zero match: tmux's own words, non-zero exit, no picker, no attach, no in-TUI error frame (§3.7) — cmd/open_search.go:234-240, internal/tui/search_decision.go:54-57, cmd/open.go:602-616 — read: `ListSessionsProbe`'s wrapped `*tmux.CommandError` is returned unchanged; on the cold route it is parked on `m.searchErr` and read out after `RestoreTerminalBackground` and the warnings write, checked *after* `FatalError()` so it takes no fatal frame; measured: `go test -count=1 ./cmd -run TestOpenCommand_SearchForm` → `ok` (covers `TestOpenCommand_SearchForm_ReturnsAnEnumerationError`, which also asserts the error is not a `*UsageError`), and `go test -count=1 ./internal/tmux -run TestListSessionsProbe` → `ok`
- The sigil emits no `resolve` component line (§3.6) — cmd/open.go:178-179 vs :222 and cmd/open_surfaces.go:37 — read: those two are the only non-test call sites of `emitResolveDecision` in the tree, and the search arm returns before either; measured: `go test -count=1 ./cmd -run TestOpenCommand_SearchForm` → `ok` (covers `TestOpenCommand_SearchForm_EmitsNoResolveLine`)
- On a cold or unlatched server the count waits for the whole bootstrap *and* the loading page's minimum span, and nothing the sigil decides fires earlier (§3.4) — cmd/open_search.go:229-232, internal/tui/model.go:1509-1520, :1641-1642, :1692-1695 — read: the deferred route hands `Decide` to the picker; `dismissLoadingGate` is reached only from the `LoadingMinElapsedMsg` and `BootstrapCompleteMsg` arms, each gated on the other flag, and `BootstrapCompleteMsg` is the orchestrator's terminal event after step 10, so the read cannot be taken mid-bootstrap
- A decision answering after a bootstrap fatal never dismisses the error frame, and a repeat gate message while one is in flight holds the page — internal/tui/model.go:1646-1652, :1509-1514 — read: the `searchDecisionMsg` arm returns early on `fatalActive`, and `searchDecideInFlight` absorbs the second gate message
- The committed-filter landing matches `-f`'s, with the cursor on the first surviving row (§3.2 K≥2, §3.3) — internal/tui/model.go:1320-1335 and bubbles/v2@v2.1.0/list/list.go:280-301 — read: `SetFilterText` runs the pass synchronously and `GoToStart()`s, `SetFilterState(FilterApplied)` `GoToStart()`s again, and `ensureSessionRowSelected` steps off a header row; `HeaderItem.FilterValue()` is `""` so no header survives an applied filter and index 0 is the first matching session row; measured: `go test -count=1 ./internal/tui -run 'TestSearchFormLanding|TestSearchFormPrecedenceOverInitialFilter'` → `ok`
- A search form lands on the Sessions page whatever the term matched, including zero matches and zero live sessions (§3.1, §3.2 K=0) — internal/tui/model.go:1303-1306 — read: `evaluateDefaultPage`'s `m.searchForm` arm precedes the "sessions exist?" arm, so an empty result does not divert to Projects; the K=0 frame renders the no-matches body (internal/tui/filtering_no_matches.go:20-29 requires exactly `FilterApplied` + non-empty value + zero visible items)
- The search matches name and recorded directory only, tested separately, case-folded, by containment, with the term literal (§4.1, §4.3) — internal/resolver/search.go:9-34 — read: `SearchFields` returns `[name]` or `[name, AbbreviateHome(dir)]`; `MatchesSearchTerm` loops the fields with `strings.Contains` over lower-cased text, never joins them, and returns false for an empty term; measured: `go test -count=1 ./internal/resolver` → `ok` (covers `TestSearchFields`, `TestMatchesSearchTerm`, `TestMatchesSearchTermUnresolvableHome`, `TestAbbreviateHome`)
- The searched form is the displayed form: a recorded directory under home is matched home-abbreviated (§4.1, §4.2) — internal/resolver/search.go:12 and internal/tui/session_item.go:88, internal/tui/session_dir_column.go:28 — read: the match fields, the fuzzy-route join and the row's directory cell all pass the raw stamp through the one `resolver.AbbreviateHome`
- A derived directory never reaches the match or the row (§4.1) — internal/tui/model.go:267-272, :1207-1229, internal/tui/grouping.go:16-21, internal/tui/session_item.go:88, internal/tui/search_filter.go:54 — read: `resolveDerivedDirs` writes only `m.derivedDirs`; `effectiveDir` is the sole reader and is called only from the two grouped builders; `FilterValue()` and `searchEntry` both take `Session.Dir`. Enumerated every `.Dir` reference in non-test `internal/tui` source — five sites, none of which assigns a derived value into `Session.Dir`
- The fuzzy routes join name, one space, and the abbreviated directory, with no trailing separator for a session carrying none (§4.3) — internal/tui/session_item.go:87-89 — read: `strings.Join(resolver.SearchFields(...), " ")` over a one- or two-element slice; measured: `go test -count=1 ./internal/tui -run TestSessionItem` → `ok` (pins `"dev"` and `"api-work ~/Code/portal"`)
- All three entry points narrow the same list through the same filter value, and only the *rule* diverges (§4.2, §4.4 table) — internal/tui/search_filter.go:114-120 — read: `installSearchFilter` re-points `sessionList.Filter` only when `m.searchForm && m.searchTerm != ""`, so `-f` and a hand-typed `/` keep `list.DefaultFilter`; measured: `go test -count=1 ./internal/tui -run TestSearchContainment` → `ok` (`TestSearchContainmentNarrowsTheList` asserts the sigil returns one row where `-f` returns two over the identical session set, and `TestSearchContainmentIsNotInstalledForATermLessForm` covers the bare slash)
- Containment narrows without reordering, and headers rank nothing (§4.4) — internal/tui/search_filter.go:85-99 and bubbles/v2@v2.1.0/list/list.go:828-830 — read: ranks are emitted in target-index order and `MatchedIndexes` is left nil; the library's `FilterMatchesMsg` arm assigns the slice verbatim with no re-sort; `HeaderItem`'s `""` value is never a key the source holds; measured: `go test -race -count=1 ./internal/tui -run TestContainmentFilter -v` → all PASS (covers `…PreservesListOrder` equivalents, `…RanksNothingForAHeaderRowsEmptyFilterValue`, `…NarrowsToTheTargetsItContains`)
- Containment stands while the committed text is character-identical to the term and falls back to the picker's own rule on any other value (§4.4) — internal/tui/search_filter.go:86-89 — read: `if query != term { return list.DefaultFilter(query, targets) }` — the test is the text, not the act; measured: `go test -race -count=1 ./internal/tui -run TestContainmentFilterReturnsToThePickersOwnRuleOnceTheTextIsNoLongerTheTerm` → PASS, and `go test -count=1 ./internal/tui -run TestSearchContainmentHoldsOnTheFilterText` → `ok`
- Every re-render of the narrowed list reproduces the containment set (§4.4) — internal/tui/model.go:1249-1254 and :1231 — read: `rebuildSessionList` is the single chokepoint (the only non-test `sessionList.SetItems` call in the package) and records the generation into the source *before* handing the items over, so the pass `SetItems` defers into a `tea.Cmd` can resolve targets it is given; every caller propagates the returned cmd (`applySessions`, `handleSwitchViewKey`, the projects-loaded arm), so the deferred pass always runs; measured: `go test -count=1 ./internal/tui -run 'TestSearchContainmentSurvivesReRenders|TestSearchContainmentAcrossGroupingModes'` → `ok`
- A colliding filter value withholds every row behind it, judged cumulatively over the picker run with nothing evicted (§4.4 and the 2026-09-16 corrigenda) — internal/tui/search_filter.go:40-61, :93, :103-110 — read: `set` clones the held map and slices and appends each distinct `searchEntry` rather than replacing, and `containmentFilter` ranks a target only when the source holds it and `everyEntryMatches`; measured: `go test -race -count=1 ./internal/tui -run TestContainmentFilter -v` → PASS for `…OnTwoSessionsSharingOneFilterValue` (both orders) and all four `…AcrossGenerationsOfOneCollidingFilterValue` subtests
- The source is safe across the Update goroutine and the goroutine Bubble Tea gives the filter command (§4.4's per-run record) — internal/tui/search_filter.go:33-36, :40-48, :65-69 — read: the mutex is held for the whole rebuild-and-swap, `set` never writes a published map or a published slice's backing array (`slices.Clone` per key), and the pointer field survives Bubble Tea's value copies of `Model`; measured: `go test -race -count=1 ./internal/tui -run TestContainmentFilterIsRaceFreeAgainstAConcurrentSet` → PASS, no race report
- An `s` regroup is reachable under the sigil's applied filter, so §4.4's "reproduces the containment set on a regroup" is not dead text — internal/tui/model.go:2545-2548 — read: the guard is `m.sessionList.SettingFilter()`, which `bubbles` defines as `filterState == Filtering` alone; the sigil lands in `FilterApplied`
- The whole tree compiles and lints clean, including the integration-tagged configuration — repository root — measured: `go build ./...` → exit 0; `go vet ./...` → clean; `gofmt -l .` → no output; `golangci-lint run ./...` → `0 issues.` (config sets the `integration` tag and uncaps both issue limits per CLAUDE.md)
- `internal/fuzzy` still has no production consumer, as §4.3 states — repository root — measured: `grep -rln 'internal/fuzzy' --include='*.go' .` → `internal/fuzzy/match_test.go` only
- No `x/term` shell function is emitted, so §2.1's ruled-out no-space form is genuinely not built — cmd/init.go:54 — read: the expansion is the literal `portal open`, with no per-term function generation anywhere in the file
