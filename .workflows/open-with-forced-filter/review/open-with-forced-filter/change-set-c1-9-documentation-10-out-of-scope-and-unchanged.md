SECTION: 9. Documentation + 10. Out of Scope and Unchanged (spec lines 388–463, plus the seven-entry `## Corrigenda` block at lines 482–496)

SCOPE: Commit range `7068abc9a^..HEAD` (58 task commits plus the phase-1 comment-correction and analysis commits). Non-`.workflows/` files in the range enumerated with `git diff --name-only` — 134 files, of which the section's subjects are `README.md`, `cmd/open.go`'s `Long`/flag registration, `cmd/open_docs_test.go`, `cmd/open_help_test.go`, `cmd/retired_surface_test.go`, and (for §10's negative claims) `internal/resolver/{path,search,query,glob}.go`, `internal/tui/{session_item,session_dir_column,search_filter,model,build}.go`, `cmd/{completion,open_search,init,root}.go`. Read: §9/§10 in full, the corrigenda in full, §2.2–2.4, §4.2, §4.4, §4.5 and §6.3 (referenced by §9.2/§10.5/§10.6), `CLAUDE.md`'s "Build & Test" and test-isolation sections before running anything. Run: `gofmt -l .`, `go build ./...`, `go vet ./...`, `golangci-lint run`, and two single-package unit-lane runs (`./cmd` and `./internal/resolver`), each `-run`-narrowed. No integration lane, no daemon, no tmux, no `pgrep`. Tree unmodified — `git status --porcelain` at the end matches the session-start snapshot exactly (the workflow manifest and the untracked review dir, nothing else); no build artefact produced (`go build ./...` writes none without `-o`).

MEASURED:
- "`go test ./...` passes" [5-3] — the criterion's subject in this section is the help-text guard task 5-3 added. Measured with `go test ./cmd -run 'TestOpenHelpMetadata|TestOpenFilterFlagUsage' -v` → all 7 subtests of `TestOpenHelpMetadata_DescribesSearchForm` PASS, `TestOpenFilterFlagUsage_StaysAOneLiner` PASS, `TestOpenHelpMetadata_DescribesRedesignedVerb` PASS; package `ok 0.332s` — holds
- "`go test ./...` passes and `go test -tags integration -p 1 ./...` passes" [5-4] — the unit-lane half for this section's subject measured with `go test ./cmd -run TestReadmeDocumentsSearchForm -v` → `--- PASS: TestReadmeDocumentsSearchForm (0.00s)`, package `ok` — holds. (The integration-lane half is forbidden to this pass by the project's safety invariant and carries no documentation subject: no integration-tagged file in the change-set touches README or `openCmd.Long`.)
- §10.7's "Project records and tags stay out" of the match domain [derived, no unsettled criterion] — measured with `go test ./internal/resolver -run 'TestMatchesSearchTerm|TestSearchFields|TestIsSearchSigil|TestIsPathArgument'` → `ok github.com/leeovery/portal/internal/resolver 0.461s` — holds
- The declared linters and the build over the whole tree [applies to every task criterion of the form "`golangci-lint run` reports nothing new" / "`go build ./...` clean" / "`gofmt -l` reports nothing" / "`go vet ./...` is clean"] — `gofmt -l .` → no output, exit 0; `go build ./...` → exit 0, no output; `go vet ./...` → exit 0, no output; `golangci-lint run` → `0 issues.`, exit 0 — all hold

NOT MEASURED:
- "`go test ./...` passes" (the whole-unit-lane clause carried by criteria [1-1] … [12-6]) — the lane-wide sweep is outside what a change-set verifier may run (a single package by the project's own command, only to confirm a suspected defect); it belongs to the review's own single suite run over the corrected tree. The two packages carrying this section's subjects were run narrowed and pass.
- "`go test -tags integration -p 1 ./...` passes" ([1-5], [1-7], [2-2], [2-4], [3-4], [4-2]–[4-5], [5-2], [5-4], [6-1] and siblings) — the integration lane is forbidden on this machine (live developer tmux server and live `portal state daemon`); no integration-tagged file in the change-set is a §9 or §10 subject.
- "In fish, completion after the function asks for `open`'s completions and offers live session names" [5-2] — §8's subject (`cmd/init.go`'s emitted script), not §9's; fish is absent on this machine in any case. §9 owes only that the README *states* the correction, which it does (README.md:175) and which `TestReadmeDocumentsSearchForm` pins.
- "A measurement contradicting the derivation closes the task with `cmd/init.go` unedited and that recorded" [5-5] — §8's subject. It reaches §9 only through the README's parenthetical at README.md:175 ("bash 4+ with `bash-completion`, zsh, or fish. macOS's stock `/bin/bash` 3.2 cannot"), which was read against the 2026-09-14 corrigendum and matches it verbatim in substance; settling the bash-completion measurement itself needs a bash with that package installed, which this machine does not have.
- "The human visual check is recorded before sign-off, with the still capture and the live-view command presented" [3-4] — §6's subject (the directory column's rendering), not §9's.
- "`go run ./cmd/capturetool --fixture sessions-search-results` … renders `~/code/portal` beside `portal-a1b2` …" [10-5] — §6's subject.
- Every other unsettled criterion in the file — their subjects are §2 (recognition), §3 (dispatch), §4 (match rule), §5 (composition), §6 (row display), §7 (cold path) and §8 (completion), each named in its own section's file.

FINDINGS:
- None

COVERAGE:
- §9.2's nine README obligations are all carried by the `x (open)` section — README.md:143-176 — read, one by one: the recognition rule at :143 (leading `/`, no further `/`, shape-only, with `./port` / `~/port` / `/Users/me/…` / `/tmp/` explicitly untouched); the single-segment cost and the `-p` escape at :155; the three outcomes by match count at :145-149; the term-less form and that it is not an error at :151; the match domain and the containment-vs-fuzzy divergence at :145 and :153; that the form composes with nothing at :157; the `-f` / `/term` distinction by outcome *and* which to reach for at :159; that the form completes against live session names after the slash at :175; the corrected completion after the function and the dropped filename fallthrough at :175; the rollout consequence ("start a new shell, or re-run the `eval` in your profile") at :175
- §9.2's sigil row belongs in the README's resolution table and is there, beside `-f` — README.md:170 (`| /<term> *(positional, not a flag)* | — | open the picker pre-filtered by the term — or attach outright when exactly one live session matches |`), with the `-f, --filter` row at :169 and the four pins at :165-168 — read; the table header widened from `Flag` to `Flag / form` (README.md:162) and the pin preamble at :161 now states that `-f`, `/<term>` and `-e`/`--` are not pins
- §9.2's five `portal open --help` obligations are all carried by `openCmd.Long` — cmd/open.go:136-152 — measured: `go test ./cmd -run TestOpenHelpMetadata -v` → the seven subtests pinning the recognition rule, the term-less form, the outcomes by match count, the match domain, the composition rule, the `-p /tmp` escape and the single `-f`//term paragraph all PASS
- §9.2's "the `-f` flag description stays a one-liner — none of these fits there" — cmd/open.go:775 (`"open the picker pre-filtered by <text> (skips resolution)"`) — measured: `go test ./cmd -run TestOpenFilterFlagUsage` → PASS; the guard asserts no newline and refuses the tokens `/term`, `search`, `match` in the usage string
- §9.1's "described by outcome — one always shows the list, one takes you there when there is only one place to go" holds in both artifacts with the same two clauses — README.md:159 and cmd/open.go:150-152 — read: both name `-f` as the script/keybinding form and `/term` as the interactive one, and the help guard requires a *single* paragraph naming both (`searchFormHelpBlockNaming`), so the pair cannot drift into separate unrelated sentences
- The README's match-domain sentence is true of the delivered matcher, not just plausible — README.md:145 against internal/resolver/search.go:9-35 — read: `SearchFields` returns `{name}` or `{name, AbbreviateHome(recordedDir)}` and `MatchesSearchTerm` lowercases both sides and uses `strings.Contains` per field with no joining, which is exactly "case-folded, as a contiguous run, against … name and its recorded directory — home-abbreviated"
- The README's `/tmp/` carve-out is true of the recogniser — README.md:143 against internal/resolver/path.go:25 (`strings.HasPrefix(arg, "/") && strings.Count(arg, "/") == 1`) — read: `/tmp/` carries two slashes and is not a sigil, so "the trailing slash is the second one" is literally the implemented rule
- The README's `-p` escape is reachable — README.md:155 (`x -p /tmp`) against internal/resolver/query.go:217-223 (`ResolvePathPin` stats the literal path and always mints) — read: `IsPathArgument`'s new sigil short-circuit (internal/resolver/path.go:14) is consumed at exactly one production site, internal/resolver/query.go:105 (the bare-positional chain), enumerated with `grep -rn "IsPathArgument" --include="*.go" .` → two hits outside tests, the declaration and that call; the `-p` pin never reads it, so the documented escape is not shadowed by the recognition rule
- The README's completion claims match `completeOpenPositional` — README.md:175 against cmd/completion.go:104-112 — read: `x <TAB>` → `completeSessionNames("")` → every live session name; `x /po<TAB>` → `completeSearchTerm` (cmd/completion.go:70-82) → `tui.PickerSessions(sessions, completionCurrentSession())`, which is the searched set less the attached session, each candidate returned with its leading `/` intact — "every live session but the one you are attached to … leaving the slash in place" is the code
- The README's bash-3.2 parenthetical matches the 2026-09-14 corrigendum's measurement rather than §2.2's superseded claim — README.md:175 — read: it attributes the switch-off to the shell (`bash 4+ with bash-completion, zsh, or fish`) and names stock `/bin/bash` 3.2 as the case where filenames still complete, which is the corrected statement, not the "Portal switches the shell's filename fallback off" wording the corrigendum retracted
- §9.3 (no CHANGELOG entry) — CHANGELOG.md — measured: `git log --oneline -1 -- CHANGELOG.md` → `9d0e2fc6c 🌿 Release v0.11.1`, which is outside the change-set range; `git diff --stat 7068abc9a^..HEAD -- CHANGELOG.md` → empty. The release process's ownership is intact
- §10.1 (the bare-positional chain unchanged) — internal/resolver/query.go:100-124 — read: `Resolve` still runs exact session name → path → alias → zoxide → miss in that order, with no branch added or removed; the only delta reaching it is that a sigil-shaped argument no longer satisfies `IsPathArgument`, and such an argument is intercepted in `RunE` (cmd/open.go:180-182) before a resolver is ever built, so the chain's own behaviour for every argument that reaches it is byte-identical
- §10.1's own supporting claim ("a glob needs one of `*?[`") — internal/resolver/glob.go:10 — measured: `grep -n 'globMeta =' internal/resolver/glob.go` → `const globMeta = "*?["`, as the specification cites it
- §10.2 (no `+` mint sigil) — internal/resolver/path.go, cmd/open_search.go — read: the only sigil predicate in the tree is `IsSearchSigil` and it tests `/` alone; `grep -rn "IsSearchSigil" --include="*.go" .` returns four non-test sites (the declaration plus cmd/open_search.go:20 and cmd/completion.go:75,108), none of which recognises a second sigil character
- §10.3 (no configurable sigil) — internal/prefs — measured: `git diff --stat 7068abc9a^..HEAD -- internal/prefs` → empty. No prefs key was added, so nothing in `prefs.json` changes parsing
- §10.4 (session naming unchanged) — internal/session — measured: `git diff --stat 7068abc9a^..HEAD -- internal/session` → empty. `{project}-{nanoid}`, `SanitiseProjectName` and `GenerateSessionName` are untouched by the change-set
- §10.5 ("nothing is retired, renamed or deprecated") — cmd/open.go:775-790, README.md:161-172 — read plus measured: `-f`, `-s`, `-p`, `-a`, `-z` and `-e` are all still registered and all still occupy their README table rows; the range's only removal from `openCmd` is `Args: cobra.ArbitraryArgs` giving way to `validateOpenArgs` (cmd/open.go:154), which adds refusals and retires no form. `go test ./cmd -run TestOpenHelpMetadata_DescribesRedesignedVerb` → PASS, and that guard enumerates `-s`, `-p`, `-a`, `-z`, `-f`, `-e`, `--` and `precedence` in `Long` (cmd/retired_surface_test.go:130-146, with `/term` and `search` added by this change-set)
- §10.5's "two changes owed to the existing surface" are the two that landed, and no third — internal/tui/session_item.go:87-89 and README.md:159 — read: `FilterValue` now joins `resolver.SearchFields(i.Session.Name, i.Session.Dir)`, which is the widening §4.2 mandates for all three entry points, and its doc comment explicitly excludes the grouping-derived directory; the `-f` dispatch itself (cmd/open.go:184-187) still opens the picker on the text with no other change
- §10.6 (the picker reached any other way is unchanged) — internal/tui/model.go:938, internal/tui/build.go:132-133, internal/tui/search_filter.go:82-89 — read: the directory column is gated on `m.searchForm`, which only `WithSearchForm` sets and which `Build` applies only when `deps.Search != nil`; `installSearchFilter` returns early for every picker but a term-carrying search form, so a `-f`, no-argument or command-pending picker keeps `list.DefaultFilter` and renders no directory cell. `grep -rn "ShowDir" --include="*.go" .` → one production assignment (internal/tui/model.go:938)
- §10.7 (match domain stays at name + directory) — internal/resolver/search.go:9-14 — measured: `go test ./internal/resolver -run 'TestMatchesSearchTerm|TestSearchFields|TestIsSearchSigil|TestIsPathArgument'` → ok. `SearchFields` takes exactly two arguments and reaches no project record and no tag; `internal/project` is untouched by the range (`git diff --stat … -- internal/project` → empty)
- The two documentation guards are guards, not decoration — cmd/open_docs_test.go:66-90 and cmd/open_help_test.go:11-76 — read: `readmeOpenSection` fatals when the `### \`x\` (open)` heading is gone or its body is empty (so a deleted section fails rather than vacuously passing), `readmeOpenExamples` extracts the first fenced block and fatals when there is none (so the prose's own `/port` mentions cannot satisfy the example assertions), and `hasCommentedExample` requires the literal `x` / argument / `#` triple; the help guard asserts on `openCmd.Long` directly and its `-f`//term check requires one blank-line-delimited paragraph to name both tokens
- The project's declared toolchain over the whole tree — repo root — measured: `gofmt -l .` → clean; `go build ./...` → exit 0; `go vet ./...` → exit 0; `golangci-lint run` → `0 issues.` (the `.golangci.yml` config sets the `integration` build tag and uncaps both issue limits, so this one invocation covers the integration-tagged files too, per CLAUDE.md)
- CLAUDE.md's `cmd/open.go` entry was corrected for the sigil rather than left stale — CLAUDE.md, commit `98503399b` — read: the "Key command" paragraph now carries the search form, its recognition rule, `resolver.IsSearchSigil` / `cmd/open_search.go` as its home, and the `Args`-validator refusal before bootstrap. §9 obliges only `--help` and the README, so this is beyond the section's requirement and is recorded as sound rather than as a gap
