SECTION: 1. Purpose and Scope + 2. The Sigil Form + 3. Resolution + 4. Match Domain and Matching Rule

SCOPE: commit range `7068abc9a559abb027ad61c6f0ab732c67a0688e^..HEAD` (199 commits), filtered to the change-set file list recovered from the task commits. Read in final state: `internal/resolver/path.go`, `internal/resolver/search.go`, `internal/resolver/query.go` (the chain the sigil must not enter), `cmd/open.go`, `cmd/open_search.go`, `cmd/root.go`, `internal/tmux/tmux.go`, `internal/tui/picker_sessions.go`, `session_item.go`, `session_dir_column.go`, `grouping.go`, `search_filter.go`, `search_decision.go`, `build.go`, and `model.go`'s landing / loading-gate / message arms; plus the delivered suites for each (`internal/resolver/path_test.go`, `search_match_test.go`, `search_fields_test.go`, `internal/tmux/list_sessions_probe_test.go`, `internal/tui/search_containment_test.go`, `search_landing_test.go`, `search_landing_termless_test.go`, `search_decision_test.go`, `session_item_test.go`, `cmd/open_search_test.go`, `cmd/open_search_deferred_test.go`), and the vendored `charm.land/bubbles/v2@v2.1.0` list API the landing depends on (`list.go:280-300`, `:1251-1275`). Run: `go build ./...`, `gofmt -l .`, `go vet ./...`, `golangci-lint run ./...`, `go test -count=1 ./internal/resolver`, `go test -count=1 ./internal/tui`, `go test -race -count=1 ./internal/tui`, `go test -count=1 ./cmd`, `go test -count=1 ./internal/tmux -run 'TestListSessionsProbe|TestListSessions'`, plus two `-run`-narrowed passes over the search suites in `internal/tui` and `cmd`. Not run: the remaining unit-lane packages (the whole suite is the corrected tree's run), the integration lane (forbidden on this machine), `internal/capture`, `cmd/bootstrap`, `cmd/capturetool`. Tree left as found (`git status --porcelain` unchanged from the session snapshot); no temp instance outstanding — one disposable `tmuxtest` server on a `/tmp/ptl-*` socket appeared mid-run and had exited by the end of the pass, the developer's server (pid 5505, socket `/private/tmp/tmux-501/default`) untouched and alive.

MEASURED:
- "`go test ./...` passes" [1-1] — `go test -count=1 ./internal/tui` → ok 35.9s; the three-argument `buildByProject`/`buildByTag`/`resolveSessionTags` form and its suites live in that package alone — holds
- "`go test ./...` passes" [1-2] — the criterion's named risk is the `FilterValue()` widening shifting a pre-existing suite's visible-row count; measured where every session-list picker lives: `go test -count=1 ./internal/tui` → ok, `go test -count=1 ./cmd` → ok 13.2s — holds
- "`go test ./...` passes" [1-3] — `go test -count=1 ./internal/tui` → ok (landing suites `TestSearchFormLanding`, `TestSearchFormLanding_TermlessForm`, `TestSearchFormPrecedenceOverInitialFilter` all PASS under the `-v` narrowed pass) — holds
- "`go test ./...` passes" [1-4] — `go test -count=1 ./internal/resolver` → ok 0.36s and `go test -count=1 ./cmd` → ok; the recognition tables (`TestIsPathArgument`, `TestIsSearchSigil`, `TestSearchTerm`, `query_test.go`) are in those two packages — holds
- "`go test ./...` passes and `go test -tags integration -p 1 ./...` passes" [1-5] — unit half: `go test -count=1 ./cmd` → ok, `go test -count=1 ./internal/resolver` → ok — holds (integration half under NOT MEASURED)
- "`go test ./...` passes" [1-6] — `go test -count=1 ./internal/tui` → ok; `TestSearchFormLanding_TermlessForm` PASS — holds
- "`go test ./...` passes and `go test -tags integration -p 1 ./...` passes" [1-7] — unit half: `go test -count=1 ./cmd` → ok (the collision suite is `cmd/open_search_test.go`) — holds (integration half under NOT MEASURED)
- "`go test ./internal/tui/... ./cmd/...` passes with no edit to any test file" [10-1] — `go test -count=1 ./internal/tui` → ok and `go test -count=1 ./cmd` → ok; `./cmd/bootstrap` and `./cmd/capturetool` (the other two members of `./cmd/...`) were not run, and the `Init` batch restructure is not reachable from either — holds for the two packages holding the subject
- "`go test ./internal/tui/... ./cmd/...` passes with no edit to any test file" [10-3] — same two runs, same qualification — holds
- "The collision suite at `cmd/open_search_test.go:337-401` passes with no edit … the same run settles the two new tests and the unchanged `--help` and admission tests" [11-1] — `go test -count=1 ./cmd -run 'TestOpenCommand_SearchForm|TestValidateSearchFormCollisions|TestValidateOpenArgs'` → ok, and the whole-package `go test -count=1 ./cmd` → ok — holds
- "`go build ./...`, `go test ./...` and `golangci-lint run` are clean" [11-2] — `go build ./...` → no output, `golangci-lint run ./...` → `0 issues.`, `go test -count=1 ./internal/tui` → ok (the three respelled call lines are in `internal/tui/staged_mint_band_test.go`) — holds
- "`go test ./internal/tui/...` passes with no test changed" [11-4] — `go test -count=1 ./internal/tui` → ok — holds
- "`go test ./cmd -run TestCompleteOpenPositional` passes" and "The four existing subtests … still pass" [11-5] — `go test -count=1 ./cmd` → ok, which runs `TestCompleteOpenPositional` at HEAD (i.e. against task 12-6's contract, as the criterion's own note requires) — holds
- "The `commandPending` branch … are unchanged, and `go test ./...` is green" [12-1] — `go test -count=1 ./internal/tui` → ok and `go test -count=1 ./cmd` → ok — holds
- "The `sync.RWMutex` and the single accessor stay, and the concurrent-`set` test is green under `-race`" [12-2] — `go test -race -count=1 ./internal/tui -run TestContainmentFilterIsRaceFreeAgainstAConcurrentSet` → PASS, and the whole package under `go test -race -count=1 ./internal/tui` → ok 64.9s, no race report — holds
- "`internal/tui/search_containment_test.go` and `go test ./...` stay green" [12-2] — every `TestSearchContainment*` subtest PASS in the `-v` narrowed pass; whole-package run ok — holds
- "A model with no decision closure dismisses exactly as it does today, and `go test ./...` is green" [12-3] — `go test -count=1 ./internal/tui` → ok (`TestSearchDecision_AbsentClosureLeavesTransitionUnchanged` among them) and `go test -count=1 ./cmd` → ok — holds (the cold-boot integration suite under NOT MEASURED)
- "`completingPreDashPositional`, `validateSearchFormCollisions`'s seventh derived arm and the plain session-name completion at a second positional are untouched, and `go test ./...` is green" [12-4] — `go test -count=1 ./cmd` → ok — holds
- "`go test ./...` is green and `golangci-lint run` reports nothing new" [12-5] — `go test -count=1 ./cmd` → ok (both edited files are `cmd` tests) and `golangci-lint run ./...` → `0 issues.` — holds
- "`go test ./...` is green and `golangci-lint run` reports nothing new" [12-6] — `go test -count=1 ./cmd` → ok and `golangci-lint run ./...` → `0 issues.` — holds
- "`go test ./...` passes" [2-1] — `go test -count=1 ./internal/resolver` → ok and `go test -count=1 ./cmd` → ok; the criterion's own note pins the package as hermetic, which the run confirms (no socket, no daemon) — holds
- "`go test ./...` passes and `go test -tags integration -p 1 ./...` passes" [2-2] — unit half: `go test -count=1 ./cmd` → ok and `go test -count=1 ./internal/tui` → ok — holds (integration half under NOT MEASURED)
- "`go test ./...` passes" and "`go test -race ./internal/tui` for `TestContainmentFilterIsRaceFreeAgainstAConcurrentSet`" [2-3] — `go test -race -count=1 ./internal/tui` → ok, named test PASS — holds
- "`go test ./...` passes" [2-4] — `go test -count=1 ./cmd` → ok and `go test -count=1 ./internal/tui` → ok; the new file is untagged and builds no binary, which the unit-lane compile confirms — holds (integration half under NOT MEASURED)
- "`go test ./...` passes" [3-1] — `go test -count=1 ./internal/tui` → ok (`fitSessionDir`'s suite is in that package) — holds
- "`go test ./...` passes" [3-2] — `go test -count=1 ./internal/tui` → ok — holds
- "`go test ./...` passes" / "A `-f` picker, a no-argument picker and a command-pending picker render their session rows byte-identically to today (existing suites green unmodified)" / "The column survives an `s` regroup, a `Space` preview and back, a `SessionsMsg` refresh, a marked-set mutation and a live theme swap" / "The column survives a hand edit of the committed filter text, including clearing it" [3-3] — `go test -count=1 ./internal/tui` → ok; `TestSearchDirColumn` and its subtests are in that package and ran green (the temp-dir path rendering whole at width 220 and dropping at width 34 included) — holds
- "`go test ./...` is green" [4-1] — `go test -count=1 ./internal/tui` → ok — holds
- "`go test ./...` passes" (unit half) [4-2] — `go test -count=1 ./cmd` → ok (`cmd/open_search_deferred_test.go`) — holds
- "`go test ./...` passes" (unit half) [4-3] — `go test -count=1 ./cmd` → ok — holds
- "`go test ./...` passes" and "The warm path, every CLI path and the concurrent route's step sequence and labels are unchanged (existing suites green unmodified)" (unit half) [4-4] — `go test -count=1 ./cmd` → ok, including `cmd/concurrent_bootstrap_gate_test.go` and `cmd/concurrent_bootstrap_route_test.go` — holds
- "`go test ./...` is green" [4-6] — `go test -count=1 ./cmd` → ok — holds
- "`go test ./...` is green" [4-7] — `go test -count=1 ./internal/tui` → ok — holds
- "`go test ./...` is green" [4-8] — `go test -count=1 ./cmd` → ok and `go test -count=1 ./internal/tui` → ok — holds
- "`go test ./...` passes" [5-1] — `go test -count=1 ./cmd` → ok — holds
- "`go test ./...` passes" [5-3] — `go test -count=1 ./cmd` → ok (`cmd/open_help_test.go`) — holds
- "`go test ./...` passes" (unit half) [5-4] — `go test -count=1 ./cmd` → ok (`TestReadmeDocumentsSearchForm`) — holds
- "`go test ./cmd` is green, including the verbatim shim expectation in `cmd/init_test.go`" [5-5] — `go test -count=1 ./cmd` → ok, which runs the real `bash`/`zsh` shell-driven cases (the `fish` ones skip: `command -v fish` resolves nothing on this machine) — holds
- "`TestOpenCommand_SearchForm_ExcludesTheCurrentSessionFromTheCount`, `…AttachesTheOtherMatchWhenTheCurrentSessionAlsoMatches`, `…CountsNothingOutWhenTheCurrentSessionReadFails`, `…ExcludesNothingOutsideTmux` and `internal/tui`'s `\"excludes the current session when inside tmux\"` all pass with no edit" [6-1] — `go test -count=1 ./cmd` → ok and `go test -count=1 ./internal/tui` → ok — holds
- "`go test ./...` is green, `gofmt -l` reports nothing and `go vet ./...` is clean" [6-1] — `gofmt -l .` → no output, `go vet ./...` → no output, `go test -count=1 ./cmd ./internal/tui` (run separately) → ok — holds
- "`TestMatchesSearchTerm`, `TestMatchesSearchTermUnresolvableHome` and the existing `FilterValue` subtests pass with no edit" [6-2] — `go test -count=1 ./internal/resolver` → ok and `TestSessionItem`/`TestSessionItemGroupMetadata` PASS in the narrowed `internal/tui` pass — holds
- "`go test ./...` is green, `gofmt -l` reports nothing and `go vet ./...` is clean" [6-2] — `gofmt -l .` → no output, `go vet ./...` → no output, the three packages run → ok — holds
- "`go test -race ./internal/tui` is green, including a test driving `set` concurrently with a filter pass" [6-3] — `go test -race -count=1 ./internal/tui` → ok 64.9s, `TestContainmentFilterIsRaceFreeAgainstAConcurrentSet` PASS — holds
- "`TestContainmentFilterFallsBackWhenTheSourceIsOutOfStep` and the `TestSearchContainment*` suites pass with no edit" [6-3] — `go test -count=1 ./internal/tui -run 'TestSearchContainment…'` → every subtest PASS (the named fall-through test was removed by 12-2, as the criterion notes) — holds
- "`gofmt -l` reports nothing and `go vet ./...` is clean" [6-3] — both → no output — holds
- "`go test ./...` passes; `go vet ./...`, `gofmt -l .` and `golangci-lint run ./...` are clean" [7-1] — `go vet ./...` → no output, `gofmt -l .` → no output, `golangci-lint run ./...` → `0 issues.`, `go test -count=1 ./internal/tui ./cmd` (separately) → ok — holds
- "The loading-gated routes are untouched: the existing `internal/tui` and `cmd` suites pass unchanged" [7-2] — `go test -count=1 ./internal/tui` → ok (the whole test binary compiles against the new two-value `createSession`) and `go test -count=1 ./cmd` → ok — holds
- "The existing search-count suites pass unchanged, including `TestOpenCommand_SearchForm_ExcludesNothingOutsideTmux`'s assertion that `CurrentSessionName` is read zero times outside tmux" [8-1] — `go test -count=1 ./cmd -run TestOpenCommand_SearchForm` → ok, whole package → ok — holds
- "`go build ./... && go test ./...` green; `golangci-lint run` clean" [8-1] — `go build ./...` → no output, `golangci-lint run ./...` → `0 issues.`, `./cmd` → ok — holds

NOT MEASURED:
- "`go test -tags integration -p 1 ./...` passes" [1-5], [1-7], [2-2], [2-4], [3-4], [4-2], [4-3], [4-4], [5-4], [11-2] — the integration lane builds portal binaries and spawns real daemons; it is forbidden on this machine by the run's safety invariant, so no member of it was executed
- "`go test -tags integration -p 1 ./cmd -run TestConcurrentColdBoot_SearchDecision`" and its eight cold-boot observations — "The decision closure is invoked exactly once across the whole boot"; "All ten real step events, and the terminal complete event, were delivered before it ran"; "`state.IsRestoringSet(client)` is false at the instant it runs"; "The closure's own live read returns the restored sessions"; "The model is on `PageLoading` after every step event"; "On the single-match term the model quits with `Selected()` set, `SearchAttached()` true, and `ActivePage()` still `PageLoading`"; "On the shared-fragment term … the model transitions to `PageSessions`"; "`FatalError()` is nil on both runs and no `BootstrapFatalMsg` is observed" [4-5] — integration-lane only, against a real cold tmux server; same prohibition. These are §3.4's subject and remain unsettled
- "`go test -tags integration -p 1 ./cmd -run TestConcurrentColdBoot_SearchDecision` for the two cold-boot suites whose pump this task rewired" [12-3] — same prohibition
- "On a cold server `open /port` … the honest loading page actually paints against a real cold tmux server" [4-4] — the integration-tagged 4-5 suite is the only observation of it; same prohibition
- "A warm single-match attach writes every accumulated warning line to stderr, in order, before `openSessionFunc` is called" (the end-to-end leg: a real `x /term` on a warm latched install with `_portal-saver` down) [4-3] — needs a live install with the saver killed; that is a mutation of the developer's running system and is out of bounds here. The in-process ordering half is covered by the `cmd` suite, which ran green
- "In fish, completion after the function asks for `open`'s completions and offers live session names" [5-2] — `command -v fish` resolves nothing on this machine, so every fish subtest skips; §8's subject
- "`go test ./...` passes and `go test -tags integration -p 1 ./...` passes" (the zsh trailing-empty-word detail) [5-2] — the unit half runs inside `go test ./cmd`, which passed; the integration half is forbidden. Subject is §8
- "A measurement contradicting the derivation closes the task …" (the bash-completion-package experiment) [5-5] — needs a bash with the `bash-completion` package installed; absent here, and the project gives no route to stand one up. §8's subject
- "`go run ./cmd/capturetool --fixture sessions-search-results` on a machine whose home is not `/home/user` renders …" [10-5] — a live 105x27 render read by eye; §6's subject (the directory column and its fixture), and the observation is beyond what this pass can take
- "`go test ./...` passes and `go test -tags integration -p 1 ./...` passes" (the deep path's `…/` truncation and the arbitrary-`$HOME` subtest at the tape geometry) and "The human visual check is recorded before sign-off, with the still capture and the live-view command presented" [3-4] — `internal/capture` was not run and the gate record is not a repository artefact; §6's subject
- "With the sessions-page key guard temporarily widened to swallow `s` under an applied filter, both subtests fail; with it reverted, both pass" [3-5] — settling it requires editing a tracked file (`internal/tui/model.go:2547`); the tree is not this pass's to modify. §6's subject
- "`go test ./...` is green" [3-5] — `internal/capture` (one of the two packages the criterion names as at risk) was not run; `internal/tui` ran green. §6's subject
- "`go test ./...` passes" — the whole-unit-lane half of [1-1] … [8-1] beyond the four packages listed under SCOPE — ~26 further packages were not run; the lane's single run belongs to the corrected tree, not to this pass

FINDINGS:
- None

COVERAGE:
- The recognition rule is a leading `/` with no further `/`, tested on shape and never against the filesystem — internal/resolver/path.go:25-27 — read: `strings.HasPrefix(arg, "/") && strings.Count(arg, "/") == 1`, so `/port` and `/` are sigils while `/tmp/`, `/Users/leeovery/Code/portal`, `./port` and `~/port` are not (§2.2's whole table); measured: `go test -count=1 ./internal/resolver` → ok (`TestIsSearchSigil`, `TestIsPathArgument`)
- The path test is narrowed for exactly that one shape and left alone for every other — internal/resolver/path.go:10-18 — read: `IsPathArgument` returns false for a sigil and is otherwise the pre-change expression verbatim
- The `-p` escape of §2.4 still mints at a single-segment absolute directory — internal/resolver/query.go:217-223 — read: `ResolvePathPin` calls `ResolvePath` directly and never consults `IsPathArgument`, so `portal open -p /tmp` is untouched by the narrowing; `-p` values are flag values, not positionals, so they are outside `searchFormPositionals` too
- A sigil never enters the resolution chain, the `-f` branch, the multi-target burst or the pin dispatch — cmd/open.go:176-180 — read: the search interception is the first branch after the `--ack` shape check and returns unconditionally, ahead of `isMultiTarget` (which would otherwise burst a metacharacter-bearing term) and ahead of `qr.Resolve`; measured: `go test -count=1 ./cmd` → ok (`TestOpenCommand_SearchForm_ResolvesNothing`, `…GlobMetacharactersAreLiteralText`)
- Recognition is positional-independent and stops at `--` — cmd/open_search.go:17-34 — read: `searchFormPositionals` scans `preDashPositionals`, which slices `args[:cmd.ArgsLenAtDash()]`, so `/word` among a trailing command's own arguments is that command's; measured: `go test -count=1 ./cmd` → ok (`TestSearchFormPositionals`, `TestSearchFormPositionals_StopsAtDashSeparator`)
- A term-less sigil takes no count and reads no session list — cmd/open_search.go:221-224 — read: the `term == ""` arm returns straight into `openTUIFunc` with a `SearchForm{}` carrying neither term nor decision closure; measured: `go test -count=1 ./cmd` → ok (`TestOpenCommand_SearchForm_TermLessFormTakesNoCount`, `…BareSigilIsNotAUsageError`)
- K = 1 attaches, any other count opens the picker — cmd/open_search.go:200-211, :234-248 — read: `searchDecision` answers a name only at `len(matches) == 1`; measured: `go test -count=1 ./cmd` → ok (`…AttachesTheSingleMatch`, `…OpensPickerWhenNothingMatches`, `…OpensPickerWhenTwoOrMoreMatch`, `…OpensPickerWhenNoSessionsAreLive`)
- The searched set is the set the picker lists — cmd/open_search.go:164-183 and internal/tui/picker_sessions.go:13-23 — read: `searchCandidates` runs the enumeration through the same `PickerSessions` the model's `filteredSessions` uses (internal/tui/model.go:1140-1145), and `currentPickerSession` degrades to "" outside tmux and on a failed or empty read, so nothing is held back on that account
- Portal's own `_portal-saver` / `_portal-bootstrap` sessions are never search candidates — internal/tmux/tmux.go:189-196 — read: the underscore filter sits in `parseSessionList`, which both `ListSessions` and the new `ListSessionsProbe` return through; measured: `go test -count=1 ./internal/tmux -run 'TestListSessionsProbe|TestListSessions'` → ok (`TestListSessionsProbe_ParsesTheSameOutputAsListSessions` seeds `_portal-saver` and `_portal-bootstrap`)
- A failed session-list read is not a zero match — internal/tmux/tmux.go:143-148 and cmd/open_search.go:234-240 — read: `ListSessionsProbe` wraps the commander's error instead of swallowing it as `ListSessions` does, and the warm route returns it after draining buffered warnings to stderr; measured: `go test -count=1 ./cmd` → ok (`…ReturnsAnEnumerationError`)
- The failed read exits non-zero with no error frame and no attach, after teardown on the cold route — cmd/open.go:604-618 (`processTUIResult` checks `model.SearchError()` before `model.Selected()`), internal/tui/search_decision.go:51-57 (`tea.Quit` with no page transition), cmd/open.go:620-630 (`finishTUI` restores the canvas and drains warnings before the result is returned) — read; measured: `go test -count=1 ./cmd` → ok (`TestProcessTUIResult_SearchDecision`)
- The attach uses the connector the invocation already selects — cmd/open.go:616-617 — read: `processTUIResult` hands the selected name to the `SessionConnector` built by `buildSessionConnector(client)`, the same value every other picker outcome uses; no third mode is introduced
- The sigil emits no `resolve` log line — cmd/open.go:178-180 vs :219 — read: `emitResolveDecision` is reached only after `qr.Resolve`, which the search branch returns ahead of; measured: `go test -count=1 ./cmd` → ok (`TestOpenCommand_SearchForm_EmitsNoResolveLine`)
- On a cold server the count is taken after the whole bootstrap, not after restore — cmd/open_search.go:229-232 (the deferred branch hands the closure to the picker), internal/tui/model.go:1509-1519 (`dismissLoadingGate` dispatches it), :1664-1694 (the gate needs both `minElapsed` and `bootstrapComplete`, and `BootstrapProgressMsg` never dismisses) — read; measured: `go test -count=1 ./internal/tui` → ok (`TestSearchDecision_HeldUntilBootstrapCompletes`, `…HeldUntilMinimumSpanElapses`, `…NotIssuedOnProgress`, `…IssuedExactlyOnce`, `…NotIssuedAfterBootstrapFatal`)
- The loading page's minimum span is untouched by the sigil — internal/tui/model.go:1634-1643 — read: `LoadingMinElapsedMsg` still gates dismissal, and the decision is a third condition on that gate rather than a step inside it, so a count answerable early leaves the page standing for the remainder of the span
- The count runs off the render goroutine — internal/tui/search_decision.go:40-46 — read: `searchDecisionCmd` returns a `tea.Cmd`, so an unbounded tmux read cannot freeze the loading page or its Ctrl-C; measured: `go test -count=1 ./internal/tui` → ok (`…HoldsLoadingPageWhileInFlight`, `…CtrlCWhileInFlightSelectsNothing`)
- A term opens the picker landing exactly as `-f` lands it — internal/tui/model.go:1320-1335 — read: `SetFilterText(term)` then `SetFilterState(list.FilterApplied)`, the same pair `applyInitialFilter` uses at :1338-1348; the bubbles implementation (`list.go:280-300`) runs the filter pass inside `SetFilterText` and re-anchors with `GoToStart`, and `ensureSessionRowSelected` steps off a group header so the cursor lands on the first surviving session row; measured: `go test -count=1 ./internal/tui` → ok (`TestSearchFormLanding`, including the By-Project and By-Tag header cases)
- The bare slash lands focused and empty on the whole live list — internal/tui/model.go:1324-1332 — read: an empty term selects `list.Filtering`, and `SetFilterState` focuses the input (`list.go:295-300`); the empty filter value makes the pass return every item; measured: `go test -count=1 ./internal/tui` → ok (`TestSearchFormLanding_TermlessForm`, both subtests)
- A search form always lands on the Sessions page, whatever the term matched — internal/tui/model.go:1300-1306 — read: `evaluateDefaultPage`'s `searchForm` arm precedes the "has sessions" arm, so K = 0 and an empty machine both stay on Sessions rather than falling to Projects
- K = 0 reads as a filter result rather than an error — internal/tui/filtering_no_matches.go:20-32 — read: the committed non-empty filter with no visible items renders `No sessions match "<term>"` with the `esc to clear the filter` hint, which is what typing `/` in the picker and filtering to nothing produces
- The matched fields are the name and the recorded directory, home-abbreviated — internal/resolver/search.go:9-14 — read: `SearchFields` returns `{name}` for an unrecorded directory and `{name, AbbreviateHome(dir)}` otherwise; measured: `go test -count=1 ./internal/resolver` → ok (`TestSearchFields`, `TestAbbreviateHome`, `TestMatchesSearchTermUnresolvableHome`)
- The sigil's rule is case-folded containment with the two fields tested separately and the term taken literally — internal/resolver/search.go:22-35 — read: `strings.Contains(strings.ToLower(field), strings.ToLower(term))` per field, no join, no glob translation, and an empty term matches nothing; measured: `go test -count=1 ./internal/resolver` → ok (`TestMatchesSearchTerm`)
- The three filter entry points share one filter value, joined name + single space + abbreviated directory with no trailing separator — internal/tui/session_item.go:87-89 — read: `strings.Join(resolver.SearchFields(…), " ")`, so `-f` and the hand-typed `/` widen with the sigil; measured: `go test -count=1 ./internal/tui` → ok (`TestSessionItem`'s four `FilterValue` subtests)
- The fuzzy routes keep the picker's stock rule and the sigil diverges on containment alone — internal/tui/search_filter.go:63-81, :85-91 — read: `containmentFilter` delegates to `list.DefaultFilter` whenever the live query differs from the supplied term, and `installSearchFilter` is a no-op for every picker but a search-opened one carrying a term; measured: `go test -count=1 ./internal/tui` → ok (`TestSearchContainmentNarrowsTheList` shows `rust-tools` surviving `-f port` and not surviving `/port`, `…IsNotInstalledForATermLessForm`)
- Containment narrows without reordering — internal/tui/search_filter.go:69-79 — read: ranks are appended in target index order with no score and `MatchedIndexes` left nil, so the list keeps whatever order the current grouping mode gave it; measured: `go test -count=1 ./internal/tui` → ok (`TestSearchContainmentPreservesListOrder`)
- Containment holds through every re-render and yields only to a hand edit of the text — internal/tui/model.go:1249-1254 (the source is re-recorded at the single `SetItems` chokepoint, which is the only one in the package) and search_filter.go:65-67 — read; measured: `go test -count=1 ./internal/tui` → ok (`TestSearchContainmentSurvivesReRenders` across an `s` regroup, a `Space` preview and a refresh that drops a killed session; `TestSearchContainmentHoldsOnTheFilterText` across an edit, an edit back, and a clear)
- The containment source is safe against the goroutine Bubble Tea gives the filter — internal/tui/search_filter.go:24-51 — read: an `RWMutex`-guarded pointer field whose `set` swaps a freshly built map rather than mutating the published one, so the unheld read of the returned map is sound; measured: `go test -race -count=1 ./internal/tui` → ok, no race report
- A grouping-derived directory never reaches the match or the recorded value — internal/tui/grouping.go:16-21 — read: `effectiveDir` is consumed by the group builders alone and the `SessionItem` they emit keeps `Session.Dir` untouched; `resolveDerivedDirs` (model.go:1207-1229) writes only `m.derivedDirs`, keyed on session name; `FilterValue` and the directory column both read `it.Session.Dir` (session_item.go:88, :268, :277), so a regroup can neither make a session findable by a path it was not findable by nor put a path beside a name that showed none
- Group headers still vanish under a sigil filter — internal/tui/session_item.go:100 and search_filter.go:31-39 — read: `HeaderItem.FilterValue()` is empty and `set` records only `SessionItem`s, so a header's target is absent from the source and ranks nothing, reproducing flatten-on-filter under the new rule; measured: `go test -count=1 ./internal/tui` → ok (`TestSearchContainmentAcrossGroupingModes`, all three modes)
- A search form and an initial filter cannot both apply — internal/tui/build.go:132-134 and cmd/open.go:593-598 — read: the `pickerLanding` sets exactly one of `Deps.Search` / `Deps.InitialFilter`, and `applySearchLanding` runs after `applyInitialFilter` so the search wins if a caller supplies both; measured: `go test -count=1 ./internal/tui` → ok (`TestSearchFormPrecedenceOverInitialFilter`)
- A sigil line is classified as a picker invocation for warning routing and the concurrent gate — cmd/root.go:173-178 — read: `isTUIPath` admits a line carrying a search form whatever its target count, so a sigil on a cold server takes the deferred bootstrap and its soft warnings never hit the alt-screen; measured: `go test -count=1 ./cmd` → ok (`cmd/concurrent_bootstrap_gate_test.go`, `cmd/open_search_warnings_test.go`)
- `portal open --help` still describes the form the code implements — cmd/open.go:136-152 — read: the help body states the leading-`/`-no-further-`/` shape, case-folded containment over name and recorded directory, the one-match attach, the never-an-error outcome and the `-p /tmp` escape, each matching the delivered behaviour above
- The declared linters and the build are clean over the whole tree — repo root — measured: `go build ./...` → no output; `gofmt -l .` → no output; `go vet ./...` → no output; `golangci-lint run ./...` → `0 issues.`
