AGENT: duplication
FINDINGS: none
SUMMARY: Every rule this feature introduces has exactly one home and every consumer reaches it there — the sigil's shape (`resolver.IsSearchSigil`, read by `IsPathArgument`, `searchFormPositionals`, `completeOpenPositional` and `completeSearchTerm`), the searchable field set and its home-abbreviation (`resolver.SearchFields`/`AbbreviateHome`, read by `MatchesSearchTerm`, `SessionItem.FilterValue` and `fitSessionDir`, so the searched form and the displayed form cannot drift), the containment predicate (`resolver.MatchesSearchTerm`, read by both the K count in `cmd/open_search.go` and `containmentFilter` in the picker), the searched set (`tui.PickerSessions` plus `currentPickerSession`, read by the count, the completer, `openTUI` and `Model.filteredSessions`), the `--` bound (`preDashPositionals`), the list-sessions read (`listSessionsArgs`/`parseSessionList` behind both `ListSessions` and `ListSessionsProbe`), the session-opening function's expansion (`openFunctionExpansion`), and the loading-gate dismissal (`dismissLoadingGate`). The remaining near-duplicates are either pre-existing and out of plan scope (`renderSessionBandSlot`/`renderProjectBandSlot`, the three `emit*Init` shell emitters) or excluded by the floor — test-helper symmetry (the three command pumps `runCmd`, `settleCommands` and `containmentDrain`; the parallel model builders across the search suites), the prefix-filter shape shared by the three completion helpers whose candidate sets are deliberately different rules, and the separator-space literal repeated inside one row renderer, whose drift breaks the column-alignment assertions loudly. No comment introduced by this work makes a false claim, a cardinality claim, a claim about tests, or carries workflow vocabulary, so there are no comment corrections.
