## Attempt 1

ISSUES:
- `cmd/concurrent_bootstrap_route_test.go:129` — `TestPersistentPreRunE_LatchedSearchForm_TakesAbridgedPath` asserts nothing that depends on the change under review. All three of its assertions (`runner.calls == 0`, no deferred bootstrap, `serverStarted == false`) are set unconditionally inside the `if latchSatisfied` block at `cmd/root.go:109-119`; the one thing that branch *does* read `isTUIPath` for is the warning emission at `cmd/root.go:112`, which the test leaves unexercised because `satisfiedLatchAliveSaverCommander()` keeps the saver alive and the sink empty. The executor's stated reason for not strengthening it — that a discriminating version would mean "seeding the sink artificially, which no real latched run does" — is factually wrong: `ensureSaverLiveness` (`cmd/abridged_saver.go:22`) adds `bootstrap.SaverDownWarning()` to that sink on exactly the real latched run where the saver is down and revive fails. The failure this leaves uncaught: on a warm/latched install with a dead saver — the everyday shape after a saver crash, and the common one, since most invocations are latched — a regression at `cmd/root.go:112` would write "Portal save daemon failed…" straight into the terminal `x /port`'s picker then claims with its alternate screen. That is the precise corruption §7.1/§7.2 make this classification exist to prevent, and no test in the suite would go red for the search-form shape.
  FIX: Swap the fixture to the one the package already uses for exactly this scenario and add the stderr assertion. Mirror `TestPersistentPreRunE_Abridged_LeavesWarningsForOpenTUIOnTUIPath` (`cmd/abridged_route_test.go:153`), which is the bare-`open` twin of this test: add `stubSaverAliveCheck(t, false)` and `shrinkSaverRetryDelay(t)` (both in `cmd/abridged_saver_test.go`), build the client from `satisfiedLatchSaverAbsentCommander()` instead of `satisfiedLatchAliveSaverCommander()`, capture `rootCmd.SetErr(&stderr)`, and assert the warning arrives at `openTUIFunc` via `bootstrapWarnings.Drain()` with `stderr.Len() == 0`. The warning reaches the sink through production code, so nothing is seeded. Keep the existing three assertions — they are the criterion's named properties and are worth holding.
  ALTERNATIVE: Leave this test as the route assertion and add a separate `TestPersistentPreRunE_Abridged_DoesNotEmitWarningsForSearchForm` in `cmd/abridged_route_test.go`, beside its bare-`open` twin. Tradeoff: better file placement (the abridged-route suite is where that fixture and its two helpers live) against a second `Execute()` for one extra assertion. The reviewer recommends the first — one test, one route, and the criterion's properties and the emission behaviour are the same run.
  CONFIDENCE: high

NOTES:
- The restructured predicate was verified verdict-identical for every input, not merely for the tested cases: `!(¬open ∨ pin) ∧ C` is `open ∧ ¬pin ∧ C` reassociated, both skipped calls are pure (`anyOpenDomainPin` via pflag's `Changed`, `searchFormPositionals` via `ArgsLenAtDash` and `IsSearchSigil`), and the one call that could panic (`args[:dash]`) is reached in strictly fewer cases than the specified form, never more. The early return also makes "the pin keeps its veto ahead of the new arm" structural rather than positional.
- The re-pointed decider-guard row is genuinely load-bearing: after the flip the `/dir` row would have become a silent duplicate of the TUI-path row. The reviewer swept the tree for other single-segment absolute positionals feeding this classification and found none — every other fixture already uses a multi-segment or `~/` path.
- The `-f` parity row computes `want` rather than pinning it, so it would stay green if both forms regressed to false together. Fine as written because the sibling subtests pin each side absolutely within the same function — worth knowing only if the parity row is ever moved away from them.
- `isTUIPath(openProbeCmd(), []string{"api", "/port"})` is true per the criteria, but unreachable in production: the args validator refuses that line first. The row pins the predicate's positional-independence rather than a live invocation, which is the right thing for it to pin.
- The added doc comment on `isTUIPath` reads correctly against the code and the specification; no correction owed.
