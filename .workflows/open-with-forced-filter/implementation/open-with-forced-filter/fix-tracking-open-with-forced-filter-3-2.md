## Attempt 1

ISSUES:
- `internal/tui/session_row_anatomy_test.go:354` — `TestSessionRow_DirColumnKeepsTheRowExactlyTheListWidth` is the designated guard for the task's edge case *"the directory arithmetic must not rely on [the `ansi.Truncate` backstop]"*, and it structurally cannot fail for that error. Because `renderSessionRow` clamps the assembled row to `total` at `internal/tui/session_item.go:319`, a directory that overruns its budget still measures exactly the list width — the overflow is paid out of the right margin instead. The reviewer demonstrated this is a live, visible defect and not a theoretical one: mutating `fitSessionDir(it.Session.Dir, remaining-1)` to `fitSessionDir(it.Session.Dir, remaining)` at `internal/tui/session_item.go:280` (i.e. forgetting to withhold the separating space from the fit) leaves the entire delivered suite green, while producing rows like

  `▌ alpha ~/Code/portal  3 windows            …`

  — the right margin replaced by a stray ellipsis — at 30 of the widths in a 28..120 sweep. The user would see a ragged right edge appearing on some rows at some terminal widths and not others, with nothing in the suite to catch the regression. The shipped code is correct; only the guard is missing.
  FIX: Add one assertion that the backstop did not fire. In `internal/tui/session_row_anatomy_test.go`, alongside the width table, add "it never leans on the pathological-width backstop": sweep `w` from `leftBarColumnWidth+nameGap+countSlotWidth+attachedSlotWidth+rowRightMargin+1` to ~120 over two or three directories whose abbreviated widths straddle the boundary (a short under-home path, a deep one, a not-under-home one) and a couple of name lengths, asserting `strings.HasSuffix(ansi.Strip(out), strings.Repeat(" ", rowRightMargin))` with `ShowDir: true`. Scope it to non-gone rows — the gone badge deliberately fills the right margin. Verified by the reviewer: this fails the mutant on 30 widths and passes the delivered code unchanged.
  ALTERNATIVE: Assert instead that the count slot's visible column equals `indent + leftBarColumnWidth + nameWidth + nameGap` at every swept width — it catches the same off-by-one (the count shifts right by one before the chop). The margin assertion is the recommended one: it states the property the edge case actually names ("nothing was truncated") rather than a consequence of it, and it needs no width arithmetic restated in the test.
  CONFIDENCE: high

NOTES:
- The unsized branch spells the display abbreviation a second way — `resolver.AbbreviateHome(it.Session.Dir)` at `internal/tui/session_item.go:271` rather than `fitSessionDir` with an unbounded width, which returns the identical value. The task directed it explicitly and the branch is a pre-first-`WindowSizeMsg` transient, so the exposure is negligible; worth knowing only if `fitSessionDir` ever grows a display rule beyond truncation.
- The three comments the diff adds each carry a *why* the code cannot (the withheld cell, the painted separator, the off-by-default rationale) and none is falsified by the code. No corrections owed.
- The reviewer independently re-derived the width arithmetic across a large sweep (widths × name lengths × directory shapes × flat/grouped × attached × selected × colour/colourless × gone) and confirmed: the row is exactly the list width wherever the flex region has a cell, the name's prefix is byte-identical with the column on and off at every width, and the directory's first cell is always exactly one column right of the name. It also mutation-tested the production file eight ways; seven mutants are caught by the delivered suite.
