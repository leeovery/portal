AGENT: duplication
FINDINGS: none
SUMMARY: A full fresh pass over the change set found every rule this feature introduces living in exactly one home — the sigil's shape (`resolver.IsSearchSigil`/`SearchTerm`), its match domain and rule (`resolver.SearchFields`/`MatchesSearchTerm`, reached identically by the CLI count, the picker's containment filter and `SessionItem.FilterValue`), the searched set (`tui.PickerSessions`, reached by the count, the completer and the model's `filteredSessions`), the home abbreviation (`resolver.AbbreviateHome`, reached by both the match and both display branches), the `--` bound (`preDashPositionals`, read by the classifier, the validator and the completer), and the session-list read (`listSessionsArgs`/`parseSessionList` shared by `ListSessions` and `ListSessionsProbe`). The near-duplicates that remain are each either justified by a stated difference in rule (`applySearchLanding` vs `applyInitialFilter`, `completeSearchTerm` vs `completeSessionNames`), or covered by a test that would fail loudly on drift (the unsized directory branch beside `fitSessionDir`, the capture fixture's home fallback beside `AbbreviateHome`'s, the emitted completion shims' function names beside their registrations), so none clears the floor's silent-and-consequential test.
