## Attempt 1

ISSUES:
- `cmd/init_completion_shell_test.go:270-282` — `TestInitCompletion_OffersNoFilenameForPartialAbsoluteDirectory`, the guard named for "filename fallback stays off on that word in every shell", cannot fail for that reason in either shell it executes. The reviewer changed the stub's hardcoded `printf ':4\n'` (line 26) to `:0` — i.e. Portal no longer asking for `ShellCompDirectiveNoFileComp` — and both the bash and the zsh leg stayed green. The assertion reads only `REPLY `-prefixed lines (line 172), and neither shell's file-completion signal is a `REPLY` line. In zsh the signal already exists and is thrown away: at directive 0 the driver prints `FILECOMP *:filename:_files` from the `_arguments` stand-in (line 71), which is exactly what the driver comment at lines 55-56 says those stand-ins are for, and nothing consumes it. In bash there is no signal at all — `cmd.Output()` (line 165) discards the stderr `compopt: not currently executing completion function` diagnostic. The failure this prevents: if `open`'s completion function later stops returning `ShellCompDirectiveNoFileComp` on some path (a `-p` domain branch that offers directories, an error path returning `ShellCompDirectiveDefault`), `x /tm<TAB>` completes to `/tmp/` and a search silently becomes a mint — the named harm — with the end-to-end guard still green, and the user discovering it by accidentally minting a session at `/tmp`.
  FIX: (a) zsh — carry the stand-ins' report into the verdict: add a `fileComp []string` field to `completionRun`, populate it with `linesWithPrefix(string(out), "FILECOMP ")` alongside `reply` in `driveCompletion`, and assert it is empty in the test. That also makes the comment at lines 55-56 true of the suite rather than only of the driver. (b) bash — do **not** stub `compopt`: cobra gates the call on `[[ $(type -t compopt) == builtin ]]`, so a function stub suppresses the call instead of recording it. Use cobra's own debug channel — add `BASH_COMP_DEBUG_FILE=<t.TempDir()>/debug` to the env `driveCompletion` builds and assert the file contains `Activating no file completion`. The reviewer confirmed both directions on this machine: the line is present at directive 4 and absent at directive 0. (c) fish needs nothing — `complete -c <name> -f` is already pinned exactly by `TestInitFish`.
  ALTERNATIVE: for bash, capture stderr and assert it carries the `compopt: not currently executing completion function` diagnostic. It works, but it asserts on bash's own error wording rather than on cobra's branch report, so it breaks on any bash that rewords the diagnostic. The debug-file route reads a line cobra emits deliberately; the reviewer recommends it.
  CONFIDENCE: high

NOTES:
- The zsh stand-ins do NOT hollow out the zsh leg on its actual subject. What the driver replaces is the compsys *rendering* layer — everything downstream of the request. What runs as real zsh over real emitted code is the whole of the thing under test: the shim's word slice and `CURRENT` arithmetic, then cobra's `_portal` body, the `requestComp` composition, the trailing-empty-word rule and the `eval`. The reviewer drove this outside the suite and got the same four requests, and the reconstructed pre-fix script gives the defect. The one place the modelling bites is `TestInitCompletion_CarriesSlashPrefixedCandidateIntoReply`: in bash that runs through real `compgen -W`, but in zsh the "shell's own prefix filter" is the hand-written `_describe` stand-in. No cheap fix exists — exercising zsh's real matcher needs an interactive ZLE session — so leaving it modelled is right; it is worth knowing that criterion is bash-verified and zsh-modelled.
- What is pinned for fish: the exact emitted registration text, both lines and their order, the `--cmd` variant, and the untouched control wrap, all unconditionally. Not pinned: any fish behaviour. Beyond that, `fishCompletionDriver` has never executed, so it is unverified scaffolding — `complete -C` output carries a tab-separated description that the driver's `printf` may split, and the exactly-one-request rule depends on cobra's once-guard holding across two `complete -c portal -n …` entries. Expect the fish legs to need work the first time they run where fish exists. The one load-bearing behavioural assumption in the emission is that fish's `--wraps` accepts a multi-word target; the reviewer believes it does (fish's own `alias` builds `--wraps='git checkout'`), but it is the thing to confirm first on a fish machine — if it does not, `complete -c x -f` leaves the user with *nothing* rather than falling back to filenames, which is worse than today.
- "Filename fallback stays off on that word in every shell" is NOT true on macOS's system bash, and the phase should know before the documentation task writes it down. `/bin/bash` is 3.2.57 here and `type -t compopt` is empty there, so cobra's script logs "No file completion directive not supported in this version of bash" and never runs `compopt +o default` — `-o default` then completes `x /tm<TAB>` to `/tmp/`. This is not a defect in this change: the `-o default` text is unchanged from the pre-existing registration, and cobra makes the same choice for `portal` itself, so `portal open /tm<TAB>` behaves identically. But the suite cannot see it (`exec.LookPath("bash")` resolves the Homebrew 5.3), and task 5-3 is asked to document that the corrected completion "no longer falls through to filenames for a path argument" — which would be a confident lie for a bash-3.2 user. Either qualify the wording or take the exposure knowingly.
- `cmd/init.go:57-58` — when the cursor is mid-line, `COMP_LINE`/`COMP_POINT` put the point at end-of-line, which disagrees with `COMP_CWORD`. Harmless on current consumers: cobra reads `words`/`cword` and truncates there, and the bash-completion helpers derive from `COMP_WORDS`/`COMP_CWORD`. Noting it so a future reader does not mistake end-of-line for the cursor.
- Isolation is sound: the stub `portal` is written into a `t.TempDir()` bin dir placed first on `PATH`, shells run with `--noprofile --norc` / `-f` / `--no-config`, and the package `TestMain`'s poison for `TMUX` and the `PORTAL_*` paths is carried into every subprocess. Nothing can reach the developer's install or tmux server.
- The suite genuinely discriminates: the reviewer reconstructed the pre-fix bash emission and it records `open|__complete|/po`, the original defect.
