## Attempt 1

ISSUES:
- `/Users/leeovery/Code/portal/.workflows/open-with-forced-filter/specification/open-with-forced-filter/specification.md:334` — the new §8.1 paragraph over-claims: "pressing Enter on the assembled line is then refused by `validateSearchFormCollisions`, **which is the sole judge of whether a whole line is legal**". It is not. `validateOpenArgs` (`cmd/open_search.go:68-73`) runs `validateSearchFormCollisions` and then `validateCommandScopeAndFilter`, and `validateSearchFormCollisions` returns nil immediately for any line carrying no search form (`cmd/open_search.go:79-81`) — so `open -f x api`, a command scoped both ways, and an empty `-f` are all refused by the *other* judge. The clause is true for the paragraph's own subject (a line carrying a search form, which that function judges totally, including its catch-all arm) and false as written. This matters because it is the same failure mode the entry it sits above exists to correct: §8.1's habit of stating a rule unconditionally. A later reader takes §8.1 at its word, goes looking for where `open -f x api` is refused, and finds the section wrong — noticed as another corrigendum, or as a rule added to the wrong validator.

  FIX: Narrow the relative clause to the line under discussion — e.g. "…is then refused by `validateSearchFormCollisions`, which judges the whole line rather than the word", or "…which is the sole judge of whether a line carrying a search form is legal". The first is recommended: it keeps the word/line contrast the paragraph is built on and makes no cardinality claim at all. Then re-run the re-index against the same path (`node .claude/skills/workflow-knowledge/scripts/knowledge.cjs index .workflows/open-with-forced-filter/specification/open-with-forced-filter/specification.md`) so the store does not retain the uncorrected sentence — the store currently holds this text, so a text-only fix without the re-index leaves the two out of step.

  ALTERNATIVE: Leave the clause and instead qualify the corrigendum entry. Worse — §8.1 is what a reader reaches first and the corrigendum is the record of what §8.1 got wrong, so the correction belongs in the section.

  CONFIDENCE: medium — the surrounding sentence makes the intended reading recoverable, so this is an accuracy tightening rather than a substantive error.

NOTES:
- `completeOpenPositional`'s new doc paragraph ends "Past the separator neither holds — the word is not a search form at all" (`cmd/completion.go:101-103`), which reads for a moment against the first paragraph's boundary exception (the word immediately after the separator keeps the sigil arm). The first paragraph carves that out explicitly two lines earlier, so it is not false; worth knowing it is the one place in the comment a reader has to hold both paragraphs at once.
- The doc comment's "The separator is the only bound" cardinality claim and its illustrative `open api /po<TAB>` case both clear code-quality's bars: the claim is about this function's own two-conjunct gate, falsified only by editing the gate, and the case is a one-clause warning about surprising behaviour rather than a hand-traced worked example.
- The corrigendum entry itself was verified correct — dated 2026-09-15 against a fresh `date`, attributed correctly, and quoting both claims verbatim against §8.1. Only the section paragraph needs the tightening.
- The new `-f` pin was verified discriminating without relying on the executor's mutation experiment: the fallback completer can return nothing for a `/`-prefixed term, so a `/`-prefixed candidate can only have come through the sigil arm, and the only dimension separating this subtest from its sibling is the set filter flag.
