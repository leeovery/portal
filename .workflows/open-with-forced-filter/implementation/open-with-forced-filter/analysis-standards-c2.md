AGENT: standards
FINDINGS: none
SUMMARY: Every decision point in the specification — the sigil's shape rule and its narrowing of the path test, the match-count outcomes and where the count is taken, the containment-vs-fuzzy divergence and the shared home-abbreviated field set, the non-composition refusals decided in the Args validator ahead of any bootstrap, the search-scoped directory column with its left-truncation and drop floor, the picker classification and the two teardown warning routes, the completion shim across all three shells, and the help/README copy — maps to a concrete implementation site that matches it, including the two corrigenda this phase raised. No candidate cleared the finding floor and no comment in the change set needs correcting.

Conformance notes (no action required, recorded so a later cycle need not re-derive them):

- §2.2 recognition: `resolver.IsSearchSigil` (leading `/`, exactly one `/`) narrows `IsPathArgument` for that one shape only; every other row of the §2.2 table (`/Users/…`, `/tmp/`, `./port`, `~/port`, `port`) is unchanged. `-p /tmp` escapes via `ResolvePathPin`, which calls `ResolvePath` directly and never consults `IsPathArgument`, so §2.4's escape holds.
- §2.3 / §5.1: `validateOpenArgs` is `openCmd.Args`. Verified against cobra v1.10.2 `command.go` that `helpVal` (926-935) is answered, then `ValidateArgs` (968), then `PersistentPreRunE` (985) — so `--help` answers outside the rule and a refused line starts no server. `preDashPositionals` stops the scan at `ArgsLenAtDash`, so `portal open ~/Code/api -- ls /tmp` is untouched and `orderedOpenTargets` breaks at `--` independently.
- §3.2: the counted set is `ListSessionsProbe` less the caller's own session via `tui.PickerSessions` — the same helper the picker's `filteredSessions` uses — and `parseSessionList` drops `_`-prefixed names, so `_portal-saver`/`_portal-bootstrap` are never candidates.
- §3.3/§3.4: `applySearchLanding` reproduces `-f`'s landing (`SetFilterText` → `GoToStart` → `SetFilterState(FilterApplied)` → `ensureSessionRowSelected`); the term-less form takes `list.Filtering`. `dismissLoadingGate` is reachable only when `bootstrapComplete && minElapsed`, so the count is never taken before the tenth step or before `LoadingMinDuration`.
- §3.7: `ListSessionsProbe` wraps the `*CommandError` (tmux's stderr survives); the error returns as an ordinary error (exit 1 via `classify`), not a `bootstrap.FatalError`, and `processTUIResult` checks it ahead of `Selected()` so nothing attaches.
- §4: `resolver.SearchFields` is the single declaration of the field set; `MatchesSearchTerm` tests the two fields separately with `strings.Contains` on folded text, and `SessionItem.FilterValue` joins the same two values with one space for the fuzzy routes. Both read `Session.Dir` only — `grouping.go`'s `effectiveDir` keeps the derived guess in `m.derivedDirs`, which neither the match nor the column reads.
- §4.4: `containmentFilter` falls through to `list.DefaultFilter` on any query other than the supplied term, appends ranks in index order (no re-ranking), and skips `HeaderItem` so grouped lists flatten under it.
- §6: `ShowDir` is keyed on `m.searchForm`, which is set once and never cleared, so it covers the term-less form and survives a hand edit and every grouping mode. `fitSessionDir` abbreviates first, then returns the longest separator-anchored tail that fits behind `…`, and the empty string when not even the last whole segment does. The directory takes `countTok`, which steps to `TextSecondary` on the cursor row.
- §7: `isTUIPath` classifies on shape; warnings reach the terminal only through `emitSearchTeardownWarnings` (attach or read failure) and the disjoint `PendingBootstrapWarnings` tail, both after `p.Run()` returns.
- §8: `completeSearchTerm` prefixes on the post-slash term, re-attaches the slash, and holds back names carrying `/`. The bash and zsh shims rewrite `COMP_WORDS`/`COMP_CWORD`/`COMP_LINE`/`COMP_POINT` and `words`/`CURRENT` respectively, follow `--cmd`, and are pinned byte-exact in `cmd/init_test.go` plus driven through real shells in `cmd/init_completion_shell_test.go` (which is correctly unit-lane: no portal binary built, execed or spawned, `--noprofile --norc` / `-f` / `--no-config`, stub `portal` first on PATH, temp dirs only).
- §9: `openCmd.Long` carries the form, the rule, the three outcomes, the term-less form, the non-composition rule and the `-f`/`/term` distinction; the README's `x (open)` section carries those plus the completion correction and its rollout consequence, and the resolution table carries the `/<term>` row.
- The `sessions-search-results` fixture is enrolled in the swap-and-diff completeness guard with real present/absent assertions (`absent: evvi-sync-engine` is what proves the narrowing), and `internal/tui/restore_source_guard_test.go` was strengthened rather than relaxed — it now resolves the writer one hop out through `finishTUI`'s parameter, so the launch-site rule still binds.
