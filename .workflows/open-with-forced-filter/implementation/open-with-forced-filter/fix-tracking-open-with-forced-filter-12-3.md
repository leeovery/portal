## Attempt 1

ISSUES:
- `/Users/leeovery/Code/portal/cmd/concurrent_search_decision_integration_test.go:52-130` (`driveSearchColdBoot`) — the integration driver discards every command the model returns (`model, _ = model.Update(msg)` at line 116), so the decision command is now dropped and the closure is never invoked. Both tests in the file fail. Verified by running the lane, not inferred: `go test -tags integration -p 1 -count=1 ./cmd -run TestConcurrentColdBoot_SearchDecision` fails with `decision calls = 0 across the whole boot, want 1`, `Selected() = "", want "cc-solo-charlie"`, `step events delivered when the decision ran = 0, want 10`, and `ActivePage() = 0, want PageSessions`. The same command run against a `git archive HEAD` export of the tree in a scratch directory passes, so this is a regression introduced by this change, not a pre-existing failure. The executor's `go vet -tags integration` only compiles the file; the lane was never run.

  The blast radius is the project's own two-lane rule — the integration lane is where every real-tmux guarantee of this feature lives, and the two tests this breaks are the only place the "decision runs after all ten steps, with `@portal-restoring` cleared, over post-restore reality" contract is pinned against a real server. Left as is, the next contributor to run the integration lane inherits a red suite with no obvious owner.

  FIX: run the command the `BootstrapCompleteMsg` update returns and feed its answer back, keeping every other command discarded. The existing discard is deliberate and its reason is still valid — the comment at lines 48-51 records that the model carries the same receiver closure, so running the command returned from a `BootstrapProgressMsg` update would put a second consumer on the progress channel. Discriminate on the message rather than on the page: the terminal `BootstrapCompleteMsg` arm never re-issues the receiver, and it is the only arm that can dispatch the decision here (the `LoadingMinElapsedMsg` at line 88 is sent while `bootstrapComplete` is still false and returns nil). Concretely, capture the command from the update at line 116 and, when the message just processed was the complete, invoke it and deliver the resulting message into the model before the loop re-tests `run.probe.calls == 0`. The probe reads `run.steps` / `run.complete`, both of which are already set before that update, so `stepsAtCall == 10` and `completeAtCall == true` still hold; on the shared-fragment case the follow-on dismissal batch carries only `surfaceBufferedWarnings` / a plain session refetch / the detection command, none of which touch the pipe, so discarding it stays correct. Correct the lines 48-51 comment in the same edit — it currently claims the model's commands are discarded outright, which will no longer be true.

  ALTERNATIVE: gate on `ActivePage() == tui.PageLoading` the way `cmd/testhelpers_test.go`'s `driveLoadingGates` does. Rejected for this file: the page is also `PageLoading` on every `BootstrapProgressMsg` update, whose command *is* the receiver, so that heuristic would reintroduce exactly the channel race the existing comment guards against. Discriminating on the complete event is the only safe form here.

  CONFIDENCE: high

NOTES:
- The task's Outcome line says the loading page "keeps animating". There is no animation loop on that page — it repaints on `BootstrapProgressMsg` and on the pad tick. The property actually delivered is that the event loop stays free, so Ctrl-C and any queued input are processed while the decision is out, which is the failure the task was written against. No code change implied.
- After an attach or read-failure decision has been applied, `searchDecide` is nil and the page is still `PageLoading`, so a further gate message would dismiss into the picker and empty the buffered warnings. This is unchanged from the pre-task code (clearing the field had the identical effect) and is unreachable in production, where each gate message is sent exactly once. Recorded only so it is not mistaken for something this change introduced.
- `driveLoadingGates` in `/Users/leeovery/Code/portal/cmd/testhelpers_test.go:262-286` identifies the decision command by "non-nil command returned while the page is still loading", because `searchDecisionMsg` is unexported and a type assertion is not available from `cmd`. That is the only discrimination available from outside the package and is correct for the models these tests build (none of them ever delivers a `BootstrapProgressMsg`). Worth knowing if a future gate starts returning a command while loading.
- No comment corrections. Every comment the diff introduced or touched checks out against the code, and the grep for stale references to the removed synchronous rule comes back empty.
- The executor's in-flight guard deviation from the task's prescribed "dispatched once" flag was verified and is inside the task's intent: a bare flag would have let a repeated gate message fall through to the dismissal while the decision was still outstanding, composing the picker and emptying the warnings buffer.
