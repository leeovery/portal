## Attempt 1

ISSUES:
- `cmd/open_help_test.go:42-48` — the subtest `it names what the term is matched against` asserts only that `Long` contains "name" and "directory". Both are present in the pre-change `Long` ("exact session name", "-p, --path  mint a new session at the given directory"), verified against `git show HEAD:cmd/open.go` — so it passes with the entire new match-domain sentence deleted. It is the only guard over that sentence, and the sentence is the only place the terminal says what `/term` searches; a later copy edit can remove it while a subtest named for it stays green.
  FIX: assert the tokens that exist only in the new copy — e.g. `for _, want := range []string{"case-folded", "contiguous run", "recorded directory"}` — keeping the same loop and failure message shape as the sibling subtests.
  CONFIDENCE: high

NOTES:
- Every claim in the rendered block is true of the code as built, checked against the implementation rather than the specification's intent: the recognition rule matches `resolver.IsSearchSigil`; "matched case-folded, as a contiguous run, against … name and its recorded directory" matches `resolver.MatchesSearchTerm` with the fields tested separately and the term literal; the outcomes match `runSearchForm`; "filter empty and ready to type" matches `applySearchLanding`, which sets the focused filtering state for an empty term; the colliding kinds match `validateOpenArgs`'s refusal set exactly, with the hidden `--ack` covered by "composes with nothing" without being surfaced.
- The executor's named judgement call is sound: §9.2's `--help` paragraph does not assign the match-domain bullet at all, so keeping the containment sentence and dropping the "picker's own filter stays fuzzy" contrast is within the assignment rather than a deviation — and the retained sentence contradicts nothing, since the help never mentions the picker's own rule.
- Read as a user, the block does not collide with its neighbours. The "bare target is resolved through the precedence chain" line seven lines above is corrected by the block's own first clause, and the multi-target paragraph below is reconciled by "another target … is a usage error". The `-f` / `/term` separation is usable cold: it states the behavioural difference first and the use each implies second, so a reader who did not already know the answer can choose.
- Two simplifications in the copy, accurate enough to ship but worth knowing: "each live session's" is really the set the picker lists (underscore-prefixed sessions are dropped at source, and the session the caller is inside is dropped by `searchCandidates`), and "its recorded directory" is matched in its home-abbreviated form, so `/Users` does not match a session under `$HOME`. Neither is a false claim and neither is in the assigned `--help` subset.
- Phase-level, for task 5-4: §9.2's bullet "What the search matches, and that it matches by containment while the picker's own filter stays fuzzy" is assigned to neither paragraph explicitly — the `--help` paragraph omits it and the README paragraph says "those same points" plus the completion material. The help now carries the containment half; the fuzzy contrast currently has no home, so the README task should pick it up.
- The help copy is hard-wrapped to the surrounding width, with the two example lines' descriptions landing on the same column as the flags block above, so the block reads as part of the page rather than as an insert.
