## Attempt 1

ISSUES:
- `internal/tui/search_containment_test.go:293-305` — "it reproduces the containment set after a refresh that drops a killed session" does not discriminate containment from the fuzzy rule. Its post-refresh set is `{portal-a1b2, other}`, which `list.DefaultFilter` narrows to `{portal-a1b2}` as well, so the assertion holds with the feature switched off — confirmed empirically: with `m.installSearchFilter()` removed this subtest stays green while 9 of its siblings go red. The acceptance criterion for this leg is explicitly "re-render the containment set **rather than a fuzzy one**", and it is currently unverified. The failure this leaves open is path-specific: a later change that repopulates the sessions list on the refresh path without routing through `rebuildSessionList`'s re-point would silently drop containment for a list refreshed after an external kill — the user sees fuzzy rows reappear — and the suite stays green.
  FIX: put a fuzzy-only decoy in the refreshed set. Extend the fixture this subtest builds from with a session the subsequence rule matches and containment does not — e.g. `{Name: "rust-tools", Windows: 1, Dir: "/opt/projects/rust-tools"}` (p·o·r·t scatters across `/opt/projects/…rust-tools`; no run of `port` appears) — keep dropping a genuine match (`app-port`) in the `SessionsMsg` refresh, and assert `[]string{"portal-a1b2"}`. The decoy needs no `HOME` override, unlike `scatteredSessions`. Re-run with `installSearchFilter` stubbed out to confirm it now goes red.
  ALTERNATIVE: drive the refresh from the existing `scatteredSessions` fixture instead of `groupedFixture` — fewer new lines, but that fixture has no second containment match to lose, so the "drops an externally-killed session" half of the criterion gets weaker. The reviewer recommends the decoy above.
  CONFIDENCE: high

NOTES:
- Same non-discrimination exists for the By Tag arm of `TestSearchContainmentAcrossGroupingModes` (fuzzy happens to agree on that fixture), but that subtest's own stated subject — headers absent, set identical across modes — is verified, and it still fails if By Tag ever stops emitting `SessionItem` rows. Not worth changing; flagged only so it isn't mistaken later for an oversight.
- Residual the length guard cannot close: `filterItems` captures the items slice at `SetItems` time and runs deferred, so if two rebuilds are in flight at once and their item slices have equal length but different content, the earlier pass judges the newer slice by index and applies the verdict to the older rows. It needs a filter already applied, two overlapping rebuild commands, equal counts and unequal content, and it self-heals on the next rebuild — narrower than the pre-existing last-writer-wins staleness `DefaultFilter` already has. The design is the one the task prescribed, so this is an observation, not a change request.
- `TestSearchContainmentNarrowsTheList`'s second subtest is named "it excludes a session the picker's fuzzy rule would have matched" but asserts the `-f` picker *includes* `rust-tools` — the exclusion half lives in the first subtest. The pair is right; only the name on the second is misdescriptive for anyone reading a failure line.
- Both divergences the executor flagged were judged to be the specification's behaviour rather than accommodations: By Tag's reversed survivor order is that mode's own order (§4.4 says rows keep the order of whatever grouping mode is current), and the three-rune term-less test is strictly stronger than the task's literal one rune, where containment and subsequence are indistinguishable.
