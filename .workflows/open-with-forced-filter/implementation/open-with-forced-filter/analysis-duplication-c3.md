AGENT: duplication
FINDINGS:
- FINDING: The session-opening function's expansion is spelled independently in each shell's function body and in that shell's completion registration
  SEVERITY: low
  FAILURE: A future change to what the session-opening function runs updates the emitted function body but not the completion shim beside it, so Tab after `x` asks Portal for the completions of a command line the shell no longer runs — the user gets candidates for a different invocation, or none, and notices only on a Tab press in a shell that has already re-evaluated `portal init`. This is the same disagreement-between-expansion-and-request class the feature was folded open to fix.
  FILES: cmd/init.go:60, cmd/init.go:61, cmd/init.go:70, cmd/init.go:79, cmd/init.go:112, cmd/init.go:132, cmd/init.go:145
  DESCRIPTION: The literal `portal open` appears seven times across `cmd/init.go`, in two roles that must stay in lockstep. Three are what the emitted function *runs* — `%s() { portal open \"$@\"; }` (bash, :79), `function %s\n    portal open $argv\nend` (fish, :112), `function %s() { portal open \"$@\" }` (zsh, :145). Four are what the emitted completion *asks for* — the bash shim's `expansion="portal open"` (:60) and `COMP_WORDS=(portal open …)` (:61), the zsh shim's `words=(portal open …)` (:70), and fish's `complete -c %s -w 'portal open'` (:132). The bash shim carries two of them itself, so its own rewrite of `COMP_LINE`/`COMP_POINT` also depends on the two agreeing. Nothing in the code ties the two roles together: they are seven unrelated string literals, and the shell-driven tests in `cmd/init_completion_shell_test.go` assert the request is `__complete|open|…` as a constant of their own, so they pin the shim's rewrite rather than its agreement with the function body. A body that changed and a shim that did not would leave every one of those assertions green.
  RECOMMENDATION: Declare the expansion once in `cmd/init.go` — a package-level `const openFunctionExpansion = "portal open"` — and interpolate it into all seven sites: the three function-body `Fprintf` formats, fish's `-w` target, and the two shim constants (turning `bashOpenCompletionShim` and `zshOpenCompletionShim` into `Fprintf` formats, or rendering them through `fmt.Sprintf` at emission). Extraction only; no behaviour change and no new emitted text.
SUMMARY: The feature's shared rules — the search-form recognition (`resolver.IsSearchSigil`/`SearchTerm`), the matched field set and containment rule (`resolver.SearchFields`/`MatchesSearchTerm`), the searched set (`tui.PickerSessions` + `currentPickerSession`), the home abbreviation (`resolver.AbbreviateHome`), the `list-sessions` argv and parse (`listSessionsArgs`/`parseSessionList`), the loading-gate dismissal (`dismissLoadingGate`) and the connector selection (`buildSessionConnector`) — are each single-sourced and reached by every consumer, warm and cold route alike. The one lockstep pair left un-tied is the `portal open` expansion in `cmd/init.go`, spelled once for what the shell function runs and again for what its completion asks.
