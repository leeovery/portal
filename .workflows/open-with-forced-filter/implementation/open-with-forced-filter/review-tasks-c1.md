# Review Tasks: Open With Forced Filter (Cycle 1)

## Task 1: Close the containment filter's map-key collision on two distinct rows
severity: high
sources: report-12-2 (12-2-1)

**Problem**: `searchItemSource.set` (`internal/tui/search_filter.go:38`) keys its map on `si.FilterValue()` — the session name and the home-abbreviated recorded directory joined by a space (`internal/tui/session_item.go:87-89` over `resolver.SearchFields`, which drops an empty directory). That value is not injective, so two rows whose joined value coincides but whose fields differ collapse onto one entry and the later item in the slice wins: a session *named* `api ~/Code/api` with no recorded directory (legal — `tmux.ValidateSessionName` refuses only `:`, a leading `$` and a leading `-`) sitting beside a session `api` whose recorded directory abbreviates to `~/Code/api` both key on `"api ~/Code/api"`. Both rows then present the same target to the filter pass and receive the same verdict from whichever entry survived the write. Under the term `api ~`, `MatchesSearchTerm` should match the first on its name and not the second on either field — the field separation `internal/tui/search_containment_test.go:161-172` exists to pin. Nothing in the suite or the compiler observes the collapse.

**Solution**: Keep the value-keyed map — the cross-rebuild property task 12-2 installed (ranks resolve for a generation the source no longer holds, and `list.DefaultFilter` is reached on exactly one condition) must survive — and let a key hold every *distinct* field pair that produced it (`map[string][]searchEntry`, identical pairs collapsing so a session repeated across By-Tag groups still contributes one entry and still ranks). `containmentFilter` then ranks a target only when its key is held and **every** entry under that key matches the term; a key whose entries disagree ranks nothing. That ambiguity rule is derived rather than chosen: the file's own contract (`search_filter.go:58-59` — "A target the source does not hold ranks nothing, so a pass running behind can only omit a row, never surface one the term does not match") and task 12-2's criterion "a target the map does not hold ranks nothing, so the worst a stale pass can do is omit a row already gone" already fix omission as the sanctioned failure direction and surfacing a non-matching row as the forbidden one, and a colliding key is the same class of uncertainty. The two rival shapes fall to that same rule: ranking on *any* matching entry surfaces a row neither of whose fields contains the term, and returning to the positional slice with its generation guard gives up the overlapping-rebuild property 12-2 was written to hold. Guard conditions carried from the review: the fix lands inside `internal/tui/search_filter.go`; `SessionItem.FilterValue()` is **not** re-keyed (`HeaderItem.FilterValue() == ""` is what makes group headings vanish the moment a filter query is typed, guarded by `internal/tui/sessions_grouped_reskin_test.go`); and it lands with a covering case, since nothing observes the collapse today.

**Outcome**: With that pair of sessions live, the term `api ~` surfaces neither row rather than surfacing the row neither of whose fields contains it; every non-colliding key — which is every key on any ordinary install — ranks exactly as it does today, and a stale pass still ranks by containment at the targets' own indexes.

**Do**:
- Widen `searchItemSource.entries` (`internal/tui/search_filter.go:24-27`) to `map[string][]searchEntry`, and have `set` (`:31-43`) append `searchEntry{name: si.Session.Name, dir: si.Session.Dir}` under `si.FilterValue()`, skipping a pair the key already holds so a session repeated across By-Tag groups still contributes one entry. `set` keeps building a fresh map and swapping it under the write hold, so the map `current()` hands back stays safe to read unheld; `current()` stays the single accessor over the retained `sync.RWMutex`.
- Rank a target in `containmentFilter` (`:63-81`) only when the map holds its key with at least one entry **and** `resolver.MatchesSearchTerm(query, entry.name, entry.dir)` is true for every entry under that key — a key whose entries disagree ranks nothing, alongside the key the source does not hold. Ranks keep indexing the `targets` slice the pass was handed, and `query != term` stays the only route to `list.DefaultFilter`.
- Leave `SessionItem.FilterValue()` (`internal/tui/session_item.go:87-89`) and `HeaderItem.FilterValue()` (`:100`) untouched — the empty header value is what flattens the grouped view the moment a query is typed (`TestFiltering_TypingFlattensGroupedView`, `internal/tui/filtering_reskin_test.go:244`; the grouped item shape itself is pinned by `TestGroupingMachineryPreserved`, `internal/tui/sessions_grouped_reskin_test.go:237`). The whole edit lands inside `internal/tui/search_filter.go` and its test file.
- Correct the file's doc comments where they state the superseded one-entry-per-key shape (`search_filter.go:17-23`, `:29-30`, `:53-62`), so no surviving statement contradicts the per-key entry set or the rule that a held-but-disagreeing key ranks nothing.
- Add the covering cases to `internal/tui/search_filter_test.go`, staging the collision with `t.Setenv("HOME", t.TempDir())` and a directory under that home so `resolver.AbbreviateHome` yields `~/Code/api` — a session named `api ~/Code/api` with no recorded directory and a session `api` recorded at `<home>/Code/api` then key alike on `"api ~/Code/api"`.

**Acceptance Criteria**:
- [ ] With that pair of sessions in the source, the term `api ~` ranks neither row — the row whose name contains the run is not surfaced on a verdict borrowed from the other, and the row neither of whose fields contains it is not surfaced at all.
- [ ] A key whose entries all match the term ranks every target carrying it, so a collision whose entries agree suppresses nothing.
- [ ] Every row of a session listed under more than one tag still ranks, and a header row's empty filter value still ranks nothing.
- [ ] A target the source does not hold still ranks nothing, ranks still index the targets the pass was handed, and `list.DefaultFilter` is still reached on exactly one condition.
- [ ] `SessionItem.FilterValue()` and `HeaderItem.FilterValue()` are byte-unchanged, and no file outside `internal/tui/search_filter.go` and `internal/tui/search_filter_test.go` is edited.
- [ ] `go test ./internal/tui` is green, `go test -race ./internal/tui -run TestContainmentFilterIsRaceFreeAgainstAConcurrentSet` is green, and `go test ./...` is green.

**Tests**:
- `"it ranks neither row when two sessions collide on one filter value and only one of them contains the term"`
- `"it ranks both rows when two sessions collide on one filter value and both contain the term"`
- `"it ranks every row of a session listed under more than one tag"` — the existing pin, unchanged, which is what holds the identical-pair collapse
- `"it ranks nothing for a target the source does not hold"` and `"it ranks nothing for a header row's empty filter value"` — existing pins, unchanged
- `"it is race-free against a concurrent set"` — existing pin, unchanged, run under `-race` against the widened map

## Task 2: Run the bash-completion measurement the `init.go` shim edit was gated on
severity: high
sources: report-5-5 (5-5-1)

**Problem**: Task 5-5 gated its `cmd/init.go:65-72` bash-shim edit on a measurement against a real `bash-completion` install, with an explicit void condition in criterion 2 — a measurement contradicting the derivation closes the task with `cmd/init.go` unedited. No measurement *result* is recorded anywhere: `tick-aceea8` carries zero notes, both of the task's commits (`4da790b0f`, `4067537e1`) are single-line with no body, and the implementation directory holds fix-tracking files for 5-2, 5-3 and 5-4 but none for 5-5. The machine still lacks the package (`/usr/share/bash-completion` absent; `/opt/homebrew/share/bash-completion` holds only a `completions` drop directory, and no file on the standard paths defines `_init_completion`), so the precondition the task stated has not changed. The only evidence the shipped shell integration needed changing is a reading of cobra's and bash-completion's sources, and the guard now protecting it (`cmd/init_completion_shell_test.go:36-48`) declares its own `_init_completion` stand-in authored from that same reading — it models the derivation in, so it confirms the shim agrees with the stand-in, never that either agrees with the package a user actually has.

**Solution**: Take the measurement on a bash that carries the `bash-completion` package and let its result decide the shim. Obtain that environment first — a container image carrying the package, or a local `bash-completion` install; neither exists on this machine and a local install changes the developer's machine, so the route is confirmed with the user before anything is installed, and an environment that cannot be obtained is reported back rather than worked around. Run the staging the task prescribes against **both** the pre-edit and the post-edit shim: the emitted `portal init bash` script over the recording stub, **no** `_init_completion` declared, `COMP_WORDS=(x /po extra)` / `COMP_CWORD=1` / `COMP_LINE='x /po extra'` with `COMP_POINT` at the end of `/po`, calling `__start_portal_open`. Record into the task's implementation record the bash and `bash-completion` versions, the `cur` cobra traced, the request the stub recorded, `COMPREPLY`, and whether file completion supplied filenames in its place. If the measured `cur` is the word under the cursor rather than the tail of the line, the finding the edit answered is void: revert `cmd/init.go:66,69-70` to the pinned form per criterion 2 and record that outcome; otherwise the shim stands with the measurement behind it. Guard condition carried from the review: the measurement keeps the unit-lane, no-binary shape it has today — the in-process emitter plus the recording stub — because `internal/portalbintest/lane_guard_test.go` (`TestBuildHelpersStayInTheIntegrationLane`) fails any `_test.go` calling a build helper that does not carry `//go:build integration`, so building or exec'ing `portal` to take the measurement breaches the lane rule.

**Outcome**: The shipped `portal init bash` shim is either confirmed against the package a user actually has or reverted to the pinned form, and the record names the versions, the traced `cur` and the recorded request — so a later reader can tell the gate was run rather than skipped, and the task's void condition is one a measurement could still have triggered.

**Do**:
- Settle the environment with the user before anything is installed: a container image carrying `bash-completion`, or a local install on this machine. Neither exists here (`/usr/share/bash-completion` absent; `/opt/homebrew/share/bash-completion` holds only a `completions` drop directory, and no file on the standard paths defines `_init_completion`), and a local install changes the developer's machine — so the route is the user's call, and an environment that cannot be obtained ends the task with that reported back rather than substituted for.
- Stage the measurement in the shape the unit-lane suite already has, adding no binary to it: emit the script in-process through `emitInitScript` (`cmd/init_test.go:11`), source it under `bash --noprofile --norc` with the recording `portal` stub first on `PATH` (`portalStubScript`, `cmd/init_completion_shell_test.go:24`) and `BASH_COMP_DEBUG_FILE` pointed at a temp file, and let the installed package supply `_init_completion` — the driver declares no stand-in of its own, which is the half of the derivation under test. Nothing here builds or execs a `portal` binary.
- Drive the mid-line Tab against **both** shims: `COMP_WORDS=(x /po extra)`, `COMP_CWORD=1`, `COMP_LINE='x /po extra'`, `COMP_POINT` at the end of `/po`, calling `__start_portal_open` — once against the shipped shim (`cmd/init.go:65-72`) and once against the pre-edit pinned form (`COMP_LINE` rebuilt from the rewritten `COMP_WORDS`, `COMP_POINT` assigned `${#COMP_LINE}`).
- Write the record to `.workflows/open-with-forced-filter/implementation/open-with-forced-filter/measurement-open-with-forced-filter-5-5.md` and point `tick-aceea8`'s notes at it: the bash and `bash-completion` versions, the `cur` cobra traced to the debug file under each of the two shims, the request the stub recorded, `COMPREPLY`, and whether file completion supplied filenames in its place.
- Let the measurement decide the shim. If the `cur` traced under the pre-edit form is the word under the cursor (`/po`) rather than the tail of the line (`/po extra`), the finding the edit answered is void: revert `cmd/init.go:66,69-70` to the pinned form, carry back with it the guards that pin the edited form — the verbatim expectation at `cmd/init_test.go:148` and the four bash cursor cases at `cmd/init_completion_shell_test.go:430-471` — and correct the driver's `_init_completion` stand-in (`:36-48`) to whatever the package was measured to do. Otherwise `cmd/init.go` is left exactly as it is and the record is what now stands behind it.

**Acceptance Criteria**:
- [ ] The route to a `bash-completion`-carrying environment was put to the user and chosen by them before anything was installed; if no route was obtainable, the task closes by reporting that, with no shim edit and no substitute measurement.
- [ ] The measurement ran against the real `_init_completion` the installed package supplies, with no stand-in declared by the driver.
- [ ] The record names the bash version, the `bash-completion` version, the `cur` traced under each of the two shims, the recorded request, `COMPREPLY`, and whether filenames appeared — and `tick-aceea8` points at it.
- [ ] Measured `cur` under the pre-edit form is the word under the cursor ⇒ `cmd/init.go:66,69-70` is back at the pinned form with the guards that pinned the edited form moved with it, and the void outcome is recorded. Any other measured value ⇒ `cmd/init.go` is byte-unchanged.
- [ ] Nothing added for the measurement builds, stages or execs a `portal` binary, so `TestBuildHelpersStayInTheIntegrationLane` (`internal/portalbintest/lane_guard_test.go`) has nothing new to catch and any file left behind in `cmd` stays unit-lane-legal.
- [ ] `zshOpenCompletionShim` (`cmd/init.go:75-80`), the fish emission and both `complete -o default -F __start_portal …` registrations are byte-unchanged on either branch.
- [ ] `go test ./cmd` is green on the branch taken, the verbatim shim expectation in `cmd/init_test.go` matching the shim that ships.

**Tests**:
- The measurement itself is a one-off run recorded in the implementation record, not a new test: the environment it needs is not one every contributor has, and a case that skips when the package is absent would be vacuous on this machine and on the lane that runs here.
- `"it defines the bash open completion shim"` — `cmd/init_test.go:147`, the verbatim expectation, matching whichever form ships.
- `"it completes the word under a mid-line cursor"` and `"it shifts the cursor rather than pinning it to the end of the line"` — `cmd/init_completion_shell_test.go:430`, `:442`; they stand with the edited shim and are removed with it on the void branch.
- `"it preserves typed spacing in the rewritten line"` and `"it completes on a line with leading whitespace"` — `:452`, `:461`, same disposition.
- `"it asks Portal for open's completions"` and `"it leaves the control function request unchanged"` — the end-of-line cases, green unchanged on either branch.
