AGENT: standards
FINDINGS:
- FINDING: The specification still records the command-pending concurrent picker as having no bootstrap gate, which the shipped code no longer matches
  SEVERITY: low
  FAILURE: A maintainer re-deriving the concurrent-bootstrap contract from the specification is told that the `-e <cmd>` / `-- <cmd>` picker "paints an actionable picker from frame one with no loading gate between the user and a half-bootstrapped server". The tree does the opposite: a pick taken while the bootstrap is still in flight is staged, announced in the notice band, and minted only from the bootstrap's terminal completion event, and a bootstrap fatal quits that picker outright. The staged-mint gate, its band copy and its first-stage-wins rule therefore have no owning requirement in the specification. The next person to touch that path reads the specification, finds the gate unaccounted for, and either deletes it as unspecified — reinstating the mint against a mid-restore server that the gate exists to prevent — or adds a second gate beside it. Noticed only when a session is minted while restore is still running, which is the failure the classification was written to stop.
  FILES: .workflows/open-with-forced-filter/specification/open-with-forced-filter/specification.md:314, internal/tui/model.go:1822-1843, internal/tui/model.go:1661-1670, internal/tui/model.go:1685, internal/tui/notice_band.go:35-37, internal/tui/notice_band.go:205-209
  DESCRIPTION: The specification's cold-path section names the command-pending member of the concurrent-bootstrap set as an unresolved live defect, and its dated corrigendum says so explicitly ("tracked as this phase's own consolidation finding"). The fix landed — `createSession` stages the directory when `bootstrapInFlight()`, `BootstrapCompleteMsg` replays it through `mintSession`, `activeProjectNoticeBand` displaces the pick-a-project banner with a wait message, and `BootstrapFatalMsg` quits a command-pending model — but no corrigendum records the resolution, so the specification's body text is now the opposite of the behaviour. It is the only place in this work where the specification and the tree disagree; everything else (the recognition rule, the match-count outcomes, the committed-filter landing, containment-versus-fuzzy by entry point, the argv-composition refusals and their pre-bootstrap timing, the directory column and its left-truncation floor, the picker classification and the teardown warning route, the completion correction across all three shells, and the help/README copy) matches what was decided.
  RECOMMENDATION: Add a dated corrigendum to the specification recording that the command-pending gap named in the cold-path section is closed, and what closed it: a pick taken while the bootstrap is in flight is staged rather than minted, the notice band announces the wait in place of the pick-a-project banner, the first stage wins so a second pick cannot silently re-target it, and a bootstrap fatal ends the run instead of parking on a page that has no error frame. Change no code.
SUMMARY: The implementation conforms to the specification's decisions across recognition, resolution, matching, composition, display, cold-path classification, completion and documentation; the one divergence is a specification paragraph that still describes a defect the tree has since fixed, leaving the staged-mint gate and its user-visible copy without an owning requirement.
