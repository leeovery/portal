AGENT: duplication
FINDINGS:
- FINDING: Gone-session (and unsupported-terminal) user-facing message templates copy-pasted across the CLI and picker paths
  SEVERITY: medium
  FILES: cmd/spawn.go:134, cmd/spawn.go:256, internal/tui/burst_observability.go:83, internal/tui/burst_preflight_abort.go:46, internal/tui/burst_preflight_abort.go:85, cmd/spawn.go:249, internal/tui/burst_progress.go:426
  DESCRIPTION: The pre-flight gone-session sentence `"%s %s gone — nothing opened"` — rendered by threading the already-shared spawn.QuoteJoin + spawn.GoneVerb primitives — is repeated verbatim at FIVE production sites: the CLI abort error (cmd/spawn.go:134, with a "spawn: " prefix), the CLI outcome log (cmd/spawn.go:256), the picker outcome log (internal/tui/burst_observability.go:83), the picker abort banner (internal/tui/burst_preflight_abort.go:46), and the capture-harness seed banner (internal/tui/burst_preflight_abort.go:85). The team correctly extracted the SUB-primitives (QuoteJoin/GoneVerb live in internal/spawn/message.go precisely so the CLI and picker "name sessions identically"), but stopped short of extracting the full sentence — so the literal copy is duplicated 5×. The spec mandates byte-identical naming across the two callers, yet a copy edit ("gone — nothing opened" → anything else) in one site now silently drifts the other four. The identical hazard, at lower multiplicity, exists for the N≥2 unsupported-terminal "— nothing opened" line: cmd/spawn.go:249 (unsupportedSpawnMessage) and internal/tui/burst_progress.go:426 (unsupportedFlashText) hand-build the same `"unsupported terminal — %s · %s — nothing opened"` / `"no host-local terminal — nothing opened"` copy, differing only in the CLI's "spawn: " prefix (the burst_progress.go comment even documents it as a manual mirror). This is genuine copy-paste-drift duplication, distinct from the sanctioned parallel LOG-EMISSION pair (logSpawnSummary/emitBurstSummary) — those emit structurally-parallel records by design; here the concern is the message TEXT, not the emission structure.
  RECOMMENDATION: Add a `spawn.GoneMessage(names []string) string` (and, since it is the same pattern, a `spawn.UnsupportedNoopMessage(id Identity) string`) to internal/spawn/message.go — the established home for cross-caller message parity, alongside QuoteJoin/GoneVerb — returning the bare body (e.g. `"'s2', 's4' are gone — nothing opened"`). All five gone sites and both unsupported sites then call the single renderer; the CLI wraps its returned body with the "spawn: " prefix where it needs it. This structurally enforces the spec's identical-naming requirement instead of relying on five hand-kept copies staying in sync. Do NOT collapse the parallel log-emission methods — only the reused message TEXT moves behind a shared helper.
- FINDING: Native osascript adapter and config recipe adapter duplicate their exec boundary and failure-detail formatter
  SEVERITY: low
  FILES: internal/spawn/ghostty.go:78, internal/spawn/configadapter.go:41, internal/spawn/ghostty.go:174, internal/spawn/configadapter.go:89
  DESCRIPTION: The osascriptRunner and recipeRunner SEAMS (interfaces) are deliberately separate per the design note, and that separation should stay — but the concrete plumbing behind the two seams is duplicated. (1) execOsascriptRunner.Run (ghostty.go:78-90) and execRecipeRunner.Run (configadapter.go:41-53) have BYTE-IDENTICAL bodies: exec.Command(argv[0], argv[1:]...) → log.CombinedOutputWithContext → return (string(out),0,nil) on nil err, else errors.As(*exec.ExitError) → (combineOutput(out,err), code, nil), else → (string(out),0,err). They already jointly consume the shared combineOutput helper, so the exec body is the only remaining un-shared half. (2) failureDetail (ghostty.go:174-186) and recipeFailureDetail (configadapter.go:89-101) are near-identical 13-line blocks that differ ONLY in the never-empty fallback label ("ghostty osascript exit %d" vs "recipe exit %d"). Each pair is currently only two instances, so this is borderline against the Rule of Three — but byte-identical exec-boundary and error-formatting logic is exactly the kind of silent-drift hazard (one side gets a bug-fix or behaviour tweak, the other does not) where a small same-package extraction is cheap insurance, and one is already partway extracted (combineOutput).
  RECOMMENDATION: Extract two package-private helpers in internal/spawn: `runArgvCombined(argv []string) (out string, exitCode int, err error)` holding the shared exec→combined-output→exit-code body, and `execFailureDetail(out string, exitCode int, err error, fallbackLabel string) string` holding the shared detail-formatting logic parameterised by the fallback label. Both execOsascriptRunner.Run / execRecipeRunner.Run call the former; both failureDetail / recipeFailureDetail call the latter (passing "ghostty osascript exit %d" resp. "recipe exit %d"). The two Adapter implementations and their two distinct runner interfaces remain fully separate — only the identical exec/formatting plumbing is shared, so the deliberately-separate-seam design intent is preserved.
SUMMARY: Two genuine duplication clusters in the spawn feature: (1) the gone-session / unsupported-terminal user-facing message text is hand-copied across 5 (+2) CLI and picker sites despite the sub-primitives already being shared — extract a spawn.GoneMessage/UnsupportedNoopMessage renderer to enforce the spec-mandated identical naming; (2) the native osascript adapter and config recipe adapter carry byte-identical exec-boundary Run bodies and near-identical failure-detail formatters — extract shared package-private helpers while keeping the deliberately-separate seams intact. Sanctioned parallel implementations (the logSpawnSummary/emitBurstSummary emission pair, QuoteJoin/GoneVerb, the separate recipeRunner/osascriptRunner seams themselves) were reviewed and left untouched.
