AGENT: standards
FINDINGS:
- FINDING: Changed files are not gofmt-clean — doc-comment quote transform mangles the '\'' idiom notation
  SEVERITY: low
  FILES: internal/spawn/ghostty.go:41, internal/spawn/ghostty.go:71, internal/spawn/ghostty_command_test.go:52, internal/spawn/ghostty_command_test.go:69, internal/spawn/ghostty_command_test.go:78
  DESCRIPTION: Both changed files fail `gofmt -l` under the standard go1.26.5 toolchain (`gofmt internal/spawn/ghostty.go | cmp -` differs at char 2117, line 41). gofmt's doc-comment formatter applies the TeX/typographic quote convention, converting the two trailing ASCII apostrophes `''` inside a declaration doc comment into a curly right-double-quote `”` (U+201D), and reflows the surrounding comment. Minimal repro confirmed: `// ... idiom ('\'') here.` → `// ... idiom ('\”) here.`. The affected comments are precisely the ones documenting the load-bearing POSIX `'\''` close-escape-reopen idiom on `wrapWithShellFallback`/`ghosttyEmbed` and the golden-literal derivation notes in the test. The golang-code-style skill lists `gofmt`/`gofumpt` among the automatically-enforced rules, and gofmt-clean is the universal Go convention. The concrete hazard is a trap: the naive "fix" (`go fmt ./...` or editor format-on-save) would SILENTLY rewrite `'\''` → `'\”` in the very comments that explain the fix's quote nesting, corrupting accurate documentation. Mitigating context (why low, not medium): (a) this is a pre-existing codebase pattern — the shared `internal/spawn/recipe.go` `shellQuote` doc comment documents the identical idiom and is likewise flagged by `gofmt -l`, so the new files simply match established local style; (b) the project's actual lint gate (`.golangci.yml`: `default: standard` + `modernize`) does NOT enable the gofmt/gofumpt linters, so `golangci-lint run` does not currently catch it. No behavioral or spec-conformance impact — comments only.
  RECOMMENDATION: Reword the affected doc comments so they are both gofmt-clean AND keep the idiom accurate — e.g. reference the idiom without a bare trailing `''` pair that gofmt reinterprets as a close-quote (write it as `` `'\''` `` in backticks, or phrase as "the close-escape-reopen sequence backslash-quote" / "the '\'  '' three-part idiom" with an intervening character), so a future `go fmt`/format-on-save cannot mangle `'\''` into `'\”`. Verify with `gofmt -l internal/spawn/ghostty.go internal/spawn/ghostty_command_test.go` returning empty. Consider the same touch-up on the pre-existing `recipe.go` comment for consistency (out of this plan's scope — note only).
SUMMARY: The Ghostty shell-fallback fix conforms fully to the specification (bash -lc wrapper via the shared shell-quote helper, wait-after-command dropped, PATH/-u TMUX prefix preserved, composeOpenArgv/renderCommandString/syscall.Exec untouched, and all Testing-Requirements unit coverage present including the quote-sensitive mint fixture and exact-byte golden literals). The only standards deviation is a low-severity gofmt non-conformance in the doc comments documenting the '\'' idiom — cosmetic, consistent with pre-existing code, and not caught by the project's lint config, but a latent trap since format-on-save would corrupt that documentation.
