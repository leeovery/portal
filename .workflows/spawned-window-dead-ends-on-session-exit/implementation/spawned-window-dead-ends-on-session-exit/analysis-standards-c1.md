AGENT: standards
FINDINGS:
- FINDING: Test attach fixture uses the retired `attach` verb and omits `--ack`, contradicting the spec's composed-argv shape
  SEVERITY: low
  FILES: internal/spawn/ghostty_command_test.go:9-17
  DESCRIPTION: `realAttachArgv()` returns `/usr/bin/env … /abs/portal attach proj-abc123`, and its doc comment claims it is "a representative composed attach argv (as Task 2.3 builds it)." Two divergences: (1) it uses the `attach` verb, which the codebase explicitly retired — CLAUDE.md states "The former `portal attach` command is retired — a spawned burst window now runs `portal open --session <name>`," and spec Acceptance Criterion 3 fixes the attach argv as `open --session <name> --ack <batch>:<token>`. (2) It omits the `--ack <batch>:<token>` element that `composeOpenArgv` (Task 2.3) always appends. So the fixture cannot be what Task 2.3 builds — `composeOpenArgv` would emit `open --session proj-abc123 --ack batch:token`, never `attach proj-abc123`. The wrap under test is argv-agnostic, so the round-trip and shape assertions remain valid and no production behaviour is affected; this is purely a fixture-accuracy / stale-verb drift. Note the sibling `mintArgvWithSpecials()` fixture correctly routes through `composeOpenArgv`, making the hand-rolled attach fixture the inconsistent one. Left as-is it can mislead a maintainer into thinking `attach` is still the spawned verb.
  RECOMMENDATION: Rebuild `realAttachArgv()` via `composeOpenArgv(exePath, path, Surface{Kind: SurfaceAttach, Value: "proj-abc123"}, "batch1", "tok1", nil)` so the attach fixture matches the real composed shape (`open --session … --ack …`) and stays consistent with the mint fixture, or at minimum replace `"attach", "proj-abc123"` with `"open", "--session", "proj-abc123", "--ack", "batch1:tok1"` and correct the doc comment.
SUMMARY: The core fix conforms tightly to the specification — the Ghostty adapter wraps the composed argv in `bash -lc '<rendered argv>; exec "$SHELL" -il'` via the shared `renderCommandString`/`shellQuote` helper (correct `'\''` nesting), drops `wait after command`, preserves the PATH/`-u TMUX` prefix, keeps the wrap Ghostty-scoped (config adapters still call `renderCommandString` un-wrapped), and leaves `composeOpenArgv`/`renderCommandString`/the connectors untouched; unit coverage exercises the quote-sensitive mint passthrough and round-trips uncorrupted. The only drift is a low-severity stale `attach` verb in a hand-rolled test fixture.
