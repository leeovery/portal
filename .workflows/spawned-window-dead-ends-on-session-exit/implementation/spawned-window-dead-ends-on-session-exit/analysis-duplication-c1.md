AGENT: duplication
FINDINGS: none
SUMMARY: No significant duplication detected across the three implementation files. The production change adds a single new helper (wrapWithShellFallback in internal/spawn/ghostty.go) that reuses the existing shared renderCommandString encoder rather than re-implementing quoting; the wrap logic appears in exactly one place and is not duplicated in command.go, configadapter.go, or any other adapter. The new test helpers (decodeRenderedArgv, reverseAppleScriptEscape, mintArgvWithSpecials) are genuinely novel inverses/fixtures with no pre-existing equivalent in the spawn test suite, and the one intentionally-duplicated constant (shellFallbackSuffix) is a documented test-independence choice pinning production bytes independently — a single line, below the extraction bar. The parallel osascriptRunner/recipeRunner and failureDetail seams already share their plumbing via runArgvCombined/execFailureDetail and are pre-existing (untouched by this implementation), so they are out of scope.
