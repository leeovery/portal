AGENT: duplication
FINDINGS: none
SUMMARY: No significant duplication detected across the implementation files. The production change (internal/spawn/ghostty.go) adds a single small wrapWithShellFallback function that cleanly reuses the shared renderCommandString helper rather than re-implementing shell-quoting, and ghosttyEmbed's escaping logic is unchanged pre-existing code. The test file (ghostty_command_test.go) shares the realAttachArgv() fixture with the pre-existing ghostty_openwindow_test.go (reuse, not duplication), and its decodeRenderedArgv shell-quote decoder has no twin elsewhere (recipe_test.go asserts renderCommandString via golden literals, not a decoder). The only repetition — two structurally-similar golden-literal subtests and two round-trip assertion patterns in TestGhosttyEmbed* — is two-instance, ~12-line test-oracle code that sits below the Rule-of-Three / proportional extraction threshold and where explicitness is deliberate (independent hand-authored oracles per acceptance criterion #7).
