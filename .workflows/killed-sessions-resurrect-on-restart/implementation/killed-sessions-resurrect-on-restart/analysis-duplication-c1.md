# Duplication Findings — killed-sessions-resurrect-on-restart (cycle 1)

```
AGENT: duplication
FINDINGS:
- FINDING: `fakeSleep` test type defined identically in two packages
  SEVERITY: medium
  FILES: /Users/leeovery/Code/portal/internal/state/signal_hydrate_test.go:19-26, /Users/leeovery/Code/portal/cmd/state_signal_hydrate_test.go:21-28
  DESCRIPTION: When `WriteFIFOSignal` / `signalHydrateRetryDelays` were extracted from `cmd` into `internal/state` (Fix 1's "Sharing mechanism" — moving the retry primitive to a shared internal package), the test-only `fakeSleep` recorder type travelled with the new tests but was not consolidated. Both packages now declare a struct with one `Durations []time.Duration` field and a `fn() func(time.Duration)` method that returns a closure recording each duration — byte-identical bodies. Each side of the migration has its own private fake instead of one shared helper. This is the "near-duplicate logic" pattern code-quality.md flags as DRY drift after a primitive moves across package boundaries.
  RECOMMENDATION: Move `fakeSleep` (and its `fn()` method) into a shared test helper. Either export it from `internal/state` as a test-only `state.RecordingSleep` (in a `_test.go` file with appropriate visibility), or lift it into a new `internal/statetest` package alongside the existing `internal/restoretest` / `internal/tmuxtest` test-only helpers. Both consumer test files then import the shared type. Net deletion: ~8 lines + one type declaration.

- FINDING: Hand-rolled sessions.json seeding repeated across six bootstrap integration tests
  SEVERITY: medium
  FILES: /Users/leeovery/Code/portal/cmd/bootstrap/eager_signal_hydrate_integration_test.go:172-197, /Users/leeovery/Code/portal/cmd/bootstrap/eager_signal_hydrate_integration_test.go:339-364, /Users/leeovery/Code/portal/cmd/bootstrap/phase2_hook_fire_integration_test.go:131-156, /Users/leeovery/Code/portal/cmd/bootstrap/phase5_integration_test.go:152-174, /Users/leeovery/Code/portal/cmd/bootstrap/phase5_integration_test.go:246-267, /Users/leeovery/Code/portal/cmd/bootstrap/phase5_marker_suppression_integration_test.go:89-112
  DESCRIPTION: Every new bootstrap-integration test added in this work unit re-implements the same ~20-line block: build a `state.Index`, loop over session names, append a single-window/single-pane `state.Session` with `Index: 0, Layout: "tiled", Active: true, ScrollbackFile: "scrollback/<name>-w0-p0.bin"`, call `state.EncodeIndex`, fail-on-error, `os.WriteFile(state.SessionsJSON(stateDir), data, 0o600)`, fail-on-error. The shape is identical at all six sites; only the session-name list and (in one case) `SavedAt` differ. A near-identical `seedSessionsJSON` / `seedSessionsJSONWithSavedAt` helper already exists at /Users/leeovery/Code/portal/cmd/reattach_integration_test.go:199-236 but is in a different test package (`cmd` vs `bootstrap_test`) and Go test-file symbols are not visible across packages — so each new bootstrap-integration test wrote its own copy. This compounds the "two builders" tax already documented in /Users/leeovery/Code/portal/cmd/bootstrap/orchestrator_builder_test.go:17-21.
  RECOMMENDATION: Promote the existing `seedSessionsJSON` / `seedSessionsJSONWithSavedAt` helpers into a non-test-file location reachable by both packages — either `internal/restoretest` (already imported by every site touched here) or a new `internal/statetest` leaf. After promotion, the six call sites collapse to a one-liner per site, and the local copy in `cmd/reattach_integration_test.go` deletes in favour of the shared one. Same one-helper-many-sites consolidation rationale that motivated `buildIntegrationOrchestrator` originally.

- FINDING: FIFO-writer goroutine boilerplate repeated 32 times in one file
  SEVERITY: low
  FILES: /Users/leeovery/Code/portal/cmd/state_hydrate_test.go (32 occurrences of the same 5-line goroutine pattern at lines 71, 119, 153, 186, 222, 251, 289, 328, 375, 412, and ~22 more)
  DESCRIPTION: Almost every hydrate test that exercises the success or file-missing path opens the test FIFO O_WRONLY in a goroutine, writes a single byte, and closes it: `go func() { f, _ := os.OpenFile(fifo, os.O_WRONLY, 0); _, _ = f.Write([]byte("X")); _ = f.Close() }()`. It appears 32 times in a single file. Each occurrence is structurally identical; only the surrounding `cfg` and assertions vary. The work unit added several new tests of this shape (timeout-path, hook-firing-on-timeout) which extended an already load-bearing duplication rather than addressing it. There is precedent for extraction in the same file: `timeoutCfg`, `instantTimeoutOpenFIFO`, `seedHookStore`, and `makeFIFO` were all extracted as helpers — but the "kick the FIFO from a goroutine" primitive was not.
  RECOMMENDATION: Extract a `signalFIFOAsync(t *testing.T, fifo string)` helper alongside `makeFIFO` at the top of `cmd/state_hydrate_test.go` that launches the goroutine and writes the single byte. Net deletion at each call site: 5 lines → 1 line. Optionally pair it with a `makeAndSignalFIFO` convenience for the dozen-plus sites that always do both. Out-of-package promotion is unnecessary — the helper is only consumed inside `cmd`.

- FINDING: Functionally identical poll-for-file-existence sentinel helpers in two integration tests
  SEVERITY: low
  FILES: /Users/leeovery/Code/portal/cmd/bootstrap/phase2_hook_fire_integration_test.go:250-262 (`pollUntilSentinelPresent`), /Users/leeovery/Code/portal/internal/restore/exit_closes_pane_integration_test.go:461-476 (`awaitSentinelExists`)
  DESCRIPTION: Two integration tests added in this work unit independently implemented a `for time.Now().Before(deadline) { if _, err := os.Stat(path); err == nil { return }; time.Sleep(tick) }` poll-for-sentinel helper. Names differ, one takes the tick as a parameter and the other hardcodes 50ms, and the failure messages diverge — but the essence is identical: poll `os.Stat` for an "expected to be created by the helper's hook command" sentinel file and fail-with-diagnostic if absent within the budget. This is the "compose, don't duplicate" anti-pattern called out in code-quality.md: parallel implementations of the same primitive that must be kept in sync.
  RECOMMENDATION: Promote one shared `WaitForFileExists(t, path, budget, tick)` helper into `internal/restoretest` (already imported by both call sites). Both existing sites then dispatch through the shared helper with their AC-specific failure-message wrappers. Net deletion: ~30 lines across two files; the result becomes the canonical "is the in-pane hook command's sentinel write observable yet" primitive future hook-firing tests can reuse.

SUMMARY: Four duplications surfaced as the work unit added new code: one test-helper type (`fakeSleep`) was duplicated when `WriteFIFOSignal` migrated cross-package without consolidating its test seam; the sessions.json-seeding boilerplate repeated 6 times across new bootstrap-integration tests despite an existing helper in a sibling test package; a single-byte FIFO-writer goroutine pattern was extended to 32 occurrences in `cmd/state_hydrate_test.go` (the work unit's new timeout/hook tests grew the duplication rather than addressing it); and two near-identical `WaitForFileExists`-shape sentinel-poll helpers were independently authored in `cmd/bootstrap` and `internal/restore` integration suites. None blocks correctness; all are mechanical extract-and-reuse cleanups.
```
