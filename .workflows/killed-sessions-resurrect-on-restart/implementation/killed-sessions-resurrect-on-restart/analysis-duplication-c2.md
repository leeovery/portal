# Duplication Findings — killed-sessions-resurrect-on-restart (cycle 2)

```
AGENT: duplication
FINDINGS:
- FINDING: Near-identical `state.FIFOSignaler` recording fakes in two test packages
  SEVERITY: medium
  FILES: /Users/leeovery/Code/portal/cmd/state_signal_hydrate_test.go:27-48, /Users/leeovery/Code/portal/cmd/bootstrap/eager_signal_hydrate_test.go:18-43
  DESCRIPTION: Two consumer test packages each declare a private struct that records every `state.FIFOSignaler.SendSignal(path)` call into `calls []string`, supports a per-path `errOn map[string]error`, supports a global `err error`, and dispatches in the same priority order (global err → per-path err → nil). The bodies are byte-equivalent — only the type names differ (`recordingSignaler` vs `recordingFIFOSignaler`). Both even ship the same `var _ state.FIFOSignaler = (*T)(nil)` compile-time assertion. This is the same DRY-drift-after-cross-package-promotion pattern cycle 1 fixed for `fakeSleep` (promoted to `internal/statetest.RecordingSleep`): when a primitive (`state.FIFOSignaler`) is shared across package boundaries, its recording test seam should also be shared rather than re-declared in each consumer test package because Go test-file symbols are not visible across packages. Cycle 1's fix established the precedent (`internal/statetest`); cycle 2's `state.FIFOSignaler` consumers did not follow it.
  RECOMMENDATION: Promote one canonical recording fake — `statetest.RecordingFIFOSignaler` — into `internal/statetest/` alongside the existing `RecordingSleep`. The new helper lifts the `calls / errOn / err` triple plus the `SendSignal` method body verbatim. Both existing sites then import it and delete their private copies (≈22 lines deletion per site). The `internal/statetest` package is already established as the canonical home for `state.*` test seams (sleep_recorder.go's package doc explicitly anticipates "two consumer test packages each duplicated the same fake before this package was introduced"); a sibling `signal_recorder.go` is the natural extension.

- FINDING: Three integration tests redundantly hand-wire an explicit `EagerSignalCore` that exactly duplicates `NewWithDefaults`' auto-default
  SEVERITY: low
  FILES: /Users/leeovery/Code/portal/cmd/bootstrap/eager_signal_hydrate_integration_test.go:209-214, /Users/leeovery/Code/portal/cmd/bootstrap/eager_signal_hydrate_integration_test.go:344-349, /Users/leeovery/Code/portal/cmd/bootstrap/phase2_hook_fire_integration_test.go:181-186
  DESCRIPTION: Three integration tests pass `EagerSignaler: &bootstrap.EagerSignalCore{Markers: client, StateDir: stateDir, Signaler: state.DefaultFIFOSignaler{}, Logger: logger}` through `buildIntegrationOrchestrator(...)`. Cycle 1's `NewWithDefaults` helper auto-defaults `EagerSignaler` to a real `*EagerSignalCore` with the same field values (`Markers: server`, `StateDir: stateDir`, `Signaler: state.DefaultFIFOSignaler{}`, `Logger: logger`) when `WithRestore` is supplied with a non-nil Restorer (defaults.go:182-195). All three of these test sites supply `WithRestore` with a real `RestoreAdapter` — so removing the explicit `EagerSignaler` field would produce a byte-identical orchestrator. The 6-line literal at each site is dead repetition of the helper's documented contract; the auto-default branch is even verified by dedicated unit tests (`TestBuildIntegrationOrchestrator_EagerSignalerDefaultsToRealWhenRestoreReal`, `TestNewWithDefaults_EagerSignalerDefaultsToRealWhenRestoreReal`). Three sites of "explicitly wire the helper's auto-default" violates the cycle 1 promotion's own docstring intent: "the EagerSignaler conditional default-to-real semantics ... is computed inside the helper".
  RECOMMENDATION: Drop the explicit `EagerSignaler:` field from all three sites. The remaining `Restore` field is sufficient; `NewWithDefaults` will produce the same `*EagerSignalCore` automatically. Net deletion: ≈18 lines (6 lines × 3 sites). Existing dedicated unit tests already pin the auto-default contract — removing the explicit wiring removes a redundancy that obscures which fields the test actually needs to pin (vs which it merely re-states).

SUMMARY: Cycle 1's seven cleanup tasks landed correctly (RecordingSleep, SeedSessionsJSON, signalFIFOAsync, WaitForFileExists, FIFOSignaler typed seam, NewWithDefaults, sh -c comment fixes are all in use). Two new duplications surfaced in cycle 2: a `state.FIFOSignaler` recording fake duplicated across two test packages following cycle 1's `state.FIFOSignaler` extraction (the same near-duplicate-helper-after-cross-package-promotion pattern cycle 1 fixed for `fakeSleep`/`RecordingSleep`), and three integration-test sites that hand-wire an `EagerSignalCore` literal exactly duplicating what `NewWithDefaults`' auto-default would produce. Both are mechanical extract-and-reuse cleanups; neither blocks correctness.
```
