# Standards Findings — killed-sessions-resurrect-on-restart (cycle 7)

```
AGENT: standards
FINDINGS:
- FINDING: gofmt -l flags internal/restore/session.go due to a `'\''` token in the buildHydrateCommand docstring that Go 1.26's gofmt smart-punctuation rewrites to typographic curly quotes; the file was clean on the work unit's pre-baseline
  SEVERITY: low
  FILES: /Users/leeovery/Code/portal/internal/restore/session.go:425
  DESCRIPTION: gofmt is one of the three declared linters for this work unit (manifest.json `linters[]` lists `go vet`, `gofmt -l .`, and `golangci-lint`). Running `gofmt -l .` from repo root reports six files dirty; five of those (`internal/browser/listing.go`, `internal/resolver/gitroot_test.go`, `internal/resolver/query_test.go`, `internal/resolver/zoxide_test.go`, `internal/session/create.go`) predate this work unit on main and are out of scope. `internal/restore/session.go` is the sole in-scope offender — it was actively rewritten by T3-1 (buildHydrateCommand bare form) and T8-1 (shellQuoteSingle deletion), and gofmt running against `main~50:internal/restore/session.go` (the pre-work-unit baseline of that file) returns clean, so the drift was introduced by this work unit. The exact site is line 425 of the buildHydrateCommand docstring: the literal sequence `` `'\''` `` (backtick + single-quote + backslash + single-quote + single-quote + backtick) describing the canonical POSIX close-escape-reopen shell idiom is rewritten by `gofmt -d` to a form using a typographic right-double-quotation-mark U+201D. Go 1.26's gofmt now applies Markdown-style smart-punctuation inside doc comments and the three-consecutive-single-quote run is the trigger. The docstring's content is still accurate and intentional — it documents why the bare buildHydrateCommand form is safe under Portal's sanitization — but the line as written fails the project's declared lint gate.
  RECOMMENDATION: Rewrite the docstring line at internal/restore/session.go:425 to avoid the bare `` `'\''` `` token. Two equivalent options: (a) reword to describe the idiom without using its literal form — e.g. drop the `'\''` literal entirely since it is illustrative, not load-bearing; or (b) escape the single quotes to a form gofmt's smart-punctuation rule does not match — e.g. backtick-escape each apostrophe individually or split the run across two backtick spans. After the edit, `gofmt -l internal/restore/session.go` must return clean. Verify no other in-scope file regressed by re-running `gofmt -l .` and confirming the result set matches the pre-fix list minus session.go.

SUMMARY: One low-severity gofmt drift introduced by this work unit at internal/restore/session.go:425 — a literal `'\''` shell-idiom token in the buildHydrateCommand docstring trips Go 1.26's smart-punctuation rewrite and fails the work unit's declared `gofmt -l .` lint gate. Cycle 6's two cleanup tasks landed cleanly (RestoreAdapter preamble collapsed across phase5_integration_test.go and reboot_roundtrip_test.go, openTestLogger shim deleted from orchestrator_builder_test.go, duplicate cmd-package OpenFIFOForSignal test removed). Spec conformance otherwise unchanged from cycle 6.
```
