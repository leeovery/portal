# Architecture Findings — killed-sessions-resurrect-on-restart (cycle 6)

```
AGENT: architecture
FINDINGS:
- FINDING: cmd/bootstrap_test's openTestLogger is now a one-line shim that exists solely to preserve 12 in-package call sites untouched, with no semantic value beyond that
  SEVERITY: low
  FILES: /Users/leeovery/Code/portal/cmd/bootstrap/orchestrator_builder_test.go:112-120
  DESCRIPTION: T8-2 promoted the OpenLogger + Cleanup preamble into restoretest.OpenTestLogger (internal/restoretest/logger.go) and migrated cmd/reattach_integration_test.go's buildReattachOrchestrator to call it directly. The same task left a single-line wrapper at cmd/bootstrap/orchestrator_builder_test.go:118-120 whose entire body is `return restoretest.OpenTestLogger(t, stateDir)`. The doc-comment at lines 112-117 explicitly states the wrapper's only purpose: "Now a one-line delegate to restoretest.OpenTestLogger — promoted from this file so the same helper is shared with cmd/reattach_integration_test.go's buildReattachOrchestrator. The 12 in-package call sites are preserved; the helper signature is unchanged."
    The architectural concern: the wrapper has zero behavioural value (signature, body, and contract are byte-identical to the imported helper) and exists exclusively to avoid touching 12 lines at the call sites. A future maintainer looking at this file will see a same-named symbol at two different package paths (cmd/bootstrap_test.openTestLogger and restoretest.OpenTestLogger) and rightly ask "why does this indirection exist?" — the answer (deliberate-scope-limit on T8-2) is itself the wart. The migration's whole motivating value (single source of truth) is partially undermined: there are now two named call sites for the same canonical helper, and a future cycle wanting to add (say) configurable rotation behaviour to the shared helper has to thread it through both surfaces.
    Two architectural failure modes follow: (a) drift — a future edit to restoretest.OpenTestLogger that should propagate to cmd/bootstrap_test callers has to be re-checked against the local wrapper's signature/contract; (b) maintainer confusion — the doc-comment's "12 in-package call sites are preserved" reads as a load-bearing structural reason, but it is purely a scope-of-work reason (T8-2 chose not to migrate them); a future contributor reading the doc may treat preservation as a contract rather than convenience.
    This is also the same drift class cycles 3-5 each paid down for adjacent surfaces (T6-1 doc-comment cross-references, T6-2 NewRestoreAdapter constructor, T7-1 marker-poll consolidation): the pattern is "extract a canonical helper, but leave a local wrapper at the original site for compatibility". Each cycle's residual finding has been the same shape: the wrapper itself becomes a wart. Notably T7-1 took the opposite shape — it collapsed pollUntilMarkersCleared into restoretest.WaitForSkeletonMarkersCleared at all seven call sites in one go, eliminating the wrapper entirely.
  RECOMMENDATION: One mechanical edit. Find/replace `openTestLogger(` → `restoretest.OpenTestLogger(` across the 12 in-package call sites in cmd/bootstrap (eager_signal_hydrate_integration_test.go, phase2_hook_fire_integration_test.go, scrollback_resumption_test.go, phase5_integration_test.go, phase5_marker_suppression_integration_test.go, reboot_roundtrip_test.go), then delete the wrapper at orchestrator_builder_test.go:118-120 (and the unused `state` import line if it becomes orphaned after the wrapper removal — verify by build). The migration mirrors the precedent set by T7-1 (which collapsed all seven call sites in one go) rather than the half-migration shape of T8-2. Net deletion: ~9 lines plus one shim function. After this edit, restoretest.OpenTestLogger is the single named call site for the helper across the entire codebase, and the doc-comment's preservation claim becomes self-evident from grep rather than load-bearing on the file.

SUMMARY: Cycle 5's two cleanup tasks landed cleanly — shellQuoteSingle was deleted (T8-1), buildReattachOrchestrator migrated to NewRestoreAdapter + the new shared OpenTestLogger helper (T8-2), and the cycle-5 finding's latent shell-parse bug for `'`-bearing session names is fully resolved. One residual low-severity item surfaced: T8-2 left a 1-line shim wrapper at cmd/bootstrap/orchestrator_builder_test.go:118-120 whose only purpose is to preserve 12 in-package call sites untouched; the wrapper itself has zero behavioural value (byte-identical to restoretest.OpenTestLogger) and propagates a same-named-symbol-in-two-places drift surface that earlier cycles' parallel migrations (T7-1 in particular) avoided by completing the call-site migration in the same task. Behavioural correctness for AC1-AC8 is unaffected; the issue is structural-housekeeping plus a doc-comment rationale that reads as load-bearing but is in fact scope-limit-of-T8-2. Fixable by a mechanical 12-site find/replace plus deletion of the shim.
```
