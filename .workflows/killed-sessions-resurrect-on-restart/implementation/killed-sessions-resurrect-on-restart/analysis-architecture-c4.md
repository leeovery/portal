# Architecture Findings — killed-sessions-resurrect-on-restart (cycle 4)

```
AGENT: architecture
FINDINGS:
- FINDING: NewRestoreAdapter docstring justifies the production-site open-coded form with an inaccurate claim, embedding a misleading architectural rationale at the canonical constructor's documentation
  SEVERITY: low
  FILES: /Users/leeovery/Code/portal/internal/bootstrapadapter/adapters.go:93-103, /Users/leeovery/Code/portal/cmd/bootstrap_production.go:111-122
  DESCRIPTION: T6-2 introduced NewRestoreAdapter as "the single canonical constructor for integration-test sites" and explicitly excluded the production wiring at cmd/bootstrap_production.go. The exclusion's stated reason (lines 98-99) is: "Production wiring at cmd/bootstrap_production.go retains its open-coded form by design (that site reuses the inner Orchestrator beyond the adapter)." Inspecting cmd/bootstrap_production.go, restoreInner is declared at line 111 and used at exactly one site — line 122, Restore: &bootstrapadapter.RestoreAdapter{Inner: restoreInner}. There is no reuse beyond the adapter; grep -n "restoreInner" /Users/leeovery/Code/portal/cmd/bootstrap_production.go returns only the declaration and the single wrapping call.
    The architectural concern is the same drift class cycle 3's T6-1 paid down for recordingFIFOSignaler / writeFIFOSignal: a doc-comment names a load-bearing seam relationship (here, the rationale for an asymmetric usage policy) that no longer matches the source. A future maintainer reading the constructor's doc and trying to understand "why doesn't production use this constructor too?" will (a) trust the doc and look for the reuse pattern that justifies the carve-out, find none, and conclude the doc is stale (and the surrounding wiring may be too); or (b) faithfully preserve the open-coded form everywhere they edit bootstrap_production.go because the doc told them production had a structural reason to keep it open-coded. Either failure mode is the same shape T6-1 fixed elsewhere.
    Production at cmd/bootstrap_production.go:111 could call NewRestoreAdapter(client, stateDir, logger) and the result would be byte-identical (same inner Orchestrator field set, same outer adapter wrap). The actual reason production stays open-coded is "T6-2's planning task scoped its adoption to the four new integration sites added by this work unit" (per analysis-tasks-c3.md task 2 step 3) — a planning-scope reason, not a structural-reuse reason.
  RECOMMENDATION: One-line edit to NewRestoreAdapter's docstring. Replace lines 97-99 ("Production wiring at cmd/bootstrap_production.go retains its open-coded form by design (that site reuses the inner Orchestrator beyond the adapter).") with an accurate reason — either drop the carve-out justification entirely (the constructor's contract is descriptive enough without it) or replace with the actual rationale, e.g. "Production wiring at cmd/bootstrap_production.go retains its open-coded form for parity with the surrounding inline-struct adapters at that site (HookRegistrar, RestoringMarker, EagerSignalCore, MarkerCleanupCore); migrating it is mechanical and out of scope for the constructor's introduction." Same shape of fix T6-1 applied to the three doc-comment surfaces in cycle 3.

SUMMARY: Cycle 3's two cleanup tasks (T6-1 doc refresh, T6-2 NewRestoreAdapter constructor) both landed cleanly and resolved the cycle-3 architecture finding's three doc-drift cases. One residual low-severity item: NewRestoreAdapter's docstring justifies the production-site open-coded carve-out with a structural-reuse claim that does not match the source — restoreInner in cmd/bootstrap_production.go is used exactly once at the adapter wrap and never reused beyond it. This is the same doc-drift class cycle 3 fixed elsewhere, surfacing at the new constructor's own documentation. Nothing blocks behavioural correctness or acceptance criteria; the work unit's architecture is otherwise sound across the cycle-1 through cycle-3 follow-throughs.
```
