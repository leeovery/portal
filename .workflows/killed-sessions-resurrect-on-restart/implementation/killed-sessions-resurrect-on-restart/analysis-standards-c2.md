# Standards Findings — killed-sessions-resurrect-on-restart (cycle 2)

```
AGENT: standards
FINDINGS:
- FINDING: CLAUDE.md step-6 description references the wrong production primitive post-Task-1
  SEVERITY: low
  FILES: /Users/leeovery/Code/portal/CLAUDE.md:78
  DESCRIPTION: The "Server bootstrap" section describes EagerSignalHydrate as writing the signal byte "via `state.WriteFIFOSignal`". After cycle-1 Task 1 landed, the production wiring at `cmd/bootstrap_production.go:131-135` injects `state.DefaultFIFOSignaler{}`, whose `SendSignal` delegates to `state.SendHydrateSignal` — the no-seam production entry point that pins `OpenFIFOForSignal` + `time.Sleep`. `state.WriteFIFOSignal` is now the seam-bearing variant retained strictly for retry-ladder unit tests in `internal/state/signal_hydrate_test.go`. Spec § Definition of Done item 4 mandates `CLAUDE.md` is updated as part of the same PR; the prior update (commit 2914d0de) correctly inserted the new step but the post-Task-1 primitive rename was not propagated into this paragraph. Other surfaces (`cmd/bootstrap/eager_signal_hydrate.go:25-28`, `cmd/state_signal_hydrate.go:13-15`, `cmd/bootstrap_production.go:126-127`) describe the post-Task-1 split correctly — only this single CLAUDE.md line is stale, mirroring cycle 1's class of doc-drift findings (live behaviour described inaccurately in a load-bearing reference). Cosmetic; does not affect runtime — but `CLAUDE.md` is the canonical onboarding / architecture description and the drift directly contradicts the post-Task-1 separation it should now reflect.
  RECOMMENDATION: Edit `CLAUDE.md:78` to replace `via state.WriteFIFOSignal` with the post-Task-1 wording — e.g. `via state.DefaultFIFOSignaler / state.SendHydrateSignal`. Keeps the description in lock-step with the production wiring at `cmd/bootstrap_production.go:134` and the doc-comments in `eager_signal_hydrate.go:24-28`.

SUMMARY: Cycle 1's seven cleanup tasks landed cleanly — every standards drift flagged in cycle 1 is now closed (stale `sh -c` doc comments at the three test-file sites have been replaced with the bare invocation form; no remaining test-file documentation describes the dropped wrapper as live behaviour). The Task 1 architecture/seam refactor introduced a new post-merge production primitive (`state.SendHydrateSignal` / `state.DefaultFIFOSignaler`) but a single stale reference to the pre-Task-1 primitive (`state.WriteFIFOSignal`) remains in `CLAUDE.md`'s "Server bootstrap" section — the only surface that did not get the post-Task-1 propagation. All other implementation surfaces (production wiring, package doc comments, test descriptions, integration-test commentary) align with the spec's Fix 1 / Fix 2 / Fix 3 requirements and the eight acceptance criteria.
```
