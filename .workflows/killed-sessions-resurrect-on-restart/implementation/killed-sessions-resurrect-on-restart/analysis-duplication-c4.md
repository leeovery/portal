# Duplication Findings — killed-sessions-resurrect-on-restart (cycle 4)

```
AGENT: duplication
FINDINGS:
- FINDING: `pollUntilMarkersCleared` is a near-byte-equivalent re-declaration of `restoretest.WaitForSkeletonMarkersCleared`
  SEVERITY: low
  FILES: /Users/leeovery/Code/portal/cmd/bootstrap/eager_signal_hydrate_integration_test.go:434-451, /Users/leeovery/Code/portal/internal/restoretest/restoretest.go:291-306
  DESCRIPTION: Both helpers run the same loop body — `state.ListSkeletonMarkers(client)` → return-on-empty → `time.Sleep(tick)` → on-expiry `t.Fatalf` with `restoretest.SortedKeySet(markers)` for stable diagnostics. The only material differences are: (a) the failure-message prefix ("AC1 violation: skeleton markers still set after %s; want empty set within budget. stuck markers=%v" vs "skeleton markers still set after %s: %v"); (b) `pollUntilMarkersCleared` exposes the tick as a parameter while `WaitForSkeletonMarkersCleared` hardcodes `50*time.Millisecond`; (c) the call sites pass different budget values (2s for AC1, 10s for the reboot round-trip). The local definition already imports `restoretest.SortedKeySet`, so it is not avoiding the dependency — only re-implementing the loop. Both files carry `//go:build integration`, so the duplication is not a build-tag artifact. The docstring on `pollUntilMarkersCleared` justifies the helper as "AC1-specific 2-second / 50ms poll loop … a longer budget would silently mask a regression," but the budget is already a per-call argument: passing `restoretest.WaitForSkeletonMarkersCleared(t, client, 2*time.Second)` would enforce the same bound without a parallel implementation. Two call sites use `pollUntilMarkersCleared` (both at the same `2*time.Second, 50*time.Millisecond` arg shape, eager_signal_hydrate_integration_test.go:240 and :355); five call sites use `restoretest.WaitForSkeletonMarkersCleared` (all at hardcoded `10*time.Second`: reboot_roundtrip_test.go:407 / :1021 / :1241, integration_full_test.go:250, exit_closes_pane_integration_test.go:410). This is the same cross-package poll-helper-after-extraction pattern cycle 1's `WaitForFileExists` extraction addressed (which collapsed two parameterised vs hardcoded variants of a sentinel-file poller into one helper in `restoretest`).
  RECOMMENDATION: Promote a single canonical helper. Two viable shapes — pick the simpler:
    1. Extend `restoretest.WaitForSkeletonMarkersCleared` to accept a `tick time.Duration` parameter (matching the cycle-1 `WaitForFileExists` shape: `(t, client, budget, tick)`), drop `pollUntilMarkersCleared`, and update the two AC1 call sites to `restoretest.WaitForSkeletonMarkersCleared(t, client, 2*time.Second, 50*time.Millisecond)`. The "AC1 violation:" prefix is purely diagnostic; the spec-AC linkage lives in the test name and the surrounding `defer dumpPortalLogOnFailure(t, stateDir)` already, so no information is lost. The five existing 10s/hardcoded-tick call sites migrate by appending `, 50*time.Millisecond`. Net deletion: ~17 lines (`pollUntilMarkersCleared` body + its 13-line docstring); five-line edit at each of five existing sites; two-line edit at each of two AC1 sites.
    2. Alternatively, leave `restoretest.WaitForSkeletonMarkersCleared` untouched and reduce `pollUntilMarkersCleared` to a one-line wrapper that calls `restoretest.WaitForSkeletonMarkersCleared(t, client, budget)` (accept the hardcoded 50ms tick on the restoretest side). Net deletion: ~13 lines; preserves the AC1 failure-message prefix as a doc-only artifact.
  Option 1 is cleaner and aligned with cycle 1's `WaitForFileExists` precedent (parameterised tick, single canonical helper). Option 2 minimises blast radius if the preference is "least intrusive change."

SUMMARY: Cycle 3's two cleanup tasks landed correctly — all three stale doc-comment cross-references (cmd/bootstrap/eager_signal_hydrate.go:28, internal/restoretest/restoretest.go:152 and :256) now name post-T1-1 / post-T5-1 primitives, and `bootstrapadapter.NewRestoreAdapter` was extracted and adopted at all four new integration-test sites (the seven pre-existing sites remain unchanged per task scope). One new low-severity duplication remains in cycle 3 scope: `pollUntilMarkersCleared` (declared in eager_signal_hydrate_integration_test.go) is a parameter-rebroadcast of `restoretest.WaitForSkeletonMarkersCleared` with byte-equivalent loop body and only a diagnostic-prefix difference. Same cross-package poll-helper-after-extraction pattern cycle 1 fixed for `WaitForFileExists`. Mechanical extract-and-reuse cleanup; does not block correctness.
```
