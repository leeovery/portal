# Architecture Findings — killed-sessions-resurrect-on-restart (cycle 2)

```
AGENT: architecture
FINDINGS:
- FINDING: Duplicated `state.FIFOSignaler` recording fakes left in two cmd-level test packages after T4-1 typed seam landed
  SEVERITY: low
  FILES: /Users/leeovery/Code/portal/cmd/state_signal_hydrate_test.go:20-52, /Users/leeovery/Code/portal/cmd/bootstrap/eager_signal_hydrate_test.go:12-43, /Users/leeovery/Code/portal/internal/statetest/sleep_recorder.go
  DESCRIPTION: Cycle 1 finding 2 anticipated that once `state.FIFOSignaler` became a typed seam, both call-site test packages would share one fake (`recordingFIFOSignaler`) — explicitly recommending "Delete `recordingFIFOWriter` … in favour of the unified shape". T4-1 introduced the typed seam, but the consolidation step was skipped. The two surviving fakes are byte-equivalent: identical `calls []string` + `errOn map[string]error` + `err error` shape, identical `SendSignal(path) error` body, identical compile-time `var _ state.FIFOSignaler = ...` assertion. They are now in two different test packages (cmd vs cmd/bootstrap), each with their own private name. Since `internal/statetest` was just introduced in cycle 2 as the cross-package test-helper home (housing `RecordingSleep` for the same de-duplication motivation across the same two callers), promoting the fake to `statetest.RecordingFIFOSignaler` would close the cycle. A third caller (a future bootstrap step writing a FIFO byte) would otherwise grow this to three private fakes.
  RECOMMENDATION: Move the recording fake to `internal/statetest/fifo_signaler_recorder.go` as `statetest.RecordingFIFOSignaler` (mirroring the `RecordingSleep` precedent in the same package). Delete `recordingSignaler` in cmd/state_signal_hydrate_test.go and `recordingFIFOSignaler` in cmd/bootstrap/eager_signal_hydrate_test.go; both consumers import the shared type. Same outcome cycle 1 finding 2 recommended — blocked then by the absence of the typed seam, now blocked only by it being a deferred follow-through.

- FINDING: `internal/restoretest` package contract drifted — package doc claims integration-only but cycle 2 helpers ship without the build tag
  SEVERITY: low
  FILES: /Users/leeovery/Code/portal/internal/restoretest/restoretest.go:1-29, /Users/leeovery/Code/portal/internal/restoretest/sessions_json.go:1, /Users/leeovery/Code/portal/internal/restoretest/waitfor_file_exists.go:1, /Users/leeovery/Code/portal/internal/restoretest/sessions_json_test.go:1, /Users/leeovery/Code/portal/internal/restoretest/waitfor_file_exists_test.go:1
  DESCRIPTION: The package doc-comment in restoretest.go opens with `//go:build integration` and states the package "is gated `//go:build integration` because every consumer is also gated — keeping the gate here means the package contributes zero compile cost and zero surface to default `go test ./...` runs". Cycle 2 added two new files (sessions_json.go, waitfor_file_exists.go via T4-4 / T4-6) that intentionally omit the `//go:build integration` tag — and their corresponding test files run under default `go test ./...` as well. The contract the package doc claims (package-level integration gating) no longer matches reality; the package is now a hybrid of integration-gated build helpers (BuildPortalBinaryDir, DriveSignalHydrate, WaitForSkeletonMarkersCleared) and always-built test fixtures (SeedSessionsJSON, WaitForFileExists). All current consumers of the new helpers happen to be integration-tagged so nothing breaks today, but the documentation no longer documents the truth and a future maintainer reading the package doc will be misled.
  RECOMMENDATION: Update the package doc in restoretest.go to describe the current mixed-tag reality: name which helpers are integration-only (those needing tmux fixtures, the go-build subprocess) and which are general-purpose seed primitives. Alternatively, add `//go:build integration` to the new sessions_json.go and waitfor_file_exists.go to restore the original "every consumer gated" invariant — current consumers already are, so this would not break anything. The choice is documentation-vs-mechanism, but the current state has the doc and the build tags out of sync.

SUMMARY: Cycle 2's seven follow-through tasks landed cleanly — the typed FIFOSignaler seam unified the production wiring, NewWithDefaults eliminated the dual-builder tax, the eager-signal default-to-real flip restored production fidelity to the reboot suite, and the shared sessions/wait helpers collapsed real duplication. Two minor architectural threads were left unfinished: the two cmd-level recordingFIFOSignaler fakes that should have collapsed into a single `statetest.RecordingFIFOSignaler` once the typed seam landed (cycle 1 finding 2's anticipated outcome), and the restoretest package's doc-vs-build-tag drift after non-tagged helpers were added beneath an integration-only package banner. Both are low-severity polish items; nothing blocks the work unit's behavioural correctness or its acceptance criteria.
```
