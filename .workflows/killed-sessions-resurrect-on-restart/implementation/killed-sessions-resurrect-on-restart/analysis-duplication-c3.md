# Duplication Findings — killed-sessions-resurrect-on-restart (cycle 3)

```
AGENT: duplication
FINDINGS:
- FINDING: `restoreInner := &restore.Orchestrator{Client, StateDir, Logger}` integration-test wiring repeated at four sites added by this work unit
  SEVERITY: low
  FILES: /Users/leeovery/Code/portal/cmd/bootstrap/eager_signal_hydrate_integration_test.go:193-197, /Users/leeovery/Code/portal/cmd/bootstrap/eager_signal_hydrate_integration_test.go:332-336, /Users/leeovery/Code/portal/cmd/bootstrap/phase2_hook_fire_integration_test.go:162-166, /Users/leeovery/Code/portal/cmd/bootstrap/phase5_marker_suppression_integration_test.go:118-122
  DESCRIPTION: Every new bootstrap-integration test added in this work unit that exercises a real RestoreAdapter open-codes the same five-line preamble: `restoreInner := &restore.Orchestrator{Client: client, StateDir: stateDir, Logger: logger}` immediately followed by `Restore: &bootstrapadapter.RestoreAdapter{Inner: restoreInner}` inside the `buildIntegrationOrchestrator` opts literal. The four new sites listed here are byte-equivalent at the field-set level (only variable scoping differs). Counting pre-existing sites the same shape also appears at /Users/leeovery/Code/portal/cmd/bootstrap/phase5_integration_test.go:169 / :263, /Users/leeovery/Code/portal/cmd/bootstrap/reboot_roundtrip_test.go:328 / :939 / :1191, /Users/leeovery/Code/portal/cmd/reattach_integration_test.go:173, and /Users/leeovery/Code/portal/cmd/bootstrap_production.go:111 — eleven total, four of them added by this work unit. This is the same "single-shape preamble repeated at every integration site" pattern cycle 1's `SeedSessionsJSON` extraction addressed for the sessions.json-seeding boilerplate (which collapsed six sites to one-liners against a shared helper); the inner-`restore.Orchestrator` wiring is the next-most-frequent integration-time literal.
  RECOMMENDATION: Promote a single helper into `internal/bootstrapadapter` (or as a sibling to `RestoreAdapter` since it is the canonical caller) — e.g. `func NewRestoreAdapter(client *tmux.Client, stateDir string, logger *state.Logger) *RestoreAdapter` — that constructs the inner `*restore.Orchestrator` and wraps it in `*RestoreAdapter` in one call. Each of the four new sites then collapses to `Restore: bootstrapadapter.NewRestoreAdapter(client, stateDir, logger)` and the pre-existing seven sites can opt in over time. Net deletion: ~16 lines across the four new sites; ~28 more if the pre-existing sites adopt. The helper has no logic — it is purely a struct-literal-and-wrap factory, so its behaviour cannot drift independently of the wrapped types.

SUMMARY: Cycle 2's four cleanup tasks all landed correctly — `internal/statetest.RecordingFIFOSignaler` is the canonical recording fake imported by both consumer test packages, the three explicit `EagerSignaler: &bootstrap.EagerSignalCore{...}` literals in the integration tests have been dropped in favour of the `NewWithDefaults` auto-default, the CLAUDE.md primitive name update is in place, and the `internal/restoretest/doc.go` reconciliation matches the mixed build-tag reality. One new low-severity duplication remains in cycle 3 scope: the `restoreInner := &restore.Orchestrator{...}` + `&bootstrapadapter.RestoreAdapter{Inner: restoreInner}` integration-test wiring is repeated at four sites the work unit added (and seven more pre-existing) without a shared constructor. Mechanical extract-and-reuse cleanup; does not block correctness.
```
