AGENT: duplication
CYCLE: 4
STATUS: findings

NOTE: Every prior-cycle production finding remains fixed. c1: discardLogger→log.OrDiscard; show-hooks WARN→showGlobalHooksOrWarn. c2: cycle-summary bookend→log.Took, hooks/project CleanStale body→storelog.EmitCleanStaleSummary. c3: the text-rendering capture handler ("captureSink") shared across cmd/state/restore→extracted into internal/logtest (task 9-1). The c2-low exec-handoff marker observation is unchanged at exactly 4 sites (cmd/open.go:111,284; cmd/state_hydrate.go:260,323) — no fifth site appeared, so it stays below the deferral bar and is not re-raised.

FINDINGS:

- FINDING: Map-attr log-capturing slog.Handler re-authored 11 times — the structured-attr flavor the c3 logtest extraction left behind
  - SEVERITY: medium
  - FILES: internal/alias/store_logging_test.go:19-112, internal/hooks/store_test.go:24-112, internal/project/store_logging_test.go:21-80+, internal/storelog/clean_stale_test.go:20-101, cmd/bootstrap/clean_sweep_summary_test.go:35-68+, cmd/state_daemon_cycle_summary_test.go:35-91, cmd/config_migrate_logging_test.go:18-54+, cmd/state_hydrate_timeout_log_test.go:32-88, cmd/state_hydrate_replayed_log_test.go:160-170+, internal/state/fifo_sweep_summary_test.go:35-68+, internal/tmux/portal_saver_lifecycle_events_test.go:41-70+
  - DESCRIPTION: c3 extracted ONE flavor of the in-process capturing slog.Handler into internal/logtest — the *text-rendering* flavor whose Records() exposes a record's ordered attr *keys* (`Record{Level, Msg, Keys []string}`). A second, distinct flavor was never consolidated: the *structured-attr-map* flavor, whose record is `captureRecord{level slog.Level; msg string; attrs map[string]slog.Value}`. This second flavor is independently re-authored in eleven test files under at least eight type names (`captureSink` ×4 in alias/hooks/project/storelog, plus `cleanSummarySink`, `captureSummarySink`, `migrateCaptureSink`, `durationCaptureSink`, `fifoSummarySink`, `saverEventSink`). The handler core is byte-for-byte identical across all eleven: the struct `{ mu sync.Mutex; records []<Record>; shared *<Self>; bound []slog.Attr }`, `owner()` (returns shared or self), `Enabled` returning true unconditionally, `WithAttrs` (append bound+attrs into a fresh derived sink sharing the owner), `WithGroup` (passthrough preserving bound + owner), and a `Handle` that builds `attrs := make(map[string]slog.Value, len(bound)+r.NumAttrs())`, copies the bound attrs, ranges `r.Attrs` into the map, then appends `<Record>{level, msg, attrs}` under the owner's mutex. `all()` (mutex-guarded snapshot copy) recurs in nearly all eleven, and `installCapture(t)` / `onlyRecord(t)` / `attrString(t, key)` recur verbatim across the four store-package copies. The four store copies even carry comments openly admitting the copy ("It mirrors the hooks store test sink", "mirrors the hooks/project store test sinks"). Only the thin per-test accessors genuinely diverge (`summaries()`/`onlySummary()`, `signalTimeoutRecord()`, `intAttr()`/value-typed getters, etc.). The risk is the same one c3 named for the text flavor, at higher cardinality: the WithAttrs/Handle attr-binding contract is the load-bearing behavior every consumer's assertions key on, hand-maintained in eleven places. The reason these eleven did not adopt logtest.Sink at extraction time is concrete and structural: logtest.Sink exposes ordered attr *keys* but not the attr-*value* map these consumers assert on (level/int/duration/string value checks), so there was nothing for them to consume — the c3 extraction addressed the wrong flavor for this set of callers.
  - RECOMMENDATION: Extend the existing internal/logtest.Sink (do NOT create a parallel helper package) to expose the attr *values*, then route all eleven callers through it. Give logtest.Record an `Attrs map[string]slog.Value` field populated in Sink.Handle (it already iterates bound + r.Attrs to build Keys — capture the value into the map in the same loop, zero extra traversal), and add the small value-typed accessors that recur across copies as methods on logtest.Record / helpers in logtest (e.g. `AttrString`, `IntAttr`, `DurationAttr`, `Only`/`OnlyRecord`, `WithMessage`). Each of the eleven files then deletes its ~70-line handler core and keeps only its genuinely-divergent convenience accessor as a thin free function or wrapper over `logtest.Sink.Records()` (mirroring how restore's recordsWithMessage already wraps logtest.Sink). The four store packages additionally collapse their identical `installCapture`/`onlyRecord`/`attrString` to logtest helpers. Pure test-scaffolding consolidation of an existing identical contract; no production change and no behavior change. Scope the extraction to the shared core only — leave each per-test accessor in its owning file as a thin wrapper.

SUMMARY: All prior-cycle production findings remain resolved. The one actionable item is a test-scaffolding duplication the c3 logtest extraction left behind: the structured-attr-map flavor of the capturing slog.Handler (record = level/msg/map[string]slog.Value) is independently re-authored in eleven test files under eight type names, with a byte-identical ~70-line handler core. logtest.Sink should be extended to expose attr values (it already captures keys) so all eleven collapse onto the single shared declaration.
