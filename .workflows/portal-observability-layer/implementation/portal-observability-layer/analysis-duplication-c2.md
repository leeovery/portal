AGENT: duplication
CYCLE: 2
STATUS: findings

NOTE: Cycle 1's two highest-impact duplication findings are both fixed — discardLogger/nil-guard consolidated into internal/log/discard.go (OrDiscard/Discard), and the thrice-repeated "show-hooks failed" block extracted to showGlobalHooksOrWarn (internal/tmux/hooks_register.go:126).

FINDINGS:

- FINDING: Cycle-summary timing-capture-and-emit scaffold hand-rolled across nine production sites
  - SEVERITY: medium
  - FILES: cmd/state_daemon.go:391+491-497, cmd/bootstrap/orphan_sweep.go:151+208, cmd/bootstrap/stale_marker_cleanup.go:125+133, internal/state/fifo_sweep.go:67+100, internal/hooks/store.go:249+294, internal/project/store.go:190+232, internal/restore/restore.go:69+85, internal/restore/session.go:261+292
  - DESCRIPTION: The same bookend — `start := time.Now()` at entry, a local counter, and a terminal `logger.Info("<noun> ... complete"/"tick complete", <counters...>, "took", time.Since(start))` — is independently authored in nine production functions. Cycle 1 flagged this at four sites (low) and explicitly deferred it with "consolidate if a fifth sweep appears." Five further sites have since landed (restore.go, session.go, hooks/project CleanStale, the daemon tick summary), so the Rule-of-Three threshold the c1 deferral set is now well past. The risk is concrete: the `took` attr key (and its `time.Duration`-not-string contract) is the spec's reserved cycle-summary attr, yet it is re-typed by hand in nine places with no compiler signal binding them. A future change to the took rendering or attr name must be applied nine times or the summaries silently diverge.
  - RECOMMENDATION: Add one tiny helper to internal/log (which every site already imports for log.For), e.g. `func Took(start time.Time) slog.Attr` returning `slog.Duration("took", time.Since(start))`. Each site keeps its own start capture and divergent counters but ends with `logger.Info("...", counters..., log.Took(start))`. This pins the took-attr key + Duration type in one place without over-abstracting the genuinely-divergent loop bodies. Do NOT attempt to unify the loop bodies — only the bookend repeats.

- FINDING: CleanStale batch-summary body near-duplicated between hooks and project stores
  - SEVERITY: medium
  - FILES: internal/hooks/store.go:248-297, internal/project/store.go:189-235
  - DESCRIPTION: The two `CleanStale` methods are structurally identical ~45-line blocks: `start := time.Now()` → Load (wrapped error) → in-memory kept/removed partition → zero-removal early return (skip Save + summary) → per-entry DEBUG loop → `Save(kept)` → on failure `logger.Warn("clean-stale", "op", "clean-stale", "entries", len(removed), "via", "internal", "error", err, "error_class", fileutil.ClassifyWriteError(err), "took", time.Since(start))` + wrapped return → on success `logger.Info("clean-stale", "op", "clean-stale", "entries", len(removed), "via", "internal", "took", time.Since(start))`. The only differences are the per-entry attr key (`hook_key` vs `project`/`path`) and the partition predicate (liveKeys membership vs os.Stat). Both even carry a verbatim-copied resolved-in-comment block cross-referencing each other ("Same reasoning as the hooks store's CleanStale"). Cycle 1 identified this exact sub-case as "the worthwhile sub-case" and recommended a shared helper; it remains unaddressed. The two emission shapes (the Warn and Info attr lists, the zero-removal skip, the took contract) must now be kept byte-identical by hand.
  - RECOMMENDATION: Extract the summary emission into a shared helper parameterized by removed-count, start, and the optional save error — e.g. `func emitCleanStaleSummary(logger *slog.Logger, removed int, start time.Time, saveErr error)` in a store-adjacent location (or internal/log). Each store keeps its Load+partition+per-entry-DEBUG (the per-entry attr key legitimately differs) and routes the terminal Warn/Info through the helper, collapsing the two divergent terminal blocks to one source of truth for the clean-stale summary contract.

- FINDING: Terminal "process: exec" handoff marker re-authored at four sites while its paired "process: exit" half is centralized
  - SEVERITY: low
  - FILES: cmd/open.go:111, cmd/open.go:284, cmd/state_hydrate.go:259, cmd/state_hydrate.go:322
  - DESCRIPTION: Four sites independently emit the pre-syscall.Exec terminal marker via `logger.Info("exec", "target", <prog>, "args", strings.Join(<argv>, " "), [optional attrs])`. The two open.go sites use `log.For("process")` and emit the bare three-attr shape; the two state_hydrate.go sites use cfg.Logger (component=hydrate) and add `"hook_present", bool`. All four share the load-bearing convention from spec § Defensive invariants — exec-handoff markers: this Info MUST be the immediately-preceding statement before ExecShell/Exec, and `args` is always `strings.Join(argv, " ")`. The paired terminal half — `process: exit` — is already centralized in `log.Close` (internal/log/init.go:151), so the asymmetry is visible. Kept low because the four sites legitimately diverge on component and extra attrs, and the args slicing semantics differ slightly (argv[1:] in open.go vs full argv in hydrate), so a naive helper could obscure intent.
  - RECOMMENDATION: Optional. If consolidated, a thin helper `func ExecMarker(logger *slog.Logger, target string, argv []string, extra ...slog.Attr)` would pin the `"exec"` message + `args=strings.Join(argv, " ")` contract in one place while letting each caller pass its own logger and extra attrs. Defer unless a fifth exec-handoff site appears.

SUMMARY: Cycle 1's two highest-impact findings are fixed. Remaining duplication: the cycle-summary timing bookend (now 9 sites — past the deferral threshold c1 set, raised to medium) and the near-identical CleanStale batch-summary body shared by the hooks and project stores (c1's deferred sub-case, raised to medium); a lower-priority observation is the four hand-rolled process:exec handoff markers whose paired process:exit half is already centralized in log.Close.
