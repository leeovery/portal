AGENT: duplication
CYCLE: 3
STATUS: findings

NOTE: Both cycle-2 medium findings are fixed — the cycle-summary timing bookend is consolidated into internal/log/took.go (log.Took) and used at every sweep/cycle/step site; the near-identical hooks/project CleanStale batch-summary body is consolidated into internal/storelog/clean_stale.go (EmitCleanStaleSummary), with both stores delegating. Cycle-1's two findings (discardLogger→log.OrDiscard; show-hooks WARN→showGlobalHooksOrWarn) also remain fixed. The cycle-2 low-severity exec-marker observation (4 hand-rolled process:/hydrate: exec markers at cmd/open.go:111,284 and cmd/state_hydrate.go:260,323) remains below its explicit deferral threshold (no fifth site appeared) and is not re-raised.

FINDINGS:

- FINDING: Test capturing slog.Handler ("captureSink") independently re-authored in three packages
  - SEVERITY: medium
  - FILES: cmd/logging_capture_test.go:51-115, internal/state/logging_capture_test.go:21-58, internal/restore/logging_capture_test.go:18-79
  - DESCRIPTION: Three packages each declare a near-identical in-process log-capturing `slog.Handler` plus its `newCaptureLogger`/`newCaptureLoggerForComponent` constructor. The core is byte-for-byte the same in all three: a `struct{ mu sync.Mutex; lines []string }`, `Enabled` returning true unconditionally, `WithGroup` passthrough, a `Handle` that renders `<LEVEL> <msg> key=value...` by writing `r.Level.String()` + `" "` + `r.Message` then ranging `r.Attrs` with `fmt.Fprintf(&b, " %s=%v", a.Key, a.Value.Any())`, a `body()` that returns `strings.Join(s.lines, "\n")` under the mutex, and a `newCaptureLogger(t)` returning `slog.New(sink), sink`. The import block (`context`, `fmt`, `log/slog`, `strings`, `sync`, `testing`) is identical. They were written in isolation by the state, restore, and cmd task executors — each carrying a comment justifying the same "substring assertions keep working after the observability migration retyped every seam to *slog.Logger" rationale — and are consumed widely (state: 3+ test files; restore: 5+; cmd: 9+). Canonical Rule-of-Three: a third independent copy of the same ~40-line capture machinery. The rendered body shape (`<LEVEL> <msg> key=value`) is the contract every consumer's substring assertions key on, yet it is hand-maintained in three places; a change to attr rendering (quoting, ordering, value formatting) must be applied three times or the three packages' tests silently diverge in what they accept.
  - RECOMMENDATION: Extract the base capturing handler + `newCaptureLogger(t) (*slog.Logger, *Sink)` + `Sink.body()` into one shared test-helper package and have all three packages consume it. The state and restore sinks live in external test packages (`state_test`, `restore_test`) and can import a fresh helper package freely; the cmd sink is in-package (`package cmd`) but its `*_test.go` can import the same helper. Note `internal/portaltest` already imports `internal/state`, so it cannot host a helper that internal/state's own test surface imports without a cycle — prefer a new leaf test-helper package (e.g. `internal/logtest`) that imports nothing portal-internal, OR a test-only file in `internal/log` (which every package already depends on and which owns SetTestHandler). The two genuine divergences layer on top of the shared base, not into it: cmd's WithAttrs/shared/bound component-rendering (so the `.With("component", ...)` binding appears on every line) and restore's `records []capturedRecord` + `recordsWithMessage` (exact attr-key-set assertions). Extract only the common base; leave the two extensions in their owning packages as thin wrappers or embedded fields over the shared sink.

SUMMARY: Both cycle-2 medium findings are resolved. The one remaining actionable item is a third-copy test-scaffolding duplication: a near-identical log-capturing slog.Handler ("captureSink") independently authored in the cmd, state, and restore test surfaces, whose shared ~40-line core should be extracted to one test-helper package.
