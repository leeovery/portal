AGENT: standards
CYCLE: 3
STATUS: findings

NOTE: All four cycle-1 findings remain resolved. Cycle-2 finding 2 (EagerSignalHydrate no cycle summary) accepted as intended (matches the authoritative cycle catalog; per-FIFO outcomes now route through signalLogger). Cycle-2 finding 3 (bare os.Exit in hydrate exec-failure fallback) RESOLVED (defaultExecShell now WARN → log.Close(1) → osExit seam). Cycle-2 finding 1 (signal-vs-hydrate attribution) PERSISTS, re-flagged below.

FINDINGS:

- FINDING: `signal-hydrate` command's FIFO-signal diagnostics still render under `hydrate`, not `signal`, and the boundary is still undocumented
  - SEVERITY: low
  - FILES: cmd/state_signal_hydrate.go:44, cmd/state_signal_hydrate.go:50, cmd/state_signal_hydrate.go:61, cmd/state_signal_hydrate.go:98
  - DESCRIPTION: Unresolved cycle-2 finding, carried into cycle 3 unchanged. The Subsystem prefix taxonomy assigns `signal` to "the FIFO signaling MECHANISM — EagerSignalHydrate and the lower-level FIFO signal send/receive plumbing in internal/state". Both structurally-parallel FIFO signalers that internal/state and bootstrap own now correctly bind `signalLogger = log.For("signal")`: cmd/bootstrap/eager_signal_hydrate.go:23 (per-FIFO WARN + success DEBUG) and internal/state/signal_hydrate.go:26 (retry-ladder DEBUG). But the `portal state signal-hydrate` command body `runSignalHydrate` still wires `Logger: hydrateLogger` (cmd/state_signal_hydrate.go:98) and normalizes via `hydrateLoggerOrDefault` (line 41), so its marker-enumeration WARN ("list skeleton markers failed", :44), per-session WARN ("list panes for session failed", :50), and per-FIFO-write WARN ("write fifo failed", :61) all render under `hydrate:`. `runSignalHydrate` is the per-session, hook-driven peer of EagerSignalHydrate — same enumerate-markers-then-write-FIFO shape — yet its diagnostics are not greppable under `signal:`, so `grep "signal:"` misses the hook-driven signaling path. A literal reading of the taxonomy is defensible (it scopes signal's plumbing to "in internal/state"; runSignalHydrate lives in cmd/ and the command's process_role is `hydrate`), which is why this is low and not a clear violation — but the attribution is still inconsistent with its EagerSignalHydrate sibling, and the cycle-2 recommendation (decide-and-document the boundary) was not actioned: the taxonomy table (specification.md:166) was not amended and no in-source note records the decision.
  - RECOMMENDATION: Pick one and make it explicit. Either (a) route runSignalHydrate's three enumeration/per-FIFO WARNs through `signal` (matching EagerSignalHydrate + the internal/state plumbing) so `grep "signal:"` is complete; or (b) keep `hydrate` (matching the command's process_role) and add a one-line note to the taxonomy `signal` row stating that the signal-hydrate command's own enumeration diagnostics render under `hydrate` while only the lower-level send plumbing renders under `signal`, so the boundary is a recorded decision rather than incidental drift.

SUMMARY: The implementation conforms closely to the specification. All four cycle-1 findings and two of three cycle-2 findings are resolved (notably the hydrate exec-failure fallback now pairs a terminal marker via log.Close(1)). The single residual drift is the low-severity, still-undocumented signal-vs-hydrate component attribution for the hook-driven signal-hydrate command relative to its EagerSignalHydrate sibling.
