AGENT: duplication
CYCLE: 5
STATUS: findings

NOTE: The c4 finding is RESOLVED — internal/logtest.Sink was extended with Record.Attrs + value-typed accessors (AttrString/IntAttr/RequireDuration/HasAttr/OnlyRecord) and the eleven hand-rolled structured-attr-map handlers collapsed onto it. All earlier-cycle production findings remain fixed. The c2-low exec-handoff marker observation is unchanged at exactly 4 sites (no fifth) — below the deferral bar. Production hydrate exec code is properly shared across all three recovery paths — no new production duplication.

FINDINGS:

- FINDING: tui's previewCaptureSink is a verbatim, unmigrated copy of the now-shared logtest.Sink rendering core
  - SEVERITY: medium
  - FILES: internal/tui/preview_attach_test.go:83-150, internal/logtest/capture.go:101-219
  - DESCRIPTION: When c3/c4 extracted the capturing slog.Handler into internal/logtest, the internal/tui preview-attach test surface was missed and still hand-rolls the entire handler core. previewCaptureSink (internal/tui/preview_attach_test.go:83-150) is byte-for-byte the same machinery logtest.Sink already provides: the struct `{ mu sync.Mutex; lines []string; shared *<Self>; bound []slog.Attr }`, `owner()`, `Enabled` returning true unconditionally, `WithAttrs` (append bound+attrs into a fresh derived sink sharing the owner), `Handle` rendering the canonical `<LEVEL> <msg> key=value...` shape (write `r.Level.String()` + `" "` + `r.Message`, range bound then `r.Attrs` with `fmt.Fprintf(&b, " %s=%v", a.Key, a.Value.Any())`, append under the owner's mutex), and `body()` returning `strings.Join(s.lines, "\n")` under the mutex — all identical to logtest.Sink's owner()/WithAttrs/Handle/Body. The `newTestLogger(t)` constructor (lines 140-144) duplicates logtest.NewCaptureLogger plus a `.With("component","preview")`. The one cosmetic deviation, `WithGroup(_ string) slog.Handler { return s }` (line 111), is arguably a latent bug versus logtest.Sink's version that preserves bound+owner — it returns the receiver rather than a sink sharing the owner — but no preview log path uses WithGroup so it is inert. internal/logtest is a verified leaf and the test file is `package tui`, so it can import logtest with zero cycle risk. This is the same class of finding c3/c4 named, at the one call site the extraction never reached; the rendered-body contract that the preview-attach substring assertions key on is now hand-maintained in parallel with the shared declaration and can drift from it. (This is the LAST text-rendering re-author — after it, the text flavor is fully consolidated.)
  - RECOMMENDATION: Delete previewCaptureSink and its body()/owner()/Enabled/WithAttrs/WithGroup/Handle and route newTestLogger through the shared helper: `logger, sink := logtest.NewCaptureLogger(t); return logger.With("component","preview"), sink`, retyping the (*previewCaptureSink) parameters on newTestLogger/readLog to *logtest.Sink and replacing the single body() call site with sink.Body(). Pure test-scaffolding consolidation onto the existing identical contract — no production change, no behavior change. Brings the WithGroup behavior into line with the shared sink as a side effect.

- FINDING: Component-binding WithAttrs/WithGroup/owner recorder skeleton independently re-authored across four tmux/bootstrap test recorders
  - SEVERITY: low
  - FILES: cmd/bootstrap/bootstrap_test.go:121-219 (RecordingLogger + recordingLoggerHandler), internal/tmux/portal_saver_test.go:2625-2666 (recordingSlogHandler), internal/tmux/hooks_register_test.go:693-747 (recordingMigrationLogger), internal/tmux/hooks_migration_test.go:39-99 (recordingLogger)
  - DESCRIPTION: Beyond the text-rendering core (covered by logtest), the `shared *<Self>; bound []slog.Attr` + `owner()` + `WithAttrs` + `WithGroup` skeleton — whose sole purpose is to make the `.With("component", …)` binding observable on every captured record — is re-authored verbatim in these four recorders. The skeleton is identical; only the storage *tail* genuinely diverges: RecordingLogger buckets into per-level []string slices with parallel component slices; recordingSlogHandler stores `r.Clone()` with bound merged via AddAttrs; the two migration recorders bucket INFO/WARN into formatted lines. The bound-accumulation skeleton is ~20 lines each, so this sits at the low-value end. Recorded (not deferred-silent) because four independent copies of the component-accumulation machinery is a genuine Rule-of-Three crossing whose drift risk is the component-attribution contract every audit-log assertion depends on.
  - RECOMMENDATION: Optional, low priority. If logtest gains a typed structured-records reader these would naturally collapse onto it. Until then, do NOT force a shared abstraction over the divergent storage tails — the cost/benefit does not yet justify it given each copy's ~20-line skeleton and distinct accessor surface. Track as a consolidation opportunity; fix opportunistically if any of these files is touched for another reason.

SUMMARY: The c4 structured-attr-map finding is fully resolved. One actionable item remains: internal/tui's previewCaptureSink is a verbatim unmigrated copy of logtest.Sink's text-rendering core that the extraction missed and should now consume the shared helper (leaf import, zero cycle risk) — the last text-rendering re-author. A second low-severity observation tracks the component-binding recorder skeleton re-authored across four tmux/bootstrap recorders whose divergent storage tails do not yet justify forced extraction.
