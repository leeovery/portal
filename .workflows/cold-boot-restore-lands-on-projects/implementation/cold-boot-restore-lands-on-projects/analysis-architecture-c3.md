AGENT: architecture
FINDINGS: none
SUMMARY: The fix is a single early-return in transitionFromLoading() gated on the canonical cold-route predicate (progressReceiver != nil), and it composes cleanly with the existing seam. The cold/warm split is well-contained: both transitionFromLoading() call sites in Update (LoadingMinElapsedMsg and BootstrapCompleteMsg arms) immediately batch refetchSessionsAfterRestore(), which is gated on the same predicate, so the interim PageSessions is always followed by exactly one decision-bearing SessionsMsg; on the cold route progressReceiver != nil guarantees refetchSessionsAfterRestore() is non-nil and fetchSessionsCmd() always emits a SessionsMsg (error → tea.Quit, success → evaluateDefaultPage off the repaired list), so the decision always resolves and the picker cannot strand on the interim page. No leaked state — sessionsLoaded stays false until the refetch's SessionsMsg arm sets it, keeping every interim evaluateDefaultPage caller (including an unconditional ProjectsLoadedMsg) a no-op via the !sessionsLoaded early-return, so the latch cannot fire prematurely regardless of ProjectsLoadedMsg arrival order. The latch and decision logic are untouched, the warm path is byte-identical, and the change introduces no new type-boundary, module-structure, or composition concerns. Test coverage maps one-to-one to the spec's seven ACs and six required cases (interim-page validity, late-ProjectsLoadedMsg interleave, failing-refetch quit, over-correction guard, filter routing, warm parity) with well-factored shared drivers/fixtures. Verdict: architecturally sound.
