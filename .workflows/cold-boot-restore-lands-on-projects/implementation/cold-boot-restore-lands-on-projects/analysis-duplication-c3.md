AGENT: duplication
FINDINGS:
- FINDING: Repeated cold-route model construction block across six tests
  SEVERITY: low
  FILES: internal/tui/coldboot_session_refetch_test.go:363-366, internal/tui/coldboot_session_refetch_test.go:441-444, internal/tui/coldboot_session_refetch_test.go:486-489, internal/tui/coldboot_session_refetch_test.go:704-707, internal/tui/coldboot_session_refetch_test.go:769-772, internal/tui/coldboot_session_refetch_test.go:838-841
  DESCRIPTION: The four-line cold-route model setup — `New(lister, WithServerStarted(true), WithProgressReceiver(func() tea.Msg { return nil }), WithProjectStore(stubProjectStore{}))` — appears verbatim in six landing-page tests (and a near-twin three-option form, without WithProjectStore, at lines 262-265 and 304-307). The option list is the cold-route definition (serverStarted + a wired progressReceiver gates the deferral/refetch); if that definition changes, every block must be hand-edited in lockstep. This is the only repeated multi-line shape that cycles 1-2 did not already fold into a driver/fixture. It is borderline against the Rule of Three: short, declarative, self-documenting option chains, and a few call sites legitimately diverge (WithInitialFilter at 486-490, WithCommand at 648-651, the warm-route two-option form at 398/555/584-587).
  RECOMMENDATION: Optionally fold the canonical cold-route construction into a small helper alongside the existing shared fixtures (e.g. `newColdRouteModel(lister)` returning `New(lister, WithServerStarted(true), WithProgressReceiver(func() tea.Msg { return nil }), WithProjectStore(stubProjectStore{}))`), with call sites that diverge (initialFilter, command, warm) constructing directly as they do now. Low impact — defensible to leave as-is given the option-list-as-setup-intent idiom; flag only because the literal repeats six times and is the cold-route's structural definition.
SUMMARY: The production change (the `progressReceiver != nil` early-return gate in transitionFromLoading) is a 3-line surgical edit with no duplication; the helpers it leans on (fetchSessionsCmd / refetchSessionsAfterRestore) pre-exist. The only candidate is the six-times-repeated cold-route model-construction option block in the test file — a low-severity, optional consolidation, since the heavier drive-sequence and fixture duplication was already extracted in analysis cycles 1 and 2.
