AGENT: duplication
FINDINGS:
- FINDING: Cold-route loading-lifecycle drive (with pre-transition ProjectsLoadedMsg) re-inlined verbatim across four landing-page tests
  SEVERITY: medium
  FILES: internal/tui/coldboot_session_refetch_test.go:264-287, internal/tui/coldboot_session_refetch_test.go:369-390, internal/tui/coldboot_session_refetch_test.go:435-455, internal/tui/coldboot_session_refetch_test.go:674-692
  DESCRIPTION: Four of the AC tests added in tasks 1-2/1-4 (TestColdBoot_NPositive_LandsOnSessions, TestColdBoot_ZeroSessions_LandsOnProjects, TestColdBoot_InitialFilter_RoutesToSessions, TestColdBoot_InterimPage_IsValidSessions) each re-inline the identical ~13-line cold-route drive sequence: WindowSizeMsg → stale SessionsMsg + "expected PageLoading after stale SessionsMsg" invariant → ProjectsLoadedMsg{Projects: []project.Project{{Path: "/p/one", Name: "one"}}} + "expected PageLoading after ProjectsLoadedMsg" invariant → LoadingMinElapsedMsg → BootstrapCompleteMsg → drainBatchToModel. The bodies are byte-identical apart from the per-test post-drain assertions. The file already ships a near-miss helper, driveColdBootToSessions (lines 70-99), which was deliberately NOT reused because it omits the mandatory ProjectsLoadedMsg delivery the spec requires (Testing Requirements: "A regression test MUST deliver ProjectsLoadedMsg before the loading-page transition"). That omission is exactly why each task re-pasted the variant instead of calling a shared driver — a textbook copy-paste-drift-across-task-boundaries pattern: four independent transcriptions of the same mandatory ordering that can silently diverge (e.g. one test dropping the pre-transition invariant check, or the stub project path drifting) and weaken the regression guard.
  RECOMMENDATION: Extract one shared driver in this file that delivers ProjectsLoadedMsg before the transition and returns the post-drain Model, e.g. driveColdBootWithProjects(t, m, stale) — encapsulating the WindowSizeMsg, the stale SessionsMsg + PageLoading invariant, the ProjectsLoadedMsg + PageLoading invariant, the two gate closes, and the drainBatchToModel. The four tests above then become a setup + one driver call + their distinguishing assertions. (TestColdBoot_InterimPage_IsValidSessions needs the pre-drain interim model too, so the driver should expose the BootstrapCompleteMsg-returned completeCmd/interim model — either return them or split the "drive to transition" and "drain refetch" halves so that test can assert the interim page between them.) This consolidates the mandatory ProjectsLoadedMsg ordering into a single source of truth so the spec-required setup cannot drift between tasks.
- FINDING: Standalone-snapshot project record `{Path: "/p/one", Name: "one"}` literal duplicated across six tests
  SEVERITY: low
  FILES: internal/tui/coldboot_session_refetch_test.go:381, internal/tui/coldboot_session_refetch_test.go:445, internal/tui/coldboot_session_refetch_test.go:560, internal/tui/coldboot_session_refetch_test.go:624, internal/tui/coldboot_session_refetch_test.go:685, internal/tui/coldboot_session_refetch_test.go:853
  DESCRIPTION: The same "one meaningful project so Projects is a real landing surface" literal — ProjectsLoadedMsg{Projects: []project.Project{{Path: "/p/one", Name: "one"}}} — is hand-written in six places. Minor magic-literal repetition; if folded into the driver above it disappears for the four-test cluster, leaving only the two strict-interleave / warm tests that deliver it standalone.
  RECOMMENDATION: Subsumed by the driver extraction above for the four-test cluster. For the remaining standalone deliveries (TestColdBoot_LateProjectsLoadedMsg_StillLandsOnSessions:793, TestWarmRoute_ZeroSessions_LandsOnProjects:560, TestCommandPending_LandsOnProjects_NoInterimFlash:624, TestColdBoot_RefetchError:853), a single file-level helper such as oneProjectLoaded() tea.Msg (or a shared var) would remove the last copies. Low priority — only act on this if the driver extraction lands.
SUMMARY: One medium-severity test-helper extraction candidate: a mandatory-ProjectsLoadedMsg cold-route driver is re-inlined verbatim across four AC landing-page tests because the existing driveColdBootToSessions helper omits the spec-required ProjectsLoadedMsg, inviting drift in the regression setup across task boundaries. Consolidating it (plus the trailing project-record literal) into one shared driver removes the duplication and pins the mandatory ordering in a single place. No production-code duplication — the single transitionFromLoading gate change is unique.
