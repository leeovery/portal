AGENT: duplication
FINDINGS:
- FINDING: Verbatim-duplicated integration-test environment scaffolding (setupAbridgedEnv ≡ setupConcurrentColdBootEnv)
  SEVERITY: low
  FILES: cmd/abridged_integration_test.go:64-98, cmd/concurrent_coldboot_integration_test.go:77-118
  DESCRIPTION: setupAbridgedEnv is a byte-for-byte copy of setupConcurrentColdBootEnv — the full ~20-line ceremony (testing.Short skip → tmuxtest.SkipIfNoTmux → ensurePortalOnPATH → IsolateStateForTest → Setenv PORTAL_STATE_DIR → Setenv HISTFILE=os.DevNull → state.EnsureDir → tmuxtest.New → client → reapSaverDaemon t.Cleanup → resetBootstrapOnce → return (ts, client, stateDir, envSlice)) is identical in both, differing ONLY in the tmuxtest.New socket-prefix string literal ("ptl-abridged-" vs "ptl-cc-coldboot-"). The abridged file's own docstring acknowledges this ("mirrors setupConcurrentColdBootEnv step-for-step ... a deliberate two-instance copy (Rule of Three not yet reached)"). At two instances this is below the strict Rule-of-Three threshold, so it is low severity, but the two copies are 95% identical and will silently drift: any change to the isolation/reap contract (e.g. a new env pin or a teardown-ordering fix) must be hand-applied in both, and the reap/isolation invariants here are load-bearing (the leaked-daemon corruption incident). Note the sibling reap/pid helpers (reapSaverDaemon, reapTmuxServer, pidIsAlive, assertDaemonSingletonNoZombie) are already shared from concurrent_coldboot_integration_test.go — only the top-level env-builder was copied.
  RECOMMENDATION: Extract one shared builder parameterised by the socket prefix, e.g. setupIsolatedSaverEnv(t, socketPrefix string) (*tmuxtest.Socket, *tmux.Client, string, []string), and have both suites call it (setupAbridgedEnv → setupIsolatedSaverEnv(t, "ptl-abridged-"), setupConcurrentColdBootEnv → setupIsolatedSaverEnv(t, "ptl-cc-coldboot-")). Distinct socket namespaces — the only reason cited for keeping them separate — are preserved by the parameter. If the Rule-of-Three deferral is preferred, this is a documented watch-item to consolidate the moment a third caller appears.

- FINDING: Entry-path context-injection + warning-drain epilogue still duplicated across PersistentPreRunE branches (carry-over from cycle 1, unaddressed)
  SEVERITY: low
  FILES: cmd/root.go:186-196, cmd/root.go:239-250
  DESCRIPTION: The abridged branch and the synchronous full-bootstrap tail each re-inline the same two-part entry-path epilogue: (1) the warning drain `if !isTUIPath(cmd, args) { bootstrapWarnings.EmitTo(cmd.ErrOrStderr()) }`, and (2) the context injection `ctx := context.WithValue(cmd.Context(), serverStartedKey, <started>); ctx = context.WithValue(ctx, tmuxClientKey, client); cmd.SetContext(ctx)`. The spec explicitly frames the abridged path as reusing "the SAME entry-path plumbing (warning sink + context injection) as the synchronous full path," yet the implementation open-codes that plumbing inline in both places rather than sharing a helper; they differ only in the serverStarted value (false vs `started`) and the sync branch's defensive `if client != nil` guard (the abridged branch is reached only with a non-nil client). This was raised in cycle 1 (analysis-duplication-c1.md finding 3) and remains unchanged. It is small (a few lines per site, two sites) and therefore low-impact, but it sits on the critical bootstrap path where the two copies must be kept in lockstep by hand: a future change to the context keys or the drain gate applied to one branch and not the other would silently diverge the abridged and synchronous entry paths.
  RECOMMENDATION: Extract a small shared helper such as injectBootstrapContext(cmd *cobra.Command, client *tmux.Client, serverStarted bool) (WithValue serverStartedKey + guarded tmuxClientKey + SetContext) and optionally drainWarningsToStderrUnlessTUI(cmd, args), then call them from both the abridged and synchronous branches so the epilogue is authored once and "reuses the same plumbing" holds structurally. Leave the concurrent-deferred branch as-is (it deliberately sets deferredBootstrapKey and no serverStartedKey).

- FINDING: Repeated inline openTUIFunc-capture closure across the latch-routing tests
  SEVERITY: low
  FILES: cmd/concurrent_bootstrap_route_test.go:93-98, cmd/abridged_integration_test.go:325-330, cmd/concurrent_bootstrap_gate_test.go:139-144
  DESCRIPTION: The latch-routing unit/integration tests each hand-roll the same openTUIFunc override to capture the routing verdict: `origFunc := openTUIFunc; openTUIFunc = func(cmd, _, _, started) error { deferredSeen = deferredBootstrapFromContext(cmd) != nil; serverStarted = started; return nil }; t.Cleanup(func(){ openTUIFunc = origFunc })`. The deferredSeen+serverStarted capture body is byte-identical in concurrent_bootstrap_route_test.go (TakesAbridgedPath) and abridged_integration_test.go (OpenSatisfied row 1); concurrent_bootstrap_gate_test.go and the two "DefersBootstrap"-style tests use trivially-varied siblings of the same closure (capturing openTUIReached, or asserting runner.calls inside the closure). That is four-plus copies of the same override-and-capture recipe across three files — over the Rule-of-Three threshold — and each carries its own local capture vars + t.Cleanup restore, so a change to the openTUI signature or the capture set must be edited in every copy.
  RECOMMENDATION: Add one shared test helper in the cmd package, e.g. captureOpenTUIArgs(t *testing.T) *openTUICapture returning a struct with DeferredSeen/ServerStarted/Reached fields and installing the t.Cleanup-restored override once. The DefersBootstrap variants that additionally assert runner.calls==0 inside the closure can read DeferredSeen from the same capture and move that assertion after Execute. This mirrors the package's existing shared-fixture style (satisfiedLatchAliveSaverCommander, installMockList).
SUMMARY: Cycle 1's two test-duplication findings are resolved (the WhenNotSatisfied table test and the shared saverAbsentReviveFailsCommander fixture now exist), and production reuse remains clean (the new daemon cleanup home reuses runHookStaleCleanup; loading styles were consolidated onto the header helpers). Three low-severity duplications remain: a verbatim-copied integration-test env builder differing only by a socket-prefix literal, the carry-over PersistentPreRunE context/warning epilogue still inlined in two branches, and a repeated openTUIFunc-capture closure across the routing tests.
