AGENT: duplication
FINDINGS:
- FINDING: Three near-identical latch-verdict "full bootstrap" tests
  SEVERITY: medium
  FILES: cmd/abridged_route_test.go:85-111, cmd/abridged_route_test.go:113-143, cmd/abridged_route_test.go:145-171
  DESCRIPTION: TestPersistentPreRunE_FullBootstrap_WhenLatchAbsent, _OnVersionMismatch, and _OnLatchReadError are three structurally identical ~25-line test functions that differ only in (a) the value the `show-option` RunFunc returns (`optionAbsentErr()` / `"v1.0.0"` / `errors.New("tmux socket connect failed")`), (b) the version override in the mismatch case, and (c) the assertion failure string. Every other line — resetBootstrapOnce, client construction via recordingCommander, recordingRunner{started:false}, bootstrapDeps wiring + Cleanup, installMockList, resetRootCmd, SetArgs([]string{"list"}), Execute, and the `runner.calls != 1` assertion — is copy-pasted verbatim. This is the Rule-of-Three threshold (three instances) and the classic table-test shape. Independently authored per-case, they will drift: a change to the shared setup (e.g. a new bootstrapDeps field or a routing tweak) must be hand-applied three times.
  RECOMMENDATION: Collapse the three into one table-driven test (e.g. TestPersistentPreRunE_FullBootstrap_WhenNotSatisfied) whose cases carry `name`, the `show-option` return `(value string, err error)`, an optional version override, and the assertion message. Keep the single shared body (setup → Execute → assert calls==1). The existing `optionAbsentErr()` helper already isolates the absent case's error shape, so the table only needs to vary the three read outcomes.

- FINDING: Cross-file duplicated "saver absent, revive fails" fake commander
  SEVERITY: medium
  FILES: cmd/abridged_route_test.go:182-196, cmd/abridged_route_test.go:231-245, cmd/abridged_saver_test.go:183-196
  DESCRIPTION: The same recordingCommander RunFunc — dispatching `list-panes` → noSuchSessionErr() (saver absent), `has-session` → errors.New("can't find session"), `new-session` → errors.New("create denied") (revive fails across retries), plus `show-option` → version on the two PersistentPreRunE paths — is inlined three times across two files. The two occurrences in abridged_route_test.go (TestPersistentPreRunE_Abridged_EmitsWarningsToStderrOnCLIPath and _LeavesWarningsForOpenTUIOnTUIPath) are byte-identical; the abridged_saver_test.go occurrence (TestEnsureSaverLiveness_FunnelsSaverDownWarning) is the same switch minus the `show-option` arm. abridged_route_test.go already extracts sibling fixtures for exactly this reason (satisfiedLatchAliveSaverCommander(), notSatisfiedLatchClient()), so this scenario is the odd one left inline. Three copies of one tmux-failure recipe will drift as the saver revive sequence evolves.
  RECOMMENDATION: Add a shared fixture in the cmd test package mirroring satisfiedLatchAliveSaverCommander() — e.g. saverAbsentReviveFailsCommander() returning the *recordingCommander with the list-panes/has-session/new-session arms — and have the two route tests wrap it with a `show-option` → version arm (or accept a bool for the latch arm). The abridged_saver test uses the base fixture directly (it drives ensureSaverLiveness, which never reads the latch).

- FINDING: Entry-path context-injection + warning-drain epilogue repeated across PersistentPreRunE branches
  SEVERITY: low
  FILES: cmd/root.go:186-196, cmd/root.go:228-250
  DESCRIPTION: The abridged branch and the synchronous full-bootstrap branch each re-inline the same two-part entry-path epilogue: (1) the warning drain `if !isTUIPath(cmd, args) { bootstrapWarnings.EmitTo(cmd.ErrOrStderr()) }`, and (2) the context injection `ctx := context.WithValue(cmd.Context(), serverStartedKey, <started>); ctx = context.WithValue(ctx, tmuxClientKey, client); cmd.SetContext(ctx)`. The spec itself frames the abridged path as reusing "the SAME entry-path plumbing (warning sink + context injection) as the synchronous full path," yet the implementation duplicates that plumbing inline in both places rather than sharing a helper; they differ only in the serverStarted value (false vs `started`) and the sync branch's `if client != nil` guard (the abridged branch reaches here only with a non-nil client). This is small (a few lines each, two sites) so it is low-impact, but because it sits on the critical bootstrap path the two copies must be kept in lockstep by hand if the context keys or the drain gate ever change.
  RECOMMENDATION: Extract a small helper such as `injectBootstrapContext(cmd *cobra.Command, client *tmux.Client, serverStarted bool)` (WithValue serverStartedKey + guarded tmuxClientKey + SetContext) and, if desired, `drainWarningsToStderrUnlessTUI(cmd, args)`, then call them from both the abridged and synchronous branches so the epilogue is authored once. Leave the concurrent branch as-is (it deliberately sets deferredBootstrapKey and no serverStartedKey).
SUMMARY: The production code cleanly reuses the existing runHookStaleCleanup helper for the new daemon cleanup home, so the notable duplication is concentrated in the new latch/abridged test files: three copy-pasted latch-verdict tests and a thrice-inlined "saver absent, revive fails" fake commander (both extractable to shared fixtures matching patterns already present in the same package). One minor production repeat — the context-injection/warning-drain epilogue duplicated across two PersistentPreRunE branches — is worth a small shared helper.
