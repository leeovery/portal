AGENT: standards
FINDINGS:
- FINDING: Stale "9-11" step-range in bootstrap_progress.go race-review comment
  SEVERITY: low
  FILES: cmd/bootstrap_progress.go:34
  DESCRIPTION: The 11→10 step reduction (spec § Affected Code Surface — "the package doc comment enumerating the 'eleven-step' sequence" must move to ten) was applied thoroughly across the codebase — bootstrap.go's package doc + totalSteps, loading_progress.go's stepLabelTable/totalBootstrapSteps, model.go's "1..10", adapters.go's step-10-final wording, and three other comments in this same file ("eleven-step"→"ten-step", "Eleven real steps"→"Ten real steps", "Index 1..11"→"1..10"). But the invariant-1 race-review block comment still reads "Clear step 8 BEFORE cleanup steps 9-11"; the orchestrator now has only steps 9-10 (CleanStale was removed). Purely a documentation inconsistency — no behavioural impact — but it contradicts the now-correct step count documented everywhere else in the same file.
  RECOMMENDATION: Change "cleanup steps 9-11" to "cleanup steps 9-10" at cmd/bootstrap_progress.go:34.

- FINDING: runHookStaleCleanup signature diverges from the spec's pinned 5-arg wiring
  SEVERITY: low
  FILES: cmd/run_hook_stale_cleanup.go:83-88, cmd/state_daemon.go:429
  DESCRIPTION: Spec § Daemon-Owned Hooks Cleanup → Dependency wiring (pinned) specifies runHookStaleCleanup(lister, store, logger, swallowListError bool, onRemoved) — five args, with the daemon supplying swallowListError=true. The implementation drops the swallowListError parameter (now 4 args) and hardcodes swallow-and-continue for both live callers (daemon + portal clean). This is behaviour-preserving: the daemon still gets exactly the pinned semantics (a non-nil ListAllPanes error is Warn-and-continue, never propagated), and every other pinned value (lister=Client, store=loadHookStore(), onRemoved=nil) is honoured. The removal was enabled by relocating the only false-caller (bootstrap step 11) out of the orchestrator, so both remaining callers want true — and dropping the now-constant boolean is consistent with code-quality.md's "Boolean parameters" anti-pattern and DRY. Flagged only because the spec pinned the exact signature; the divergence is an acceptable, quality-aligned simplification, not a behavioural drift.
  RECOMMENDATION: No code change required. If strict signature-fidelity to the spec is desired, either restore the swallowListError param or add a one-line note in the helper's doc block recording that the pinned 5th arg was intentionally collapsed to a constant once the only false-caller (bootstrap step 11) was removed.
SUMMARY: The skip-bootstrap-when-warm implementation conforms to the specification on every load-bearing decision — latch storage/semantics (version-stamped @portal-bootstrapped, parse-free equality, read-error folds to full bootstrap), set-point (final action after the last soft step and fatal gate, before the summary and terminal Done event, best-effort WARN never in warnings/StepEvents), the three-way single-read branch in PersistentPreRunE with the abridged gate upstream of shouldRunConcurrentBootstrap (ServerRunning probe dropped, keyed to latch-not-satisfied), liveness-only abridged EnsureSaver (no version-gate/kill-barrier), the 11→10 orchestrator with CleanStale removed entirely, and the daemon-owned throttled hooks cleanup on the idle branch. Project conventions hold (no t.Parallel() in cmd tests, daemon-spawning integration tests under IsolateStateForTest, internal/state leaf preserved, production never unsets the latch). Only two low-severity items: one stale step-range comment and one behaviour-preserving signature simplification versus the spec's pinned wiring.
