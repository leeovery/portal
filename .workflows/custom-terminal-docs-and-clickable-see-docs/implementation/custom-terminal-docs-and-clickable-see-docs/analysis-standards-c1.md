AGENT: standards
FINDINGS: none
SUMMARY: Implementation conforms to specification and project conventions. The docs page covers all eight mandated Scope sections and is accurate to internal/spawn/ (precedence tiers in configmatch.go, both-or-neither/argv-{command} rules in recipe.go, script $1/exec-bit/~-expansion in configadapter.go, NULL-before-config + skip-to-native in resolver.go, tolerant-decode table in terminalsconfig.go); the portal doctor host-terminal strings match cmd/doctor.go exactly and correctly use `portal doctor` (not the template's `xctl doctor`). README is trimmed to a one-line pointer with the terminals.json table row + PORTAL_TERMINALS_FILE note retained. unsupportedDocsURL is package-level, unexported, defined once, single production call site; .Hyperlink() is chained unconditionally in renderUnsupportedHeader with no change to detection/gating/NULL path/layout/copy; only the blueRun test assertion changed.
