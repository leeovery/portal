AGENT: architecture
FINDINGS: none
SUMMARY: Architecture is sound. The production change is a 3-line reuse of the existing `selectedSessionItem()` seam in `handleMultiSelectToggle`'s entry branch (internal/tui/model.go:3516-3518), keyed on `Session.Name` identically to the toggle branch and handling the HeaderItem/empty-list case via the seam's `ok=false` — no new API surface, no boundary shift, no special-casing. The test refactor composes cleanly: a single well-documented `enterMultiSelectEmpty` helper (multi_select_keymap_test.go:57) reproduces the pre-mark-on-entry starting point for the ~13 burst/multi-select setups that need a deterministic empty start, exercising the real double-`m` "zero-selected reachable" production path (not internal state pokes) with a load-bearing precondition that fails loudly on regression; every preserved edge case (header no-op entry, By-Tag single-identity, Esc clear, N=0/N=1/N≥2 Enter boundary) still routes through the real dispatch. Change is fully contained in internal/tui with the spec-confirmed near-zero footprint elsewhere; build passes and the whole internal/tui lane is green.
