AGENT: standards
FINDINGS:
- FINDING: WithInitialMultiSelect docstring now over-claims parity with the changed enter step
  SEVERITY: low
  FILES: internal/tui/model.go:999
  DESCRIPTION: The `WithInitialMultiSelect` docstring says it "mirrors handleMultiSelectToggle's enter step". This change added mark-on-entry (seeding the highlighted session) to that enter step, but `WithInitialMultiSelect` deliberately seeds an explicit named set instead — the spec's Exclusions list it as "unaffected". The comment is not wrong (it enumerates the three mechanical steps it actually performs, and later states it seeds "the named sessions pre-marked ... production enters via the `m` key, never this option"), but the leading "mirrors ... enter step" phrase now points at a function that gained a cursor-derived marking behaviour this option does not share, so a future reader could conflate the two. Marginal — the docstring is self-consistent; the spec did not ask to touch this option.
  RECOMMENDATION: Optional. Tighten the opening clause to reference the wiring steps it shares (set mode / seed set / refresh delegate) rather than the enter step as a whole, so it does not imply the cursor mark-on-entry behaviour. No behavioural change.
SUMMARY: The production change to `handleMultiSelectToggle` matches the spec exactly (mark-on-entry keyed on Session.Name, ok=false header path enters with zero, toggle branch and WithInitialMultiSelect untouched), the docstring was updated as required, and CLAUDE.md:184 / README.md multi-select prose + key tables were flipped to entry-marks-highlighted with the zero-selected-still-reachable note. The sibling test flips are consistent and idiomatic — the new `enterMultiSelectEmpty` helper cleanly preserves the pre-mark-on-entry build-from-scratch setups, and all retained edge cases (header no-op enter, double-`m` and Esc zero paths, By-Tag identity marks once) have explicit coverage. No t.Parallel(), no daemon/real-tmux in the touched unit tests; `go test ./internal/tui/...` and `go build ./...` both green. Only one low-severity docstring-accuracy nit found.
