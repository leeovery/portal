# Plan: Surface X Toggle In Keymap Hints

## Phase 1: Apply Change

Widen the two page-jump footer hints in `internal/tui/model.go` to display `p/x` (sessions page) and `s/x` (projects page), surfacing the existing `x` toggle binding. Display-only — no behaviour change.

#### Tasks
status: approved

| Internal ID | Name | Edge Cases |
|-------------|------|------------|
| surface-x-toggle-in-keymap-hints-1-1 | Widen page-jump footer hints to show x | none |
