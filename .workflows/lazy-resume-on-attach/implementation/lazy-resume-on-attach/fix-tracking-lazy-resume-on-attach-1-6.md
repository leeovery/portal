## Attempt 1

ISSUES:
- `/Users/leeovery/Code/portal/cmd/hooks_resume_mode_test.go:119-138` — "it refuses --resume-mode with no --on-resume and writes nothing" passes for the wrong reason and does not pin what the task asked it to pin. It injects `paneKeyPathSeams()` (line 123), whose resolver and stamper are *armed with errors*, then asserts only `err != nil` (131) and that hooks.json is absent (135). Both assertions still hold if `MarkFlagRequired("on-resume")` were removed and `RunE` ran: `resolveCurrentPaneKey` would call the poisoned resolver, get its error, and return before any write. So the criterion's "performs no tmux read" clause is unverified, and the Do-item's "pins the existing one with a test instead" is not achieved — in production, dropping the required-flag mark would let `hook set --resume-mode lazy` stamp a pane and write an empty-command registration, which is exactly the regression this case exists to catch. (Secondary: the poisoned errors read "must not be called on the --pane-key path", so a real misfire here would report the wrong path in the failure message.)
  FIX: after the `Execute()` block, add the existing helper `assertNoPaneTmuxCalls(t, resolver, stamper)` (`cmd/hookkey_vocabulary_test.go:180`) — zero resolver calls and zero stamps is the observable proof the body never ran, i.e. no tmux read. Pair it with an assertion on the refusal's origin, comparing `err.Error()` against the verbatim `required flag(s) "on-resume" not set`. The reviewer confirmed that is the exact message the built binary emits (exit 1). Both hold against the current implementation.
  ALTERNATIVE: swap the poisoned pair for plain recording seams (`&mockKeyResolver{key: hookstest.SubjectSeedA}`, `&recordingPaneStamper{}`) and keep the existing `os.Stat` assertion — then a body that ran would write an empty-command entry and the file-absence assertion fails honestly, and the seam errors no longer name the wrong path. Tradeoff: it relies on the write happening rather than proving the read did not, so it is weaker on the "no tmux read" clause. The reviewer recommends the primary fix (call-count assertion), optionally with the seam swap on top.
  CONFIDENCE: high

NOTES:
- README example-block alignment: the surrounding comments in the `xctl hook` block align at column 50; the new line is 56 characters, so its comment sits at column 59 and breaks the column. Purely cosmetic and unfixable without reflowing the whole block — flagged, not a requested change.
- `resumeModeFlagName` is introduced as a const while the sibling flags `on-resume` and `pane-key` stay string literals in both `cmd/hooks.go` and `resetRootCmd`. The asymmetry is defensible (three production uses plus the test reset) and no change is asked for, but it is the kind of half-applied convention a later reader will ask about.
- The "explicitly empty mode" and "names both accepted values" cases assert less than the unrecognised-mode case (no `save.requested`/byte-identity checks). Acceptable — all three share one code path and the unrecognised case carries the full assertion set.
- The reviewer verified the "goes to the command's error stream" clause outside the suite (it is unobservable through `rootCmd.Execute()`) with a scratch binary under a scrubbed env: exit codes 1/2/2 and the exact stderr lines. No tmux server contacted, nothing written outside the scratchpad.
