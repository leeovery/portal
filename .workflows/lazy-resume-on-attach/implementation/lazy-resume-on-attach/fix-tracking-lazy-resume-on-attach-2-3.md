## Attempt 1

ISSUES:
- `internal/state/capture.go:168` — the token the frozen merge matches on is read from the fresh index *after* `mergeSkippedPanes` (`capture.go:104`) may have replaced that pane wholesale with a previous record (`mergePane` does `w.Panes[i] = pp`, carrying prev's `PortalPaneID` and `Active`). For a pane carrying both markers the lookup therefore runs on the *previous* pane's token, not the live pane's, so the stated property — "a pane carrying both markers ends on the token-matched record" — does not hold, and the pane lands on the positional record instead. The reviewer demonstrated this rather than inferring it: with prev holding `work:0.0 → tok-y/work__0.0.bin` and `work:1.0 → tok-x/work__1.0.bin`, a live pane carrying `tok-x` and the pending marker now at `work:0.0` whose stale skeleton key is also `work__0.0` comes out as `{Index:0 CWD:/other Active:false CurrentCommand:vim ScrollbackFile:scrollback/work__0.0.bin PortalPaneID:tok-y}` — its real file `scrollback/work__1.0.bin` is absent from `ComputeReferencedSet`, so the housekeeping pass reclaims it. That is precisely the permanent transcript loss this task exists to close, reached by the one path the task's own ordering instruction was written to cover. The overlap is not hypothetical: §7.3 requires the pending marker to be set *before* the mid-restore marker is cleared, so every lazily-restored pane carries both for at least one tick, and §7.2 holds the pending marker indefinitely when a clear fails. A second consequence of the same root cause: the merged record's `PortalPaneID` is then the previous pane's token, so the *next* tick's token lookup for that pane misses entirely and the fresh path takes over — the loss lands even if nothing moves again.
  FIX: take the pane's token from the live enumeration instead of from the post-merge index. Collect it where the pending set is already built from the same rows (`addPendingPaneKeys`, `capture.go:91/115`) — e.g. a `map[paneKey]paneRow` or a `map[paneKey]string` of live tokens — pass it to `mergeFrozenPanes`, use `liveTokens[key]` in the `takePrevRecord` call at `capture.go:168`, and write the live token back onto the merged pane (`p.PortalPaneID = liveTokens[key]`) so the record the next tick reads as `prev` still answers to the live pane. This leaves `mergeSkippedPanes`, its live-structure guard and the positional key untouched, as the task requires. Cover it with a subtest in `internal/state/capture_frozen_merge_test.go` — "it ends a pane carrying both markers on the token-matched record" — staged exactly as above (skipSet holding the pane's live key, prev holding a different pane's record at that address and the frozen pane's real record elsewhere), asserting both the `ScrollbackFile` and its presence in `ComputeReferencedSet`.
  ALTERNATIVE: also restore the live `Active` from the same map (`p.Active = live[key].Active`). Without it, a both-markers pane keeps prev's `Active`, which is the pre-existing skip-merge behaviour but contradicts this merge's stated rule that liveness comes from the enumeration — and the task's own edge case gives the reason (`activePanePosition` in `internal/restore/session.go` takes the first, so a stale flag in a window that already has an active pane restores two). The reviewer recommends including it: it is the same one-line reach, it costs nothing on the non-overlap path where the values are already identical, and criterion 8 is unaffected because no existing skeleton-merge case sets the pending column. The narrower fix (token only) is defensible if the skip-merge path's semantics are to be left bit-for-bit alone.
  CONFIDENCE: high (that the behaviour is wrong — reproduced); medium on the exact shape of the remedy, per the alternative above.

NOTES:
- The path is dormant in production today: no production code writes `@portal-resume-pending` yet (only `internal/state/markers.go` declares the setter; the hydrate-side writer is a later task), so nothing regresses in the working tree while the issue stands. It becomes live the moment the writer lands.
- `CaptureStructure`'s doc comment (`capture.go:32-44`, untouched by this diff, so outside the task's comment scope) still describes only the skipSet merge. A caller reading the exported contract learns nothing of the second merge that now also takes a pane's content from `prev`. Worth folding one clause into whichever later task next edits that doc.
- Narrow corner in the two lookups: a token match consumes only the `byToken` entry, so the same previous record is still reachable through `byAddress`. A tokened pending pane and an untokened pending pane sitting at that record's old address would both take it and both point at one scrollback file. Unreachable through Portal's own writers (a waiting pane always carries a registration and is therefore stamped), and the task frames the untokened path as hand-cleared-option degradation — recorded rather than a requested change.
- The CLAUDE.md sentence is accurate for the mainline and covers the `ComputeReferencedSet` consequence; it does not mention the positional fallback for an untokened pending pane. The task asked for one sentence, so this is an observation, not a gap.
- The commit-side test is named `TestCommit_KeepsAFrozenPanesScrollbackFileAfterThePaneMoves`, near-duplicating the wording of the capture-side subtest. Both halves are covered, so this is naming only.
- The reviewer's mutation checks were done in a scratch copy of the tree; the repository working tree is exactly as the executor left it.
