## Attempt 1

ISSUES:
- `internal/tui/resume_pane_canvas_test.go:134-144, 219-220` — the card's placement on the canvas is pinned by nothing. `nonEmptyRows` (`:81`) `TrimSpace`s every row, which is exactly the leading/trailing padding that encodes where the card sits, and the one test that compares untrimmed rows ("it renders the card when the pane fits it") runs at a pane exactly the card's size, where centring is a no-op. The reviewer replaced `placeModalOnClearedCanvas(card, w, h)` with `lipgloss.Place(w, h, lipgloss.Left, lipgloss.Top, card)` and then with `Right, Bottom`: both pass this file AND the whole `internal/tui` package (35s run, green). Rendered proof at 60x11 — centred gives three blank rows then the card indented 18 columns; the mutant gives the card flush in the top-left corner and six blank rows under it. The failure this prevents: the resume panel draws jammed into a corner of every restored pane bigger than the card, which is the normal case; the user sees a panel that reads as a rendering fault, and the spec's stated shape ("the decision sits in a compact bordered card in the middle", §5.2) is silently gone. Nothing else in the repo would catch it — the panel has no capture fixture yet, and Phase 4 is what puts it on a terminal.
  FIX: add a subtest to `TestRenderPaneScreen_SizeLadder`, `"it centres the card on a pane larger than it"`, rendering the existing 24x5 fake at 60x11 and asserting the **untrimmed** `paneRows` output against the exact expected rows — three empty rows, then `strings.Repeat(" ", 18)+f.card[i]` for i in 0..4, then three empty rows. `paneRows` already `TrimRight`s only, so the left padding survives the helper and both axes are pinned in one comparison. Deriving the pads as `(w-fakeCardWidth)/2` and `(h-fakeCardHeight)/2` in the test keeps it readable without restating 18 and 3.
  ALTERNATIVE: assert only that the first non-blank row's leading-space count equals the trailing count (centred, not left/right) and that the blank rows above and below are balanced. Cheaper to write but weaker — it would not catch a card placed centred-horizontally but top-aligned. The reviewer recommends the exact-rows form; the fake's content is fixed, so the expected block is deterministic and there is no reason to assert less than all of it. The orchestrator agrees.
  CONFIDENCE: high

- `internal/tui/resume_pane_canvas_test.go:277-295` — the clamp table has no case at the tightest boundary, a stack exactly one row taller than the pane, and that is the one place the rule can break silently. The four cases are 20-in-5, 6-in-3, 6-in-2, 6-in-1; none has `len(rows) == h+1`. The reviewer changed the guard `if len(clamped) <= h` to `<= h+1`, so a stack one row too tall returns unclamped and `fillPaneCanvas` then truncates it from the **end** — dropping the last row. Measured directly: a 4-row stack in a 3-row pane renders `["stack-0" "stack-1" "stack-3"]` today and `["stack-0" "stack-1" "stack-2"]` under the mutant. The whole `internal/tui` package passes under it. That mutant is precisely the failure the degraded form exists to prevent: the row carrying `⏎ resume` / `d discard` (or `y discard   esc cancel`) is what goes, on a pane that swallows every key it is not answered with — the user reads it as a panel with a dead keyboard, and on the discard confirmation what is at stake is the only copy of a user-authored command. A pane 3-6 rows tall against a 4-7 row stack is an ordinary restore outcome, not a corner.
  FIX: add one row to the existing table at `:284` — `{"four rows in three", 4, 3, []string{"stack-0", "stack-1", "stack-3"}}` — and, since the h==1 arm has its own boundary, `{"two rows in one", 2, 1, []string{"stack-0"}}`. Both fit the table's existing `rows`/`h`/`want` shape with no helper changes.
  CONFIDENCE: high

COMMENT_CORRECTIONS:
- `internal/tui/resume_pane_canvas.go:18-20` — the middle clause restates the `if`/`else` on the four lines below it; the contract ("exactly w by h cells") and the reason are what the code cannot say.
  OLD:
  // Renders parts into a string of exactly w by h cells: the card centred on the
  // canvas when the pane can hold it, and the same parts stacked plainly when it
  // cannot. A clipped frame would hide the key hints that say which keys act.
  NEW:
  // Renders parts into a string of exactly w by h cells. The fallback to a plain
  // stack exists because a clipped frame would hide the key hints that say which
  // keys act.

NOTES:
- **The `model.go` refactor is genuinely behaviour-identical**, verified rather than assumed: the old body's two `<= 0` guards are carried unchanged into `dimsOrFallback` and `termDims` delegates. No test file anywhere in the tree was modified or deleted, so no picker sizing coverage was weakened to accommodate it. The reviewer confirmed the fallback is still pinned by mutating `dimsOrFallback` to a pass-through and to width-only — both caught.
- **The degradation rule holds at every height**, traced against a 20-row stack: 1->[0], 2->[0,19], 3->[0,1,19], 5->[0,1,2,3,19]. First and last survive at every h>=2, the drop is from the end of the middle, and the three-index slice caps the backing array so the append cannot alias.
- 20 mutations run, the reviewer's own list, 18 caught — including the ones that matter most for content-versus-geometry: stack rows reversed, the fill painting blanks over the content, drop-from-the-front-of-the-middle, `h==1` keeping the last row instead of the first, stack builder called at a constant width, width clamp removed, backfill removed, blank-row padding removed, row clamp removed, colourless branch disabled, threshold strict-on-width, threshold ignoring height, and the card path rendering the stack. The assertion helpers are honest: `paneRows` fatals on the line count and errors per over/under-wide line before returning stripped rows.
- The task's edge-case list asks for the smallest pane to still draw "the title, the command and the key hints", including "a three-row pane holding a command that wraps past one row". That assertion has no home in this file by construction — the Do list and the Tests list both specify fake parts builders precisely so the ladder is tested independently of either screen's content, and the ladder's own share of it is covered. **It needs to land in the screen tasks (3-3/3-4) against real parts.** Not discharged here.
- `fillPaneCanvas` does not truncate an over-wide line — it inherits `padLineToCanvasWidth`'s contract, and the `w`-cell guarantee rests entirely on `clampStackToPane`'s width clamp upstream. Correct as built (one caller, which clamps), but worth knowing before a Phase 4 caller reaches `fillPaneCanvas` directly.
- `dimsOrFallback(w, h int) (int, int)` returns two same-type ints unnamed, while the `termDims` it was extracted from names them. The naming skill's stated case for named returns is exactly "multiple return values have the same type". Non-blocking.
