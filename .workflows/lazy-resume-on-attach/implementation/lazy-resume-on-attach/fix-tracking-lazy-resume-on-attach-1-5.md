## Attempt 1

ISSUES:
- `internal/prefs/resume_mode_test.go` — the round-trip asymmetry the task names as "this task's call" has no test on either side. The suite pins preservation only for the recognised value `"eager"` (lines 138, 152); nothing pins that an *unrecognised string* round-trips verbatim, and nothing pins that a *wrong-typed* value is dropped rather than preserved. The failure this leaves open is silent and reaches the user's file: a later tidy-up routing `resumeModeValue.UnmarshalJSON` through `resumemode.Parse` — a very plausible edit, since the type exists to hold a mode — would erase a hand-typed-but-misspelled `resume_mode` on the user's next `s` keypress or theme commit, with the whole existing suite still green. The user notices only by reopening `prefs.json` and finding their edit gone, and there is no other copy of it. The sibling tolerant field has exactly this pin (`theme_keys_test.go:228`, "an unrecognised appearance value is preserved verbatim, never parsed"), so this field is the one preserved-verbatim key in the record without one.
  FIX: Extend the existing `"it preserves a hand-set resume_mode across a grouping-mode toggle"` subtest (`internal/prefs/resume_mode_test.go:137-147`) into a small table over the seeded value — `"eager"`, `"lazy"`, and an unrecognised `"off"` — asserting `assertWrittenValue(t, decoded, "resume_mode", c.seeded)` for each, so preservation is pinned on a value `Parse` refuses. Add one further subtest beside it for the other half: seed `{"session_list_mode":"flat","theme":"nord","resume_mode":7}`, `Save(ModeByTag)`, then `assertKeysAbsent(t, decodeWritten(t, path), "resume_mode")` alongside `assertWrittenValue(t, decoded, "theme", "nord")` — which pins that the wrong-typed value is dropped *and* that its neighbours survive the write, following the shape `TestSave_WrongTypedFieldDoesNotAbort` (`store_write_path_test.go:247-262`) already uses for `theme`.
  CONFIDENCE: high

COMMENT_CORRECTIONS:
- `internal/prefs/store.go:95` — cardinality claim of the exact shape code-quality.md forbids ("nothing consumes this yet"), falsified by ordinary additive change far from the comment: `preferences-ui` (spec §10) is parked precisely to add a writer for this key. The durable half of the claim is the re-encode reason, which the sibling field two lines above already states in the canonical wording.
  OLD: // Nothing in Portal writes this; it is hand-set and must round-trip.
  NEW: // Must stay declared for the same re-encode reason as Appearance.

NOTES:
- The other two new comments earn their place and are true. `store.go:70-72` carries the absorb-or-zero-the-record reason (mirroring `migrationMarker`'s) plus the non-string consequence, which the reviewer verified by probe; `store.go:209-212` is API doc on an exported symbol for error behaviour the signature cannot state, and its "non-`ErrNotExist` read error propagates" claim holds — `readBytes` converts `ErrNotExist` to `(nil, false, nil)`.
- CLAUDE.md's `prefs` row and README's config-table row are both accurate against the code: the key shape, the tolerant/independent decode including the unreadable-file case, "nothing in Portal writes it", and the leaf clause widened to stdlib + `fileutil` + `internal/resumemode` with the no-`internal/log` rule and its reasoning left verbatim.
- The leaf guard judges production dependencies only (`go list -deps` with no `-test`), so the test-side `themetest` import is outside its rule. That is pre-existing and correct — the package doc's leaf claim is about the package, not its test binary — but worth knowing when reading the acceptance criterion "across both lanes".
- Documenting `resume_mode` in the user-facing README ahead of any reader existing is deliberate per the task's Do list; the phase lands the reader before release, so the reviewer is not treating it as drift.
